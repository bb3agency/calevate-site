# Alarm index — every code that can reach a phone

You were paged. The subject line is `[calevate/<env>/<service>] <code>`. Find the code
here: what it means in one line, and where to go next.

**This file is checked, not curated.** `scripts/check_alarm_wiring.py` derives the alarm
vocabulary from the tree — every `alert()` call, every `ProblemError` carrying a
`failure_stage` (which `core/errors.py` relays into `alert()` verbatim), every `*_code`
field on an ack meter, and every alarm the host backup chain emits in shell — and fails CI
in **both** directions: a row here with no call site behind it, and a code that can page
somebody with no row here. It is in `make guardrails` and in CI.

**Read the stage first.** `ROUTE_HANDLER` = something arrived and was refused;
`CORE_LOGIC` = a decision inside the app; `OUTBOX_DISPATCH`/`WORKER_*` = the reliability
path; `PROCESS_RESTART` = a service going down; `HOST_BACKUP` = the host chain, outside
every Python process (OPERATIONS §4).

**Noise bounds apply to all of them.** One page per `stage:code` per 15 minutes, 20/hour
globally with a burst of 6; suppressed and dropped counts ride the next delivery
(`apps/api/core/alerting.py`). "Happened once" and "still broken, 199 times" look
different — read the `note:` lines.

## Alarm codes

### The voice path — calls, campaigns, the engine

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `engine_error_spike` | CORE_LOGIC | The voice platform failed 10+ requests in 5 minutes (5xx, or no answer at all). Dials, publishes and the reconciliation poller are all affected. | `runbooks/calls-stopped.md` §9. Check the vendor's status page; the poller (D-31) is the guarantee of record, so calls already placed are not lost. Nothing to replay. |
| `engine_unreachable` | CORE_LOGIC | One request to the voice platform got no answer (DNS, TCP, TLS, timeout). Individually routine; it feeds `engine_error_spike`. | Only actionable in bulk — wait for the spike alarm or read `platform_engine_health`. |
| `engine_rejected` | CORE_LOGIC | The voice platform refused one request (4xx/5xx). Usually OUR request being wrong: a bad agent config, a wrong path, an unknown id. | Search logs for `engine_error` with the same `route`, and read the two fields on it: `status` and **`vendor_error`**, the vendor's own integer code from their error envelope — that is the value to quote at their support desk, and it is the only part of their body we keep (the human `message` quotes our request back at us, hard rule 6). If it follows a publish, the agent is not live. **On a DIAL, the status decides what happened to the contact**: 400/401/403/404 are documented refusals of the request, so no line was seized, the `calls` row is closed `failed` and the campaign contact keeps its retry ladder (`dial_not_placed` in the log); anything else could not rule out a ringing phone, so the contact is settled terminally and the client is escalated to (`campaign_dial_unconfirmed`). A burst of 401s is a revoked `BOLNA_API_KEY` and every campaign on the platform is stalled behind it. |
| `engine_rate_limited` | CORE_LOGIC | The vendor throttled us past our retry ladder. Nothing is lost; the caller sees a transient error. | Only act if sustained: it means concurrency above what the account allows. |
| `engine_bad_response` | CORE_LOGIC | The voice platform answered, and the answer is not one we can use: a 2xx whose body is not JSON — a WAF page, a CDN interstitial, a proxy error — **or a 3xx redirect off the API root the adapter pins** (`engine_redirect_response` in the log carries the status and the route). We never follow one: a 307/308 re-sends the body, which on `POST /call` would dial one contact twice, and a cross-host redirect strips our credential. | Vendor-side or edge-side. Retry is safe; if it persists, the account or the region is being interfered with. A redirect that persists means the API moved or an intermediary is answering for it — read the `Location` at the edge, not here (we log the status and the route, never the target). |
| `engine_agent_drift_detected` | WORKER_STALL | A client's live agent is running a script we did not publish (D-123). Someone edited it in the vendor console. | `runbooks/agent-engine-drift.md`. **Do not re-publish blindly** — the vendor-side edit may be the correct emergency change. |
| `handoff_destination_unknown` | CORE_LOGIC | **An agent handed a caller to a person and neither the voice platform nor that agent's handover list could say to which number.** The leg is being placed — the notification fires before the transfer and nothing here can stop it — so somebody's phone is ringing and no `handoff_attempts` row can be written for it, which means the client's screen will never show the call as escalated. The alert names the execution id and nothing else (hard rule 6). | The likely cause is a Transfer Call tool added in the VENDOR CONSOLE, pointed at a number nobody in this system chose: our own publish always writes exactly one destination from the roster. Find the agent through `engine_agent_routes`, then open its drift check — `handoff_applied` is `false` on exactly this case, and the read-back names how many destinations the platform holds. `runbooks/agent-engine-drift.md` for the console-edit conversation. If the read-back is clean instead, the roster went off duty between the publish and the call and the publish is simply stale: re-publishing the agent resolves it. |
| `handoff_leg_recording_unretained` | CORE_LOGIC | **A caller was handed to a person, that second leg was recorded by the voice platform, and this system does not hold the recording.** It is not copied to our storage, not covered by our retention policy, and NOT REACHED BY A DPDP ERASURE — the vendor serves it from a route of its own (`GET /recordings/transfer/{execution-id}`). `handoff_attempts.leg_recording_present` records that it exists; nothing records what is in it. Raised once per fingerprint, from the post-call pipeline. | **This is a data-protection obligation, not a bug to fix in code.** Note the execution id against its tenant: if that caller later exercises erasure, the deletion certificate must either cover this object or say plainly that it does not. OPERATIONS §2 gate 46b is where the decision is settled — copy the transfer recording under our own retention, or stop the platform recording the transferred leg. Until it is, every escalated call adds one. Do not silence this alarm to make the count go down. |
| `handoff_brief_channel_absent` | CORE_LOGIC | **A caller was handed to a person and that person's phone rang with no context at all.** The handover notice reached us and the `handoff_attempts` row was written, but there is no channel to deliver the brief on: no WhatsApp Business Account is configured, and SMS to an Indian handset needs a DLT-registered template through a registered sender. The client was told their team would know why a call was coming. | **EXTERNAL BLOCKER, not an incident** — nothing in this repository closes it. OPERATIONS §2 gate 46d: a WABA plus an approved template, after which `workers/handoff._send_brief` calls the transport that already exists. Until then the honest client-facing statement is that the person taking the call sees it on the dashboard when the call ends, not while their phone rings. Expect one of these per escalated call; it is a standing condition, and the runbook answer is to check the gate rather than to investigate the call. |
| `handoff_agent_unmapped` | WORKER_TERMINAL | A handover notice arrived for an agent reference no row of ours knows, so the escalation cannot be attributed to a tenant and no row is written. `engine_agent_unmapped`'s exact shape, on the handover path. | Check `engine_agent_routes`; usually an agent deleted on the vendor side, or a stale publish. The caller was still put through — the leg is the platform's — so this is a lost RECORD, not a lost call. |
| `handoff_unresolved` | WORKER_TERMINAL | The authenticated execution fetch behind a handover notice failed every retry, so nothing about the escalation was recorded. | Same two causes as every other `*_unresolved` in this family: credentials or vendor availability. The post-call pipeline still settles the call itself; what is lost is the mid-call record and the brief. |
| `engine_agent_semantic_routes_present` | CORE_LOGIC | **A read-back of a live agent carries vendor-side `routes` — semantic routes that answer a caller from a STATIC STRING without consulting the model.** The vendor's layer is `{route_name, utterances, response, score_threshold}`, matched on similarity of what the caller said (default 0.85). Hard rule 5's truthful-answer floor lives in the system prompt, and every instrument we have (publish read-back, `verification.judge`, the half-hourly drift sweep) scores the PROMPT — so anything a route matches never reaches the directive, including "am I talking to an AI?". Nothing in this tree sends `routes`; they were added in the vendor console. Raised from the publish read-back and from every drift sweep. The detail carries the agent ref, the route count and up to five operator-authored route names (never utterances or responses — that is caller conversation, hard rule 6). | Treat as a compliance incident, not a config drift: a published agent can be denying it is an AI while its prompt reads back perfect. Find the agent through `engine_agent_routes` and read the named routes in the vendor console. **A route that could plausibly match an AI-disclosure or recording question must be removed before anything else** — until it is, the agent can answer a caller untruthfully and no check here will say so. Then `runbooks/agent-engine-drift.md` for the console-edit conversation, which is the same one. Adopting routes deliberately would need the disclosure floor proven to survive them (every route's response composed with the same floor); today nothing here can prove that. |
| `engine_agent_multilingual_speech_override` | CORE_LOGIC | **A read-back of a live agent carries an ENABLED `multilingual_config` whose language entries bring their own voice or their own transcriber.** Their `MultilingualLanguageEntry` declares `synthesizer` required, so one **+ Add Language** click in the vendor console gives that language a text-to-speech provider of its own — and the platform switches to it mid-call. `_agent_models` reads the conversation task's speech legs and nothing else, so the publish read-back and the half-hourly drift sweep both score `voice_applied` against the base language while a caller who switches is heard and answered somewhere else. Nothing in this tree sends `multilingual_config` (`_agent_body` states it as `null`), so this is always a console edit. The detail carries the agent ref, how many languages, up to five language CODES and the provider names — never any prompt text (hard rule 6). | Two questions, in this order. **(1) Disclosure:** if a named provider is not our own speech vendor, a client's callers' AUDIO is reaching a sub-processor no client agreement of ours names — that is a DPDP question, not a config one, and it is the reason this pages. **(2) Voice:** the agent is speaking in a voice this product never published, and no read-back scores it. Find the agent through `engine_agent_routes` and read the language tabs in the vendor console; then `runbooks/agent-engine-drift.md` for the console-edit conversation. Note the PROMPT half of the same click is caught separately and refuses the publish outright (`AgentSnapshot.every_prompt_carries`), so a silent prompt verdict here does not mean the language tabs are compliant — it means their prompts carry the floor. |
| `engine_inbound_binding_failed` | CORE_LOGIC | **The voice platform was not told which agent answers a client's number.** Our `phone_numbers.agent_id` says the receptionist is assigned; the engine does not know it, so an incoming call reaches whatever the vendor console was last set to, or nothing. The alert names the agent id, the number's ROW id (never the number), what was intended (answer / stop answering) and the refusal code. | `engine_number_not_linked` means the number was never connected to the voice platform account — an onboarding step a person does, not an error to retry. `engine_rejected` means the vendor refused the binding: check **OPERATIONS §2 gate 25**, which is open on whether that route accepts a non-Twilio Indian number at all. Either way the number is not answering; until it is fixed the client's inbound line is dark, and re-publishing the agent retries it. |
| `engine_agent_route_withdrawn` | WORKER_TERMINAL | An event arrived for an agent whose routing was WITHDRAWN — an offboarded client whose number is still pointed at the voice platform. The only other symptom is a stranger ringing it. | Release the number with the telephony provider and remove the agent at the engine. Nothing else in the system can discover this. |
| `engine_agent_unmapped` | WORKER_TERMINAL | The engine sent an event for an agent reference no row of ours knows. The call cannot be attributed to a tenant. | Check `engine_agent_routes`; usually an agent deleted on the vendor side, or a stale publish. |
| `engine_ingest_abandoned` | WORKER_TERMINAL | The post-call event could not be ingested after every retry. The call has no row. | `runbooks/calls-stopped.md`. The 10-minute reconciliation poller is what recovers it; if it does not, the execution is unreadable at the vendor. |
| `engine_drift_sweep_abandoned` | WORKER_TERMINAL | The half-hourly drift sweep failed every attempt. **Every client's live agent is unwatched until it succeeds.** | `runbooks/agent-engine-drift.md` §1. Credentials or vendor availability. |
| `engine_kb_drift_detected` | WORKER_STALL | Live agents hold knowledge on the voice platform that is not what was approved here. | `runbooks/kb-out-of-sync.md`. |
| `kb_drift_sweep_abandoned` | WORKER_TERMINAL | The KB drift sweep failed every attempt; published knowledge is unwatched. | `runbooks/kb-out-of-sync.md` §1. |
| `engine_violation_open` | WORKER_STALL | **The voice platform has raised compliance flags against our account and nothing has been submitted against them.** Their word, not ours: "content policy, regulatory, or fraud". The detail names how many, how many tenants they touch, the age of the oldest, and up to five violation ids. | `runbooks/engine-violations.md`. This is the one alarm in this family only a person can clear — evidence submission is manual and the vendor publishes no deadline, so treat the age in the detail as the urgency. |
| `engine_violation_sweep_incomplete` | WORKER_DELIVERY | The violations listing could not promise it saw everything (page cap, a stuck `has_more`, or a missing one). **The open count in the other alarm is a FLOOR, not a total.** | `runbooks/engine-violations.md` §3. Re-read the list by hand in the vendor console before concluding the account is clean. |
| `engine_violation_sweep_abandoned` | WORKER_TERMINAL | The hourly compliance-flag sweep failed every attempt. The platform's flags are unwatched until it succeeds. | `runbooks/engine-violations.md` §3. Credentials or vendor availability — the same two causes as `engine_drift_sweep_abandoned`. |
| `postcall_pipeline_stalled` | WORKER_STALL | Calls ended more than ten minutes ago and still owe an extraction. The 2-minute lead SLO is being missed now. | `runbooks/campaign-stall.md`; check worker liveness and queue depth at `/healthz/ready`. |
| `post_call_abandoned` | WORKER_TERMINAL | One call's post-call pipeline exhausted its retries. No lead, no extraction, no usage event for that call. | Re-drive by the poller, or investigate the call id in the alert. |
| `recording_copy_failed` | WORKER_DELIVERY | The call recording could not be copied into our storage. The call is otherwise intact. | Object storage credentials or bucket policy. The vendor's copy expires — act before it does. |
| `call_billable_without_cost` | WORKER_TERMINAL | The engine says the execution was billable and the adapter could not read a cost. We are being charged for something we cannot meter. | Money: reconcile against the vendor invoice. `runbooks/topup-payments.md` for the billing side. |
| `engine_cost_implausible` | CORE_LOGIC | One call metered at a per-minute cost orders of magnitude away from what a minute costs (band ₹0.10–₹100/min; a real call is ₹0.5–6/min). **The row is already written** — this alarm is about a number that landed, not one that was refused. | `runbooks/vendor-cost-unit.md`, which carries the whole sequence and what NOT to do. In short: read the ratio in the detail — **near 100** is the minor-unit assumption (`_MINOR_UNITS_PER_MAJOR` in `engine/bolna.py`), **near 90** is the currency one (a rupee figure multiplied by the USD rate) — settle it against one vendor invoice line (OPERATIONS §2 gate 7), fix the constant, then restate the rows already metered with `uv run python -m scripts.correct_cost_unit` — `usage_events` is append-only, so the repair is a compensating row and never an edit. Do not change the divisor on a hunch. |
| `engine_llm_ttft_degraded` | CORE_LOGIC | **The language-model leg of a call was broken, not merely slow.** The MEDIAN turn on one call spent more than 1000ms waiting for the model's first token — the engine's own bottleneck threshold (`bolna-findings/mirror/pages/concepts/call-latencies.md:178`), roughly three times our 350ms budget (TRD §4). The median over at least three turns, so a cold first turn cannot fire it and one slow turn is not an incident. | Read `GET /v1/ops/engine-latency` first: it groups every measured turn by engine and **region**, and the shape of the answer tells you which of three things this is. (1) **One region only, and it is `us`** — this used to be the geography bill: the execution ran far from a South India model deployment (D-410). **D-449 removed that gap** — the deployment is `eastus2` and the engine's orchestrator is us-east-1, so a `us` row that is STILL slow is no longer explained by the ocean and the lever is the model, not the region. Read it beside OPERATIONS §2 gate 4, which is where the ~180–230 ms this move was supposed to recover gets measured for the first time. (2) **Every region degraded at once** — the model deployment or Azure East US 2 itself; check the deployment is still Regional Standard (OPERATIONS §2 gate 20c) and not silently re-created as Global. (3) **One call, nothing else** — a long prompt on one agent; TRD §4 budgets ~2.5k tokens of system prompt and a bloated one raises TTFT and hallucination together. Nothing is lost or mis-metered by this alarm: the call completed and its row is written. |
| `calls_never_finished` | WORKER_STALL | The outstanding-call sweep found calls the engine will never say more about — **never transcribed, never metered, never invoiced**, and invisible to the stall report because a lost terminal webhook leaves the row at `in_progress` rather than `completed`. | `runbooks/calls-stopped.md`. The alert carries the ids; each is a call that happened and has no record. Check the vendor's execution directly before writing them off. |
| `outstanding_probe_incomplete` | WORKER_DELIVERY | Some outstanding calls could not be probed this sweep, so the recovered count is a FLOOR rather than a total. | Transient at first. Sustained means the engine is refusing reads and the guarantee of record (D-31) is degraded. |
| `outstanding_probe_budget_exhausted` | WORKER_DELIVERY | The sweep hit its per-tick vendor-request ceiling and stopped early. **The same tail of the tenant ordering is starved until the incident clears** — the truncation is deliberate and this alarm is what stops it being silent. | Expect it during a large backlog. If it repeats once the engine is healthy, the backlog is bigger than one sweep can drain and needs a manual widening. |
| `call_duration_negative` | WORKER_TERMINAL | The engine reported a negative call duration — a sentinel for "unknown", not a real length. The call is metered at ZERO minutes rather than failing to settle at all. | No action per call: the client is billed for no minutes, which is the safe direction. Repeated = a vendor payload change worth reading (D-251). |
| `reconciliation_fetch_failed` | WORKER_DELIVERY | The poller could not read an execution back from the engine. | Transient at first; sustained means the guarantee of record (D-31) is down. |
| `reconciliation_probe_incomplete` | WORKER_DELIVERY | Some executions could not be probed or re-driven, so the repair count is a FLOOR rather than a total. A sweep that skipped work reports fewer repairs, which reads like a healthy fleet. | Search the worker log for `reconciliation_probe_failed`. Those calls are recoverable only while they stay inside the 30-minute listing window. |
| `reconciliation_listing_incomplete` | WORKER_DELIVERY | A poll could not promise it saw the whole window. Executions inside the gap have no webhook, no repair and no metric. | Widen the window by hand or re-run the poll; treat the gap as calls to check. |
| `engine_listing_window_too_wide` | CORE_LOGIC | **A reconciliation listing asked the engine for more history than it will serve** (7 days, `_LISTING_MAX_WINDOW` in `apps/api/engine/bolna.py`), and the adapter REFUSED rather than quietly narrowing the window — a narrowed window would make `complete=True` a claim about a period nobody agreed to skip. This one is OURS, not the vendor's: no production caller can reach it, because `reconcile_executions` polls a 30-minute window. | Something hand-run asked for too much: a backfill, a script, or new code. Split the request into windows of 7 days or fewer and repeat it to cover the period; never widen the bound (D-414). Nothing was lost and nothing was mis-metered — the listing did not run, so no rows were read and none were skipped. If it fired from a scheduled job, the job is the bug. |
| `dial_recall_unstopped` | WORKER_STALL | **Outbound was halted and one or more dials the voice platform had already accepted could not be pulled back.** The engine's stop route *"cannot stop a call already in progress"*, so a dial that started ringing between the scan and the stop runs to its end. The detail carries the count, the total the scan found, and up to five call ids. | `runbooks/campaign-stall.md` §1. The named calls are ringing or about to; nothing here can stop them, and the poller (D-31) will close their rows normally. What to check is WHY — if the count is most of the batch, the account's concurrency ceiling is far below the outbound pool and the vendor was holding a queue (OPERATIONS §2 gate 31). Re-post the halt to run another pass over anything still queued. **The count cannot yet separate "already ringing" from "the vendor does not know this id"** — the stop route's refusal codes are OPERATIONS §2 gate 35 — so read it as an upper bound on phones that will still ring. |
| `dial_recall_incomplete` | WORKER_STALL | The recall scan hit its 500-dial cap, so the "stopped" count is a FLOOR and more dials may still be queued at the vendor. | Re-post the halt (same confirmation header) to run another pass; the stamp on each recalled dial means a second pass sees only what the first did not reach. A fleet that reaches this cap is one nobody here has sized — read `RECALL_SCAN_LIMIT` in `apps/workers/dial_recall.py` before raising it. |
| `dial_recall_impossible` | WORKER_TERMINAL | Outbound was halted, but the voice platform holds no credentials in this environment, so no queued dial could be recalled. | Only reachable with a named engine and no key — the same condition that stops publishes and the poller. Restore `BOLNA_API_KEY` and re-post the halt. Until then the halt stops NEW dials only. |
| `dial_recall_abandoned` | WORKER_TERMINAL | The recall job failed every attempt. Outbound is halted but dials already accepted by the vendor were not recalled and will ring. | `runbooks/campaign-stall.md` §1. Read the worker log for `dial_recall_run_failed` — the scan and the engine are the two things that can fail this. Re-post the halt to retry once the cause is cleared. |
| `dial_recall_not_queued` | CORE_LOGIC | **The halt landed but its recall job could not be put on the queue** — Redis was unreachable at that instant. The switch IS thrown; the recall never started. | This is the one alarm here that fires from the API rather than a worker, and it is deliberately not a refusal: telling an operator the switch failed when it did not would send them to throw it again. Fix the queue, then re-post the halt to run the recall. |
| `dnc_recall_undetermined` | WORKER_STALL | **A number was suppressed and one or more dials the voice platform was already holding could not be CONFIRMED as prevented.** Distinct from `dial_recall_unstopped` in the only way that matters: this one is about a DNC entry, so the question it leaves open is whether a suppressed number was called after we were told to stop. Covers three cases together — the vendor said the call had already started, the vendor accepted the stop but did not say what it caught (`RecallOutcome.UNKNOWN`), or the stop was refused outright. The detail carries the count, the total found, and up to five call ids. | `runbooks/dnc-complaint.md`. Open each named call: `calls.status` and `started_at` say whether it connected, and that — not this alarm — is the record to answer a complaint from. Nothing here can stop them. If the engine is Cartesia, EVERY recall is `UNKNOWN` by construction (its stop route is inferred, gate 19b) and this alarm will fire on every suppression that finds a queued dial. |
| `dnc_recall_incomplete` | WORKER_STALL | The DNC recall scan hit its 100-dial cap, so the prevented count is a FLOOR and more dials to the suppressed numbers may still be queued. | Reachable on a bulk import of thousands of suppressions, not on a single opt-out. Re-import the same list: the stamp on each recalled dial means a second pass sees only what the first did not reach. Read `DNC_RECALL_SCAN_LIMIT` in `apps/workers/dnc_recall.py` before raising it. |
| `dnc_recall_abandoned` | WORKER_TERMINAL | The DNC recall job failed every attempt. The suppression IS in force for the next dispatch tick, but dials already accepted by the vendor were never pulled back and may ring. | `runbooks/dnc-complaint.md`. Read the worker log for `dnc_recall_job_failed` — the scan and the engine are the two things that fail this. The outbox row is exhausted, so re-add the number to re-enqueue the recall once the cause is cleared. |
| `outbound_pool_empty` | WORKER_STALL | The inbound reserve is at or above the total line pool, so no outbound line exists. No campaign can dial. | Config: `INBOUND_RESERVE_RATIO` vs the platform line count (DEPLOYMENT §2a). |
| `dispatch_tick_overrun` | WORKER_STALL | One campaign dispatch tick took longer than its 30-second interval. | Read `dispatch_tick_seconds` for the trend. Sustained = the dispatcher is behind and campaigns dial late. |
| `dispatch_tick_overlap` | WORKER_STALL | The previous tick was still running when the next one started. The lease held, so nothing double-dialled. | Same investigation as `dispatch_tick_overrun`. |
| `dispatch_budget_starved` | WORKER_STALL | **One or more tenants dialled NOTHING this tick because the shared outbound pool ran out before their turn — and the spend order does not rotate, so it will be the same tenants next tick.** The order is `dispatch_scan()`'s `ORDER BY tenant_id`, and `tenant_id` is uuid_v7 (time-ordered), so the tail is the NEWEST clients and the starvation is indefinite, not transient. Every tick still reports a healthy `dialled=N`; this alarm is the only thing that says who got none. The detail carries the pool, the platform-wide active count, and up to five starved tenant ids. | `runbooks/campaign-stall.md` §4a. Read the suppressed counts: once is a busy minute, a sustained repeat is a client dialling zero for as long as it has been firing. There is no floor to raise — we model caps only — so the levers are the pool (`PLATFORM_LINES_TOTAL`, and whether the engine account's real ceiling is higher) and the sliders of the tenants at the HEAD of the order. The durable fix is guaranteed floors plus proportional surplus, which is a plan field and a commercial decision, not a config change. |

### Compliance and consent

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `campaign_complaint_spike` | CORE_LOGIC | Five or more of a campaign's connected calls ended in an opt-out, and that is at least 10% of them. **The campaign has been paused automatically** (FLOWS §5). | `runbooks/dnc-complaint.md` §9. Under TCCCPR five unique complaints in ten days suspends the client's outgoing service — treat this as the last cheap moment. Resuming re-pauses while the 24h window still holds the spike. |
| `in_call_optout_unresolved` | WORKER_TERMINAL | A caller asked to be left alone mid-call and the fast path could not resolve the execution. The post-call transcript pass is the backstop. | `runbooks/dnc-complaint.md`. Confirm the suppression landed; the gap is a window in which a campaign can still dial. |
| `in_call_optout_agent_unmapped` | WORKER_TERMINAL | An in-call opt-out arrived for an agent reference no row of ours knows, so no tenant owns it. | Suppress the number by hand from the ops DNC screen. |
| `in_call_optout_unattributable` | WORKER_TERMINAL | An in-call opt-out with no usable caller number to suppress. | Same: find the call and add the number by hand. |
| `opt_out_unattributable` | WORKER_TERMINAL | The post-call transcript pass detected an opt-out on a call with no usable number. | Same as above, from the call record. |
| `in_call_callback_unresolved` | WORKER_TERMINAL | A caller was told on the phone that we would ring them back, and the booking job could not fetch the execution after every retry — so the promise was never written down. **Nothing else recovers this.** Unlike an opt-out, a call-back has no post-call transcript pass behind it: the caller is expecting a call that will not come. | Find the call (the alarm carries its execution id), read what was promised, and either ring them or tell them. Then treat a repeat as the engine's Get Execution being unreachable, which is the same cause as `in_call_optout_unresolved` beside it. |
| `in_call_callback_agent_unmapped` | WORKER_TERMINAL | A call-back was booked against an agent reference no row of ours knows, so no tenant owns the promise. | Same triage as `in_call_optout_agent_unmapped`: the routing row is missing or the agent was re-published elsewhere. The caller is still waiting — find the call and place it by hand. |
| `in_call_callback_unattributable` | WORKER_TERMINAL | A call-back with no usable number to ring. | Find the call and place it by hand; the promise cannot be stored without a number. |
| `caller_memory_distil_worklist_failed` | WORKER_TERMINAL | The hourly cross-call memory distillation could not read its worklist after every retry, so it looked at no tenants and wrote down nothing about anybody's callers. Only the worklist read reaches this alarm — one tenant's provider error or lock timeout is isolated and logged per tenant. | **Advisory: nothing is lost and nothing is late**, for `copilot_distil_worklist_failed`'s reason one row up. The calls it did not read keep their `pending` marker and the next tick picks up the same conversations; the only cost of a missed hour is that a caller who rings back inside it is greeted as a stranger. The cause is the database or the worker's pool, never the model — a missing language credential returns `no_provider` without alerting. Act if it repeats across several hours, and read it as a symptom of the pool. |
| `retention_below_trai_floor` | WORKER_TERMINAL | A tenant's retention policy would delete records earlier than the regulatory floor allows. The sweep refused. | `docs/SECURITY-COMPLIANCE.md` §4. Fix the policy row; do not relax the floor. |
| `retention_sweep_incomplete` | WORKER_TERMINAL | Some tenants did not complete tonight's retention sweep. Their expired recordings, transcripts and leads are still held. | Re-run the sweep. A DPDP obligation missed by a night is a fact to record. |
| `copilot_distil_worklist_failed` | WORKER_TERMINAL | The hourly copilot-memory distillation tick could not read `retention_worklist` after every retry, so it enumerated no tenants and distilled nothing. The worklist read is the ONLY failure that reaches this alarm — a single tenant's provider error, lock timeout or malformed row is isolated and logged per tenant, because one account must not end the tick for everyone behind it. | **Advisory: nothing is lost and nothing is late.** No agent, call, lead, bill or legal deadline depends on this job; the episodes it did not read keep their NULL `distilled_at` and the next tick picks up the same conversations. The cause is the database or the worker's pool, not the model — the credential is checked before this point and its absence returns `no_provider` without alerting. Act only if it repeats across several hours, and then read it as a symptom of the pool rather than of this feature. |
| `kb_gloss_worklist_failed` | WORKER_TERMINAL | The knowledge-gloss sweep could not read `retention_worklist` after every retry, so it enumerated no tenants and glossed nothing. The worklist read is the ONLY failure that reaches this alarm — a single tenant's provider error or lock timeout is isolated and logged per tenant, because one account must not end the tick for everyone behind it. | **Advisory: nothing is lost and nothing is late.** No call, lead, bill or legal deadline depends on this job. The chunks it did not reach keep `gloss_state = 'pending'` and the next tick (twice hourly) claims the same rows; the only cost of a delay is that a Telugu caller's question retrieves less well until it runs. The cause is the database or the worker's pool, not the model — the credential is checked before this point and its absence returns `no_provider` without alerting. Act only if it repeats across several hours, and read it then as a symptom of the pool rather than of this feature. |
| `erasure_requests_overdue` | WORKER_STALL | A DPDP erasure request has passed its deadline and is still not executed. | `docs/SECURITY-COMPLIANCE.md` §4; execute it by hand from the ops screen. |
| `processor_erasure_overdue` | WORKER_STALL | **An erasure ran correctly and a copy of the caller's recording and transcript is still sitting at a sub-processor, unasked-for or unanswered, past 30 days.** This is NOT a stuck job and re-queueing does nothing: our systems are clean, and the voice platform publishes no API that deletes one person's calls (`docs/evidence/subprocessor-erasure-reach.md` §1 enumerates every `DELETE` route they document — the Executions surface is four GETs). The alert splits the count two ways and the halves have different owners: **`unasked`** is OUR failure to send the request, **`unanswered`** is the vendor's failure to reply. | `runbooks/processor-erasure.md`. Deal with `unasked` first — it is the half we control. `uv run python -m scripts.processor_erasure list` prints the open tasks with the vendor ids to quote; send the written request, then record it with `... sent <task-id>`, and record the reply with `... answered <task-id> --outcome confirmed\|refused`. **A `refused` is not a failure to fix here** — it is the evidence that makes OPERATIONS §2 gate 37 (the DPA deletion clause) urgent, so put it in front of whoever is negotiating the contract. Never tell a data principal the vendor copies are gone before the task says `confirmed`. |
| `erasure_probe_deadline_exhausted` | WORKER_DELIVERY | The overdue-erasure walk ran out of its time budget and stopped part-way through the fleet. **`overdue_erasures=0` from that tick means "none among the tenants it reached", not "none".** The starved tail is stable (the directory is ordered by tenant id), so the same tenants are skipped every hour until this is acted on. | Not a longer deadline — the next stop after it is arq's `job_timeout`, and past that the alarm stops running entirely. The walk costs one session per organization because `deletion_requests` is FORCE-RLS'd; the fix is a cross-tenant probe. |
| `action_audit_unrecorded` | WORKER_TERMINAL | An in-call or after-call ACTION ran and the `audit_log` row recording that it ran could not be written after every attempt. **The tool's side effect happened; the audit trail does not show it.** The executor acks the caller and queues this job directly (hard rule 3 — no DB write on the tool path), so there is no outbox row to replay and the invocation cannot be reconstructed from anywhere else. | The cause is the database or the worker pool, not the action: the detail names the exception type, and the tenant and tool ids are on the alert. Check the pool and `/healthz/ready`. If a client or a regulator later asks for that invocation, the honest answer is that the execution succeeded and the audit write failed — record the alert as the evidence of the gap rather than back-filling the ledger, which is append-only (hard rule 4). |
| `tenant_erasure_mark_failed` | WORKER_TERMINAL | An offboarding erasure ran and `organizations.deleted_at` was not set — so nothing was erased. | Re-run; the mark is the gate the whole erasure hangs off. |

### Leads, notifications, integrations

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `auth_email_exhausted` | WORKER_DELIVERY | A sign-in email — password reset, OTP second factor, or an invitation — failed every attempt. **Somebody is locked out and the screen truthfully told them mail was on its way.** | Check the email transport (`EMAIL_PROVIDER`, `RESEND_API_KEY`, sender-domain verification). The ladder is deliberately tight (10s, 30s) because an OTP expires in ten minutes, so this fires while the person is still watching the screen. |
| `hot_lead_notification_exhausted` | WORKER_DELIVERY | A hot-lead email failed every attempt. The client did not hear about a lead. | Check the email transport (`EMAIL_PROVIDER`, sender domain verification). |
| `hot_lead_no_channel` | WORKER_TERMINAL | A hot lead arrived for a tenant with no billing email and no other channel. Nothing can be delivered. | Fill in the tenant's contact details. |
| `account_notice_exhausted` | WORKER_DELIVERY | A client's ACCOUNT CLOSURE notice failed every attempt. They have not been told their account was closed, or reopened — and a closure notice carries the date their records are erased, so this is a legal deadline the client has not seen. | Check the email transport (`EMAIL_PROVIDER`, `RESEND_API_KEY`, sender-domain verification), then tell the client another way. Do not let the erasure date arrive on an unnotified client: bring it forward only on their instruction, and `PATCH` nothing — reopen the closure with DELETE `/v1/admin/tenants/{id}/closure` if the notice cannot be delivered at all. |
| `account_notice_no_channel` | WORKER_TERMINAL | An account was closed (or reopened) for a client with no billing address and no active owner. Nothing can be delivered. | Retrying cannot conjure an address. Find the client's contact another way and tell them, then fill in `billing_email` so the next notice lands. |
| `account_notice_tenant_missing` | WORKER_TERMINAL | A closure notice was queued for an organisation the worker cannot read. | A data or RLS problem, not a transport one — the closure that queued this committed in a transaction that COULD see the row. Check whether the tenant was erased between the enqueue and the run. |
| `account_notice_unknown_event` | WORKER_TERMINAL | A closure notice payload named an event this worker does not send. | A producer and the consumer disagree about the contract. Check `apps/workers/account_closure.NOTICE_EVENTS` against every `enqueue_closure_notice` call site; the queued row will never deliver until they agree. |
| `closure_sweep_incomplete` | WORKER_TERMINAL | One or more accounts past their erasure date were not filed this tick. Their data is still held past the date the client was given. | The sweep runs again in an hour and retries them, so this is a deadline slipping rather than data lost. Read the per-tenant `closure_sweep_tenant_failed` log lines for the cause; a repeat over several hours is an obligation going undischarged and needs a person. |
| `hot_lead_whatsapp_exhausted` | WORKER_DELIVERY | The WhatsApp hot-lead alert failed every attempt. | BSP credentials or template status. Email is the other channel. |
| `hot_lead_whatsapp_rejected` | WORKER_TERMINAL | The BSP refused the WhatsApp alert outright (template, opt-in, number). | Fix the template registration or the opt-in record; retrying will not help. |
| `campaign_escalation_exhausted` | WORKER_DELIVERY | A campaign follow-up message failed every attempt. | `runbooks/campaign-escalation-refused.md`. |
| `campaign_escalation_rejected` | WORKER_TERMINAL | The BSP refused the campaign follow-up. | `runbooks/campaign-escalation-refused.md`. |
| `campaign_escalation_unrecordable` | WORKER_TERMINAL | A campaign contact exhausted its attempts and there is no lead row to record the follow-up on. | Data problem: the contact never produced a lead. Check the ingest path for that campaign. |
| `outbound_webhook_exhausted` | WORKER_DELIVERY | A client CRM webhook failed every attempt and is now dead-lettered. | `runbooks/webhook-delivery-failures.md`; replay with `POST /v1/ops/outbox/replay` after the endpoint is fixed. |
| `delivery_body_not_retained` | WORKER_DELIVERY | The delivered webhook body could not be written to object storage. **The delivery itself succeeded** — only the evidence is missing. | Object storage; no client impact. |
| `meta_page_token_invalid` | ROUTE_HANDLER | A client's Meta page token is dead. Every lead from that source refuses until it is replaced. | Replace the token in the client's integration settings. |
| `meta_leads_retrieval_denied` | ROUTE_HANDLER | The Meta token is alive but not permitted to retrieve leads. | The app's permissions or review status; the client must re-authorize. |
| `meta_signature_rejected` | ROUTE_HANDLER | A Meta webhook arrived with a signature we could not verify. | If sustained, the app secret is wrong; a single one is somebody probing. |

### The reliability path — outbox, inbox, webhooks in

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `job_function_not_registered` | WORKER_TERMINAL | **arq accepted an enqueue for a job name no worker has registered, and dropped it.** The enqueuing code saw success; the side effect never happened. arq logs a warning, retries nothing, and nothing in this repo reads its result keys — so without this alarm the only symptom is an outcome that silently never arrives. | A deploy skew (a producer shipped ahead of its worker) or a rename that missed `WorkerSettings.functions`. `scripts/check_job_wiring` fails the build on this in CI; reaching production means it was bypassed. Re-enqueue after fixing the registration. |
| `job_retries_exhausted` | WORKER_TERMINAL | A job spent its whole retry ladder and arq gave up. This is where every `Retry` ends when the condition never clears, and where a cron cancelled three times at `job_timeout` ends — e.g. the nightly retention sweep gone until tomorrow with only a log line. | Read the job name in the detail and go to that job's own runbook row. The next scheduled tick is the replay for crons; queued jobs need re-enqueuing. |
| `outbox_dead_letter` | OUTBOX_DISPATCH | Messages spent their whole attempt budget and need an operator. | `runbooks/webhook-delivery-failures.md` §3; `POST /v1/ops/outbox/replay`. |
| `outbox_queue_unreachable` | OUTBOX_DISPATCH | The dispatcher could not reach Redis and handed the batch back with a backoff. **Nothing is lost and there is nothing to replay.** | Fix Redis; the next tick drains it. Do NOT replay. |
| `webhook_source_rejected` | ROUTE_HANDLER | An engine webhook arrived from an address outside the allowlist. For an unsigned engine this is the whole authenticity control. | If the vendor renumbered, update `BOLNA_WEBHOOK_SOURCE_IPS` — until then every call waits for the 10-minute poller. Otherwise it is a stranger. |
| `webhook_payload_too_large` | ROUTE_HANDLER | An engine webhook body exceeded the accepted size and was refused. | The poller recovers the call. Repeated = a vendor payload change. |
| `webhook_unkeyable` | ROUTE_HANDLER | A webhook body carried no usable execution id, so it could not be deduplicated or attributed. | The poller recovers the call. Repeated = a vendor payload change. |
| `webhook_body_timeout` | ROUTE_HANDLER | The request body did not finish arriving inside the deadline. | Network, or a sender that opened a request and stalled. |
| `webhook_ack_slow` | ROUTE_HANDLER | The webhook receiver breached its 500ms ack budget. Usually CAPACITY, not a bug. | OPERATIONS §5's triage ladder: flat distribution = add `--workers`; long tail = a dependency. |
| `webhook_claim_timeout` | ROUTE_HANDLER | The receiver hit its 2-second abandon deadline and answered 503. Calls are now waiting on the 10-minute poller. | An incident, not a warning. Same triage, acted on immediately. |
| `webhook_payload_mismatch` | ROUTE_HANDLER | A settled webhook transition was re-delivered with different body bytes. | Recorded, not acted on (the payload is a hint; the poller is truth). Investigate if it repeats. |
| `tool_source_rejected` | ROUTE_HANDLER | An in-call tool call arrived from an address outside the allowlist. | Same as `webhook_source_rejected`, on the in-call path — the agent's opt-out tool is not working. |
| `tool_payload_too_large` | ROUTE_HANDLER | An in-call tool body exceeded the accepted size. | The caller's opt-out may have been missed; the transcript pass is the backstop. |
| `tool_call_unkeyable` | ROUTE_HANDLER | An in-call tool call carried no usable execution id. | As above — check the transcript pass suppressed the number. |
| `tool_enqueue_timeout` | ROUTE_HANDLER | The in-call tool endpoint could not queue its work inside the budget. | Redis. The caller's request is at risk of being dropped. |
| `tool_body_timeout` | ROUTE_HANDLER | An in-call tool request body did not arrive inside the deadline. | Network between the engine and us. |
| `tool_ack_slow` | ROUTE_HANDLER | The in-call tool endpoint breached its 100ms budget (TRD §6.2). | Read `tool_ack_ms`. The in-call experience degrades before anything fails. |

### Money and platform state

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `fx_rate_stale` | CORE_LOGIC | **The pulled USD/INR rate is older than `core/fx.MAX_QUOTE_AGE` (5 days), so every vendor cost is now converting at the configured `USD_INR_RATE` instead of a published one.** Nothing is lost and nothing is wrong yet — the fallback is a real, human-set number and it is what this platform used before the pull existed — but the margin model is drifting with the market and the rate on every new `usage_events` row reads `configured:usd_inr_rate`. Raised by the FX poll in whichever process notices, so it can fire when no pull job has run at all. | The rate comes from `apps/workers/fx_pull.py`; `fx_pull_silent`/`fx_pull_failed` usually fired first and name the cause. Check the worker is running its cron, then run OPERATIONS §2 gate 39's one `curl` by hand to see whether the upstream is answering. Nothing to replay: the next successful tick installs a fresh rate within a minute and rows already written keep the rate they used (that is what `meta.fx_rate` is for). If the upstream is down for longer than the drift is tolerable, update `USD_INR_RATE` in the ops console and RESTART — it is classified `on_restart` and the console says so. |
| `fx_pull_silent` | WORKER_TERMINAL | **No USD/INR rate has been fetched for over 30 minutes** — six missed ticks. The rate in force is still usable (that is a separate alarm), so this is the early warning, not the incident. | Check the worker process and its cron registration first: this fires from the pull's own failure path, so the job IS running and failing rather than absent. Then the upstream — gate 39's `curl`. |
| `fx_pull_failed` | WORKER_TERMINAL | The five-minute rate pull spent its whole retry ladder. The detail names what was wrong with the response — a status code, a missing field, a pair that was not USD/INR. **Costs keep converting at the last stored rate**, so this is not an outage. | `alert()` de-duplicates per fingerprint with an hourly bucket, so a feed down all day pages once. If the detail names a FIELD (`the response carried no numeric rate`, `the response is for EUR/INR`), the vendor changed their contract and the parser in `apps/workers/fx_pull.py` must be re-read against their OpenAPI before anything is trusted — do NOT loosen the parser to make the alarm stop. If it names a status, it is availability and the next tick handles it. |
| `fx_rate_implausible` | WORKER_TERMINAL | **A pulled rate was REFUSED for moving more than 10% from the previous one, or for falling outside the 1-1000 band.** USD/INR does not move 10% in a day, so this is a changed unit, a changed currency pair, or a field read wrong — the same class of defect that once metered every call at 1/100th of cost. The previous rate keeps serving until it ages out. | NOT retried, and deliberately: the feed answered and we refused its answer, so asking again gets the same number. A human decides. Run gate 39's `curl` and compare the number against any published USD/INR reference. If the vendor genuinely re-based their units, the fix is in `apps/workers/fx_pull.py`'s parser plus a decision-log entry — never widening `MAX_PLAUSIBLE_MOVE` to let the number through. |
| `razorpay_webhook_unconfigured` | ROUTE_HANDLER | A payment webhook arrived and no webhook secret is configured, so it cannot be verified. | Set the secret. Until then top-ups do not credit. `runbooks/topup-payments.md`. |
| `razorpay_webhook_bad_signature` | ROUTE_HANDLER | A payment webhook failed signature verification. | One is somebody probing; a burst after a rotation means the secret is stale. |
| `razorpay_unknown_tenant` | ROUTE_HANDLER | A verified payment names a tenant we cannot find. Money arrived with nowhere to credit it. | `runbooks/topup-payments.md`; reconcile by hand. |
| `razorpay_money_unapplied` | ROUTE_HANDLER | **A payment or refund event that PASSED signature verification could not be applied, so real money moved at the provider and the wallet did not.** The delivery is genuinely Razorpay's — the HMAC verified — and the refusal is one of ours: `problem_code` names which. `payment_payload_unrecognized` = our reading of their payload shape is wrong (the likely one on a FIRST live payment); `payment_tenant_unresolved` = the payment carried no `notes.calevate_tenant_id`, which is every payment taken outside our own checkout (a payment link, a dashboard payment); `payment_currency_unsupported` = not INR; `payment_amount_unrecognized` = the amount was not a whole number of paise; `payment_amount_conflict` = that payment id is already on the wallet for a different amount. | The client has been debited and their balance has not moved, so treat it as money owed, not as a bug report. Confirm the payment in the Razorpay dashboard, then credit it by hand on `/admin/tenants/<tenantId>/credits` using the **payment id** as the reference (the same key the webhook would have used, so a later redelivery dedupes rather than double-credits). Then fix the cause: a shape refusal is a one-function change in `apps/api/billing/payments.py::extract_captured_payment` with a fixture captured from the real delivery — never a loosened parser. `runbooks/topup-payments.md` §4. |
| `topup_attempt_not_recorded` | ROUTE_HANDLER | A client's top-up order WAS created at the provider, and we then failed to write the provider's order id onto our own `topup_attempts` row. **No money is at risk and the payment is unaffected** — the wallet is credited by the signed webhook against `credit_ledger`, which is untouched by this. What is lost is the narrative: that webhook keys on the order id to mark the attempt, so the client's credits screen will keep saying "still settling" for this payment until it ages out, even after the credit lands. | Confirm the money is fine first — the `credit_ledger` `topup` row keyed on the payment id is the record, and the balance on `/c/<slug>/credits` is the proof. Then find why the write failed (the log line is `topup_attempt_order_unrecorded`, carrying the tenant id and no payment identifier). One is a blip; a burst means the database was unreachable for writes, which will be showing up in louder alarms too. `runbooks/topup-payments.md`. |
| `topup_settlement_silent` | WORKER_STALL | **An order was created at the payment provider, nothing settled it, and the webhook leg has shown no sign of life since it was created.** This is the only alarm on the top-up path that does NOT need a delivery to reach us first — every other one (`razorpay_webhook_unconfigured`, `razorpay_webhook_bad_signature`, `razorpay_unknown_tenant`, `razorpay_money_unapplied`) fires from inside the webhook handler, so a webhook posted to the wrong address trips none of them. Raised twice hourly by `apps/workers/topup_settlement.py` when a `topup_attempts` row carrying a provider order id is older than 30 minutes and **no attempt anywhere** — captured or failed — has settled since it was created. The detail names up to five ORDER ids, which is what you type into the provider dashboard. | **Check the webhook URL first.** It must be `https://api.<domain>/hooks/v1/razorpay`; `hooks.<domain>` is voice-runtime and answers that path with a 404, so every delivery is lost and nothing here would ever know (`runbooks/topup-payments.md` §0.4). Then the webhook secret, then whether the events are subscribed at all. **An abandoned Checkout window looks identical from here** — a client who opened the payment sheet and closed it leaves exactly this row — so confirm in the provider dashboard whether each named order was actually PAID before crediting anything. If one was, it is money owed: credit it by hand on `/admin/tenants/<tenantId>/credits` using the **payment id** as the reference, which is the same `credit_ledger.ref` the webhook would have used, so a late redelivery dedupes rather than double-credits. `runbooks/topup-payments.md` §5. |
| `topup_settlement_scan_incomplete` | WORKER_DELIVERY | The payment-settlement sweep spent its walk budget before it reached every tenant. **The verdict covers only the tenants it asked**, so a quiet sweep is not a quiet fleet. | The walk costs one database session per organization, so this is the client list outgrowing one tick rather than a transient failure. The starved tail is stable (the directory is ordered), so the same tenants are skipped every tick until somebody acts. |
| `topup_settlement_sweep_abandoned` | WORKER_TERMINAL | The payment-settlement sweep failed for every tenant it reached, on every attempt. **Until it succeeds, a payment webhook that stops arriving is unwatched** — nothing else in the platform can see that. | Every tenant failing is a database or a deploy, not a client. The next tick is the replay; check worker liveness and the database first. |
| `wallet_alert_no_billing_email` | WORKER_DELIVERY | A prepaid client's calling credit crossed a warning line — low, or run out — and **there is no billing email on their organisation, so nobody was told.** Outgoing calls will stop (or have stopped) with no notice, which is the exact failure the warning exists to prevent. Inbound is unaffected either way. | NOT retried on purpose: the row will be just as empty in five minutes, so this is a DATA fix rather than a delivery one. Set `organizations.billing_email` for the tenant named in the log line, then tell the client directly — the crossing has already happened and the warning is not re-sent until the balance recovers and falls again. |
| `setup_fees_unissued` | WORKER_TERMINAL | The one-time setup-fee job failed every attempt for some tenants. Those clients were not charged. | Re-run; the ledger is append-only, so a second successful run is idempotent by key. |
| `tenant_spend_capped` | CORE_LOGIC | **Outbound calling has stopped for one tenant**: this month's usage reached their effective spend cap. Every dial is refused with `rule=spend_cap` and their campaigns read 'running' while dialling nothing. Inbound is unaffected. | `runbooks/calls-stopped.md` §3 (causes 4-6). Clears on the IST month roll, by raising `plans.hard_cap_*`, or by the client raising their own cap. |
| `tenant_spend_cap_approaching` | CORE_LOGIC | A tenant has used 80% of their effective spend cap. At 100% their campaigns stop dialling silently. | Check the campaign and the ceiling before it gets there — the same screens as above. |
| `ai_platform_brake_tripped` | CORE_LOGIC | Platform-wide dashboard-AI spend crossed the brake. **AI help is now paused for every tenant, and since D-499 for the ADMIN CONSOLE too** — an operator investigating this will find their own assistant refusing with `admin_ai_paused_platform_wide` (a 503 on `POST /v1/admin/copilot/ask`, not a separate ceiling). Calls, campaigns, leads and every admin screen are unaffected. | Clears on the IST month roll. Releasing sooner is a code change to `PLATFORM_AI_BRAKE_INR`. Find the tenant, the operator or the loop that spent it — `platform_ai_usage` names the operator, `usage_events` names the tenant. |
| `ai_platform_brake_near` | CORE_LOGIC | Platform-wide dashboard-AI spend passed 80% of this month's brake. | Same investigation, before it stops. |
| `ai_assist_unmeterable` | CORE_LOGIC | A dashboard assist ran and the provider returned no usage metadata, so it could not be metered. | We paid for something we cannot bill or attribute. Check the provider response shape. |
| `ai_assist_unknown_provider` | CORE_LOGIC | A dashboard assist ran on a provider the meter has no price for, so it was recorded as free. | **Only two providers can reach this meter today, and both are handled — so this firing means a third was added upstream and the meter was not told.** Find it in `extraction.assist_capability`'s ladder. If it bills us, that spend is invisible to the tenant's AI ceiling and to the platform brake, and the assists already run are unbillable: usage_events is append-only, so the correction is a compensating entry, not an edit. Add the model to `billing/rates.py` and the provider to `meter_assist`'s ladder together — one without the other re-arms this alarm or, worse, silences it while still not charging. |
| `kb_gloss_unmeterable` | CORE_LOGIC | A knowledge chunk was translated into English and the provider returned no usage block, so the call could not be metered. | `ai_assist_unmeterable`'s situation on the one AI surface NO CLIENT ACTION TRIGGERS: this sweep runs on a timer, so an outage here is spend accruing with nobody in front of it. Nothing was estimated (D-140) — the gloss itself is stored and the chunk is not re-swept, so this is a hole in the RECORD and not a hole in the work. That spend is invisible to the tenant's AI ceiling and to the platform brake; `usage_events` is append-only, so a correction is a compensating entry, never an edit. Check whether Azure has changed its response shape, and if this fires in a burst rather than singly, consider pausing the cron (remove it from `settings.CRON_JOBS`) until the meter can see the calls again. |
| `kb_ocr_unmeterable` | CORE_LOGIC | A client uploaded a PHOTOGRAPH as knowledge, a model read the text off it, and the provider returned no usage block — so the call could not be metered. | `kb_gloss_unmeterable`'s situation on the upload lane (D-534). The text IS stored and the client's document is not re-read, so this is a hole in the RECORD, not in the work, and nothing was estimated (D-140). The spend is invisible to the tenant's AI ceiling and to the platform brake; `usage_events` is append-only, so the correction is a compensating entry and never an edit. Check whether the OCR leg's provider has changed its response shape (`apps/workers/document_ocr.py`). A burst of these means every photograph uploaded since is unbilled — the interim is to leave uploads working and correct the ledger, because refusing a client's upload is worse than an unbilled model call. |
| `admin_ai_assist_unmeterable` | CORE_LOGIC | An ADMIN-CONSOLE assist ran and the provider returned no usage block, so it could not be metered. | `ai_assist_unmeterable`'s situation on the surface whose payer is US (D-499). It matters more here, not less: the platform brake is the ONLY ceiling the admin copilot has — there is no operator allowance and nothing to sell one — so unmetered spend here is spend with no bound at all. Nothing was estimated. Check whether the provider stopped sending the block; `platform_ai_usage` is append-only, so a correction is a compensating entry, never an edit. If it fires in a burst, the honest interim is to lower `PLATFORM_AI_BRAKE_INR` rather than to keep answering blind. |
| `admin_ai_assist_unknown_provider` | CORE_LOGIC | An ADMIN-CONSOLE assist was answered by a provider `meter_platform_assist` has no price for, so it was recorded as free. | `ai_assist_unknown_provider`, on the platform's own ledger. A third provider was added to `extraction.assist_capability`'s ladder and neither meter was told. Teach `crm/assist.py::meter_platform_assist` AND `meter_assist` together — one without the other leaves half the platform's AI spend invisible — and add the model to `billing/rates.py` in the same change. |
| `platform_secret_unreadable` | CORE_LOGIC | Stored credentials could not be decrypted with this deployment's `PLATFORM_KEK`. | The wrong KEK is deployed. Put the correct one back — do NOT rotate on top of it. |
| `platform_secret_set` | CORE_LOGIC | A platform credential was installed or rotated. **Informational, and deliberately loud.** | If this was not you, treat it as a compromise of the admin realm. |
| `platform_config_never_loaded` | CORE_LOGIC | This process has never read `platform_settings`, so it is running on environment variables alone. | Console changes are not reaching it. Check DB connectivity from that service. |
| `platform_config_stale` | CORE_LOGIC | `platform_settings` could not be re-read; the last known values are still in force. | A console change has not taken. Same investigation. |

### Identity, auth and the request edge

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `unhandled_exception` | ROUTE_HANDLER | An exception escaped every handler and the caller got a generic 500. The exception class is appended to the code. | Always a bug. Find the traceback by the `path` id in the alert. |
| `client_ip_unresolved` | ROUTE_HANDLER | No trusted hop vouched for a caller's address. `audit_log.ip` and per-caller rate limits are degraded. | The edge's real-IP chain (SEC-COMP §5). Usually a proxy config change. |
| `signal_received` | PROCESS_RESTART | A service received SIGTERM/SIGINT and is draining. Expected during a deploy. | Only actionable if unexpected — an unplanned restart is an OOM or a crash loop. |

### Quality

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `qa_sample_draw_failed` | WORKER_DELIVERY | The weekly QA draw failed for some tenants. | Re-run; the draw is reproducible by design. |
| `qa_sample_draw_truncated` | WORKER_DELIVERY | The weekly QA draw ran out of its wall-clock budget part-way through the client directory and stopped. **The tenants it never reached have no sample for the week, so `samples_drawn` is a floor rather than the week's draw.** The walk costs one `tenant_session` per organization because `calls` is FORCE-RLS'd; the budget exists because past `WorkerSettings.job_timeout` arq cancels the tick and retries it from the top, which re-walks the same head of the ordering and never reaches the tail on any attempt. | Not a longer budget — the next stop after it is the job timeout. The directory is ordered by id, so the starved tail is the same tenants every week: draw them by hand for the weeks that matter (`draw_for_tenants` takes an explicit list) and treat the alarm as the fleet having outgrown one tick. |
| `trial_erasure_blocked` | WORKER_DELIVERY | **A client whose trial ended without them buying is past the grace period we agreed, and their personal data is still here.** The nightly sweep (`apps/workers/trials.py`) cannot file the tenant erasure because the account is still commercially OPEN: `compliance/tenant_erasure.assert_erasable` refuses any tenant that is not already `churned`, which is what keeps `organizations.deleted_at` a strict refinement of "account closed". The sweep will not churn an account on a timer — ending a commercial relationship is a human act with a stated reason and a step-up — so it says so instead. The detail names the tenant. | Close the account: admin console → Account state → churned, with a reason (it needs `X-Confirm-Action: close_account:<tenant_id>`). **The next nightly tick then files the erasure by itself** — there is nothing else to do and nothing to re-run. If the client actually DID buy, the trial was recorded with the wrong outcome; that is the thing to fix, and their `erase_after` should be NULL. Until one of the two happens this account is being kept past the period we told them we would keep it for, which is a storage-limitation exposure in its own right (DPDP s.8(7)). |
| `trial_sweep_truncated` | WORKER_DELIVERY | The nightly trial sweep ran out of its wall-clock budget part-way through the client directory. **Every count it reported is a floor**: a trial that ended today may still read as running on a client's screen, and an erasure that fell due may not have been filed. Same walk shape and same budget as the QA draw — one `tenant_session` per organization, because `tenant_trials` is FORCE-RLS'd. | Not a longer budget; the next stop after it is the job timeout. Nothing is lost and nothing is wrong on the money leg — a trial past its end date has already stopped bypassing the credit gate whatever the row says (`billing/trials.TrialState.is_active` asks the clock) — so tomorrow's tick is the repair for a one-off. If it repeats, the fleet has outgrown one tick: the directory is ordered by id so the starved tail is the same tenants every night, and the answer is to narrow the walk to tenants that have ever had a trial rather than to widen the clock. |
| `trial_sweep_failed` | WORKER_DELIVERY | The nightly trial sweep failed for some tenants. One client's error does not stop the others. | Advisory. Tomorrow's tick retries every one of them — both acts are idempotent (`erasure_filed_at` guards the erasure, and an ended trial is no longer `active`). Look at the `trial_sweep_tenant_failed` log lines, which carry the tenant id and an exception, if it repeats on the same client. |
| `qa_sample_draw_abandoned` | WORKER_TERMINAL | The weekly draw failed for every tenant after every attempt. **This week's 5% sample is undrawn.** | Re-run before the week closes, or the sample is lost. |
| `kb_digest_content_refused` | WORKER_TERMINAL | **The aggregate guard refused a weekly knowledge digest**: a distilled pattern, or a line of the rendered digest, named something the agent's own vocabulary does not declare (`apps/api/kb/patterns.py`). The sweep STOPS — no further digests go out — because the guard's premise is that every token was written down before any call happened, and a token that was not is most plausibly caller speech. The detail carries the tenant and agent ids and deliberately never the token (hard rule 6). | This is a CODE defect, not a configuration one, and nothing an operator can retry. Read `apps/api/kb/patterns.py`'s two checks and find what changed: either an aggregator now emits a token from somewhere other than our closed enums / the client's extraction schema, or the digest renderer interpolates a value it did not before. Do not re-run the job until the diff that widened it is understood — the whole point of the refusal is that it declines to send output it cannot vouch for. `tests/kb_aggregate_guard_test.py` is the negative control and should have failed first. |
| `kb_digest_undelivered` | WORKER_DELIVERY | One or more clients did not get their weekly knowledge digest. Two causes, both named in the detail: some sends FAILED, and/or the live-agent fleet exceeded the per-tick ceiling (`KB_DIGEST_MAX_AGENTS`) so the tail of the ordering was never reached. | Failures first: it is almost always the mail transport, so check `/healthz/ready`'s email leg and the `kb_digest_failed` log lines (ids and an exception type, no addresses). Nothing is lost — the digest summarises a closed week and next Monday's tick is the retry — so only act if it repeats. If the detail names the ceiling, the fleet has outgrown the tick: raise `KB_DIGEST_MAX_AGENTS` with the arithmetic in its docstring re-checked against the interval, or split the sweep. **The tail is starved every week until somebody does**, which is why truncation alerts rather than being counted. |
| `kb_digest_sweep_abandoned` | WORKER_TERMINAL | The weekly knowledge digest failed every attempt. **No client received one this week.** | Advisory only — no agent, call, lead or bill is affected, and the window it summarises does not move. Usual causes are the same two the other weekly sweep has: the database or the worker. Re-run by hand if the week matters to a client conversation; otherwise the next tick is the repair. |

### The host backup chain (`HOST_BACKUP`)

These reach the same inbox through `notify.sh` → `alert-to-app.sh` →
`python -m scripts.host_alert`. They come from the host, as `postgres`, outside every
Python process. Full procedures: `runbooks/backup-heartbeat-silent.md`,
`runbooks/database-restore.md`, `runbooks/backup-restore-drill.md`.

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `basebackup_failed` | HOST_BACKUP | `wal-g backup-push` exited non-zero — no new base backup exists tonight. | `runbooks/database-restore.md`. Every hour without one lengthens restore time. |
| `basebackup_prune_failed` | HOST_BACKUP | Old base backups were not pruned. Storage grows; recoverability is unaffected. | Housekeeping — but a full bucket becomes the first alarm. |
| `base_backup_stale` | HOST_BACKUP | The newest base backup is older than expected. | Find out why the timer stopped producing one; restore time is growing meanwhile. |
| `no_base_backup` | HOST_BACKUP | `wal-g backup-list` returned no base backup with a readable timestamp. **WAL alone cannot be restored.** | The most serious backup alarm. Take a base backup now. |
| `backup_list_failed` | HOST_BACKUP | `wal-g backup-list` failed; we cannot tell whether a base backup exists. | Credentials, network or bucket. Treat as unknown, not as fine. |
| `backup_list_unparseable` | HOST_BACKUP | The newest base backup's timestamp could not be parsed. Backup age is UNKNOWN. | wal-g output shape changed; fix the parser before trusting the age. |
| `archiver_failing` | HOST_BACKUP | `pg_stat_archiver` reports the most recent archive attempt FAILED. WAL is not reaching the bucket. | Point-in-time recovery is degrading right now. |
| `archiver_never_succeeded` | HOST_BACKUP | Archiving has never succeeded on this cluster. | The archive command has never worked. Nothing is recoverable past the last base backup. |
| `archive_stale` | HOST_BACKUP | No WAL segment archived recently — the 15-minute RPO (OPERATIONS §5) is being missed now. | Same investigation as `archiver_failing`. |
| `pg_wal_backlog` | HOST_BACKUP | Unarchived WAL is accumulating in `pg_wal`. If it keeps growing the cluster stops accepting writes. | An outage precursor. Clear the archive path before the disk fills. |
| `wal_chain_broken` | HOST_BACKUP | `wal-g wal-verify` reports a gap or a timeline problem. **Point-in-time recovery across that gap is impossible.** | Take a fresh base backup immediately; the gap is permanent. |
| `wal_verify_failed` | HOST_BACKUP | `wal-g wal-verify` could not run. The archive is UNVERIFIED. | Credentials, network or bucket. Unverified is not verified. |
| `wal_verify_unparseable` | HOST_BACKUP | `wal-verify` returned JSON with no recognisable status. The archive is UNVERIFIED. | wal-g output shape changed; fix before trusting the verdict. |
| `walg_unavailable` | HOST_BACKUP | wal-g or its config is missing on this host, so the destination cannot be checked from here. | The check is blind, which is not the same as the backups being fine. |
| `health_db_unreachable` | HOST_BACKUP | The health check could not read `pg_stat_archiver`; the archiver's state is unknown. | Postgres, or the health check's role. |
| `health_pg_wal_unreadable` | HOST_BACKUP | `pg_ls_waldir` could not be read (needs `pg_monitor`); the disk-fill precursor is unmonitored. | Grant the role. |
| `backup_health_gap` | HOST_BACKUP | The health check did not run for a long stretch and has just resumed. **Backups were unmonitored for that period.** | Check the named nights by hand — nothing else looked at them. |
| `backup_timer_missing` | HOST_BACKUP | A backup timer's unit is not installed on this host. That backup is not scheduled at all. | Install the unit (`infra/backup/README.md`). |
| `backup_timer_inactive` | HOST_BACKUP | A backup timer is not armed. Nothing will trigger that backup. | `systemctl enable --now` the timer. |
| `backup_timer_not_firing` | HOST_BACKUP | A timer is armed but has never fired, or has not fired for longer than its schedule allows. | Being enabled is not the same as having run. Check the unit's last trigger. |
| `backup_heartbeat_undelivered` | HOST_BACKUP | Backups are healthy but the external dead-man heartbeat could not be sent. | `runbooks/backup-heartbeat-silent.md`. The monitor will page on the silence — that is the correct outcome. |
| `offsite_pgdump_failed` | HOST_BACKUP | `pg_dump` exited non-zero; no logical dump exists tonight. | The offsite copy is the one that survives a Cloudflare account event. |
| `offsite_dump_shrank` | HOST_BACKUP | Tonight's dump is far smaller than last night's — suspect an RLS-filtered dump, dropped objects, or a truncated write. | **Do not overwrite the previous dump.** Investigate before the next run. |
| `offsite_encrypt_failed` | HOST_BACKUP | `age` encryption failed; the chain refused to upload plaintext. | Fix the identity/recipient; the refusal was correct. |
| `offsite_upload_failed` | HOST_BACKUP | `rclone` could not upload the encrypted dump to the offsite provider. | Credentials or the provider. No offsite copy tonight. |
| `offsite_readback_failed` | HOST_BACKUP | The dump uploaded but could not be read back. **Treat it as absent.** | An unreadable backup is not a backup. |
| `offsite_digest_mismatch` | HOST_BACKUP | The uploaded dump's digest does not match what was written. | Corruption in transit or at rest. Re-upload and re-verify. |
| `offsite_redis_missing` | HOST_BACKUP | No Redis RDB found to copy; in-flight jobs are not covered tonight. | Lower severity — the queue is rebuildable — but a real gap in the night's coverage. |
| `alert_delivery_failed` | HOST_BACKUP | The host alert command exited non-zero: the alert above reached journald and **nobody else**. | Fix `BACKUP_ALERT_COMMAND` / the email transport, then read journald for what was missed. |
| `alert_delivery_unconfigured` | HOST_BACKUP | No `BACKUP_ALERT_COMMAND` and no relay beside the script; host alerts reach journald only. | OPERATIONS §8's pre-launch gate. Configure it before relying on any alarm above. |

### Schedules and certificates

| Code | Stage | What it means | What to do |
| --- | --- | --- | --- |
| `tls_certificate_expiring` | CORE_LOGIC | The certificate nginx is serving for our public hostnames expires in 21 days or fewer. Certbot renews at 30 days, so renewal has been failing for a week or more. | `runbooks/tls-expiry.md`. All four public surfaces expire together. |
| `tls_certificate_unreadable` | CORE_LOGIC | We could not read the origin's certificate at all — a stopped nginx, a wrong `TLS_ORIGIN_ADDRESS`, a firewall change. | `runbooks/tls-expiry.md` §3. Being unable to see the certificate hides an expiry rather than causing one. |
| `campaign_schedule_expired` | WORKER_TERMINAL | A scheduled campaign start was still blocked 24 hours later; the campaign is back in draft. | Read the blocker rules on the campaign screen and re-schedule. |
| `campaign_schedule_unparseable` | WORKER_TERMINAL | A campaign's schedule carries no offset or is not an ISO-8601 instant. The campaign did not start. | Data problem in `campaigns.schedule`; fix and re-schedule. |
| `campaign_schedule_kind_unknown` | WORKER_TERMINAL | A campaign's schedule names a kind nothing reads. The campaign did not start. | Only `one_time` is implemented (FLOWS §5). |
| `campaign_recurrence_unreadable` | WORKER_TERMINAL | A campaign's repeat rule is not usable. The campaign did not start. | Same. |
| `campaign_recurrence_skipped` | WORKER_STALL | A repeating occurrence came due late and was skipped rather than fired. | Deliberate: a late unrequested call is what generates complaints. Check why the tick was late. |
| `engine_credential_not_replaced` | CORE_LOGIC | The voice platform APPENDED the LLM credential we wrote beside the superseded one instead of replacing it, so it now holds several under one name and chooses between them itself. Which key the in-call model leg authenticates with has stopped being a function of anything we do. | Open the vendor console, delete every entry under that credential name except the newest, and re-run `uv run python -m scripts.probe_bolna_providers` to confirm the store now replaces rather than appends. This also answers an open vendor question: their `POST /providers` documents `added` with no `updated`, so replace-vs-append was never written down — if this fires, it is append. |
| `engine_kb_document_missing` | CORE_LOGIC | **The publisher handed this engine approved TEXT and this engine's knowledge base only ingests a FILE.** The port carries both (`KBSourceRef.text` for engines that take prose, `.document` for engines that take a document); an adapter that needs a document and is given `None` refuses here rather than uploading something nobody approved. Nothing was attached and the client's live knowledge is untouched. | This is a DEPLOYMENT fault, not a client's mistake: the renderer is `apps/api/kb/pdf_render.py` and it is imported directly, so the only ways to reach this are a broken install (its `fpdf2` dependency missing from the image) or a future engine wired without a renderer. Check the API container actually has `fpdf2` — `uv.lock`/`pyproject.toml` route to all three Python services in `scripts/vps-deploy.sh`'s path map, so a partial deploy is the thing to rule out. The client can retry the publish once the image is right; nothing needs replaying. |
| `engine_kb_agent_config_required` | CORE_LOGIC | **Attaching knowledge on this engine is a WRITE TO THE AGENT, and the caller did not supply the agent's configuration.** The vendor's partial-update route cannot write the field that references a knowledge base, so the attach must send the agent's whole configuration — and that body cannot be rebuilt from a read-back, because the vendor's own agent object does not declare the spoken AI-disclosure/recording lines or the webhook URL. The adapter refuses rather than sending a body that would silently blank either. | A programming error in the caller, not an operational one: whoever called `attach_kb`/`detach_kb` passed `agent=None`. `apps/api/kb/service.py::_publish_config` is the one place that assembles it. Nothing was attached; no client state moved. |
| `engine_kb_ambiguous_source` | CORE_LOGIC | **A knowledge source named BOTH a document and a web page**, and the vendor's route takes one or the other — *"Provide either `file` or `url`, not both"*. The adapter refuses rather than choosing, because choosing would publish an artefact the publisher did not mean to send. Nothing was attached; the live version is untouched. | A DEPLOYMENT fault and never a client's mistake: `kb/service._publish_payload` sets exactly one of the two per source kind, so this firing means that branch was changed or a `KBSourceRef` was built somewhere else. Read the `kb_uploads.source_kind` of the source named in the log line — a row that is `url` with a `document_key`, or a `pdf` with a `source_url`, is a data defect and the CHECK constraint `ck_kb_uploads_one_origin` should have refused it. |
| `engine_kb_processing_failed` | CORE_LOGIC | **The voice platform accepted the knowledge document, indexed it, and reported the result as `error`.** The document is unreadable to the vendor. The adapter deletes its own upload and re-raises, so nothing is left attached and nothing is billed — but the client's newly approved knowledge is NOT live and the previous version is still serving. | Almost always the document, so look at it first: `apps/api/kb/pdf_render.py` renders it and has three refusals of its own (missing glyphs, oversize, empty) that fire BEFORE upload, so a failure here is something they do not cover. Re-run the publish once — if it fails identically, capture the rendered bytes for **OPERATIONS §2 gate 43b**, which is open on whether `error` ever appears on the read at all. Do not retry in a loop: each attempt is a fresh billed object at the vendor. |
| `engine_kb_processing_timeout` | CORE_LOGIC | **The voice platform was still indexing the knowledge document when our wait ran out** (`KB_READY_TIMEOUT_S`, 180s, polling every 2s). The budget is OURS — the vendor publishes no ingestion SLO — so this does not necessarily mean anything is wrong at their end. The adapter deletes its own upload and re-raises; nothing is attached, and the previous version keeps serving. | First decide whether it is slow or stuck, because the two have different answers. Check the vendor status page and re-run the publish once; a second timeout on the same document points at the document (see `engine_kb_processing_failed`). **If timeouts become routine on documents that eventually index fine, the BUDGET is wrong, not the vendor** — it is an estimate with no measurement behind it, and **OPERATIONS §2 gate 43b** exists to replace it with an observed distribution. Raise `KB_READY_TIMEOUT_S` only from that measurement, never from one impatient afternoon. |
| `engine_kb_listing_incomplete` | CORE_LOGIC | **The voice platform's account-wide knowledge listing could not be read to the end, during a publish.** The adapter needs that listing for one thing: turning the handle an agent references into the id the vendor's DELETE route takes (`_rag_id_of`). A walk that stopped early cannot prove a knowledge base is ABSENT, so rather than report a live object as missing — which would tell a client their old version had been withdrawn when it had not — the publish refuses and the previously approved version stays live. | Nothing is broken on our side and nothing was changed. Check the vendor's status page and re-run the publish; the walk is bounded at 20 pages of 50, so a persistent failure on an account below 1,000 knowledge bases means the vendor is repeating pages rather than paging (the log line names which of the two stopped it). If the account is genuinely that large, the residue is the subject of `engine_kb_orphans_detected` below — an account that big is one nobody has swept. |
| `engine_kb_account_listing_incomplete` | WORKER_STALL | **The daily account-level knowledge sweep could not read the platform's listing to the end, so it can say nothing about what that account holds that no client of ours claims.** This is an alarm about the INSTRUMENT, not about the account: reporting "0 findings" from a truncated walk is exactly the silence the sweep exists to break. Nothing was deleted and nothing is wrong with any client's knowledge because of it. | Read the reason in the alert body. `page_cap_reached` means the account has more knowledge bases than the walk's bound (20 x 50) — a real finding in itself, and the bound is in `engine/bolna.py`. `next_link_no_progress` means the vendor returned a full page carrying nothing new, which is a vendor-side paging fault: re-run `GET /v1/ops/kb-orphans` after checking their status page. `partial_fan_out` belongs to an engine whose documents are per agent and means one agent's read failed. |
| `engine_kb_orphans_detected` | WORKER_STALL | **The voice platform's account holds knowledge bases that no client of ours claims, or we claim ones it does not hold.** Three findings, three different meanings — `unrecorded` (our own file naming is on it, no record of ours names it: a publish whose transaction rolled back), `unclaimed` (nothing attributes it at all: a hand-made upload in the vendor console, or a build older than the naming convention), `stranded` (we believe a client's version is live and the platform does not hold it). The first two are money and a DPDP exposure — one shared account holds every tenant's documents; the third is a client whose agent knows less than was approved. **NOTHING HAS BEEN DELETED and nothing will be, automatically, ever.** | `GET /v1/ops/kb-orphans` for the rows: it names each handle and, where anything attributes it, our source id and tenant. Then decide per row, with the client in front of you. An `unrecorded` object is safe to delete at the vendor ONCE that client's live version is confirmed good — do not try to adopt it by writing a claim row, because the digest and the agent linkage are unknown. An `unclaimed` object is never yours to delete on inference: somebody may have uploaded it by hand during an incident. A `stranded` claim means a republish is needed for that source. If the count is large and sudden, suspect an agent deletion at the vendor and see **OPERATIONS §2 gate 43f**. |
| `kb_orphan_sweep_abandoned` | WORKER_TERMINAL | **The daily account-level knowledge sweep failed every attempt of its retry ladder.** Nothing is watching the voice platform for knowledge bases no client of ours claims, and it will stay unwatched until this cron succeeds — one day, or longer if the cause persists. No client is affected today; the exposure is that a stranded document goes unseen. | The exception class is in the alert body and the attempt count with it. A vendor timeout is the usual cause and the next tick fixes it. If it repeats, run `GET /v1/ops/kb-orphans` by hand — it runs the identical code path on demand and will show the failure interactively. |
| `kb_embed_unmeterable` | CORE_LOGIC | **A knowledge embedding was bought and the provider returned no usage block, so it could not be metered.** The vector was stored and is being used, but the spend is invisible to that tenant's AI ceiling and to the ₹25,000 platform brake — so the two controls that stop AI spend running away are both blind to it. Nothing was estimated: a guessed token count on an append-only ledger is worse than a gap, because it reads as measurement (hard rule 7). | Rare and it should stay rare — treat a repeat as a vendor contract change, not noise. Check whether the embedding deployment is returning `usage` at all (a deployment misconfigured to omit it makes EVERY embedding unmeterable, and the alert then fires per chunk during a backfill). While it persists the ceilings under-count, so read `platform_ai_spend` as a floor rather than the truth, and do not raise a spend cap on the strength of it. The vectors themselves are sound — this is an accounting gap, not a retrieval one, and nothing needs re-embedding once metering is restored. |
| `kb_embed_unusable_response` | CORE_LOGIC | **The embedding provider returned vectors this schema cannot store** — the wrong width, or fewer than were asked for. Those chunks are marked `refused`, which is deliberate and not a give-up: `embedding IS NULL` cannot distinguish "not looked at yet" from "we asked and the answer was unusable", and without the distinction the sweep re-buys the same bad answer every tick for ever. The client is NOT cut off — those chunks stay searchable by their keyword arm; retrieval is degraded, not down. | Almost always the deployment, not the text. We SEND the width we want (`dimensions`) and re-check it on the way back, so a mismatch means the Azure embedding deployment is not the model we think it is, or is ignoring the parameter. Check the deployment named by `azure_openai_embedding_deployment` is `text-embedding-3-small`. Once fixed, the refused chunks need re-queuing — they will NOT retry on their own, which is the point of the state. |
| `kb_embed_width_mismatch` | CORE_LOGIC | **The `kb_chunks.embedding` column is not as wide as the vectors this deployment buys, so the knowledge-embedding sweep bought nothing and ended the tick.** `retrieval/embedding.EMBEDDING_DIMS` both sized that column in migration `dc1aaeeeff02` and is sent as the request's `dimensions`, so this is a DEPLOYMENT CONTRADICTION: the constant and the applied schema did not ship together. It is the preflight, not the failure — without it every purchase in the tick is paid for and then destroyed by `DataError: expected N dimensions`, which rolls back the `usage_events` row written beside it, so the sweep would spend on every tick for ever and record none of it (hard rule 7). Nobody is cut off: published chunks stay searchable by their keyword arm. | Compare the alert's `want` (what the code buys) with `column` (what the database will hold, read from `pg_attribute`). Either the migration that resizes the column has not been applied — run `uv run alembic upgrade head` and check `alembic heads` prints one head — or `EMBEDDING_DIMS` was changed without one, in which case restore the previous value and ship the migration first. A narrowing is the likely cause: these models are Matryoshka-style, so a smaller width is a free re-request at the provider and looks like a one-constant change. Nothing needs re-queuing; the sweep resumes on the next tick and re-embeds anything whose stored `embed_dim` no longer matches. |
| `caller_embed_width_mismatch` | CORE_LOGIC | **The same contradiction on `caller_chunks`**, raised by the caller-data sweep, which ends the whole tick (discovery included) rather than projecting under a schema it cannot fill. Nothing was bought and nothing was projected; recent calls and leads stay keyword-searchable. | `kb_embed_width_mismatch`'s remedy exactly — the two tables are sized from the same constant, so if one fires the other almost certainly will on its own tick, and fixing the width fixes both. |
| `kb_embed_worklist_failed` | WORKER_TERMINAL | **The knowledge-embedding sweep could not read the retention worklist**, after its retry ladder was exhausted. No chunk was embedded on this tick and none was lost — newly approved knowledge simply stays keyword-searchable until a later tick succeeds. | Read `error` on the alert: a database error points at the shared worklist (D-368), not at this feature, and the KB gloss sweep reads the same table, so check whether it is failing too — if both are, the worklist is the subject and this alarm is a symptom. Self-healing once the read succeeds; nothing needs re-queuing. |
| `caller_embed_unusable_response` | CORE_LOGIC | **The embedding provider returned vectors the caller-data store cannot hold** — the wrong width, or fewer than were asked for. `kb_embed_unusable_response`'s condition on the other corpus, and the same deliberate `refused` state: `embedding IS NULL` cannot distinguish "not looked at yet" from "we asked and the answer was unusable", so without it the sweep re-buys the same bad answer every tick for ever. Nobody is cut off — those chunks stay searchable by their keyword arm. The alert names the `scope` (`lead`, `call_turn`, `call_summary`, `caller_memory`) and never a chunk. | Almost always the deployment, not the text: we SEND the width we want (`dimensions`) and re-check it on the way back, so a mismatch means the Azure embedding deployment is not the model we think it is. Check `azure_openai_embedding_deployment` is `text-embedding-3-small`. Once fixed the refused chunks need re-queuing — they will NOT retry on their own, which is the point of the state. If only ONE `scope` is affected, suspect that scope's projection producing empty text rather than the provider. |
| `caller_embed_unmeterable` | CORE_LOGIC | **A caller-data embedding was bought and the provider returned no usage block, so it could not be metered.** Worse here than on the KB corpus: this job bills the PLATFORM ledger (D-503), so the ₹ brake is the ONLY ceiling it has — there is no tenant ceiling underneath it to catch the overspend. Nothing was estimated; a guessed token count on an append-only ledger reads as measurement (hard rule 7). | Treat a repeat as a vendor contract change, not noise. Check whether the embedding deployment returns `usage` at all — one misconfigured deployment makes EVERY embedding unmeterable and this fires per batch during a backfill. While it persists, read `platform_ai_spend` as a floor rather than the truth and do not raise `PLATFORM_AI_BRAKE_INR` on the strength of it. The vectors are sound: this is an accounting gap, not a retrieval one, and nothing needs re-embedding once metering is restored. |
| `caller_embed_worklist_failed` | WORKER_TERMINAL | **The caller-data embedding sweep could not read its worklist or the platform spend counter**, after its retry ladder was exhausted. Nothing was projected or embedded on this tick and nothing was lost — recent calls and leads simply stay keyword-searchable until a later tick succeeds. | Read `error` on the alert. The worklist is `engine_agent_routes`, the same bridge the retention sweep reads, so check whether `retention_sweep_incomplete` is firing too — if both are, the database is the subject and this is a symptom. Self-healing once the read succeeds; nothing needs re-queuing. |
| `caller_subject_key_unreachable` | WORKER_TERMINAL | **Rows of caller data are UNREACHABLE BY A DPDP ERASURE.** `caller_chunks.subject_ref` and `caller_memories.subject_ref` are MACs under a `PLATFORM_KEK`-derived key; `envelope.build_ring` DROPS an undecodable `PLATFORM_KEK_RETIRED` with a log line rather than refusing to boot, and the dropped generation is exactly the one whose rows `caller_refs()` can no longer derive. A §12 request for those people would match nothing and the certificate would report **zero** — a compliance document asserting a deletion that did not happen. Raised by the nightly retention sweep, per tenant, with the number of stranded generations and never a ref. | **Do not answer another erasure request until this is fixed.** Restore the retired `PLATFORM_KEK` value into `PLATFORM_KEK_RETIRED` (it is env-only by construction — `ops/secret_routes.py` and the deployment's environment, never the database) and restart the workers; the alarm clears on the next nightly sweep because the ring can derive the generation again. If the retired key is genuinely lost, the rows cannot be erased by subject and cannot be re-keyed — escalate to the founder, because the remedy is then a tenant-wide scrub of the affected stores rather than a per-subject one, and any certificate issued while this was firing needs re-examining. |

## Metric names

Named recorders in `apps/api/core/alerting.py`. They are the vocabulary an SLO rule may
use; nothing else may emit a metric name (the guardrail fails on a direct
`metrics_log.info("metric", ...)`). There is no scraper yet — these are structured log
lines (DEPLOYMENT §8), so "read the metric" means searching them.

| Name | What it measures | Read it when |
| --- | --- | --- |
| `webhook_ack_ms` | The engine-webhook receiver's ack latency, against hard rule 3's 500ms budget. | `webhook_ack_slow` fired, or after a campaign burst. |
| `tool_ack_ms` | The in-call tool endpoint's ack latency, against TRD §6.2's 100ms budget. | `tool_ack_slow` fired. Deliberately a separate series from the above so neither dilutes the other. |
| `retrieval_ms` | One knowledge retrieval end to end, labelled `provider`, `tier` and `cached` (TRD §6). The label is on ONE series on purpose: hit and miss are two paths to the same answer under one budget, so the blend — what a question costs given this account's real hit rate — is the number worth reading, and the halves are still recoverable by filtering. | A client says the assistant is slow to answer questions about their own knowledge, or before changing anything about the cache: `cached=false` climbing is the store, `cached=true` climbing is Redis or the epoch read. |
| `inbox_handling_ms` | How long one inbound webhook event sat between being recorded and being handled — the receiver's own half of the reliability triad. | Leads or call records are late and `pipeline_lag_seconds` looks healthy: the delay is upstream of the pipeline, in the inbox. |
| `inbox_lag_seconds` | The age of the OLDEST unhandled inbox event at each dispatch tick. A rising floor means the drain is behind, not that one event was slow. | Read it beside `inbox_handling_ms`: handling fast + lag rising is a throughput problem, both rising is a dependency. |
| `pipeline_lag_seconds` | Hangup → lead visible, per stage. The 2-minute SLO. | `postcall_pipeline_stalled` fired, or a client says leads are late. |
| `speed_to_lead_seconds` | Web form submitted → outbound dial placed. FLOWS §4 targets < 60s. | A client says the instant callback is not instant. |
| `outbox_lag_seconds` | How long a side effect waited in the outbox before dispatch. | Anything downstream looks slow. |
| `outbox_dlq_depth` | How many outbox messages are dead-lettered right now. | `outbox_dead_letter` fired. Read beside the console's `deferred` count — the pair is the diagnosis. |
| `extraction_failures` | Post-call extractions that failed, by reason. | Leads are arriving empty. |
| `reconciliation_repairs` | Calls the poller had to fix, by kind. `missing_call` = a webhook we never got; `unfinished_pipeline` = our own bug. | A rising `unfinished_pipeline` is ours to fix. |
| `reconciliation_listing_incomplete` | Polls that could not promise they saw the whole window, by reason. | Any non-zero value means calls may have gone unseen. |
| `webhook_replay_divergence` | A settled transition re-delivered with different bytes. | Deliberately not an alarm. Investigate a sustained rate. |
| `compliance_blocks` | Dials refused by the gate, by rule. `complaint_spike`, `spend_cap`, `dnc`, `no_consent`, … | A campaign stopped and nobody knows why (`runbooks/campaign-stall.md` §8). |
| `campaign_dials` | Contacts rung per dispatch tick, with how many the gate blocked. | The pair is the signal: 0 dialled with 40 blocked is very different from 0 and 0. |
| `dispatch_tick_seconds` | How long one campaign dispatch tick took, against its 30-second interval. | `dispatch_tick_overrun` or `dispatch_tick_overlap` fired. |
