"""Quality — the two surfaces that make D-15 visible (SURFACES §1 and §2).

`report*` is the CLIENT's monthly QA report, stored per run by `make qa-report` and
rendered in-app. `sampling*` is OUR weekly 5% spot-check queue, in the admin console.
One module because they are one control seen from two sides: the report is the claim,
the sample is the evidence.
"""
