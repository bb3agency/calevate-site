# Calevate — Engineering Practices & Executable Governance

Version 1.0 · July 2026 · Decision D-29. Sources: the raghava-organics production
repo's guardrail system (~20 drift/parity/discipline checks in CI — the proof that
this style works for a tiny team) + 2026 industry practice (fitness functions,
trunk-based development, coverage ratchets).

Principle: **rules that matter are executable.** A Hard Rule enforced only by memory
or review will be violated exactly when the codebase grows fastest. Every check below
is a script in CI; docs describe them, they don't substitute for them.

## 1. Git workflow (D-29)

- **Trunk-based**: short-lived branches (hours–days), merged to `main` daily;
  `main` is always deployable (CD deploys it on green CI). No long-running branches.
- **Conventional Commits**, hook-enforced: `feat:` `fix:` `docs:` `refactor:`
  `test:` `chore:` `ci:` — scope optional (`feat(crm): …`). Machine-readable history
  feeds changelogs and makes `git log --oneline` a project narrative.
- **Pre-commit hooks** (`.pre-commit-config.yaml`): ruff check+format on staged
  files, commit-message lint. Fast (<2s) — anything slower belongs in CI, not hooks.
- Small changesets; a PR (even self-reviewed) for anything touching auth, billing,
  or compliance modules — mirrors SEC-COMP §5's two-person-review-while-team-of-2
  self-review checklist.

## 2. The guardrail pack (Calevate's fitness functions)

Adapted from raghava's checks to OUR hard rules. **Critical four ship in M1** with
the first real code; the rest land incrementally with the milestone that creates
what they guard. All run in `make check` + CI; a red guardrail blocks merge.

### Critical four (M1)

| Check | Enforces | Mechanism |
|---|---|---|
| `check:engine-isolation` | Hard rule 2 — only `engine/` sees vendor SDKs/payloads | **import-linter** (`make guardrails` → `lint-imports`), 2 contracts SHIPPED and passing, both in `pyproject.toml`: (1) `calevate_shared` imports no `apps.*`; (2) no business module may **directly** import a VENDOR ADAPTER — `forbidden_modules` is `apps.api.engine.bolna` / `apps.api.engine.fake`, **not** `apps.api.engine`, and `allow_indirect_imports = true`. That narrowing is deliberate: hard rule 2 is about vendor PAYLOAD SHAPES, not about the word "engine". Workers and business modules legitimately need an engine (the post-call pipeline and the reconciliation poller cannot exist without one, TRD §8) — they reach it through the `apps.api.engine` factory, which hands back the `VoiceEngine` Protocol and normalized models. A factory necessarily imports its adapters, so banning the transitive chain would ban having a factory; what leaks a vendor shape is a module NAMING an adapter in its own imports, and that stays banned. Add each new business module to `source_modules` (currently `admin`, `agents`, `billing`, `campaigns`, `compliance`, `core`, `crm`, `ingest`, `integrations`, `kb`, `ops`, `reliability`, `tenancy` under `apps.api`, plus `apps.workers`). The voice-runtime twin is enforced by its own deploy boundary. |
| `check:rls-coverage` | Hard rule 1 — every tenant table has FORCEd RLS | `scripts/check_rls_coverage.py` queries `pg_tables`⨝`pg_policies` in the CI database after `alembic upgrade head`; any tenant_id-bearing table without a FORCEd policy = fail |
| `check:redaction-exposure` | Hard rule 5 — raw transcript text never in default responses | `scripts/check_redaction_exposure.py` — serializer-exposure scan: any Pydantic response model exposing `text` (vs `text_redacted`) outside the role-gated endpoints = fail (raghava's `serializer:exposure-check` pattern) |
| `check:env-parity` | fail-fast config doctrine | `scripts/check_env_parity.py` — `.env.example` keys ⟷ `calevate_shared.config.Settings` fields, both directions; drift = fail |

### Incremental (with the milestone that creates the surface)

| Check | Lands | Enforces |
|---|---|---|
| `check:ledger-immutability` | M1, with billing tables — **SHIPPED** (`scripts/check_ledger_immutability.py`, in `make guardrails` and CI) | Every table in `apps/api/db/registry.APPEND_ONLY_TABLES` — the CONSTANT, never a count in prose, which is what hard rule 4's own commentary asks for — is INSERT-only. Three independent failure modes, so three checks: no `UPDATE`/`DELETE`/`TRUNCATE` against a ledger in raw SQL (over the file text AND every parsed string literal, so implicit concatenation and schema qualification do not hide it); no ORM `update()`/`delete()`/`query().delete()`/`__table__.update()` and no cascade that would delete ledger rows as a side effect (AST, not grep); and each ledger carrying trigger cover that ACTUALLY BLOCKS — enabled, raising, `ENABLE ALWAYS` rather than the default ORIGIN, and firing on UPDATE, DELETE **and** TRUNCATE. A trigger that merely exists is decoration, and an `ENABLE ORIGIN` one stops firing under a plain `SET session_replication_role = replica`. The one bounded exception (D-97's KEK re-wrap) is a file-scoped registry entry that `check_allowances()` re-validates, never a relaxation of the trigger |
| `check:wiring` | **SHIPPED** (`scripts/check_wiring.py`, in `make guardrails` and CI; D-48) — not on D-29's original list, added when the rule proved it needed enforcing | CLAUDE.md's "leave no half-wired feature". FOUR questions, each asked against a live REGISTRY rather than against symbol references: every module declaring an `APIRouter` is mounted by the running app (**including `apps/voice-runtime`, which import-linter structurally cannot see** — D-18's hyphen is not a legal module name, so grimp's package walk misses the service); alembic has exactly one head; every ORM column is named by some executable line; and the deferral list is checked AGAINST ITSELF, so an entry naming a column that has since been wired — or one that no longer exists — fails. That fourth question is what makes `UNWIRED_BASELINE` a deferral list rather than an exemption file: it may only shrink. Deferrals are keyed **per column** and each states what closes it. Deliberately does NOT check read-vs-write (most access is raw `text()` SQL), enum-member reachability, or ARQ jobs — which is `check:job-wiring`'s subject, not an unchecked gap (D-199) |
| `check:image-paths` | **SHIPPED** (`scripts/check_image_paths.py`, in `make guardrails` and CI) — catalogued here after the fact: it ran in both gates and appeared in no row, which `TestMakefileWiring::test_every_guardrail_script_is_named_in_the_catalogue` is for | The production image's builder and runtime stages must agree about WHERE the app lives. `calevate-shared` is an EDITABLE install, so site-packages holds a `.pth` file containing one ABSOLUTE path baked from the builder stage's `WORKDIR`; the runtime stage then copies the tree. It works only because both stages say `/app`. Move either and nothing complains at build time — `uv sync` succeeds, the image builds, `pip list` shows the package — and every process dies at `import calevate_shared`, the first import of all four entrypoints. Decidable from the Dockerfile's syntax; needs no build and no daemon |
| `check:job-wiring` | **SHIPPED** (`scripts/check_job_wiring.py`, in `make guardrails` and CI; D-199) — the half `check:wiring` declines by name | The same "leave no half-wired feature" rule on BACKGROUND work, as a three-way agreement computed from the tree and from the live `WorkerSettings`: a job function under `apps/workers` that nothing registers (it can never run), a registration nothing enqueues (dead code wearing a feature), and — the silent one — an enqueue by a name no worker answers to. arq accepts that enqueue for any string and `Worker.run_job` drops the job with a `function not found` warning nothing in this repo reads, so the outbox row says `published` while the side effect never happened; `notify_hot_lead_whatsapp` shipped exactly that way. Crons are exempt from the second question BY CONSTRUCTION — the schedule is their trigger. A job name the scan cannot resolve FAILS unless recorded in `DYNAMIC_ENQUEUE_SITES` with a reason (one entry: the outbox drain), and each of the four scans states a floor so a scan that has gone blind refuses rather than agreeing with an empty registry — `check:wiring`'s own `blind_spots()` (D-176) is the precedent, followed rather than re-invented. Negative controls in `tests/job_registration_test.py`, which was the previous single-question scan and is now this gate's evidence |
| `check:wiring` | **SHIPPED** (`scripts/check_wiring.py`, in `make guardrails` and CI; D-48) — not on D-29's original list, added when the rule proved it needed enforcing | CLAUDE.md's "leave no half-wired feature". FOUR questions, each asked against a live REGISTRY rather than against symbol references: every module declaring an `APIRouter` is mounted by the running app (**including `apps/voice-runtime`, which import-linter structurally cannot see** — D-18's hyphen is not a legal module name, so grimp's package walk misses the service); alembic has exactly one head; every ORM column is named by some executable line; and the deferral list is checked AGAINST ITSELF, so an entry naming a column that has since been wired — or one that no longer exists — fails. That fourth question is what makes `UNWIRED_BASELINE` a deferral list rather than an exemption file: it may only shrink. Deferrals are keyed **per column** and each states what closes it. Deliberately does NOT check read-vs-write (most access is raw `text()` SQL), enum-member reachability, or ARQ crons (`cron()` takes the coroutine by reference; `tests/job_registration_test.py` covers the real failure — a job enqueued by a name no worker answers to) |
| `check:half-wired` | **SHIPPED** (`scripts/check_half_wired.py`, in `make guardrails` and CI; D-235) — the sweep `check:wiring` names in its own docstring as out of scope | The same CLAUDE.md rule, asked about POSITION and BODY rather than about registry membership. Six questions: a column something WRITES and nothing reads (the read/write distinction `check:wiring` says it cannot make — decided by SQL clause position, with a `CheckConstraint` counted as the reader it is); a `Settings` field nothing consumes outside its own declaration and classification sites; a public module-level function no file references, `__all__` and docstrings excluded because neither is a caller; a stub body (`pass`/`...`/`NotImplementedError`) outside a `Protocol` and without a docstring saying what it stands in for; a broad `except` whose body does nothing AT ALL — no log, no alert, no re-raise, no state change; and a `TODO`/`FIXME`/`XXX`/`HACK` marker, which is a deferral with no closer. Two shrink-only deferral registries on `UNWIRED_BASELINE`'s terms. Exit 2 = REFUSED when a scan cannot see its own subject. Deliberately does NOT check frontend consumption of a mounted route (legitimate and common), whether a recorded metric is anybody's alarm, or whether a read is a MEANINGFUL read |
| `web-check` (frontend gate) | **SHIPPED** (`make web-check` — `pnpm -C apps/web typecheck`, `lint`, `test`; a prerequisite of `make check`, and in CI's frontend job, which also gained `pnpm -C apps/web lint` — previously defined and wired into nothing) — D-53 | The frontend's own behavioural contracts, which `tsc` structurally cannot see: fail-CLOSED vs fail-VISIBLE lookups, verdicts keyed on the server's computed answer rather than a raw status, money as a string that is never parsed, a null cost that renders "we cannot say" and never ₹0. Vitest + jsdom + Testing Library, rendered tests going through the real provider, session and request layer with `fetch` as the only seam. `next build` stays CI-only (too slow for the local gate). It found two live prototype-chain bugs on its first run |
| `coverage:ratchet` | **SHIPPED** (`scripts/check_coverage_ratchet.py`; `make coverage-ratchet` runs the suite under `coverage run` and then scores it, `make check` calls that INSTEAD OF `make test`, and CI's existing test step gained the instrumentation — ONE suite run, never two) | D-29's words are "coverage ratchets not targets"; BACKEND-PATTERNS §9 says where: "scoped to the HIGH-RISK surfaces … rather than a repo-wide number". Both are literal here. **The unit is a COUNT of uncovered units — statements never executed, plus branches taken in only one direction, plus `# pragma: no cover` exclusions — per HARD-RULE SURFACE.** Not a percentage: "the number of bugs is not a function of the *percentage* of uncovered code, but rather the *amount*" (coveragepy#815), and a percentage moves when you delete a covered helper or add 400 covered lines beside an untested branch. Six areas, one budget each, each naming the rule it protects — `tenancy-session` (rule 1), `compliance-gate` (5), `dial-path` (5), `ledgers-and-money` (4+7), `redaction` (5+6), `voice-runtime-ack` (3) — because a single repo-wide number lets coverage bought in a serializer pay for a branch lost in the dispatch gate, and the aggregate never moves. Branch coverage, not line: every rule guarded here is a rule about the branch NOT taken, and a line-only run is satisfied by executing the `if` once. **The gate is an EQUALITY, and that is what makes it a ratchet rather than a target**: above budget is a regression; BELOW budget also fails, because an improvement nobody locked in is headroom, and headroom is where the next deleted test hides (#815's second half — "contributors will have to update the number … to lock in the newer, lower number"). One consequence worth knowing: a budget that is merely EDITED UPWARD fails on the very next run by itself. Raising one is possible and deliberately loud — a `RAISED_BUDGETS` waiver in the script carrying the ceiling, the reason (≥80 chars) and what closes it; `--update-baseline` refuses to write a bigger number without one, `stale_waivers()` deletes it the moment the area improves past it, and `tests/coverage_ratchet_guard_test.py` pins the set so a new waiver costs a diff in a test too. **The area list stays honest by DERIVATION, not memory**: `required_surfaces()` reads the live `APPEND_ONLY_TABLES`, the modules that EXECUTE the tenancy GUC (off the AST, docstrings excluded — a grep matches four modules whose only mention of it is a comment about it), the `Numeric` money columns, `check_compliance_invariants.dial_sites()`, the redaction primitives resolved by import, and every file under `apps/voice-runtime`; a hard-rule surface in no area FAILS the gate. It earned that on its first run, adding two dial surfaces the hand-written list had missed — `crm/routes.py` (the D-21 lead button and callback) and `ingest/service.py` (the instant-lead webhook). It also REFUSES TO SCORE a run it cannot vouch for — a distinct third outcome (exit 2; a verdict-shaped failure is exit 1, both red) rather than a verdict it has no standing to reach, because reporting an artefact as a regression is how a guardrail teaches people to re-baseline past it. Two halves: the REPORT (no branch data, a measurement older than the code, an area matching no file, an area with zero executed statements) and the RUN. The second is evidence, not a flag: the suite is started with `-p scripts.check_coverage_ratchet`, the gate's own pytest plugin, which records into `.coverage-run.json` beside the data file what the run consisted of — outcomes, deselection and every selection option in force, collection errors and module-level skips, whether Postgres and Redis answered a probe at `pytest_sessionstart`, and whether EITHER already held data before the first test — tenant rows (off the live `TENANT_TABLES` registry) or Redis keys. Any of those and the gate scores nothing, and `--update-baseline` writes nothing, which is the half that matters because the baseline file is where a wrong number enters the repo. Those last two are the ones with history: this gate failed CI twice on a two-unit delta, both times "fixed" by copying CI's number into the fixture, and the diagnosis in those commits (a dev database holding 31k test organizations) was WRONG — a freshly migrated and seeded local database still measured 70 and 24. Measuring it properly found three causes, which is itself the argument for refusing rather than guessing: Postgres state; **Redis state**, which nobody had suspected and `make db-reset` cannot reach (`compliance/audit.py:_current_head` queries Postgres only on a cache MISS, so a laptop Redis holding 71,984 keys from previous runs deletes that fallback from the measurement while CI's fresh container always takes it); and machine SPEED, which is not detectable from inside the process at all — `webhook_routes.py:182` raises `webhook_ack_slow` only past the 500ms budget of hard rule 3, so it is covered on a contended CI runner and not on an idle laptop, and that one line plus its partial branch IS the `voice-runtime-ack` delta. The first two became refusals; the third is written down in the script's docstring and the baseline fixture, its durable fix is a test that drives the slow path deterministically (a change to the CODE, not the gate), and until then CI's number is the authority. A FAILING verdict now also prints the uncovered lines of the offending area, because "70 vs 68" is a number and `webhook_routes.py:182` is a diagnosis. What it deliberately does NOT gate on: the skip COUNT, which legitimately differs between a laptop and CI; the counts and the commonest reason are printed with every verdict instead, and the two causes that actually bit us (a service down, a dirty database) are detected at their source. **Measured cost** (this machine, same tree, both runs contended by other work so read them as a ratio, not a clock): the suite is 1110s uninstrumented and 1637s under `coverage run` — about +45%, one run either way. It does not double the suite, which is the constraint that decides the placement: the gate lives in CI's existing test step and in `make check`, and `make test` stays clean for the dev loop. `COVERAGE_CORE=sysmon` would cut most of that overhead and is NOT usable here — "In Python 3.12 and 3.13, it does not support branch coverage" (coveragepy `doc/config.rst`, `[run] core`), and this repo is pinned to 3.12; it becomes a one-line change on 3.14, where sysmon is the default. The check itself (loading the data, reporting, deriving the surfaces) is ~5s. **Baseline as seeded** (2026-08-12): tenancy-session 5 uncovered (96.2% of statements+branches), compliance-gate 70 (94.5%), dial-path 123 (80.2%), ledgers-and-money 49 (95.0%), redaction 13 (94.1%), voice-runtime-ack 24 (92.6%). Two of those six do not reproduce in CI, which is where they count: CI measures `compliance-gate` 68 (its Redis is a fresh container, so the audit-head fallback runs) and `voice-runtime-ack` 22 (its runner is slow enough to breach the 500ms ack budget). Those are the committed numbers, and the evidence behind the refusal described above. Four of the six were bit-identical across two independent full runs; the two that moved moved because the tree did — which is the metric behaving. Those numbers are a FLOOR, not a target, and the first act after any in-flight work lands is `make coverage-ratchet-accept` to click them down. Deliberately NOT: no percentage anywhere in the gate and no `fail_under`; no per-file budgets (every refactor would re-bless the baseline and nobody would read the diff); nothing on `apps/web` (its gate is `make web-check`, D-53); nothing on the engine adapters (the conformance suite is a stronger statement than a line count and `lint-imports` guards the boundary — one way per problem); and no claim that a covered branch is a TESTED branch, since no coverage tool can tell an assertion from a call. That last one is why the ratchet never demands a number the suite has not already earned: the initial budgets are simply what the tree measured on the day it shipped, so its first act rewards nobody |
| `check:alarm-wiring` | **SHIPPED** (`scripts/check_alarm_wiring.py`, in `make guardrails` and CI; D-202) — not on D-29's original list, added when three alarms OPERATIONS §4 promised turned out to have no call site anywhere | The alarm vocabulary, **both directions**, derived from the tree. `alert()` is called five ways and only the first is obvious: a literal code; an f-string whose literal PREFIX is the alarm (`unhandled_exception:<ExcType>`); a `ProblemError` carrying a `failure_stage`, which `core/errors.py` relays into `alert()` verbatim — the shape a reader misses, and the one that pages for `engine_rejected`; a `*_code` on a module constant or a frozen ack meter; and the HOST BACKUP CHAIN in shell, which cannot call `alert()` at all but reaches the same inbox through `notify.sh` -> `host_alert.py`. The check enumerates all five, parses the alarm index in `runbooks/alarm-index.md`, and fails on either half: a documented alarm nothing raises (an operator reads the runbook and stops looking) or a raised alarm with no row (a page at 3am with nothing to look it up in — **44 of 113 codes were in that state** when this landed). It also enforces `core/alerting.py`'s own stated rule that metrics are NAMED RECORDERS, checks metric names both ways, refuses a runbook citation that resolves to no file, and refuses a backticked `snake_case` name in the operator docs that matches nothing in the tree. Two narrowings keep that last one precise rather than noisy: a name a document's own fenced block defines (a SQL alias) resolves, and one composed leading word may be stripped (`f"blocked_{rule}"` is a real string in a log and in no file). **It REFUSES when it matches nothing** (`check_wiring`'s doctrine), on either side and on the shell scan separately. The two call sites whose code is genuinely a runtime value are named in `DYNAMIC_ALERT_SITES` with the codes they raise and the reason, and each claimed code is re-verified as a literal in that file, so a rename breaks the build rather than the index. Negative controls in `tests/alarm_wiring_guard_test.py` |
| `check:image-paths` | **SHIPPED** (`scripts/check_image_paths.py`, in `make guardrails` and CI) | The production image's two stages must agree about WHERE the app lives: `calevate-shared` is an EDITABLE install, so site-packages holds a `.pth` file carrying one absolute path baked from the BUILDER stage's `WORKDIR`. A runtime stage with a different `WORKDIR` gets a venv pointing at a directory that does not exist there, and nothing in the Dockerfile makes the two look related |
| `check:docs-drift` | M2 — **SHIPPED** (`scripts/check_docs_drift.py`, in `make guardrails` and CI) | The docs are authoritative (CLAUDE.md), which is exactly why their MECHANICAL claims have to be executable: an agent reads the doc, runs the command it names, and gets `No rule to make target`. Four questions, each against a live artefact and none of them about prose: (1) every `make`/`pnpm`/`python -m` command written **as a command** in a code span resolves against the real Makefile, the real `package.json` **of the package the command would actually run in** (a script named with no `-C` is a claim about the ROOT manifest, which declares none — a different claim from the same script named with `-C apps/web`, and only the second one runs) and the real tree; (2) every `D-xx` in docs, runbooks, code, migrations and infra names a row of ROADMAP §6, and the log numbers no decision twice; (3) every backticked name in SEC-COMP §3 is still spelled somewhere in `apps/`/`packages/` **outside a docstring** — the rule-name-drift check `check:compliance-invariants` declined and pointed here, and the docstring exclusion is what makes it fire on a rename instead of being satisfied by prose about itself; (4) the §5.4 rate-zone table against the nginx template's `limit_req_zone` directives. **Clause 4 WAS deferred and is now live**: it was written while nginx config had never lived in this repo, `DEFERRED_MIRRORS` recorded the absence, and the deferral was built to FAIL the day a `rate-zones.conf.template` landed anywhere in the tree. `infra/nginx/rate-zones.conf.template` landed, the deferral fired as designed, and `DEFERRED_MIRRORS` is now empty — the §5.4 table is compared against the real `limit_req_zone` directives on every run. This sentence said otherwise for a wave after that happened, which is the same defect class the check itself exists for. Deliberately NOT checked: backticked file paths (measured — the docs legitimately name planned and rejected artefacts, so that class needs an ever-growing allowlist), `uv run <tool>` names, flags, and the reverse direction (a target no doc mentions). First run found live drift: the `gen:api` and `lint` scripts named against the root manifest in five places, and the regression harness named as the module `eval.run`, which has been `scripts.eval` since it shipped. One consequence to know: a doc cannot quote a BROKEN command inside a code span — the check cannot tell a counter-example from an instruction, and the instruction is the reading that costs somebody an afternoon, so write the wrong spelling in prose (raghava's `edge:drift-check` + `docs:runtime-drift-check`) |
| `check:web-env-parity` | **SHIPPED** (`scripts/check_web_env_parity.py`, in `make guardrails` and CI) — not on D-29's original list, added when Clerk sign-in landed several browser keys that nothing guarded | `check:env-parity`'s other half: the WEB tier reads its configuration straight out of `process.env`, answers to no `Settings` class, and was checked by nothing. It is the more dangerous half, because `next build` **inlines** every `NEXT_PUBLIC_*` value — a key that is misspelled, renamed or absent does not throw and does not fail the build, it becomes the empty string in the bundle and surfaces as a broken screen in production. Four questions against the live `apps/web` tree: (1) every variable the package reads is declared; (2) every declared variable is read (the stale direction — a key that survives a refactor is configured in three environments and decides nothing); (3) no `NEXT_PUBLIC_` key is shaped like a secret (`_SECRET`, `_PASSWORD`, `_TOKEN`, a trailing `_KEY`), since nothing with that prefix is private — publishable keys are the legitimate case and each needs a `PUBLIC_BY_DESIGN` entry stating why the VENDOR calls it public, verified live so the registry can only shrink; (4) nothing reaches `process.env` in a form Next cannot inline (`process.env[name]`, a destructure, an alias — all `undefined` in the browser, all type-check clean). **The declarations live in `apps/web/.env.example`, a second file rather than a section of the root one**: the root file is checked both ways against `Settings`, so a real `KEY=` line there fails `check:env-parity` — which is precisely why the browser keys had been written as COMMENTS, a declaration no machine could read. Next also loads `.env*` from the package directory, so the template now sits beside the `.env.local` it is a template for. Two smaller rules keep that file honest: a duplicate declaration is refused (dotenv keeps the last one) and so is an empty declaration whose reader supplies a non-empty `??` default (`??` does not fall back on the `""` a bare `KEY=` line produces, so a copied template would silently override the code's own default). Deliberately NOT: value validation (`@t3-oss/env-nextjs` is the standard tool for that half and is argued and declined in the script's research note — it answers "is the value well-formed", never "does anything still read this key"); `apps/web/tests/` (a test is a consumer, and `signup.test.tsx` ASSIGNS a key, which would keep question 2 green for a key the app had stopped using); non-public reads in server code (a Server Component may legitimately read a secret at request time — the tree has no server module today, and that assumption is stated in the script as the thing to revisit rather than exempted later). Comments are stripped by a small lexer so a renamed key cannot stay "alive" in the prose describing it — the narrowing `check:wiring` and `check:docs-drift` both make. Negative controls in `tests/web_env_parity_guard_test.py` |
| `check:openapi-client-fresh` | M1, with first typed-client use — **SHIPPED** (`scripts/check_openapi_fresh.py`, in `make guardrails` and CI) | regenerating the TS client from the live OpenAPI produces no diff (stale `gen:api` = fail) |
| `check:compliance-invariants` | M2, with campaigns — **SHIPPED** (`scripts/check_compliance_invariants.py`, in `make guardrails` and CI) | static asserts: campaign-launch path calls the compliance gate; `disclosure_line` NOT NULL in schema; DNC propagation job registered |
| `stress:webhook-storm` | M2 — **SHIPPED** as `tests/webhook_storm_test.py` rather than a script, because its subject is BEHAVIOUR under concurrency and pytest already owns that | replay N campaign-completion webhook bursts against intake; assert dedupe, ack-latency SLO, zero duplicate side effects (raghava's `flash-sale-contention` analog) |
| `release:guard` (error-budget gate) | M3+, needs Prometheus | deploy blocked while SLO error budget is burned (raghava's `release:guard` + burn-rate rules; OPERATIONS §4 alerts become recording rules first). **Read `docs/evidence/raghava-platform-teardown.md` §3 before building it**: their version is structurally incapable of failing — the generator that writes the error-budget state file and the guard that reads it run back-to-back in one CI job, `artifacts/` is gitignored so the file never exists, and the generator's Prometheus query has no `PROMETHEUS_BASE_URL` in CI and falls back to a default of 0% consumed. Their burn-rate ALERTS are worse than absent: the annotations claim multiwindow (5m/1h, 30m/6h, 6h/3d) while every expression evaluates one 5m ratio, and `clamp_min(denominator, 1)` pages continuously on any traffic below 1 req/s. Ours takes the SHAPE (recording rules first, promtool-tested rules, budget gates the deploy) and none of the arithmetic |
| `check:raw-sql` | **SHIPPED** (`scripts/check_raw_sql.py`, in `make guardrails` and CI; D-172) — not on D-29's original list, added when a teardown of raghava's `security:sql-injection-guard` showed how much stronger the question could be asked | Hard rule 1 at the layer nothing else watches. Theirs greps for two Prisma API names; ours asks whether the SQL STRING could contain text we did not type. Every `text(...)` in `apps/`+`packages/` (493 statements) must be *literal-derived* — constants, names resolving across module and package boundaries, f-strings/`%`/`.format()`/`.join()`/comprehensions whose parts all are, a call to one of our functions whose every `return` is, or a call to a declared identifier validator. **The half that is not a linter**: when the rule reaches a function PARAMETER it goes to the CALL SITES and checks the argument there, recursively — which is how `plan_in_effect_sql`'s docstring promise ("a caller that passes a user string is writing an injection, and no caller does") stops being a promise. `exec_driver_sql`/`literal_column` refused by name so a second door cannot open silently. Runs in 2.5s after two call-name indexes; no database, no app boot. `tests/` and `alembic/` are OUT of scope with the reason stated. `SPLICE_ALLOWANCES` is empty and meant to stay so. Negative controls in `tests/raw_sql_guard_test.py`, including the two-step one that pins what `_identifier()` buys |
| `check:public-routes` | **SHIPPED** (`scripts/check_public_routes.py`, in `make guardrails` and CI; D-173) — added for the same reason, from raghava's `route-discipline-check` | The complement of `core/rbac.assert_policy_registry_complete`, which is the strongest check here and opens by skipping everything under a `PUBLIC_PREFIXES` entry. A prefix is default-OPEN for whatever is mounted under it next, and `/v1/auth/` is about to hold the whole first-party auth module, which is designed and landing in a parallel slice. So the exempt set is ENUMERATED — ten routes, each with a reason and the credential it verifies in place of a session (Svix signature, Razorpay HMAC, per-source ingest secret, or a verified token with no membership yet), and that credential is checked against the module actually serving the route. Also: a declaration outliving its route fails, a prefix covering no live route fails, and an exempt route carrying `x-calevate-permission` fails, because that label reads as protection in the OpenAPI schema and the generated TS client while the registry never reads it. `tests/public_routes_guard_test.py` mounts a route on the REAL app under a public prefix and proves it is named, and pins the PARTITION: guarded ∪ public = the route table, guarded ∩ public = ∅ |
| `check:idempotency-scope` | **SHIPPED** (`scripts/check_idempotency_scope.py`, in `make guardrails` and CI; D-175) — added after a teardown of raghava's `common/idempotency/idempotency.ts:24-38`, whose scope key falls back to `anon:fingerprint(request.ip)` for an unauthenticated caller | **We do NOT have that defect, and this is what keeps it that way.** An idempotency record is a cache of a completed response, so two callers who can compute one scope means the second is served the first's payload before any tenant-scoped query runs — behind nginx, `request.ip` is our own edge. Four properties: `scope_key` keeps its keyword-only `UUID | None` signature (the load-bearing one — that annotation is WHY mypy strict already refuses a header at every call site, so widening it would retire the protection everywhere at once with no call site visibly changing); `scope_key` is the only producer of `claim_idempotency(scope=)`; its arguments are plain id references, never a call, a subscript or a literal; and no socket peer reaches `claim_idempotency`, `claim_inbox_event` or `enqueue_outbox_once` across api + voice-runtime + workers + shared — wider than `check_audit_ip`, which scans `apps/api` only and stayed green on a sabotage that put `request.client.host` into the voice-runtime inbox key. Zero claim sites or zero producers is a FAIL. `claim_idempotency(key=)` is deliberately NOT guarded: the `Idempotency-Key` header is the caller's to choose by contract, and is safe only because the scope beside it is not. Negative controls in `tests/idempotency_scope_guard_test.py` |
| `check:deploy-workflow` | **SHIPPED** (`scripts/check_deploy_workflow.py`, in `make guardrails` and CI; D-290) — added when `workflow_dispatch` gained a rollback and the workflow became a real caller of the deploy script | A CD workflow and a deploy script are two descriptions of ONE procedure, and the workflow is the copy nobody runs by hand: migrations strictly before the swap, the health-gated swap, one image per commit, `--no-deps` so an `api` deploy never restarts `voice-runtime` (hard rule 3) all live in `scripts/vps-deploy.sh`, and the moment `.github/workflows/deploy.yml` does any of it itself there are two orderings and the unattended one is unreviewed. Six syntax-decidable questions: the workflow INVOKES the script (in command position — the preflight step names the same path inside an `[[ ! -x ]]` test, and a substring search would be satisfied by a workflow that only checks the file exists); no executed command builds an image, runs compose, runs alembic, moves the git checkout, touches pm2 or reloads nginx (`echo`/`printf` lines excluded, because a step that TELLS the operator to run `git checkout main` is doing the right thing); every `--flag` it passes is one the script's own `case` arms parse — the assertion that fires on the ordinary well-intentioned rename; every `secrets.*`/`vars.*` it reads is named in `docs/DEPLOYMENT.md`, since a value that exists only in a GitHub settings page is one the next person to set this up cannot know to create; `bash -n` on every `run:` block, because CI shellchecks `git ls-files '*.sh'` and inline workflow bash is in no such file; and the four safety properties are still present (kill switch in the job `if`, the `workflow_run.conclusion == 'success'` re-check, the `head_branch == 'main'` pin, `cancel-in-progress: false`) — each one line, each deletable by accident, each failure silent until it costs a deploy nobody asked for or a cancellation between the migration and the swap. **Exit 2 = REFUSED** on an absent workflow, no `run:` steps, an argument loop it can no longer parse, or a workflow reading no secrets/vars at all — same third exit code and same reason as `check_metadata_columns` (D-176) and `check_raw_sql` (D-172). Deliberately NOT checked: whether the runner label matches the one registered on the VPS (a fact about a host, not this repo — OPERATIONS §2), and whether the secrets are SET (GitHub is the authority; the workflow's own preflight step reports it with an actionable `::error::`). Negative controls in `tests/deploy_workflow_guard_test.py`, including the printed-command control and all three refusals |

| `check:metadata-columns` | **SHIPPED** (`scripts/check_metadata_columns.py`, in `make guardrails` and CI; P4.3) | `alembic/env.py` autogenerates against `Base.metadata`, so a live column no model declares is a proposed DROP in a diff somebody skims — eight had accumulated. Columns only, both directions; the other 38 live `compare_metadata` diffs are indexes and constraints the ORM deliberately does not declare. **Exit 2 = REFUSED**, added by D-176: an absent TABLE arrives as one `add_table` op rather than one op per column, so against an unmigrated database this printed `OK (61 tables agree)` having compared nothing. Negative controls in `tests/metadata_columns_guard_test.py`, including the empty-database one, which needs no Postgres |
| `check:bootstrap-keys` | **SHIPPED** (`scripts/check_bootstrap_keys.py`, in `make guardrails` and CI; D-95 §4) | The six keys that may only ever come from the environment stay that way. The list is spelled in the script INDEPENDENTLY of `core/settings.ENV_ONLY_KEYS` — a guard importing the list it checks asks the code whether it agrees with itself — and the filter is proved by pushing a store-supplied value through `apply_platform_overrides` rather than by reading it |
| `check:config-applies` | **SHIPPED** (`scripts/check_config_applies.py`, in `make guardrails` and CI; D-101) | Every console-managed setting says WHEN a change takes effect and is BOUNDED. `applies: live` on a key really read once at boot is a lie that costs an outage. The managed set is computed from `Settings` (D-96), so a new field arrives managed and unclassified and fails here on the day it lands. D-176 added the section that runs first: the derived registry can EMPTY itself, and four checks over an empty set are four green ones |
| `check:audit-ip` | **SHIPPED** (`scripts/check_audit_ip.py`, in `make guardrails` and CI; D-131/D-139) | `audit_log.ip` records the CALLER. Eighty handlers read the socket peer inline, which behind nginx is our own edge, so SEC-COMP §5's fourth field was honest-looking and wrong on every audited route. Syntax-decidable. The one permitted peer read is NAMED and must still be there — an allowance that stops matching a live call site fails, which is what stops this check going vacuous |
| `check:session-nesting` | **SHIPPED** (`scripts/check_session_nesting.py`, in `make guardrails` and CI; D-182) | How many pooled connections ONE task may hold at once, and the pool is sized against that number: `max_overflow=1` makes depth 2 survivable and depth 3 a self-deadlock against a saturated pool. The invariant `db/session.py` used to claim — "no code path holds two sessions at once" — was false on the day it was written; fifty functions hold two, and the shape that produces a third is a cross-module call chain (`Depends(db)` → `check_dispatch` → `get_platform_status` → `_read_durable`) no single-file reviewer can see. Resolution goes THROUGH the imports, so `match.start()` is not `experiments.start`, and the openers are asserted against `db/session.py`'s own `@asynccontextmanager` list so a new one cannot make the check silently under-report. Negative controls, including a doctored three-deep tree, in `tests/session_nesting_guard_test.py` |
| `check:model-residency` | **SHIPPED** (`scripts/check_model_residency.py`, in `make guardrails` and CI; D-127) | Every Google model endpoint is Vertex AI `asia-south1`, and the region is a `Final` constant rather than a console field — a residency posture an operator can change from a web form is one a click can invert. No network and no credential; decidable from syntax. Carries its own blindness canary (`MINIMUM_TEMPLATES`): a provenance scan that finds nothing says so instead of passing |
| `check:observability-ready` | **SHIPPED** (`scripts/check_observability_ready.py`, in `make guardrails` and CI; D-169) | A component DECLARED on and silently off reports nothing, which is indistinguishable from a platform that is not failing. Off skips, on-and-broken FAILS by name. Also holds the two export filters hard rule 6 rests on. **Reachability is deliberately never simulated** — that is OPERATIONS §2 gate 15, a live call from a real host. Exit 2 = refused |
| `check:deploy-env` | **SHIPPED** (`scripts/check_deploy_env.py`, in `make guardrails`, CI and `scripts/vps-deploy.sh` before migrations; D-168) | Parity asks whether a key is DECLARED; this asks whether the VALUE behind it is coherent — the two DSNs naming one database through two roles, a `REDIS_URL` a container can reach, a value still carrying `.env.example`'s text. Most refusals are scoped to `APP_ENV != local`, so CI exercises the subset that applies to it and the substance is `tests/deploy_env_preflight_test.py`, which builds a bad `.env` per refusal |
| `check:drill-freshness` | **SHIPPED** (`scripts/check_drill_freshness.py`, in `make guardrails` and CI; D-166) | Restore-drill evidence EXPIRES. Reads the newest `docs/evidence/restore-drill-<YYYY>-Q<N>.md` and refuses one more than a quarter stale, post-dated, unfilled or FAILing. The property that outranks the expiry: it is structurally incapable of PRODUCING that evidence — `ALLOWED_IMPORTS`, `FORBIDDEN_CALLS`, and an AST audit the module runs over ITSELF first thing in `main()`. Today's verdict is NOT RUN, printed on every build. **This is the worked example the D-176 audit of the other nineteen was measured against** |

### Patterns deliberately adopted from raghava

- **Evidence artifacts**: DR drills, stress runs, and the engine-verification
  scorecard write JSON/markdown evidence committed to the repo — auditable history,
  and DPDP/client-facing proof material for free.
- **Contract-proposal flow** for config keys: new Ops-config/env keys go through a
  script that updates example + Settings + docs together (their
  `ops:config-contract-proposal`) — the anti-drift move is making the RIGHT way the
  EASY way.
- **`parity:scorecard`** idea: one command that runs every guardrail — `make
  guardrails`. Ours prints each check's own verdict line and STOPS at the first
  failure, which is make's behaviour and is right for the dev loop (you fix one and
  re-run); it is not a table. **The half we did not adopt is theirs' "evidence
  artifact" column** — `parity-scorecard.js:24-27` calls a file's presence "evidence
  that the gate was recently run", and `artifacts/` is gitignored and regenerated in
  the same CI job, so it proves only that something earlier in this run wrote it. The
  gate that BLOCKS MERGE is CI, where each guardrail is its own step under
  `if: ${{ !cancelled() }}` — so one red guardrail reports its own failure and the
  other nineteen still run and report theirs. That is the property their
  `ci:reliability-gates` loses by being a single 23-command `&&` chain, and
  `tests/coverage_ratchet_guard_test.py` bans `continue-on-error` from the workflow
  outright so it cannot be regained by making a step ignorable.

### Explicitly NOT adopted (and why)

- **core-drift / core-sync / release-train** (their template-repo→client-repo
  versioning): Calevate is ONE multi-tenant product, not a repo-per-client agency
  model — there is no template repo to drift from. If a white-label tier ever
  un-defers (ROADMAP §7), this machinery is the reference. Read the teardown's §6
  first: `check-core-purity.mjs` exits 0 with "skipping purity check" when its
  denylist file is missing and again when it is empty, so the guard that stops one
  client's brand overwriting another's is disabled by deleting one file.
- **Design-token contract check**: no cross-repo design system to protect yet.
- **`serializer:exposure-check`** as they wrote it — a hardcoded token list over four
  named service files, where one rule's "required" token is `return {`. We took the
  IDEA and `check:redaction-exposure` walks the live OpenAPI schema transitively
  instead; adopting the mechanism too would be two checks answering one question.
- **`route-discipline-check` / `admin-layer-drift-check`** as mechanisms: their
  question is ours and `core/rbac.assert_policy_registry_complete` already asks it
  against the LIVE route table at boot, which their regex over `.routes.ts` cannot.
  What we took instead is the thing they got wrong — an exemption list nothing
  re-validates (`check:public-routes` above, D-173).
- **`deep-endpoint-smoke`** (hit every route, fail on 5xx): a good idea for Fastify
  and a thin one for us — FastAPI answers a malformed body with a 422 before any
  handler runs, so the class it catches is mostly already caught. Theirs is also
  shallower than its name: CI sets no `ADMIN_OTP`, so `loginAdmin()` returns null and
  the entire `/api/v1/admin/**` surface is "smoke tested" as returning 401.
- **`slo-burnrate-simulate`**: it re-declares the thresholds as JavaScript constants
  rather than reading `slo-rules.yml`, prints, asserts nothing, and always exits 0.
  A simulation that cannot disagree with the rules it simulates is a print statement.

## 3. Dev loop conventions

- `make dev` / `make check` stay the only two commands a session needs; every new
  routine task gets a Makefile target (discoverability beats memory). `make web-check` is
  the frontend third of `make check` and exists separately only so a frontend-only change
  can be gated without a database.
- Fixtures over mocks-of-our-own-code: fake-engine adapter for pipeline work
  (DEV-SETUP §3), golden-transcript fixtures for extraction, recorded Telugu
  utterances for the regression harness.
- Windows-first dev reality (this machine): scripts must run under Git Bash/cmd;
  no bash-isms in Makefile recipes that Windows make can't run; see the
  scaffold-gotchas memory for the uv/pnpm traps already found.
- AI-agent hygiene: rememory search before non-trivial work; store decisions after;
  the docs set is authoritative and drift-checked (see §2), so agents cite docs
  rather than re-deriving.

## 4. Release discipline trajectory

M1: green `make check` + guardrails = deployable; CD auto-deploys main (DEPLOYMENT §3).
M2: + regression harness red blocks promote (OPERATIONS §3 wiring); webhook-storm in CI.
M3: + error-budget release gating once Prometheus recording rules exist.
Always: migrations reversible, RLS in-migration, two-step column deprecation
(hard rule 8) — checked by `check:rls-coverage` and migration review.

Cross-references: root CLAUDE.md (hard rules) · OPERATIONS §3 (regression harness) ·
DEPLOYMENT §3 (CD) · SECURITY-COMPLIANCE §5 (SDLC) · ROADMAP D-29.
