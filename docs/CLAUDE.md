# CLAUDE.md (docs) — moved

The full operating manual now lives in the root `CLAUDE.md` (merged July 2026 so
Claude Code loads one canonical file: hard rules, commands, conventions, domain
vocabulary, plus the rememory memory-system instructions).

- Root `CLAUDE.md` — the manual for Claude Code.
- `docs/AGENTS.md` — the same manual for other coding agents (open standard).
- `docs/README.md` — the blueprint reading order. `docs/` remains authoritative
  and wins over the manual when they conflict; flag conflicts, don't silently pick.
