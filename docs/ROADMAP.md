# Calevate — Roadmap, Build Order & Decision Log

Version 1.0. No calendar dates (by choice); order and gates are binding. Guiding rule:
**client #1 live on a partly-manual stack beats a finished platform with zero clients.**

---

## 1. Milestone 0 — Unblock (parallel, non-code; start immediately)

- Decide legal entity (BuiltByThree entity vs new LLP) — blocks DLT. [Risk R-01]
- DLT PE registration (₹5,900) + telemarketer/header registrations once entity exists.
- **Bolna Pilot** (Engine Verification Session, OPERATIONS §2, per D-31) — closes the
  open engine unknowns (BYOK fee, webhook behavior, Telugu, latency), confirms primary
  engine, gets commercials in writing.
- Sarvam account + ₹1,000 credits; a GCP project with Vertex AI enabled in `asia-south1`
  plus a service-account key (D-127 — an AI Studio API key is disqualified, not a
  substitute); Exotel/Vobiz rate card in writing.
- Reserve calevate.tech DNS zones (admin., app., api.), Clerk apps (two realms), DO BLR
  project, R2 bucket, GitHub org + repo skeleton, Terraform state.
- Sales asset #0: the live Telugu clinic demo agent on a real number (can be built on
  the Bolna dashboard alone — no Calevate code required; doubles as pilot Gate 3
  material). This is also the pitch tool.

Gate G0: engine scorecard passed + demo agent taking calls.

## 2. Milestone 1 — Client #1 Live (thin vertical slice)

Build ONLY what one client needs; admin can be CLI + SQL where UI would slow us:
- Repo: monorepo (apps/web, apps/api, apps/voice-runtime, apps/workers, packages/shared).
- DB core: organizations, users/memberships/invitations, agents(+prompt_versions,
  extraction_schemas), calls/transcript_turns/call_extractions, leads, usage_events,
  plans, consent_ledger, audit_log — with RLS from the first migration.
- VoiceEngine adapter (Bolna) + webhook receiver (source-IP-verified, idempotent —
  TRD §5) + reconciliation poller (the guarantee of record, D-31).
- Post-call pipeline: recording copy → redaction → extraction → lead upsert → metering →
  hot-lead notification (email first; WhatsApp next).
- Client app v1: login/invite accept, dashboard (calls, outcomes, sentiment), call detail
  (redacted transcript + recording + summary + extracted fields), Leads table (schema-driven
  columns, status, filters, CSV export, owner-role "Call this lead" with optional per-call
  context note — D-21; same compliance pre-checks as webhook dispatch).
- Admin v1: new-client wizard steps 1–5 minimal + test-call gate; KB upload+approve;
  publish/promote.
- Regression harness v1: the mandatory five scenarios + fixture replay + report.
- Ops: Sentry, LLM tracing, alerts that reach a person, backups, pre-launch checklist run.
  — **OpenTelemetry tracing shipped and it crosses every boundary** (BUILD-LOG §24;
  `apps/api/core/observability.py`): `TracingMiddleware` opens the server span and only
  trusts an inbound `traceparent` where the caller is ours — voice-runtime's receiver is
  reachable from the internet, and a stranger's sampled flag is a lever on our trace
  bill — and the W3C traceparent then rides the ARQ payload (`TRACE_KWARG`), so
  voice-runtime → Redis → worker → adapter → Postgres is ONE trace and "where did the two
  minutes go" is answerable. The pipeline and the webhook fast path carry named stage
  spans; span attributes are an ALLOWLIST, not a denylist, because a denylist on a
  tracing API fails open (hard rule 6). **Sentry shipped**, config-gated, with the
  `scrub_event` redaction hook that is not optional. **Langfuse and PostHog config was
  REMOVED, not wired** (D-49): three settings with no client are worse than none, because
  the next reader assumes traces are being recorded. Per-call token cost and the latency
  breakdown TRD §2 describes remain a stated gap; restoring Langfuse is a vendor decision
  plus a decision-log entry, not a wiring job — and it now inherits a REQUIREMENT rather
  than a hook: hard rule 6 is enforced by `_RedactingSpanExporter`, which every span leaves
  through, so a direct Langfuse SDK client would be a second UNFILTERED path. The
  hand-called `redact_trace_payload` this bullet used to name is deleted (D-61).
  **Alerts now reach a person** (D-49): `alert()` still writes the structured ERROR log
  first and unconditionally, and then delivers by email through the same transport as
  hot-lead notifications, with per-fingerprint repeat suppression (15 min) and a global
  token bucket, off the request path on a daemon thread and touching neither the outbox
  nor Redis. It needs `ALERTS_EMAIL` + an SMTP host to be configured; a deployment with
  neither says so at boot. **Backups: the mechanism ships, unapplied** (D-50) —
  `infra/backup/` carries wal-g continuous archiving with `archive_timeout = 300`, a
  nightly base backup, an independent offsite encrypted dump, a four-check watchdog and
  systemd timers, with every secret a reference. Nothing has been applied and no wal-g
  command has been run, so the pre-launch checklist's "backups verified" is ticked by the
  drill (`runbooks/backup-restore-drill.md`) passing once, not by this tree existing.

- **Inbound receptionist is the headline capability (D-38)** — it is Outpero's confirmed
  unbuilt flank and is consent-clean by construction (the caller initiated), so it also
  de-risks the self-serve motion. Build and demo it first.
- **Schema-for-scale (D-39):** credits/prepaid ledger, `plan_tier` on organizations, and
  invitations ship in M1 even though the self-serve UI does not — metering and tenancy
  are not retrofittable (D-12).

Gate G1: a paying client (pilot pricing allowed) live on inbound; lead-in-2-min SLO met
for 7 consecutive days; zero cross-tenant test failures.

## 3. Milestone 2 — Outbound + Multi-Client

- Campaigns end-to-end with the compliance gate (blocked features visibly explained);
  instant-lead-callback webhook ingest (Meta/website/Sheets); retry/escalation; DNC.
  — **shipped** (BUILD-LOG §30, §31, §32, §41, §47). **Escalation after retry exhaustion
  now shipped too**: a contact whose dial ladder is spent enqueues
  `escalate_campaign_contact` (registered in `WorkerSettings`, once per contact, its own
  retry ladder), which runs through `check_dispatch` itself rather than a
  messaging-shaped copy of the rules, and every non-delivery is recorded on the lead
  timeline — "we never followed up" is a fact the client can see. What it cannot do is
  send: see the WhatsApp bullet below. Meta Lead Ads ingest is now NATIVE
  (`GET|POST /hooks/v1/ingest/meta/{webhook_id}`) rather than "a webhook you could point
  at us", with one honest hole — the Graph read that carries the form answers needs a
  Page token this deployment does not hold (SURFACES §2b).
- WhatsApp follow-up + hot-lead WhatsApp alerts. — **the TRANSPORT is the single thing
  blocking a finished feature, and as of this pass it is the only thing**
  (`apps/workers/whatsapp.py`: the `WhatsAppTransport` protocol, a console dev sink, the
  delivery record and the retry ladder). This bullet used to read "still seam only",
  which was true of the transport and quietly false of everything upstream of it:
  `enqueue_hot_lead_whatsapp` had NO CALLER, so the seam was not merely unfinished, it
  was unreachable — flipping a BSP on would have delivered nothing and looked
  configured. `notify_hot_lead` now calls it on the same detection that sends the email,
  and the email dedupe carries a `channel` predicate so the two do not eat each other's
  delivery record. Deliberately NO vendor adapter: §6 contains no decision choosing a WhatsApp
  Business Solution Provider, and an adapter written against an imagined API looks
  finished. Messages are template-name + locale + positional variables — the module has no
  free-text body field at all, because a business-initiated message needs an approved
  template. What DID land is the consent the send needs: the `messaging` purpose on
  `consent_ledger` (migration `c2f7a91b4e63`) plus
  `POST /v1/compliance/messaging-consent`, because **consent to be CALLED is not consent
  to be MESSAGED** and nothing was backfilled from one to the other. So the escalation
  path is complete end-to-end except for the transport, and a contact who has opted in is
  refused today as `whatsapp_disabled` (the channel stays off until a human finishes the
  WABA/template checklist) and, once it is switched on, as `no_provider_configured`.
  Picking a BSP is a decision-log entry, and it is what unblocks this bullet.
- One-click AI callback on needs-follow-up calls (D-21): re-dispatch the agent with
  prior-call context via the engine's context-injection webhook. — **shipped**
  (BUILD-LOG §35).
- Outbound CRM sync (D-23): HMAC-signed outbound webhooks + Google Sheets sync
  (lead.created/updated, call.completed events; config table shipped in M1).
  — webhook half **shipped** (BUILD-LOG §34); **Google Sheets sync shipped**
  (BUILD-LOG §47) — `POST /v1/integrations/endpoints/sheets`, delivered through the SAME
  job as the webhook half so it reuses the delivery id, the retry ladder and the
  exhaustion alert rather than minting a second definition of "we delivered a lead", with
  column order held in a JSON array (JSONB does not preserve key order) and an event with
  no declared order REFUSED rather than guessed. **The vendor adapter is now built too**
  (`apps/workers/google_sheets.py`, `GOOGLE_SHEETS_PROVIDER=service_account`): a service
  account the client shares their own document with — chosen over an installed-app OAuth
  flow because sharing is per-document, revocable by the client without us, and holds no
  per-tenant refresh token — minting its own RS256/JWT-bearer token and calling
  `spreadsheets.values.append` with `RAW` + `INSERT_ROWS`. No SDK was added; `pyjwt[crypto]`
  and `httpx` were already in the lockfile. Sheets has NO idempotency key, so a retried job
  reads the delivery-id column before writing and reports `already_present` rather than
  duplicating a row, and a probe that cannot read refuses to write at all.
  `sheets_delivery_available()` remains the one selector both the config route and the
  worker ask, and a deployment with no credential still refuses the endpoint at creation
  rather than accepting rows into silence. **Still owed: a live confirmation against a
  real Google project** — nothing in CI has spoken to Google (OPERATIONS §2 gate).
- Billing surfaces: usage panel (client), margin panel (admin), invoice generation,
  Razorpay links, caps UI. — usage + margin panels **shipped** (BUILD-LOG §36),
  invoice generation **shipped** (BUILD-LOG §40); the **caps-editing UI shipped**
  (client-settable `plans.client_cap_min`/`client_cap_spend`, effective cap =
  `LEAST(admin, client)`, `GET`/`PUT /v1/billing/caps`, SURFACES §2b) and the
  **value-tier rate column shipped** (`plans.overage_rate_value`; NULL = bill everything
  at `overage_rate`, which is every existing plan — **the retail number itself is an
  OPEN founder decision**, deliberately not derived from TRD §10.1's unmeasured bands).
  **Server-side order creation shipped** (D-98) and this line said the opposite for a
  wave: `PROVIDER_CREATES_ORDERS` is True, `RazorpayOrders.create_order` exists and
  `POST /v1/orders` is mounted, creating the order server-side so
  `notes.calevate_tenant_id` reaches the provider by construction. The payment CAPABILITY
  is expressible through one `payment_capability()` selector shared by the intent route
  and the receiver, refusing in problem+json and writing nothing. **What is still not
  true is that anyone can pay**: `PaymentCapability.creates_orders` is False on every
  deployment for a different reason than it used to be — `no_api_secret`, carried
  separately from `reason` so "why can I not pay" and "why is there no order" stay
  distinguishable — and no checkout WIDGET was built at all (hard rule 9; the signed
  webhook is the source of truth, never the browser callback). So `provider_order_pending`
  still stays true in practice, on a credential rather than on missing code (SURFACES §2c). Two additions since: every money reader resolves the plan **in effect**
  rather than the newest row (D-46), and ops can re-derive a stuck cap flag —
  `POST /v1/ops/tenants/{tenant_id}/spend-cap/recompute`, which recomputes and never
  writes the flag directly, closing the dead end where a client raised past their ceiling
  stayed stopped until they acted themselves or the IST month rolled over.
- Admin polish: onboarding wizard complete, client health overview, prompt rollback UI.
  — prompt rollback UI **shipped** (BUILD-LOG §33, §40, §41), and the **client health
  overview now genuinely has** — this line previously claimed it on the strength of
  `GET /v1/admin/tenants`, which is the client DIRECTORY (roster + counters) and answers
  "who are my clients", not "which of them is about to churn". Both surfaces carried the
  title; only one carries it now, and the directory's summary was renamed rather than left
  to compete. The board is `GET /v1/admin/client-health` (`org:read`, admin realm,
  `/admin/health`): an EXCEPTION report of live accounts with at least one signal, most
  broken first, and an account with nothing wrong is absent rather than green. FIVE
  signals, each one an operator can act on today — `calls_stopped`, `outbound_blocked`
  (composed from the dial gate's own predicates, so it cannot disagree with the refusal
  the client sees), `spend_cap_near`, `deliveries_failing`, `knowledge_waiting`. Latency
  and answer-rate tiles from SURFACES §1 are deliberately NOT on it: `calls.latency` was
  dropped in `f1a7c39d5be2` and D-49 removed the trace config, so neither can be observed
  honestly and a tile would be a fabricated number. The call trend carries a `basis`
  (`measured` | `too_new` | `no_baseline`) for the same reason `after_hours_basis` exists,
  because "your calls collapsed" said about a four-day-old account is an accusation the
  data does not support. Cross-tenant with NO RLS policy widened: the directory under
  `app.admin`, then each tenant's own session (`admin/health.py` states the measured cost,
  ~6.5ms per account, rather than asserting it is cheap). The **intake step shipped as an
  API** (BUILD-LOG §45;
  `GET|POST /v1/admin/tenants/{tenant_id}/agents/{agent_id}/intake`, FLOWS §1's eight
  fields and no invented ninth, writing the `compiled_t0_context` D-39 had reserved) —
  and **the wizard now calls it**: `/admin/new` is three steps (account → intake →
  invite), with the draft held above the step swap so answers survive walking forward and
  back, a failed prefill GET WITHHOLDING the form rather than showing an empty one to
  retype over, and the eight fields with no invented ninth. Number provisioning and the
  test-call gate stay absent rather than stubbed, because they are pilot-gated and a stub
  for either would be a screen that lies. **One honest hole, stated on the screen**:
  ~~`save_intake_draft` … NO ROUTE EXPOSES IT~~ — **CLOSED, and this paragraph was
  stale for several sessions.** `POST …/intake/draft` exists (`admin/routes.py`), and
  `IntakeStateOut` carries both `language_primary` and the sheet's `agent_id` provenance;
  all three landed in `60944da`. Kept struck through rather than deleted because the entry
  is now evidence of a defect class this repo produces: a doc that describes a hole
  somebody already filled sends the next session to rebuild it. A survey caught this one
  by grepping for the route instead of trusting the sentence.
- Vertical template #2 fully dressed (whichever client #2 is: clinic or real estate).
  — **real estate shipped** (not written up in the BUILD-LOG; the artefacts are the
  evidence): the `VERTICAL_TEMPLATES["real_estate"]`
  extraction schema AND the half that makes it a template rather than a column list —
  its own `re_*` golden transcripts covering the mandatory five, with
  `tests/vertical_real_estate_test.py` failing when schema and fixtures drift apart. A
  field nothing scores is a column no model is ever measured on.

- **Self-serve tier goes live (D-34):** signup + Google OAuth, credit top-up, number
  purchase with KYC gate. Ships WITH its R-11 mitigations — fixed calling hours, DNC on
  every path, mandatory disclosure, consent ledger, spend caps, and manual review of each
  account's first campaign. No self-serve calling before those exist.
  — signup, the top-up intent and the payment receiver **shipped as surfaces, not as a
  checkout** (SURFACES §2c: `self_serve_signup_enabled` defaults OFF, and server-side
  order creation is not built). **KYC shipped** (D-47), and it was the second-to-last
  mitigation with nothing behind it. Of the R-11 list, five hold in code today
  (platform-fixed calling hours in `within_calling_hours`, DNC on every dispatch path, NOT NULL disclosure, the
  consent ledger, admin + client spend caps) and **the sixth now does too**: the
  first-campaign manual-review hold ships as `first_campaign_reviews`, scoped to the
  TENANT so it cannot be skipped by launching a second campaign or deleting the held
  one, enforced in both `launch_blockers` and `dispatch_blockers`, released only through
  the audited ops route. All six R-11 mitigations are therefore in code — which removes
  the build-side objection to turning `self_serve_signup_enabled` on, though the switch
  itself stays a founder call. The discovery half shipped with it:
  `GET /v1/admin/compliance/holds` is the ops work list of accounts waiting on a human,
  covering BOTH gates, so neither mitigation depends any longer on the client emailing to
  say they are stuck.
- Integration DX (SURFACES §2b): webhook activity view, test-webhook, "Needs attention"
  queue, two-speed publishing. — activity view, test-webhook and the attention queue
  **shipped** (BUILD-LOG §37, §38, §39); **two-speed publishing shipped** (BUILD-LOG §47)
  — admin `POST …/apply` (staged version as the CAS token) and `POST …/undo`, the
  per-agent call cap on the same page, and the client-side unsaved-changes banner read
  from `GET /v1/agents/{agent_id}/pending`. Pending is DERIVED from two pointers, never
  stored as a flag that can disagree with them.

Gate G2: 2–3 clients incl. one outbound campaigner; DLT template approved and a
compliant campaign completed; margin per client visible and ≥ target.

## 4. Milestone 3 — Moat Features

- Managed RAG/memory service (D-28) in the call path: in-call retrieval moves off the
  engine's built-in KB onto the provider (custom tool / thin endpoint, whichever the
  bake-off measured faster); T0 compiled-context automation; knowledge-gap report
  driving KB updates. CRM-side provider integration lands earlier (M2, with the
  bake-off).
- Regression suite growth to 50–100 scenarios/client + red-team set; client-facing QA
  report (sales asset). — **both shipped.** 110 golden cases (58 clinic / 52 real estate)
  of which 30 are red-team, across disclosure suppression, direct AND indirect prompt
  injection (OWASP LLM01:2025 — the indirect form is the one that reaches an extraction),
  PII exfiltration, DNC evasion in both directions, extraction poisoning and degenerate
  input. One executable honesty rule governs the set: `test_every_red_team_case_bites_
  without_a_model` requires each case to carry at least one assertion our own code scores
  against the real attack text, because the offline stand-in is not an LLM and a case whose
  only failure mode is "a model was persuaded" cannot be scored today. `must_not_say` stays
  reference-only until OPERATIONS §3's replay-into-call exists, and says so where a run
  enforces it. **QA report**: `make qa-report CLIENT=<slug> VERTICAL=<...>`, Markdown,
  deterministic, headlined by the DEFECT COUNT rather than a pass rate — defects map onto
  `NON_WAIVABLE_KINDS`, the one promise the harness keeps, whereas a raw pass rate against
  the offline model underclaims a client's agent. Aggregation IS the redaction (per-case
  rows would carry titles that paraphrase caller content), and `basis` follows
  `after_hours_basis`: no percentage under 12 scenarios, no trend from one month. The
  month-over-month comparison is a stated seam, not a half-wired field.
- Call-corpus embeddings (learn from resolved calls). — **not built**, and blocked by D-28:
  RAG is a managed API service and no provider is chosen.
- A/B greeting/script testing with conversion attribution. — **shipped for OUTBOUND**
  (BUILD-LOG §54). The variant a call ran is a STORED FACT written in the same transaction
  as the `calls` row, never re-derived — the split is mutable, so a read-time recompute
  silently re-attributes history the moment anyone ramps the weights. Wilson score
  intervals per arm and Newcombe's hybrid interval for the difference, with
  `MIN_CALLS_PER_VARIANT = 40` taken from Fagerland/Lydersen/Laake rather than from taste;
  below it no comparison is published at all, and only a `winner` verdict gets a badge.
  **Inbound is attributed by FACT or not at all** (D-60). `workers/pipeline.py` now
  records the arm when the engine reports that an ARM's own agent ran the call — its own
  ref, so no inference — and records nothing when it reports the agent's. An inbound
  caller reaches the client's number, which answers with the agent (FLOWS §3), so in
  today's provisioning they carry no arm; drawing a bucket for them after the call would
  credit a script they never heard. An inbound-only agent is still refused, for that
  reason rather than for a missing lookup, and `attributed_directions` is now READ from
  the assigned calls instead of being the literal `("outbound",)`. The same lookup closes
  a real outbound gap: a webhook that beat `dispatch_call`'s commit used to leave an
  arm-dialled call carrying no arm at all. Because an arm can now be credited with a call
  nobody dialled, each arm's counts are resolved by DIRECTION (D-62): `outbound_dialled`
  is the calls we placed, and `inbound_attributed` says how much of the rate's
  denominator was never split — a number, so the screen can qualify the rate it renders
  rather than leaving it to the coverage sentence.
- Client staff roles, lead assignment, lead_events timeline. — **all three shipped**
  (BUILD-LOG §53, §54). Assignment closed `Lead.assigned_to`, an `UNWIRED_BASELINE` entry
  since the first migration; the timeline PROJECTS `lead_events.payload` key-by-key rather
  than serialising a schemaless column six producers write. Staff roles complete it: a
  client owner can now invite, re-role and remove, where every membership change used to be
  an operator running SQL. The last owner cannot be removed or demoted, and the guard locks
  the whole owner SET rather than the target row — EvalPlanQual is blind to rows the query
  never selected, and a `count(*) > 1` check lets two concurrent demotions both commit to
  zero owners, which is a tenant recoverable only by hand.

Gate G3: retrieval T3 p95 ≤ 100ms measured; QA report shipped to every client monthly.

## 5. Milestone 4 — Phase-2 Engine (own orchestration) [enter only on trigger]

Trigger (pre-committed in TRD §10): sustained > ~10–15k min/month across tenants OR a
feature blocked by rented pipeline (T1/T2 tiers, custom turn-taking) OR engine risk event.
- Runtime behind the same VoiceEngine interface; Vobiz/Exotel SIP direct; models
  unchanged (the Sarvam in-call stack; Gemini is post-call only and on Vertex AI
  `asia-south1` per D-127, so a self-hosted runtime does not touch it). Two candidates, decided at the gate on measured
  evidence, not now (D-32): **Pipecat on CPU droplets** (D-02's original plan; first-party
  Exotel serializer + Sarvam plugins; we build the per-call dispatcher and scale-out) or
  **LiveKit Cloud** (ap-south Mumbai region verified; first-party Sarvam plugin; turn
  detection + barge-in in-SDK; managed scaling — but concurrency tiers cap at 5/20/600
  with a 10× cost cliff past 20, and its economics only beat the alternatives above
  ~5k min/month). Both carry the same Indic caveat: **turn detection is an orchestration
  property BYOK models do not fix** — LiveKit's detector excludes Telugu and degrades to
  VAD-only; budget for endpointing work (e.g. Sarvam stt-mode) on either path.
- Shadow mode: mirror a % of calls; compare latency/quality/cost vs the rented engine via the
  regression suite + usage_events; migrate tenant-by-tenant.
Phase 3 (GPU self-host) stays parked until >~50k min/month AND open Telugu TTS reaches
Bulbul-class quality — revisit quarterly, decide on usage_events data, not vibes.

## 6. Decision Log (binding; append entries to change anything above)

| # | Decision | Choice | Why (short) |
|---|---|---|---|
| D-01 | Brand/domain | Calevate / calevate.tech (WHYB rejected) | Distinct product brand under BuiltByThree |
| D-02 ⚠SUPERSEDED by D-31 (engine pick only; phase-2/3 legs stand) | Engine strategy | Rent now (ThinnestAI primary, Bolna fallback) → own orchestration phase 2 → GPU phase 3 | Utilization math + adapter portability |
| D-03 | Not OmniDimension | — | Competitor's supplier; no differentiation; USD; prompt-size ceiling |
| D-04 ⚠SUPERSEDED by D-20 then D-36 | Models | Saarika STT · Gemini 2.5 Flash-Lite (→3.x Oct '26) · Bulbul v2 (v3/Aero pending bake-off) | Cost/quality/India-residency balance |
| D-05 | Telephony | Vobiz/Exotel; inbound-priced favorably | India rates; DLT maturity |
| D-06 | Architecture | Modular monolith + isolated voice-runtime + workers; NOT microservices | 2-person team; bulkheads+interfaces give the isolation/swappability |
| D-07 ⚠AMENDED: pgvector→D-28 managed RAG service; auth reaffirmed by D-37 | Stack | FastAPI/Python + Next.js/TS; Postgres+pgvector; Redis+ARQ; Clerk; DO BLR | One backend language; voice ecosystem is Python |
| D-08 ⚠SUPERSEDED by D-28 (its latency physics still governs provider REGION) | RAG | In-Postgres pgvector hybrid, 4-tier latency design; Cohere Embed v4 (BGE-M3 later); no external vector DB | Network RTT kills in-call retrieval |
| D-09 | Extraction | Schema-driven, post-call, per-client custom fields + vertical templates | Product-vs-service core |
| D-10 | Tenancy | Flat (no white-label) ; admin/client separate realms; slug-based client URLs | Direct-clients-only decision |
| D-11 ⚠EXTENDED by D-34 (managed tier unchanged; self-serve tier added) | Pricing | Setup ₹40–75k + retainer ₹15–30k incl. minutes + overage ₹6–8/min; never pure per-minute | Fixed-cost math at 1–2 clients; sell vs telecaller |
| D-12 | Billing | Append-only usage ledger with our cost per unit, from day 1 | Pricing deferable, metering not retrofittable |
| D-13 | Hosting region | DO Bangalore despite "no residency constraint" | Latency co-location; DPDP follows the caller anyway |
| D-14 | Compliance posture | Disclosure always; consent ledger; PII redaction; 90-day recording floor; campaign gate | TRAI/DPDP as researched July 2026 |
| D-15 | Quality process | Regression on every change; mandatory-five scenarios; client-facing QA reports | Industry norm + differentiator vs Outpero |
| D-16 ⚠READ 'ThinnestAI'→'Bolna' per D-31; sub-accounts now exist (TRD §5) | Engine account structure | ONE ThinnestAI account for all clients (agents mapped to tenants in OUR db); NOT account-per-client | Their docs show no workspace/sub-account feature; separate accounts fragment BYOK keys/webhooks/billing and likely breach ToS; noisy-neighbor risk solved by our dispatcher's inbound-reserve + per-tenant ceilings (FLOWS §5); confirm ToS + concurrency in verification item 8 |
| D-17 | Python module layout inside `apps/api` | FLAT (`apps/api/engine/`), not `apps/api/src/engine/` | Resolves a v1.0 conflict: the CLAUDE.md hard rule named a `src/` layout while DEV-SETUP §2's run command (`apps.api.main:app`) requires a flat one — both could not hold. Flat keeps the documented uvicorn/arq module paths working with the repo root on `sys.path`, and `apps/*` stay virtual uv workspace members (not installed packages). Hard rule 2 is about the ENGINE ISOLATION BOUNDARY, not the literal path; it is unchanged in force. CLAUDE.md updated to say `apps/api/engine/`. |
| D-18 | `voice-runtime` directory name | Keep the HYPHEN (`apps/voice-runtime`); start it with `--app-dir` | Resolves a v1.0 conflict: the canonical repo layout is hyphenated but DEV-SETUP §2 gave `apps.voice_runtime.main:app`, which Python cannot import (hyphens are illegal in module names). The layout wins because it is stated in CLAUDE.md, AGENTS.md and TRD §1, and the deployable's name appears in infra/CI. Run command becomes `uvicorn main:app --port 8100 --app-dir apps/voice-runtime`. DEV-SETUP updated. |
| D-19 | Next.js major version | PIN Next.js 15 (15.5.21); do NOT track `latest` | `create-next-app@latest` now installs Next 16; TRD §2 locks 15. The frontend is not on the critical path for client #1 (ROADMAP M1) and a major-version bump buys nothing while it costs App Router breaking-change risk and a re-verification of the generated OpenAPI client. Revisit as a deliberate migration with its own entry, not as a scaffold side effect. Corollary: `create-next-app` also emits an `apps/web/AGENTS.md` pointing at `node_modules/next/dist/docs/`, which does not exist in 15 — deleted as misleading; delete it again if a re-scaffold recreates it. |
| D-20 ⚠CORRECTED by D-35 (Bulbul v2 is NOT discontinued — live at half price) and extended by D-36 (LLM→Sarvam) | Sarvam model versions (supersedes the model versions in D-04; the D-04 rationale stands) | Saaras V3 STT · Bulbul V3 TTS | ThinnestAI's docs (Jul 2026) list only Saaras V3 (22 languages, streaming, code-mixed) and Bulbul V3 (11 languages); Saarika v2.5 and Bulbul v2 appear discontinued on the platform. TTS cost line moves to the V3 band (TRD §10). Ear-test at verification is now V3 vs bundled voices. |
| D-21 | Client CRM control boundaries (post Outpero teardown, Jul 2026) | Clients (owner role) CAN: dispatch a single AI call from the Leads table with an optional per-call context note (M1); trigger an AI callback on needs-follow-up calls — agent re-dispatched with prior-call context via the engine's context-injection webhook (M2). Clients CANNOT in v1: edit extraction schemas (admin-only) or customize lead statuses (fixed enum). | Competitor parity where it demos well — Outpero ships manual dispatch and a human-callback button; our callback is AI-closed-loop, one better. The managed-service moat (we curate schemas and quality) stays intact: schema edits regenerate prompt hints and need a regression run, so they route through us; analytics and hot-lead rules key off the fixed status enum. Both client-initiated paths run the same compliance pre-checks as webhook dispatch (DNC scrub, calling hours, caps) and meter like any call. BRD §4.7. |
| D-22 | Admin impersonation | READ-ONLY "view as client" from the admin console, every session + page view audit-logged (actor=admin_user, tenant, at, ip). No acting-as: mutations still go through admin surfaces. | Debugging "my dashboard looks wrong" without screenshot ping-pong; read-only keeps the audit trail unambiguous (no dual attribution). Shapes auth from the first migration: impersonation issues a scoped read-only session against the client realm, never a client credential. Industry-standard support pattern. |
| D-23 | Outbound CRM sync (us → client's tools) | Outbound webhooks (HMAC-signed, our standard envelope, retries + delivery log per SEC-COMP §5) + Google Sheets sync, landing in M2 with campaigns. The `outbound_webhooks` config table is designed in M1 alongside `inbound_webhooks` so no migration pain. | Outpero markets CRM integrations (Zoho/Salesforce/Sheets); a visible checkbox in sales conversations. M2 not M1: client #1 gets CSV export first, revenue before plumbing. webhook_deliveries (direction=out) already covers the forensic side. |
| D-24 | Real-time UI transport | v1 (M1): TanStack Query polling (interval + window-focus refetch) — meets the 2-min post-call SLO with zero new infra. Upgrade with M3: SSE event stream pushing invalidation hints ("what changed", never payloads), TanStack Query refetches. WebSockets explicitly NOT planned. | Boring-solutions doctrine. Our flow is strictly server→client (live call tiles, lead toasts); SSE is plain HTTP (no proxy/infra changes), simpler than WebSockets on both ends, and 30–50% cheaper per connection; WebSockets only pay off with client→server streaming, which we don't have. Events-as-hints keeps auth and staleness out of the stream. SURFACES.md §3.2. |
| D-25 ⚠READ 'ThinnestAI'→'Bolna' per D-31 | Site hosting decoupled from voice path (narrows D-13's scope; its latency reasoning stands) | The calevate.tech site stack (web, api, workers, webhook receiver) hosts on a general-purpose VPS (Hetzner-class) — India co-location NOT required for it. The India requirement applies only to future IN-CALL-PATH services: the M3 pgvector RAG tool endpoint (100ms budget) deploys to an India-region host when built; nothing else relocates. | Founder correction (Jul 2026): in v1 ThinnestAI hosts the entire live call — our site never sits in the caller's audio path, so D-13's co-location argument doesn't apply to it. It DOES apply to the M3 in-call tool endpoint (EU→Mumbai RTT ~120–150ms alone blows the 100ms budget), so the requirement is pinned to that service, not the site. DEPLOYMENT.md §0. |
| D-26 | Production database hosting | Host PostgreSQL 16 (+pgvector) on the VPS — raghava-proven pattern; NOT managed Postgres for now. Consequence accepted and closed: nightly dumps alone would break OPERATIONS §5's RPO 15min, so continuous WAL archiving (wal-g → R2) is REQUIRED, plus nightly pg_dump offsite + quarterly restore drills. | Zero marginal cost; the exact pattern already runs in production for raghava-organics (host PG, containers reach it via host.docker.internal). Managed PG (~$15–30/mo, built-in PITR) is the fallback if wal-g operations prove annoying — migration is dump/restore + connection string. DEPLOYMENT.md §7. |
| D-27 | Edge: Cloudflare proxied | All calevate.tech zones proxied (orange), SSL mode Full (strict), origin locked to Cloudflare IP ranges, Cloudflare Origin CA cert on the default_server block (the 525 fix), certbot certonly (never --nginx). PLUS one fix raghava lacks: real_ip restoration (set_real_ip_from CF ranges + real_ip_header CF-Connecting-IP) so rate limits and audit logs see real caller IPs. | DDoS protection + hidden origin at no cost, and the full config with its failure modes (525/redirect-loop/default-block traps) is already documented and running for raghava. hooks.calevate.tech (engine webhooks) stays proxied too — HMAC authenticates the caller and the CF hop is irrelevant to a 5s webhook budget. DEPLOYMENT.md §5. |
| D-28 ⚠READ 'ThinnestAI'→'Bolna' per D-31; refined by D-33 (KB is not a BYOK slot) | RAG/memory = managed third-party API service (supersedes D-08's self-hosted pgvector plan; D-08's latency physics now governs provider-region choice) | One managed RAG/memory service, consumed via API, serving BOTH the ThinnestAI pipeline AND the client CRM (semantic search, caller memory, future CRM features). We run no vector infrastructure. v1 in-call retrieval stays on ThinnestAI's built-in KB (zero network from their pipeline); the provider takes the in-call path only when its measured p95 from GCP Mumbai fits the 100ms budget. Provider TBD via the bake-off gate in TRD §6 (candidates: Qdrant Cloud, Pinecone serverless [Singapore], Turbopuffer, Weaviate Cloud; memory-API class: Mem0, Supermemory, Zep) — scored on Mumbai latency, per-tenant namespace isolation, hybrid search, ingestion API, memory semantics, DPDP deletion-with-proof, cost. Ingestion approval gate and dual-push (engine KB + provider) remain ours. | Founder direction (Jul 2026): fast, comfortable, API-only RAG shared by pipeline and CRM. Ops-light beats infra ownership for a 2-person team, and the CRM-side uses are latency-tolerant today. The in-call gate keeps D-08's physics honest: nearest serverless regions are Singapore-class (~35–70ms RTT from Mumbai) — plausible but unproven; measure before committing the call path. kb_chunks/pgvector tables in DATA-MODEL become contingency, built only if the bake-off fails. |
| D-29 | Executable governance + git workflow | Guardrail pack (fitness functions) in CI: the critical four ship in M1 (import-linter engine-isolation contract; RLS-coverage check against pg_policies; serializer redaction-exposure scan; .env↔Settings parity), the rest incrementally with the milestone that creates the surface (ledger-immutability, coverage ratchet, docs-drift, openapi-client freshness, compliance invariants, webhook-storm stress, error-budget release gate). Git: trunk-based, short-lived branches, Conventional Commits enforced by pre-commit hooks (ruff + commit-msg lint, <2s). ENGINEERING-PRACTICES.md is the canonical spec. | Raghava-organics runs ~20 such checks in production with a tiny team — proof the style works; industry consensus (fitness functions run with tests, boundaries enforced in CI, coverage ratchets not targets). Our Hard Rules are exactly the kind that get violated when the codebase grows fastest if enforcement is human-only. Critical-four-first respects R-08 (no overbuild before client #1). Seed import-linter contract + hooks installed at decision time. |
| D-30 | Backend construction patterns | BACKEND-PATTERNS.md adopted as a CORE BLUEPRINT doc (reading order #4, alongside TRD/DATA-MODEL — the construction manual for the whole backend build; this entry only records its adoption). Prescriptive for apps/api, voice-runtime and workers: module anatomy (routes/service/schemas, NO repository layer, one-test-file-per-behavior), locked bootstrap order with raw-body preservation on webhook paths, RFC-9457 ladder with kind/retryable/fields extensions, the reliability triad (idempotency table + transactional outbox + webhook inbox, all claimed via conditional-UPDATE CAS), tolerant worker boot with /healthz/ready as the go-live gate, degradation_mode enum, audit hash chain on audit_log, failure_stage alert taxonomy, named metric recorders, coverage ratchet scoped to high-risk modules. Two new M1 tables: outbox_messages, webhook_inbox_events (+ idempotency_records). | Patterns lifted from the raghava-organics production backend (proven under live traffic) and confirmed against 2026 industry practice (service layer + DI, async SQLAlchemy, outbox with SKIP LOCKED-style claiming, effectively-exactly-once = at-least-once + idempotent consumers). Skips recorded with reasons: no repo layer (swap seams belong at external boundaries), no success envelope (RFC-9457 already decided), Clerk owns sessions (their rotation design is the self-host reference), their ops-config overlay is the reference for per-tenant BYOK key storage. |

| D-31 | Primary voice engine | **Bolna replaces ThinnestAI** (supersedes D-02's rented-engine pick; the adapter architecture and phase-2/3 legs of D-02 stand). ThinnestAI retired before any adapter code existed. No fallback engine is designated: engine risk is carried by the VoiceEngine adapter contract (normalized schemas + conformance suite keep a second adapter cheap), and D-02's phase-2 self-orchestration trajectory stands unchanged on its own triggers. Adoption is gated on the Bolna pilot scorecard: webhook behavior, Telugu quality (STT + KB multilingual mode), real-PSTN latency, BYOK platform fee ≤ ~₹1.5/min in writing, agency/sub-account confirmation, INR/GST billing. Conformance discipline continues with the `bolna` + `fake` adapters until a second rented adapter exists. | Aug 2026 due diligence: ThinnestAI = domain registered Feb 2026, zero verifiable customers, no SLA/status page/agency clause, unresponsive (no-showed the scheduled meeting). Bolna = YC F25 + $6.3M General Catalyst seed (Jan 2026), named production customers (Varun Beverages, Spinny, Snabbit; ~200K calls/day per TechCrunch), verified Sarvam saaras v3 + Telugu te-IN + free BYOK + Exotel/Plivo/Vobiz/Twilio guides + published 140/160 DLT runbook + Enterprise sub-accounts and India data-residency. Consequence accepted and closed: **Bolna webhooks are unsigned and at-most-once** (docs + OSS delivery code verified: single POST, no retry/timeout/signing, errors swallowed; only control = static egress IP 13.203.39.153) — webhook doctrine amended in TRD §5, SEC-COMP §5, SURFACES §3.1 and FLOWS §3: authenticity = source-IP allowlist + execution-id dedupe, and the List-Executions reconciliation poller is promoted from safety net to guarantee of record. |

| D-32 | Engine-platform evaluation: what is a decision variable, and what is not (reaffirms D-31; does not change it) | **Vapi ELIMINATED permanently.** **LiveKit Cloud recorded as the best-evidenced phase-2 self-orchestration target** (does NOT displace D-02's Pipecat-on-CPU plan; both are phase-2 candidates, decided at the phase-2 gate on measured evidence). **Bolna reaffirmed as primary for M1.** Evaluation doctrine going forward: (a) inputs identical across platforms — Sarvam rates, Exotel rates, the Gemini-3.x migration, and BYOK model QUALITY (incl. Telugu STT/TTS accuracy) — are NOT decision variables; only platform-differentiated properties are; (b) vendor latency numbers are marketing until we measure them on real PSTN (Bolna publishes an undefined "<300ms"; a third-party comparison inflated this to "sub-600ms end-to-end" — both unusable); (c) at launch volume, MONTHLY FLOORS dominate per-minute rates, so a no-floor prepaid model beats a lower headline rate. | Verified Aug 2026 (adversarial multi-agent verification of first-party pages). **Vapi:** its $0.05/min "Vapi Hosting" fee is a separate line item that survives full BYOK = ₹4.40/min orchestration tax alone, exceeding our entire ₹3–3.6/min all-in target before any Sarvam/Exotel second; AND it has no India region (SIP+RTP terminate US/EU only, forcing an India→US→India hairpin of +230–260ms/turn). Either fact disqualifies; together it is not close. **LiveKit Cloud:** ap-south (Mumbai) region VERIFIED; first-party maintained Sarvam plugin (saaras:v3 / bulbul:v3 / sarvam-105b) with true BYOK and no resale markup; turn detection AND barge-in ship in the SDK by default. But its "$0.01/min" is two meters on one telephony minute (agent $0.01 + third-party SIP $0.004 ≈ $0.014 marginal), concurrency is hard-capped 5/20/600 — our 10–20 target sits exactly at the Ship ceiling with a 10× cliff to $500/mo beyond it — and the $50/mo Ship floor makes it ₹4.40/min at 1,000 min/month, i.e. identical to Vapi at OUR launch volume; the advantage appears only at ≥5,000 min/month. **Bolna wins the rows that bind now:** no monthly floor (prepaid credits, $5 free to start), 100 concurrent on Pilots with 250+ concurrent proven in production by two independent customers (GoKwik, Futwork), 8 named customers, campaigns/KB/post-call extraction/sub-accounts/DLT guidance included (4–8 engineering weeks we do not have pre-client-#1), and fastest time to revenue. Its known costs stay: unsigned webhooks, unpublished BYOK fee, US-region recordings pre-Enterprise, medium lock-in — all carried as pilot gates (OPERATIONS §2). **Telugu turn-detection finding (generalizes):** LiveKit's turn detector covers 14 languages incl. Hindi, EXCLUDING Telugu, and on an unsupported language it disables model-based end-of-utterance entirely rather than falling back (VAD-only silence timers). Endpointing is an ORCHESTRATION property that BYOK models do NOT fix — so Indic turn-taking is a build-and-verify line item on every DIY path, and a measured gate on every rented one (OPERATIONS §2 gate 5). |

| D-33 | Memory horizons + KB integration under BYOK (refines D-28; does not change it) | **Three memory horizons, named and separated** (TRD §6.1): **H1 in-call working memory** = the running dialogue, held by the ENGINE's orchestrator, discarded at hangup — not a component we build, buy or key, and not a BYOK slot; **H2 call-level durable memory** = transcript/summary/extraction/sentiment written by our existing post-call pipeline (TRD §8) into Postgres; **H3 caller/tenant long memory** = the D-28 managed service, injected PRE-call via the context webhook (~5s budget), never mid-call. The H1→H2 hand-off is the post-call pipeline; from H2 onward this is entirely mechanisms we already have. **KB under BYOK: the knowledge base is NOT a BYOK slot** — BYOK covers STT/TTS/LLM only, and no vector store of ours can be plugged into the engine's retrieval pipeline. Built-in KB (`rag_id`) requires no BYOK and carries no KB line on the pricing page → **inferred included in the platform fee (confirm in writing, gates 8 + 12)**. External KB is a different architecture (engine custom function → our endpoint → provider), not a different key. **v1 keeps in-call retrieval on the built-in KB** (reaffirms D-28's in-call gate, now with Bolna-specific numbers). Also: engine references inside D-28's text read as Bolna per D-31. | Founder question (Aug 2026), answered from first-party evidence. **Cost is not the deciding variable:** an external KB adds ≈₹0.02–0.05/min against a ₹1.75–2.30/min BYOK model stack — noise. **Latency is:** the external route adds two network hops (engine → our endpoint → provider and back) ≈ +150–400ms against a 100ms budget, and Bolna documents **no tool-call timeout at all**, so the ceiling is unmeasured. The built-in KB has zero network hops from the orchestrator. Its two known costs are carried as gates, not assumptions: Bolna's multilingual KB mode names Hindi and Tamil but **not Telugu**, and the mode is **immutable at KB creation** (gate 8) — if Telugu retrieval is poor, the custom-function route becomes the fallback and we accept the round trip, masked by the T3 filler utterance. Dual-push ingestion (engine KB + managed service) and the preview-and-approve gate stay ours regardless, so the choice is reversible. Corollary on H1 economics: the full conversation is resent to the LLM every turn, so in-call token cost grows through the call — the ₹0.15–0.20/min LLM figure is a blended average that long calls skew above; whether Bolna truncates/summarises history or enables provider context caching on BYOK keys is UNVERIFIED (gate 8). |

| D-39 | Build doctrine for the dual-motion era: **schema for scale, surface thin** — and **measure latency before optimising it** (founder delegated both calls) | **(a) SCOPE.** Resolve the "thin slice vs million-dollar SaaS" tension by splitting it: build the **data model and boundaries** for the end state in M1 — orgs/memberships/invites, credits + prepaid ledger, per-tenant metering, plan/tier column, engine adapter, RLS on every table — but ship the **narrow surface** first (admin-driven onboarding for client #1). Self-serve UI, campaigns and the credit top-up flow land M2. Rule of thumb: **anything that would require a migration or a re-meter later is built now; anything that is only a screen is built when a user needs it.** D-12 already establishes the precedent (metering is not retrofittable). **(b) LATENCY.** For M1, optimise *inside* Bolna and do not plan self-orchestration: we have **zero real-PSTN measurements**, so any optimisation now is guesswork. Sequence is measure → budget → optimise: pilot gate 4 produces p50/p95, then the cheap application-layer levers do most of the perceived-latency work — T0 compiled context (answers ~80% with no retrieval), filler-utterance masking on every tool call, a hard in-call tool budget, prompt-size discipline, and streaming the first sentence. Phase-2 self-orchestration stays gated on its existing numeric triggers (TRD §10). | **(a)** A 2-person team pre-client-#1 cannot afford either failure mode: shipping a thin slice that needs a rewrite to scale, or building a platform before anyone pays for it. Splitting on "migration cost" is the cheapest correct line. **(b)** Honest constraint recorded so it is not re-litigated: on a **rented** engine the audio path is largely Bolna's, so **we cannot beat a competitor on latency by platform choice** — if Outpero is also on Bolna (unresolved, teardown §10.3), our structural latency is identical to theirs. Our real levers are application-layer, and they are cheap; a genuine structural latency advantage requires phase-2 self-orchestration, which also removes the BYOK-keys-with-vendor exposure (TRD §5). Do not promise a latency edge in sales copy until gate 4 numbers exist. |

| D-37 | Auth: **keep Clerk — do NOT build our own** (reaffirms D-07/D-10 against a founder proposal to self-build, now that self-serve is in scope) | Two Clerk applications stay: **admin realm (invite-only, signup disabled)** and **client realm (self-serve signup: email/password + Google OAuth + admin invite links)**. Clerk **Organizations** back tenant membership and invitations; every user and org is **mirrored into our Postgres via Clerk webhooks**, so `organizations`/`users` remain OUR system of record and RLS keeps keying off our `tenant_id` — Clerk authenticates, it does not own our data model. Custom domain on `accounts.calevate.tech` so the flow is ours end to end. | **Verified pricing (clerk.com/pricing, 11 Aug 2026) removes the cost argument entirely:** free tier = **50,000 monthly retained users per app**, **unlimited applications**, **custom domains included**, and **100 monthly retained organizations** free (≤20 members each). Beyond 100 orgs, ~**$1/org/mo** (101–1,000 band); Pro is $25/mo. Concretely: **0–100 client businesses cost us ₹0**; 500 clients ≈ ₹35k/mo against ₹1,899–30,000/mo of revenue *per client*. Building our own would save ~₹0–2k/mo today and cost **3–6 engineering weeks pre-client-#1 plus permanent security ownership** of sessions, password hashing, MFA, recovery, OAuth, rotation, CSRF and rate-limiting. **The decisive argument is not cost, it is blast radius:** Hard Rule 1's RLS model trusts `tenant_id` from a verified session, so an auth defect is a cross-tenant data breach, not a login bug — that is the last surface a 2-person team should hand-roll before its first client. The founder's actual requirement ("directly integrated with Calevate") is satisfied without owning the crypto: custom domain, our branding, our DB as source of truth, our invite flow. Reversibility is already documented — D-30 records Clerk's session-rotation design as our self-host reference if we ever outgrow it. Revisit only if org count passes ~2,000 or an enterprise client demands SAML we cannot buy. |

| D-38 | Inbound receptionist is the **headline differentiator** | Ship the inbound 24/7 receptionist as a first-class channel and lead with it in positioning, rather than treating it as parity with outbound. It stays inside the same agent model (one agent = one channel, per D-21's config boundaries) and reuses the same script/extraction/compliance machinery. | Outpero **markets inbound but has not shipped it** — their own docs state inbound employees "can't be created yet" and an inbound-configured number "plays a short message rather than holding a full live conversation, even if your setup looks complete on screen" (teardown §9c). It is their only confirmed unbuilt flank, it is already in BRD §4.1 scope, and it is the highest-value use case for the SMBs we target (missed calls after hours = lost revenue they can feel). It also plays to our compliance edge: inbound is consent-clean by construction (the caller initiated), which sidesteps the DLT/DNC exposure that constrains outbound. |

| D-36 | **CANONICAL M1 STACK (locked)** — supersedes the model picks in D-04/D-20 on the LLM leg; D-31 (engine) and D-05 (telephony) unchanged | **Orchestration: Bolna (BYOK)** · **Telephony: Exotel** (140 via Vobiz / 160 via Plivo per their DLT runbook) · **STT: Sarvam Saaras (STT+Translate), ₹30/hr** · **LLM: Sarvam 105B — free per token (DEFAULT; replaces Gemini 2.5 Flash-Lite)** · **TTS: Sarvam Bulbul v3 default, v2 as the value tier** (₹30 vs ₹15 per 10k chars). Gemini 2.5 Flash-Lite is **retained as a configurable fallback**, not the default — it stays the reference for post-call extraction quality until the Sarvam LLM is measured against the golden-transcript fixtures. Target all-in **≈₹3.1–3.6/min**; verified floor ₹2.9 on v2+Sarvam. | Three reasons, in priority order. **(1) Data residency, not cost** — SEC-COMP §4 already flags that Gemini sends transcript text to Google and that an "all-India mode" was a client option at a quality tradeoff; with Sarvam's LLM free *and* sovereign, that tradeoff mostly disappears and all-India becomes our DEFAULT posture. This is a live differentiator: Outpero's own privacy policy admits "some providers may process data on servers located outside India" (teardown §9b). **(2) Cost** — the LLM leg goes ₹0.15–0.20 → ₹0.00 and R-04's forced Gemini-3.x step (~₹0.55–0.65/min) is avoided entirely (D-35). **(3) Vendor simplicity** — one commercial relationship for STT+LLM+TTS, one rate card, one rotation. Accepted risks, each with a mitigation: Sarvam LLM quality on Telugu function-calling/extraction is UNMEASURED → keep Gemini configurable and run the golden-transcript fixtures before switching the extraction path; Sarvam rate limits (60/200/1,000 rpm by plan) are now a CONCURRENCY input, not a price input → size the plan at pilot gate 13; "free per token" may be promotional → confirm in writing, and the fallback is a ₹0.15–0.20/min step, not a redesign. Single-vendor concentration is the real cost of this choice — mitigated by keeping Cartesia as a second TTS candidate (D-35) and Gemini as a second LLM. |

| D-35 | Sarvam rate card verified live (corrects D-20 and R-04's premise) | Read from `sarvam.ai/api-pricing`, 11 Aug 2026. **(a) Sarvam 105B/30B chat LLM is FREE PER TOKEN** — the long-standing open question is closed, and R-04's "forced Gemini-3.x migration costs ~₹0.55–0.65/min" is now an *avoidable* step rather than an inevitable one; Gemini becomes a quality choice, not a cost necessity. **(b) Bulbul v2 is NOT discontinued** — it is live at **₹15/10,000 chars, half the v3 rate**; D-20 recorded it as "appears discontinued," which was wrong (that read came from ThinnestAI's docs listing, not from Sarvam). D-20's *choice* of V3 for quality still stands as the default, but v2 is now a deliberate cost lever. **(c)** STT ₹30/hr and Bulbul v3 ₹30/10k chars are confirmed unchanged. **(d)** No monthly minimum; ₹1,000 free credits; credits never expire and are universal across APIs; **the binding constraint is RATE LIMIT, not price** — 60 rpm Starter / 200 rpm Pro (₹10k) / 1,000 rpm Business (₹50k), which is a concurrency input for FLOWS §5 and pilot gate 13. | Consequence: the cheapest verified BYOK stack is **Bulbul v2 + Sarvam LLM ≈ ₹1.04–1.31/min**, ~45% below the v3+Gemini combination we had been planning against (₹1.73–2.32). This does not change the default model choice — Telugu quality decides that, by ear, at the pilot — but it gives us a real value/premium ladder built from published rates, mirroring how Outpero's ₹3/₹5/₹7 voice tiers work (TRD §10.3). Full recomputed model in TRD §10.1. |

| D-34 | Pricing collision with Outpero's self-serve product (OPEN — revisits D-11's evidence, does not yet change D-11) | An authenticated in-product teardown of Outpero (Aug 2026, `docs/evidence/outpero-teardown-aug2026.md`, BRD §5) found they now ship a **self-serve product at ₹0 setup + ₹1,899/employee/mo + ₹3–7/min** (voice-tier priced, 30-sec-block billing, volume-bonus credits, Enterprise waives the hire fee). D-11's stated rationale cites "Outpero's ₹60k+ setup pricing norms" as an anchor — that evidence is now **stale**: the ₹60k+ figure lives only on their high-touch agency packages, while the product an SMB actually shops costs ~₹2,241 first month vs our ₹55–105k. **RESOLVED 11 Aug 2026 (founder): option (b) — run BOTH motions.** (1) **Managed tier** keeps D-11 unchanged (setup ₹40–75k + retainer, we curate schemas, run QA, own compliance) — this is the client-#1 path and where the margin is. (2) **Self-serve tier** added beneath it: sign-up with **email/password + Google OAuth**, plus **admin-generated invite links** from the admin console; prepaid credits, per-minute talk-time, no setup fee. The two motions share one product and one tenancy model — a self-serve org is the same `organizations` row as a managed one, distinguished by a plan/tier column, so nothing forks. **Consequence for build order:** the *schema* for credits, metering, orgs, memberships and invites lands in M1 (D-12 already says metering is not retrofittable); the self-serve *UI and signup flow* land in M2. **New risk accepted — self-serve + Indian telecom compliance is the sharp edge** (anyone can sign up and dial): mitigations are non-negotiable and ship with the self-serve flow — platform-fixed calling hours, DNC scrub on every dispatch path (Hard Rule 5), mandatory non-null AI disclosure, a durable consent-attestation ledger, per-account concurrency and spend caps, and number provisioning gated behind KYC. Tracked as **R-11**. | The teardown also confirmed OpenDG (Outpero's parent) is a 2014 Hyderabad agency executing our exact service→product thesis, and that two July-2026 "gaps we exploit" (no QA process; not schema-driven) are now closed by them — so the competitive case rests on typed extraction, hard compliance/DPDP, engine isolation, closed-loop QA, and an inbound receptionist they have not shipped. Stack verified as React+Supabase+FastAPI-style REST on Sarvam+Cartesia (supersedes the OmniDimension-class inference). |

| D-40 | The webhook inbox's unit of work is the **transition**, not the execution | Key `webhook_inbox_events.event_key` on `{execution_id}:{raw_status}`, matching `job_id_for(INGEST_JOB, engine, execution_id, raw_status)`, which had assumed this since it was written. One durable inbox row per status change; a repeat delivery of the same transition is still one job. | Bolna fires one webhook per status change (queued → in-progress → completed, TRD §5) carrying the same execution id. Keying the inbox on the execution alone made the inbox and the job id disagree: the FIRST transition claimed the row and enqueued, and every later one — including `completed`, the only transition where cost, recording and transcript exist — came back `duplicate` and enqueued nothing. The post-call pipeline therefore never ran from a webhook at all: every call waited for the 10-minute reconciliation poller, which makes FLOWS §3.6's "lead + summary visible < 2 min after hangup" unmeetable and turns D-31's "poller = guarantee of record, webhook = low-latency hint" into "the poller does everything". `pipeline.py` returning `awaiting_completion:{raw_status}` was the other half of the evidence. No migration: `event_key` is Text under a `(provider, event_key)` unique constraint. |

| D-41 | The KB **detach contract**: a published knowledge version must be withdrawable, so `attach_kb` returns the engine's handle and the protocol gains `detach_kb` + `list_kb` | `VoiceEngine.attach_kb(ref, source)` now returns `EngineKBRef` — the vendor's own id for its copy (Bolna: `rag_id`), opaque above the adapter exactly like `EngineAgentRef`. Two methods join the Protocol: `detach_kb(ref, kb)` removes ONE attached source, `list_kb(ref)` reads back what the agent actually holds. The handle is recorded in `kb_documents.meta.engine_kb_ref` on the source's first chunk — the home the KB migration already designated for provider-side ids — so this costs no column and no migration. Publish order in `apps/api/kb/service.py::publish_source` is **detach-then-attach**, and **a failed detach ABORTS the publish, leaving the previously approved version live**. | Our `kb_sources.id` addresses nothing on the engine's side: the engine names its own copy, so `DELETE /knowledgebase/<our uuid>` would 404 forever while looking like a working detach. Without a returned handle a published document can never be retracted — which is exactly what went wrong: "supersede" only ever happened in OUR tables, so a superseded knowledge base stayed live on the agent, which kept answering from v1 while every screen reported v2. That divergence between the published KB and the approved one is the single thing the FLOWS §7 approval gate exists to prevent, and after a rollback it was worse — every version attached at once. **Failure policy, with both alternatives rejected on the record.** Publishing over a version we could not withdraw is the original defect (two versions live, the agent free to quote the old prices, our tables reporting success). Dropping the old and publishing nothing is an outage we caused to avoid an inconsistency. Refusing keeps the client whole: their agent still answers, from text a human approved; what they lose is the UPDATE, and the retry costs nothing because nothing has been attached yet. A live version we hold no handle for is the same refusal for the same reason (`kb_engine_ref_unknown`) — we must not publish over what we cannot address; the remediation is to withdraw the stale copy vendor-side once, not to weaken the rule. The detach must be REAL: an adapter that accepts the call and does nothing turns publish into a silent lie nothing downstream can detect, so the conformance suite observes removal through `list_kb` rather than trusting the call, and detaching a handle the engine never issued must RAISE. Accepted cost of the ordering: between detach and attach the agent holds no copy of that source and answers T4 "I don't know" — one request of silence is cheaper than a stale price the client is then held to. Two vendor facts stay UNVERIFIED and are carried as pilot gate 8 questions (OPERATIONS §2), not assumptions: whether the KB list response carries the agent linkage `list_kb` filters on, and whether deleting a KB also clears the agent's reference to it. **Both now have an executable probe** (`scripts/pilot/knowledge.py`, tested in `tests/pilot_knowledge_test.py`), and what each will answer is fixed in advance so the result cannot be argued about afterwards. (a) LINKAGE, answered behaviourally — attach to two agents, list both, and if both lists come back empty, DELETE the handle `attach_kb` returned: a delete the engine ACCEPTS proves the knowledge base existed and the list simply did not attribute it, so the answer is "linkage absent or renamed" and the consequence is that `list_kb` today reports EVERY agent as holding no knowledge base — a cross-tenant wrong answer that looks like a working feature, and D-41's detach read-back proves nothing until the adapter filters on the real field. A delete the engine REFUSES leaves the question INCONCLUSIVE (a broken attach cannot testify about a list), and one agent's handle appearing in the other's list is the worst answer of the four: the list is not agent-scoped at all. Reading the row's fields directly is NOT available to the probe — that is a vendor shape, so it stays inside `apps/api/engine/` (hard rule 2) and the raw capture belongs to the fixture-recording slice. (b) DANGLING REF: if the agent still references the deleted `rag_id`, `detach_kb` grows a SECOND call (an agent update) and stays mandatory — the detach-then-attach ordering above is unchanged, its first step becomes two operations. Our Protocol has no agent read-back (`create_agent`/`update_agent` only), so the probe answers this only when the runner supplies an agent read-back or a live call, and otherwise reports INCONCLUSIVE with that gap named rather than guessing. |

| D-42 | Outbox claim exclusivity is a **lease on the row** (`locked_until`), not a fourth `status` value | `outbox_messages` gains a nullable `locked_until timestamptz` (migration `7c04ab5f9e26`; no default, no backfill, catalog-only DDL). `claim_outbox_batch` commits the `attempt_count` bump and the lease in one statement on its own connection; the claim predicate becomes `status = 'pending' AND (locked_until IS NULL OR locked_until <= now()) ORDER BY created_at, id`. `status` keeps its three values, and `OUTBOX_CLAIM_LEASE` is 2 minutes — its own constant, not the 10-minute `CLAIM_LEASE`, because a claimed outbox row is held for milliseconds. No new index. | The bug it fixes: the attempt bump lived inside the dispatcher's transaction, which does not commit until after the publish, so a SIGKILL (OOM kill, container eviction, a deploy that does not wait) rolled it back and the row returned to `pending` with `attempt_count = 0`. A message whose payload is what kills the worker therefore looped forever, on a retry budget that reset every pass, toward a DLQ it could never reach — and silently, because the evidence died with the process. There is no fix without a COMMIT, and committing the bump alone is a different bug: the commit drops the `FOR UPDATE` locks that were the only thing making the claim exclusive. Exclusivity has to survive the commit, which means moving it out of the lock and onto a durable fact. Lease over status on four counts. (1) **It adds no state anyone has to learn** — the `AND status = 'pending'` CAS guards in `mark_outbox_published`/`mark_outbox_failed`, the outbox-lag metric, the DLQ-depth query and `replay_dead_letters` all stay correct untouched; a `claimed` status changes the CHECK constraint and puts every one of them in this migration's blast radius. (2) **Recovery needs no new machinery** — a lapsed lease is picked up by the claim query the dispatchers already run each tick, with `attempt_count` where the dead worker left it, so the recovery path IS the normal path; an abandoned `claimed` row needs a reaper with its own schedule, its own failure mode and its own argument about how long is too long. (3) **It is the lease this module already runs on** (`CLAIM_LEASE`, idempotency and inbox claims), with the deadline STATED rather than inferred from `updated_at`, which every status transition also bumps and which therefore answers the wrong question. (4) **The deciding one: a mixed-version deploy degrades to the old behaviour instead of to a stall.** An old dispatcher ignoring the lease can double-publish — which `job_id_for(job, message.id)` already dedupes at the queue, and which is exactly what the system did before this migration; the same overlap under a `claimed` status strands rows in a state old code never selects and silently no-ops against. Choosing a failure mode we already survive is the only honest thing to do to a queue carrying a client's CRM notifications. What the status value is genuinely better at is legibility, and that is bought back for one predicate on one screen (`locked_until > now()`). No index, deliberately: `ix_outbox_pending (status, created_at)` already walks pending rows in order and stops when the batch is full, while an index leading `(status, locked_until, …)` cannot deliver that ordering for an OR predicate and would force a sort of the whole pending set to answer a LIMIT. |

| D-43 | **Calevate's own telemarketer registration is ONE row on `platform_state`**, not a column on every tenant | Four columns on the single global row (migration `d7f2a3c9b410`): `tm_registration_status` NOT NULL DEFAULT `not_registered` under a five-value CHECK (`not_registered`, `submitted`, `active`, `suspended`, `revoked`), `tm_id`, `tm_registered_at`, `tm_verified_at`, plus a CHECK that `active` requires both a `tm_id` and a `tm_registered_at`. Read by `campaigns.service.launch_blockers` as **`tm_registration_missing`**, written only through `POST /v1/ops/platform/tm-registration` (`ops:manage`, step-up confirmed in both directions, audited in the same transaction). `platform_state` carries no `tenant_id`, so hard rule 1 does not reach it and no RLS exemption entry is needed. | It is ONE fact about ONE entity: **us**. Copied per tenant it becomes N facts that drift — a client onboarded in March saying we are registered and one onboarded in July saying we are not — and the launch gate's answer would then depend on which copy it happened to read. So it sits beside the load-shed mode and the big red switch, which answer the same question ("is this platform allowed to place calls right now"), and every tenant's launch preview reads the same value. **The read deliberately bypasses the load-shed cache.** `core.loadshed` reads `platform_state` through a three-layer cache with a 15-second window, which is right for a mode consulted on every request and wrong here: a compliance fact checked once per launch must not be 15 seconds stale, because a registration revoked ten seconds ago must block the campaign being launched right now. `read_tm_registration` therefore runs on the caller's own session — one query on a connection that is already open, and never stale. **And it fails CLOSED, the opposite of that cache's deliberate fail-open.** A missing row makes `loadshed` answer `normal`, because a fresh database is not an emergency and refusing every request over an absent seed row would be a self-inflicted outage; an unreadable registration, by contrast, is not evidence of a registration, and the cost of guessing wrong is dialling India's network as an unregistered telemarketer. No row, no registration. **Seeded `not_registered`, deliberately**: R-01 is precisely that our TM registration requires an entity we are still standing up, and seeding `active` would mean inventing a registration number — which the second CHECK makes impossible to do by accident. The consequence is intended: on a fresh database no campaign launches until ops records the registration. That is not a regression, it is the gate finally being able to ask. |

| D-44 | **The erasure record stops being the last copy of the number**: `deletion_requests.phone_e164` is cleared on completion and `subject_ref` takes over | Migration `f4a8e1c07b62`. `subject_ref TEXT NOT NULL` (sha256 of the number, hex, first 32 chars) is backfilled, then `phone_e164` drops NOT NULL and every completed row is cleared. Two CHECKs — an OPEN request must still name its subject (`completed_at IS NOT NULL OR phone_e164 IS NOT NULL`) and `subject_ref IS NOT NULL` — plus `INDEX (tenant_id, subject_ref)` and a BEFORE INSERT trigger that fills the reference when a writer did not, and never overwrites. `retention.execute_deletion_request` nulls the number in the SAME UPDATE that stamps `completed_at` and `proof`; `compliance/deletion.py` writes `subject_ref` on insert and reads it back rather than re-deriving it. `phone_e164` is NOT dropped (hard rule 8). | After a successful DPDP erasure the last place the person's number survived was the record proving we removed it — and no retention policy sweeps `deletion_requests` at all, so that copy was not merely surviving, it was permanent. **Clearing it in the same UPDATE as the proof is the whole design**: there is then no window in which a request is complete and still holds its subject. It cannot be cleared any earlier, because the worker's only handle on the subject IS that row — it resolves the number from it and the outbox payload deliberately carries ids only, so a queue dump is not a list of people who asked to be forgotten (hard rule 6). What clearing costs is the question support actually gets asked — "have we already erased this person?" — and `subject_ref` buys it back: it is the identical construction to the erasure proof's `subject_hash` and the subject-access export's `subject_ref`, which is why all three still line up, so an access request and an erasure for the same person stay correlatable without any record carrying the number. The hash is not reversible, and that asymmetry is the point: it answers questions ABOUT a number you already have, and nothing at all to a reader holding the table and not the number. The OPEN-request dedupe keeps using `phone_e164` (`uq_deletion_requests_open_subject`, migration `e2c47b90d5a1`) because an open row always carries it — now a CHECK rather than an assumption — and NULLs never conflict in a unique index, so making the column nullable WITHOUT that CHECK would have let a cleared-too-early row silently opt out of the guarantee the previous migration had just established. **The DOWNGRADE deletes completed rows**, stated here rather than discovered at 3am: restoring NOT NULL requires a number, a cleared number cannot be reconstituted from a hash, and a rollback that reconstituted it would be a fresh breach. The rows that survive a revert are the open ones, which are the only ones the pre-migration code needs. |

| D-45 | **An ops step-up confirmation names the TRANSITION, not the endpoint** | `X-Confirm-Action` on `POST /v1/ops/platform` must echo the exact move being made: `halt_outbound`, `release_outbound`, `set_load_shed:<mode>`, or — for a request that does both — the two joined in a fixed order (`halt_outbound+set_load_shed:reduced`). The string is built in ONE place, `ops.routes.platform_confirmation`, which the runbooks quote and `tests/platform_halt_test.py` pins literal by literal. The single generic string `set_platform_state` now authorises **nothing**, in either direction. A body that changes neither field is refused `platform_state_no_change` rather than reaching the writer, changing nothing and still writing an audit row for a platform change nobody made. | One string across three moves meant a header captured for the smallest authorised the largest: a confirmation an operator sent for a routine Tuesday load-shed tweak also satisfied **lifting the global outbound halt**, which is the single action that puts every tenant's dialling back on the network. BACKEND-PATTERNS §7 asks for the confirmation to be bound to the specific action, and the spend-cap recompute had always done it properly — its string carries the tenant id, so a header sent for one client cannot be replayed against another. The load-shed string carries its TARGET MODE for exactly that reason: `reduced` is a routine change and `maintenance` sheds reads, and consent to one is not consent to the other. A combined request is strictly more dangerous than either half, so both halves must be confirmed rather than either alone. **Breaking on purpose, and deliberately not grandfathered**: the old string's whole defect was that it authorised more than the operator meant, so accepting it "for compatibility" would keep the hole open under a different name. The cost is real and is paid down rather than denied — an operator's muscle-memory curl now 403s, so the refusal names the exact header to repeat with in its `remediation`, and both callers moved in the same change (the console's `useSetPlatformState`, and `runbooks/campaign-stall.md` §1). What this is NOT: a second factor. A header the caller supplies is not authentication and is not pretending to be — it stops the accidental and the drive-by until the admin realm's Clerk re-auth binding replaces it (TRD §2), and it is here now because adding it later would mean changing the callers, which is the cost this entry just finished paying. |

| D-46 | **A tenant's plan is the row whose EFFECTIVE WINDOW contains the instant** — not the newest row | `plans.effective_from` / `effective_to` become the row's valid time, resolved through one helper (`billing/plans.plan_in_effect_sql`) that every money reader now calls: the derived invoice, the usage panel, both cap reads, the worst-case call-cost quote and the dispatch concurrency ceiling. The period is **half-open `[from, to)`** per SQL:2011 application-time period tables, and the instant is PASSED IN rather than assumed to be now — a closed month prices at its last instant, the current month at now, a future month at its first instant (`month_pricing_instant`). One plan per month; **no proration**. The SQL:2011 non-overlap constraint (`PERIOD … WITHOUT OVERLAPS`; on pg16, `EXCLUDE USING gist` + `btree_gist`) is **DECLINED**: instead, resolution is a **total order** — latest `effective_from` wins (NULL sorting as `-infinity`), then `created_at DESC, id DESC`. A tenant with plan rows and none in effect is **UNPRICED** (no fee, no included minutes, no rate, no ceiling, default concurrency) and logs `plan_window_leaves_tenant_unpriced`. | The two columns were declared, migrated and read by nothing while every reader took `ORDER BY created_at DESC LIMIT 1`. Those are not the same rule, and the gap was a money bug in both directions: a price change staged for next month re-priced TODAY's bill, panel, quote and ceiling the moment the row was inserted; and because an invoice here is a DERIVED statement rather than a stored row, re-rendering July after any plan change re-priced July — a statement that changes when you look at it twice is not a statement. **Half-open is adopted verbatim because closed-closed makes the changeover instant belong to both rows**, so the day a plan is superseded is priced by a coin flip; with `[from, to)` the obvious operator gesture (`old.effective_to = new.effective_from`) is exactly right, with no gap and no double-priced instant. **The exclusion constraint was refused on evidence, not taste**: every `plans` row that exists today is NULL/NULL — an unbounded window — so any two of them overlap and the constraint would refuse the table's own contents; `billing/caps.py::apply_client_caps` MINTS a windowless row for a tenant with no plan so the client's own cap has somewhere to live, and that must keep working; and NULL bounds are not a defect to be migrated away — `effective_from IS NULL` means "since forever" and `effective_to IS NULL` means "until further notice", which is what an open-ended retainer actually is. So overlap is resolved deterministically rather than forbidden, and the order has two properties worth stating: a dated row beats an undated one once it comes into effect (so the ordinary gesture — leave the old windowless row alone, INSERT the new terms with a start date — is correct without a second statement), and **with every window NULL the order collapses to `created_at DESC, id DESC`, the newest-row rule this repo already had — so this re-prices nobody on the day it lands.** The `id DESC` tail is the same load-bearing tail as `ix_credit_ledger_tenant_recent`: "newest" has to be a TOTAL order or two readers disagree about which row it is, and two rows inserted in one transaction share a `created_at`. **The consequence recorded rather than discovered:** an operator who sets `effective_to` on a live plan WITHOUT inserting a successor drops that tenant's ceiling and overage rate at that instant, silently. That is deliberate — falling back to the expired row would charge a client at terms whose end date we were explicitly told, which is the one thing an end date must prevent — so instead both money readers call `warn_no_plan_in_effect` and the misconfiguration gets a log line an operator can act on rather than a quietly free month. Corollary: `?month=` is now strict (`invalid_billing_month`, 422). A month string selects WHICH PLAN prices the answer, so a month we cannot parse is one we cannot honestly price, and a 422 beats a ₹0.00 invoice for `?month=july`. |

| D-47 | **Subscriber KYC is two gates with two different scopes** — and no identity document is stored anywhere | `kyc_records` (migration `a3f6b1e02d95`; one mutable row per tenant, standard §1 RLS, FORCEd) records that a person at Calevate verified a business's identity against a named **public business-registry** document, on a date, with the evidence pack filed under a reference. **Dialling** is gated for `self_serve` / `trial` only — `compliance.service.kyc_blocker` returns `kyc_missing` / `kyc_not_verified`, asked identically by `check_dispatch` (per dial) and `campaigns.service.launch_blockers` (launch preview). **Buying a number** — `POST /v1/numbers/purchase` — is gated for **every** tier, with no `plan_tier` test at all. Ops records the verification at `POST /v1/admin/tenants/{tenant_id}/kyc` (`admin:tenants`, audited); the client reads their own at `GET /v1/compliance/kyc` and there is deliberately **no client-realm write**. Inbound is untouched (D-38). Provisioning itself remains NOT IMPLEMENTED (`campaigns.provisioning.PROVISIONING_IMPLEMENTED = False`) — the gate exists, the vendor account does not. | D-34 listed "number provisioning gated behind KYC" among R-11's non-negotiable mitigations and it was the only one with nothing behind it — no table, no column, no blocker, no route — while SURFACES §2b, FLOWS §2 and BRD §245 all described the control as though it existed. **The research decided the shape** (sources cited in `apps/api/compliance/kyc.py`): DoT replaced bulk connections with **business connections** (31 Aug 2023, expanded May 2024) requiring the entity's CIN/licence, address, GST where applicable and an end-user list; a **June 2025 circular** extends the same protocols to internet telephony, so a cloud number is not a lesser thing than a SIM; **Telecommunications Act 2023 s.3(7)** obliges authorised entities to identify their users, with three years and ₹50 lakh for obtaining an identifier on another person's identity — which is R-11's exposure exactly, because on a self-serve motion the applicant is a stranger; and **Exotel, our own D-05 pick, blocks outgoing calls until KYC clears**, so a product that let a client "buy" a number without a record would be writing a cheque the TSP will bounce. **DLT PE registration overlaps and does not subsume it** — same entity documents, but held by an access provider for headers and templates, with no address tied to the number's city, no end-user list and no CAF — so this record stores a REFERENCE and never a second copy of `pe_id`. **The tier split is the load-bearing part, and it is two questions rather than one.** A tier-blind DIAL gate would halt every existing client over a data-entry backlog without closing a risk: a managed tenant is not anonymous — we contracted with them, we executed their PE registration, and their dialling is already gated by `pe_registration_*` and `number_not_registered` — and this repo has already paid that price once with `tm_registration_missing`. A tier-blind PROVISIONING gate, by contrast, is the only version that holds: the DoT obligation attaches to the CONNECTION regardless of who pays us, and `plan_tier` is an admin-settable column, so a legal control keyed on it alone is one support ticket away from being switched off — the "bypass for testing" hard rule 5 forbids. It blocks no existing client because it can only ever refuse a NEW request. **Never the document itself**: `document_kind` names entity registries only (CIN/LLPIN/GSTIN/Udyam/shop-establishment/trade-licence), `evidence_ref` is a reference to where the pack is filed, and a CHECK refuses a bare 12-digit `document_ref` — an Aadhaar pasted into a business field is unstorable rather than merely discouraged. `signatory_name` is a name and deliberately carries no identity-document number. **Two things left open on purpose.** Whether a non-licensee reseller must itself hold the CAF, or whether furnishing the entity's documents to the licensed operator discharges us, is NOT settled by any source found — so no CAF, form workflow or document store is modelled. And the residual risk is stated rather than hidden: a managed tenant that somehow holds a number without KYC keeps dialling; that closes with an ops sweep requiring a verification for every tenant holding a number, which is a `platform_state`-shaped surface, not a change to this predicate. |

| D-48 | **"Leave no half-wired feature" becomes a build gate with a named baseline** (extends D-29's pack; changes none of its four) | `scripts/check_wiring.py`, in `make guardrails`. Four framework-aware questions, each asked against a live REGISTRY rather than against symbol references: (1) every module that declares an `APIRouter` is mounted by the running app — including `apps/voice-runtime`, which import-linter structurally cannot see (D-18's hyphen is not a legal module name, so grimp's package walk misses the service entirely); (2) no revision sits on a second head, so nothing `alembic upgrade head` would skip is in the tree (alembic's own `ScriptDirectory` walks the map, so this is not a second revision graph that can disagree with the real one); (3) every ORM column is named by some executable line; (4) the baseline itself still holds. Shape (3) carries `UNWIRED_BASELINE`, a **per-column** dict of deliberate deferrals each of which must state its reason and what closes it — a model-level exemption would silently cover the next column somebody adds — and shape (4) is what keeps that dict a deferral list rather than an exemption file: an entry naming a column that has since been wired, or a column that no longer exists, FAILS the gate. **The baseline may only shrink, and only by wiring something.** What it does NOT check is written down too: not read-vs-write (most access here is raw `text()` SQL, so `agents.business_hours` — a writer with no reader — is out of reach), not enum-member reachability (a member is legitimately produced by a DB row or a vendor payload), and not ARQ crons (`cron()` takes the coroutine by reference, so the failure cannot be expressed; `tests/job_registration_test.py` covers the real one). | D-29's argument is that a rule enforced only by human vigilance gets violated exactly when the codebase grows fastest, and this rule had just proved it four times over: `apps/api/agents/publishing_routes.py` sat complete, tested and mounted by nothing. **An unmounted router is not merely inert — it is unaudited.** Every authorization sweep this repo relies on enumerates the routes of the LIVE app (`assert_policy_registry_complete` at boot, `impersonation_reads_test`, `authz_audit_test`, `check_redaction_exposure`), so a router nothing mounts is in none of them, and it can carry a D-22 violation, an undeclared permission or a raw-PII response for as long as it stays unmounted — with every check reporting green, right up to the day somebody mounts it and the violation arrives with it. **The off-the-shelf tools were evaluated and rejected on the record** (research note at the foot of the file): `vulture`/`deadcode` answer "is this symbol referenced", which on FastAPI/SQLAlchemy/Pydantic is a false-positive machine whose own remedy is a whitelist simulating usage — the exemption treadmill this design forbids; `ruff`'s F401/F841 see one file at a time; `import-linter` constrains which modules may import which, and a module imported by nothing breaks no contract. Alembic's own `ScriptDirectory.get_heads()` is ADOPTED rather than re-implemented, because parsing `down_revision` by hand is a second revision map that can disagree with the real one. **The baseline is the feature, not a compromise**: it turned the standing list of "declared but not yet wired" from folklore into a diffable artefact, and the effective-dating and halt-reason work in this same wave closed three of its entries (`Plan.effective_from`, `Plan.effective_to`, `PlatformState.halt_reason`) by deleting them — which is what a baseline is for. |
| D-49 | **An alert is not an alert until it leaves the process — and observability config with no client is deleted, not kept** | `apps/api/core/alerting.py` gains a delivery path. `alert()` still writes its structured ERROR log FIRST and unconditionally (that is the durable record; delivery is best-effort on top of it), then enqueues to a bounded in-process queue drained by one daemon thread, which sends through the SAME transport as hot-lead notifications (`apps/workers/transport.py`: SMTP / console / null). Two bounds, both named: per-fingerprint repeat suppression on `stage:code` at `ALERT_REPEAT_INTERVAL_S = 900`, and a global token bucket (`ALERT_BURST = 6`, `ALERT_BUDGET_PER_HOUR = 20`); what either drops is counted and reported in the next delivered body, and a FAILED delivery clears the dedupe stamp. Recipient is `ALERTS_EMAIL`; a non-local deployment without it logs `alert_delivery_unconfigured` at boot. **`LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY` and `POSTHOG_KEY` are REMOVED from `Settings` and `.env.example`**, with the restore steps written beside `SENTRY_DSN` and a test pinning that they stay gone; `redact_trace_payload` is kept as hard rule 6's hook shape with a docstring saying nothing calls it. | OPERATIONS §4 promised alerts to a person and §8 makes "alerts firing to Sri's phone" a pre-launch gate; `alert()` wrote a log line and stopped, in a deployment with one operator and no log search — so the post-call stall, the outbox exhaustion, the unkeyable engine payload and the bad payment signature all fired into a file nobody reads at 3am. **Two deliberate departures, both stated because the documents they depart from are binding.** First, this path does NOT go through the transactional outbox (BACKEND-PATTERNS §4), which every other side effect in this repo does: the alarms that matter most are `outbox_dispatch_exhausted`, `pipeline_stalled` and `enqueue_failed`, and an alert routed through the broken component is an alert nobody gets. It therefore touches no database and no Redis, and the honest cost is that a process dying with alerts queued loses those SENDS, never the log lines. Second, what runs inline is bounded to three non-blocking operations — one log record, one dict lookup under a NON-BLOCKING lock, one `put_nowait` — because `alert()` is reachable from voice-runtime's 500ms ack path (hard rule 3), from the global exception handler and from a SIGTERM handler; the lock is non-blocking specifically because a signal handler runs on the main thread that may already hold it, and deadlocking a drain to gain one deduplication is the wrong trade. **The storm shape is adopted, not invented**: per-fingerprint suppression is Alertmanager's `repeat_interval` and PagerDuty's caller-supplied `dedup_key` — which is why every call site carries a stable code rather than a formatted string — and the bucket exists because dedupe alone does not survive a bad deploy, where 500 distinct codes are 500 distinct fingerprints. Fifteen minutes against Alertmanager's 4h default, and no batching, because there is ONE operator and no incident console: first-signal latency beats grouping. **One transport, not two**: a second mechanism for alerts is a second thing to configure and a second thing to be broken on the night it is needed, and the import happens inside the delivery thread so voice-runtime's forbidden `apps.workers` import surface stays clean. **The removal is the other half of the same principle.** Three settings that were no-ops EVEN WITH CREDENTIALS are worse than absent: absent prompts a question, present tells the next reader that traces are being recorded. Restoring Langfuse needs a project nobody holds and a decision-log entry choosing a second tracing pipeline beside the OTel one already shipped; PostHog is browser analytics that never belonged in a backend `Settings` and restores as `NEXT_PUBLIC_POSTHOG_KEY` in `apps/web`. TRD §2 and ROADMAP §2 are corrected rather than left describing the old inventory. |
| D-50 | **Continuous WAL archiving is built, unapplied, and its retention window is 35 days on both chains** | `infra/backup/`: wal-g PITR to a Cloudflare R2 bucket that is NOT the recordings bucket (chain A), plus a nightly encrypted `pg_dump` to a NON-Cloudflare provider (chain B), a nightly base backup, a four-check health watchdog, and systemd timers (`OnFailure=` so a dead run has somewhere to go, `Persistent=true` so a missed night runs at reboot rather than being skipped in silence). `archive_timeout = 300`. **Both chains retain 35 days**, and that number IS the erasure SLA for backups. `FailureStage` gains `HOST_BACKUP` for the one alarm that says the database is unrecoverable. It DOES travel D-49's email path, by default and without configuration: `notify.sh` defaults `BACKUP_ALERT_COMMAND` to the relay beside it, which subprocesses into `python -m scripts.host_alert` → `alert()` → the SMTP transport. An override remains, and stays ONE command, because two delivery paths are two dedupe windows and the day one stops nobody notices. **Nothing is applied**: every secret is a `<<SECRET:…>>` reference, no bucket, endpoint, account id or key appears in the tree, no wal-g command has ever been run, and `runbooks/database-restore.md` carries an UNVALIDATED banner instructing its first user to remove it. "Backups verified" on the OPERATIONS §8 checklist is ticked by `runbooks/backup-restore-drill.md` PASSING once with the record committed to `docs/evidence/`, never by this tree existing. | D-26 chose host-PostgreSQL over managed and closed by ACCEPTING a consequence — nightly dumps alone break OPERATIONS §5's 15-minute RPO, so continuous WAL archiving is REQUIRED, plus an offsite dump and quarterly drills. None of it existed while DEPLOYMENT §9 already assumed wal-g was there, so the RPO the runbooks promised was fiction and nobody would have found out until they needed it. **`archive_timeout` is the line that makes the RPO true**: without it a partial 16MB segment is never shipped, so on a quiet SMB night "continuous archiving" ships nothing between 23:00 and 07:00 — the design fails silently exactly when the promise is being made. **Four checks because one is not enough**: PostgreSQL does not record an archiver killed by a signal or exiting above 125 in `pg_stat_archiver`, and `command not found` is 127, so deleting the wal-g binary leaves the obvious check green; freshness, a `wal-verify` against the BUCKET (the only check that catches "exited 0, the object is not there") and `pg_wal` growth each cover the previous one's blind spot, and the drill induces the exact failure `pg_stat_archiver` cannot see. **Two chains with independent credentials** because our edge and our R2 archive are one Cloudflare account, so collapsing chain B into R2 "to simplify" would put every copy behind one compromise. **Why 35 and not 7 or 365**: the floor is the longest routine reason to reach back into a backup, the ceiling is SEC-COMP §4 — every extra day is a day in which an erasure cannot fully reach our data — so 35 is the smallest window that covers the first and the smallest exposure that satisfies the second. The R2 bucket lock is set at 30 days on purpose, BELOW the retention window, because a lock that outlives the retention rule turns every prune into a failure nobody reads. **A restore un-erases people**, and that is a mandatory numbered step rather than a footnote: anyone whose DPDP erasure completed after the recovery target returns holding a certificate that says otherwise, and the authoritative list must come from the PRESERVED pre-restore cluster, because requests raised after the target do not exist in the restored one — which is why the runbook forbids deleting the broken PGDATA. **The largest unverified assumption is named**: wal-g against R2 is documented-compatible and not confirmed here — R2's multipart implementation has open rejection issues across several clients and wal-g #1639 records `backup-push` hanging after an S3 409 — so the first hand-run push is a watched test and the honest fallback is that chain A moves providers. Two questions this raises are deliberately NOT decided here. The external dead-man heartbeat, now narrowed to exactly three residuals that no in-repo check can cover because each removes the observer along with the observed: the host off or off-network (no timer fires, and absence of alerts is indistinguishable from health), systemd itself not running (which takes the schedule and heartbeat checks with it), and the alert path broken beyond us (wrong recipient, provider refusing, spam rule — every alert 'succeeds' locally and lands nowhere). Only an observer outside the failure domain turns SILENCE into a page; the shape is one line and it is unbuilt because it adds a vendor. And whether `ERASURE_LIMITATIONS` gains a backup clause, which is a legal call recorded as open in SEC-COMP §4. **Partly superseded twice, and both are recorded here rather than left for a reader to discover.** (1) The external dead-man heartbeat named as open here is BUILT (D-54) — success-only ping from `backup-health.sh` to a hosted check, so silence is what pages. (2) **The `ERASURE_LIMITATIONS` question is DECIDED (D-164), not open**: the clause ships, with `BACKUP_WINDOW_DAYS`, an index-aligned `backup` exception and a test pinning the number across the runbook, the DPA and the certificate. This sentence still said "stays open" after the entry had already taken one supersession marker — an entry that can carry one and not the second is the drift `check_docs_drift` is structurally blind to, and it is why the 17 Aug docs pass looked for exactly this shape. |
| D-51 | **The first-campaign manual review is a hold on the ACCOUNT, and absence of a decision means held** | `first_campaign_reviews` (migration `c4d9e18a72b6`; one mutable row per tenant, standard §1 RLS, FORCEd): `status` in (`approved`,`rejected`), `decision_note` NOT NULL with a length CHECK, `decision_source` in (`operator`,`migration_backfill`) with a CHECK that an operator decision names its operator, and `reviewed_campaign_id` → `campaigns` `ON DELETE SET NULL` as EVIDENCE of what was read. `compliance.service.first_campaign_hold_blocker` returns `first_campaign_review_pending` / `first_campaign_review_rejected` for `self_serve`/`trial` tenants only, and is asked by **both** `campaigns.service.launch_blockers` and `dispatch_blockers` — deliberately NOT by `check_dispatch`. The client reads their own state at `GET /v1/compliance/first-campaign-review` (`org:read`); ops decides at `POST /v1/admin/tenants/{tenant_id}/first-campaign-review` (`admin:tenants`, audited, tenant in the PATH). There is no client-realm write and no request path. | D-34 lists "manual review of each account's first campaign" among R-11's non-negotiable mitigations, FLOWS §2 and BRD §245 describe it as though it existed, and `tenancy/signup.py` named it in prose — it was the last of the six with nothing behind it. **"First" had to be a property of the account, and the obvious alternative loses twice in a minute.** A `campaigns.review_required` flag on the first row is defeated by launching a SECOND campaign, which dials unreviewed, and by DELETING the flagged one, which lets a DELETE decide what "first" means — neither an attack, both ordinary client behaviour. Account scope removes both: while held, every campaign is refused, and the campaign an operator actually read is kept as evidence rather than as the thing the hold hangs on, which is why losing that pointer to a delete changes nothing. **Absence means held**, with no `pending` row and no request surface: a stored `pending` would be a second representation of one fact that can disagree with the absence, written on a path being refused anyway, and the read fails CLOSED to held on an unscoped session — the same shape `kyc.NOT_RECORDED` uses. **Enforced at dispatch as well as at launch**, so a release withdrawn after complaints arrive stops a RUNNING campaign at the next tick instead of only blocking new launches. **Released once, released for good on this rule**: the requirement is review of the FIRST campaign, not a signature before every one, and the ordinary gates (DNC, DLT, consent provenance, caps, KYC) carry the rest. **Out of `check_dispatch` on purpose** — that gate is also D-21's single-lead button and the instant callback, and neither is a campaign; every document that asks for this control scopes it to the first campaign. The migration grandfathers self-serve tenants that had already launched, stamped `decision_source = 'migration_backfill'` so a NULL decider is self-describing rather than an anonymous release. **Consequence:** all six R-11 mitigations now hold in code, so the build-side objection to `self_serve_signup_enabled` is retired and the switch becomes a business decision. **Residual, reported rather than papered over:** the hold has no cross-tenant queue, so an operator must be TOLD an account is waiting; `kyc_records` has the identical gap, and both close with ONE change to the admin tenant directory rather than two. That change was in flight as this entry was written — check `apps/api/admin/` before rebuilding it. |

| D-52 | **`calls.latency` is dropped, and per-call latency storage is re-opened by a measurement, not by a schema** | Migration `f1a7c39d5be2` drops the column; the ORM attribute goes with it (or autogenerate re-proposes it), the `UNWIRED_BASELINE` entry is deleted, and `tests/call_latency_column_test.py` guards all four ways it returns — the SQL column, the model attribute, the baseline entry, and prose in `apps/`/`packages/` naming it. The numbers TRD §4 argues from now come from OPERATIONS §2 gate 4, which additionally captures Bolna's `latency_data` as an adapter fixture and records whether it agrees with the stopwatch. | The column had never been written and TRD §4 said every call logs stage timings, so the rule stood while the mechanism did not exist. **The case for filling it was real and was answered, not skipped**: a column is queryable in SQL beside its call, survives sampling — the trace ratio is 0.1, so nine calls in ten have NO span — and outlives trace retention. But every span this repo opens is on OUR side of the call (the post-call pipeline serving the two-minute lead SLO); the in-call audio path runs inside the rented engine, in a process we do not run. Filling a column called `latency` from pipeline stages would have named the engine's two-to-three-minute wait for `completed` as a caller-perceived latency, which is worse than either honest option. Confirmed at the adapter rather than assumed: `ExecutionSnapshot` carries nothing finer than `duration_s`, and the transcript arrives as prefix-tagged plain text, so `TranscriptTurn.start_ms`/`end_ms` are NULL for every Bolna turn and no turn latency is derivable from what we ingest. **Supersedes the adapter's standing claim that per-turn timings are not exposed**: Bolna documents a `latency_data` object on Get Execution (`time_to_first_audio` plus per-component blocks), corroborated by their OSS types. It still cannot fill this column, for three independently sufficient reasons — it is a DIFFERENT set of numbers (voice-to-voice turn latency would be our own arithmetic aligning three components by sequence id); nothing validates that arithmetic, and D-39(b) is explicit that we hold zero real-PSTN measurements, so measure → budget → optimise; and its documented `transcriber.turns` entries carry recognised TEXT, which a naive mapper would land in a JSONB column with no redacted counterpart (hard rules 5/6). Their doc pages are egress-blocked from this environment, so the reading rests on search-surfaced text plus their OSS source — one more reason to capture the payload before writing a parser against it. **A column that always reads NULL is worse than none**, because the next reader builds a dashboard on it. |

| D-53 | **`apps/web` gets a behavioural test suite, and the toolchain is chosen for dependency count as much as for ergonomics** | Vitest 4 + jsdom + `@testing-library/react@16` (the React 19 line, where `@testing-library/dom` is an explicit peer that must be installed separately). 40 tests across 6 files, wired into a new `make web-check` (typecheck → lint → test) that joins `make check`'s prerequisites, and into the CI frontend job — which also gained `pnpm -C apps/web lint`, previously defined and wired into nothing. `next build` stays CI-only. **Three conventional packages deliberately omitted**, each argued in `vitest.config.mts`: `@vitejs/plugin-react` (its only job here is overriding `jsx: "preserve"`, which one `oxc` line does without pulling a Babel toolchain in), `vite-tsconfig-paths` (one alias, spelled out) and `@testing-library/jest-dom` (matcher sugar, where these assertions are about presence AND count). | Every frontend guarantee rested on `tsc`, ESLint, a successful build and human reading — while the frontend had accumulated decisions a type checker structurally cannot see: `firstCampaignState` fails CLOSED and `holdRule` fails VISIBLE (opposite directions, both deliberate); verdicts key on the server's `is_verified`/`messageable`/`held` and never on `status`, so a lapsed consent cannot render green; money arrives as a string that must never be parsed; a null worst-case renders "we cannot say", never ₹0. Several of those had been bugs in earlier drafts caught by reading, which is not a control. **The suite earned its keep on its first run by finding two live bugs of one kind**: `holdRule()` and five sites in `kyc.ts` tested membership with `in`, which walks the prototype chain — so `holdRule("constructor")` returned `Object` typed as a hold rule and the next line blanked the entire ops queue with a TypeError, and `isKnownKycStatus("constructor")` answered true, rendering a verdict box with no headline and an undefined tone. A screen that goes white is the one way to fail an operator cannot see past, and the documented contract was fail-VISIBLE. Fixed with `Object.hasOwn` in both, one way per problem. The same shape survives in six `Record`-with-fallback lookups fed by our own enums — lower risk, recorded for a sweep. **Hard rule 9 was the gate on this entry, not a footnote**: 50 new packages, the full added list read, `pnpm-workspace.yaml` byte-for-byte unchanged with `allowBuilds` still only `sharp`, no build script blocked, `pnpm audit` identical before and after (9 findings, all pre-existing paths, zero entering through the new dependencies) verified by restoring the baseline lockfile and re-running rather than by eyeballing, and D-19's pins (`next@15.5.21`, `react@19.2.4`) confirmed untouched. One unfamiliar transitive — `obug`, a `debug`-adjacent name of exactly the shape the July 2025 campaign exploited — was chased to a zero-dependency fork by a Vite core maintainer **published via npm OIDC trusted publishing rather than a laptop token**, which is the specific failure mode that incident turned on. |

| D-54 | **The backup dead man is EXTERNAL, it is fed only by success, and silence is the alarm** | `scripts/backup/backup-health.sh` gains one final step, reached only when `failures == 0`: `heartbeat.sh` → `python -m scripts.host_heartbeat` → one GET at `BACKUP_HEARTBEAT_URL`, a hosted **Healthchecks.io** check configured at 15-minute period / 1-hour grace. No new deployable, no infrastructure we run, no second scheduler — the emitter is the check that already computes the verdict. The URL is a `Settings` field (`.env.example` + `check_env_parity`), injected from the secrets manager, treated as a CREDENTIAL (anyone holding it can silence the alarm) and never logged: operator output names a 12-char digest. Unset = a stated no-op, announced once per state transition in journald, never a silent pass. An undelivered ping is `backup_heartbeat_undelivered` in the journal and does NOT touch the failure count, the exit status or the mail path. `app-python.sh` is extracted so the alert relay and the heartbeat share ONE implementation of "run a repo module as `postgres`, unattended". `tests/backup_heartbeat_test.py` (12 tests) drives the real script with stubbed `psql`/`wal-g`/`systemctl` against a local HTTP monitor; `runbooks/backup-heartbeat-silent.md` is the triage, drill §7.8 is the proof. | **Closes D-50's named residual**, which was the last alarm in the system with nowhere to go. Every other alarm here is a MESSAGE and therefore dies with its sender: the host off or off-network, systemd not running, or the mail path broken beyond us each remove the observer along with the observed, and absence of alerts is indistinguishable from health. Only an observer outside the failure domain turns SILENCE into a page. **The asymmetry is the mechanism, and the tests are load-bearing on the half that is easy to "improve" away**: a failing run must ping NOTHING, or the check silently degrades into "this script ran" and sits green through a broken chain — so the vendor's `/fail` and `/start` endpoints go deliberately unused (failure already has a delivery path, and a second one is two dedupe windows on one fact — `notify.sh`'s own argument). **Vendor chosen against three real alternatives, argued with citations in `scripts/host_heartbeat.py`.** Self-hosting the same BSD-licensed software was the strongest-looking option and loses twice: new infrastructure we run, and an observer inside the failure domain. **Sentry Crons was the serious contender** — already a vendor here, also a plain check-in URL — rejected because `SENTRY_DSN` is optional and unset by default (the last alarm standing would depend on a credential error reporting may not have), because that credential is the platform project key rather than a single-purpose bearer secret we would be putting on the database host, and because a missed check-in rides Sentry's quota and alert rules, so an unrelated error storm could silence backup monitoring. Dead Man's Snitch (1 free check, no self-host path) and Cronitor / Better Stack / UptimeRobot (heartbeat as a side feature, priced with a bundle) are dominated for one check and one operator. The exit door matters more than the price: the protocol is a GET at a URL, so migrating providers — or self-hosting under duress — is a config change. **Accepted residual, stated rather than solved:** the monitoring vendor being down tells nobody anything; a monitor watching the monitor is a regress that costs more than it buys at this size, and the quarterly drill re-proves the check still turns red instead of assuming it. Doc pages were egress-blocked from the build environment (as Bolna's were for D-52), so the ping semantics rest on search-surfaced documentation and are re-verified by hand at the first drill. |
| D-55 | **The webhook ack budget is a per-process CAPACITY rule, and the only defect behind the convoy was a connection pool that churned** | Diagnosed by measurement, then split into the two things it actually is. **(a) The code fix**, `apps/api/db/session.py`: the shared async engine now runs `pool_size=DB_POOL_SIZE` (new `Settings` field, default 16), `max_overflow=0` and `pool_timeout=5s`, replacing SQLAlchemy's defaults of 5 + 10 overflow and an unbounded-in-practice 30s wait. **(b) The sizing rule**, DEPLOYMENT §2a: `ack_p50 ≈ in-flight ÷ acks-per-second-per-process`, so `max in-flight per process = 0.5 x T`; at the measured T≈250/s that is ~125, giving 2 uvicorn workers for a 100-concurrent pilot and 4 for 250+ production, with a connection budget table that needs `max_connections = 200` on the VPS. OPERATIONS §5 gains the triage (flat distribution = add processes; `webhook_claim_timeout` = incident). **(c) The enforcement**, `tests/voice_runtime_ack_budget_test.py` §1b: three DETERMINISTIC assertions — a warm receiver opens ZERO new Postgres connections under a storm twice the pool's width, the engine has no overflow and takes its ceiling from config with a bounded `pool_timeout` above the durable deadline, and the connection is checked in BEFORE the post-commit Redis write. No millisecond bound was added. | The storm suite (commit 7c9f143) recorded `X-Ack-Ms` p50 going 110ms at 1 concurrent → 669ms at 96 → 1389ms at 384 with a FLAT distribution, breaching hard rule 3 at roughly 100 in flight per process — against D-32's Bolna numbers of 100 concurrent on Pilots and 250+ in production. **Four plausible causes were measured and three were eliminated.** Pool exhaustion: NO — the connection is acquired lazily at first execute, and instrumented checkout wait was p50 0.1ms at 192 concurrent; pools of 8, 16 and 32 performed identically. The enqueue inside the transaction: NO — 26ms p50 at 192 concurrent, and a bare-Redis control ran 384 concurrent GETs in 44ms. Lock contention on the unique index: NO — the distinct-key storm, which contends for nothing, was as slow as the same-key herd. CPU work on the ack path (json, hashing, pydantic): NO as an isolated hotspot — accept-to-first-await was 0.5ms p50. **What it IS: the process is CPU-saturated on ONE core.** Measured out of process against real uvicorn, `utime+stime` sat at 92–99% of a single core from 8 concurrent upward, ~3.5ms of CPU per delivery, and throughput pinned at ~250 acks/s while latency tracked `in-flight ÷ throughput` to within a few percent at every width — Little's Law, i.e. a queue, which is exactly why the distribution is flat. A cProfile attribution found no hotspot to remove: psycopg 14%, asyncio 13%, SQLAlchemy 10%, Starlette/FastAPI 9%, redis-py 7%, our own code 2.8%. **That makes it a sizing question, and a sizing rule that is not written down is not a rule** — hence §2a rather than a code change nobody can point at. **The one genuine defect** was found in the same profile: connections above `pool_size` are SINGLE USE (SQLAlchemy closes them on return — sqlalchemy/sqlalchemy#11707, where the maintainer's answer is "avoid the overflow and make the pool size bigger"), so at 100 concurrent the receiver burned **186 fresh Postgres backends for 1448 requests, 34 per second**, while never exceeding 15 concurrent. Each costs ~6ms of CPU in this process — `password_encryption = scram-sha-256` means PBKDF2 on every connect — so ~20% of the single core was being spent re-authenticating connections it had just discarded. A same-process A/B at 128 in flight, with the old configuration re-run afterwards as a control, measured 200 → 232 acks/s (+20%) and p50 628 → 542ms, and churn falling from 5.9–8.0 to 1.2 new connections per 100 requests; against real uvicorn the backend count fell 186 → 18. **`pool_pre_ping` was measured and KEPT**: turning it off bought a further 12% (232 → 265 acks/s), and it is declined, because the receiver's traffic is bursty by nature and a connection killed by an idle NAT between campaigns would surface as a failed ack at an endpoint whose vendor never retries (D-31) — a lost call is not worth 12% of a throughput number we can buy back with a `--workers` flag. `pool_recycle` was the alternative and guesses an interval instead of asking. **Deliberately NOT done, with the arithmetic:** folding the claim and the forensic INSERT into one round trip would cut 1 of the 4 statements per ack (~10–15% throughput) at the cost of complicating a durable claim shared by four callers — a quarter of what one extra process buys, on the code that decides exactly-once. **Why the enforcement is a connection COUNT and not a millisecond bound**: a wall-clock assertion under concurrency measures the CI runner, which both the storm file and `voice_runtime_security_test.py` already refuse on the grounds that a flaky latency test gets deleted along with the guarantee it carried. A count of connections opened is exact on any machine at any speed and is the mechanism the latency was made of; it was seen to fail by restoring `max_overflow=10` — 8 connections opened where 0 are allowed — and the sibling assertions were seen to fail by moving the fast-path write back inside the transaction and by re-defaulting the pool. Correctness was untouched throughout — every assertion in the storm suite still holds unchanged. |
| D-56 | **An in-call opt-out is caught in TWO layers with ONE write path, and the detector is a phrase list that says so** | `apps/api/compliance/optout.py` is the whole seam: a curated Telugu/Hindi/English phrase list (`detect_opt_out`, romanised **and** native script, caller turns only) plus `record_call_optout`, which suppresses through the existing `compliance.service.add_to_dnc` (`source='call_optout'`, `ON CONFLICT DO NOTHING`), appends ONE `consent_ledger` withdrawal (`purpose='marketing'`, `consent_source='inbound_call_verbal'`, evidence = detector, rule, language, turn index and the caller's matched words) and writes one `audit_log` entry. Two callers reach it and nothing else does: **(a)** the post-call pipeline, as step 2b — before extraction, because extraction is a 30s model round trip and the deadline is 30s; **(b)** an in-call tool, `POST /tools/v1/{engine}/opt-out` in voice-runtime (source-IP verified, bounded body, no DB writes, ack measured) → the `record_in_call_optout` ARQ job, which resolves tenant, number and direction from the authenticated Get Execution rather than from the payload. Dedupe is on `(tenant, phone, purpose, status, call_id)`, so a replay, a double tool call and both layers firing on one call produce ONE record. The xfail pin `eval_known_gaps_test::test_the_post_call_pipeline_writes_an_in_call_opt_out_to_the_dnc_list` is promoted to `tests/call_optout_test.py`, whose proof is the real dispatcher answering `dnc_blocked`; `scripts/eval.py` now scores the detector against every fixture in both directions. | **The hole:** `dnc.SOURCES` carried `call_optout` from the day the module shipped and only TESTS wrote it — a caller could opt out, the agent could confirm, and the next tick dialled them again. **Why both layers rather than the one the xfail proposed:** they fail differently. The tool depends on the model invoking it and on a vendor behaviour that is UNVERIFIED (OPERATIONS §2 gate 8 — Bolna custom functions, no documented tool timeout; D-31/D-32 say that is a gate or a marked assumption, never a silent premise), so it cannot be the only layer; the transcript pass runs on every completed call but is late by the length of the call, so a 40-minute call leaves 38 minutes in which the number is dialable. The union is what makes hard rule 5 true for the population. **Why a phrase list and not a model:** an LLM call on a compliance path adds a per-call cost, a second dependency and the failure mode 'the extractor was down, so nobody was suppressed'. The engine's own tool call IS the comprehension layer; this is the deterministic floor under it, and the file says out loud that it will miss phrasings nobody wrote down. **The failure direction is chosen:** a false positive suppresses a caller who did not ask (annoying, lawful, recoverable); a false negative dials someone who did (a TCCCPR violation with the CLIENT's registration on the line, 5+ complaints in 10 days ⇒ TSP action). So the patterns favour recall — except where the negation carries the meaning (`call cheyandi` = 'please call' vs `cheyakandi` = 'don't call'), which would suppress the caller who asked for a callback. **Regulatory basis, researched not recalled** (TCCCPR 2018 Reg. 6, the Second Amendment of 12 Feb 2025, PIB PRID 2102413; gazette PDFs are cited in the module because this environment's proxy blocks trai.gov.in): preferences must be given effect **in near real time and in no case beyond twenty-four hours** — our 30-second tick deadline is stricter and stays that way; revocation must be recorded **immutably and non-repudiably** (hence the append-only ledger row rather than a flag); a sender may not re-solicit an opted-out customer for **90 days** (hence `call_optout` staying outside `dnc.REMOVABLE_SOURCES`); and the obligation sits with the **Principal Entity — the client — regardless of using a third-party telemarketer**, which is why the suppression is tenant-scoped and the evidence has to be legible to them. What this is NOT: a DND/preference registration. That is an Access Provider function (1909/DLT) we do not hold, and the module says so. **Known residual:** consent withdrawal and erasure demands (`rt_cl_consent_withdrawn_midcall`, `rt_re_erasure_request_midcall`) are deliberately out of scope here — they are DPDP §12 requests with their own surface, and folding them in would file a suppression nobody asked for while still leaving the erasure unserved. |
| D-57 | **The dispatch tick's screening loop moved INTO Postgres, and the tick became single-flight** | `dispatch_scan(active_statuses, active_horizon)` (migration `a8d4f21c9b06`) — **SECURITY INVOKER** plpgsql that walks `engine_agent_routes` and, per tenant, sets `app.tenant_id` and reads that tenant's live outbound lines and running-campaign flag under that tenant's own policies, returning ONLY the tenants that hold a line or have a campaign. `apps/workers/campaign_dispatch.py` therefore opens tenant sessions for WORK instead of for existence, and takes a Redis `SET NX PX` lease (`_tick_lease`) so two ticks can never both spend the shared line pool. Two partial indexes (`ix_calls_outbound_live`, `ix_campaigns_running`) keep each probe index-only. No new table, so no new RLS policy. | **Measured, not estimated:** 12,070 dispatchable tenants cost 12,070 sessions and 22.9s on a job scheduled every 30s — and the split was 48% session setup / 30% query / 21% commit, i.e. two thirds session machinery, 80% of it CPU inside the worker, which is why parallelising measured only 22.9s→17.2s at 8-way. Deleting the sessions gives 0.25s (1M call rows, warm). **Rejected:** `SECURITY DEFINER` owned by a `BYPASSRLS` role (faster still, and hard rule 1 in a wig); a GUC widening of `campaigns`/`calls` (a `calls` row carries `to_e164` — a platform-wide read of it is a PII leak, not a refactor); a trigger-maintained global (tenant, lines, campaigns) index (a new table, a new RLS exemption and write amplification on the busiest table, to answer a question Postgres already answers in 0.25s); bounded-parallel tenant sessions (the cost is client CPU, and `max_overflow=0` means the pool queues rather than bursts — sqlalchemy#11707). **Overlap was a real bug:** an arq cron id embeds its intended execution time, so consecutive ticks are different jobs and DO overlap; the claim CAS already prevented a double DIAL, but the platform pool was a read-then-act (BACKEND-PATTERNS §5), and arq's own remedy — a fixed `cron(job_id=...)` — would turn a 30s tick into a 60s one (`keep_cronjob_progress`), which hard rule 5's DNC deadline forbids. |
| D-58 | **Every WRITE on the ops router is step-up confirmed, and a confirmation that cannot be SIZED is not one** | `POST /v1/ops/outbox/replay` was the one mutation on that router with no `X-Confirm-Action` check, and the console collected a typed word it never sent — a confirmation dialog that confirmed nothing to the server. It now takes `replay_dead_letters`, or `replay_dead_letters:<job>` when scoped, refused in BOTH directions so a header can never authorise a wider act than the request performs; `GET /v1/ops/audit/verify` is the router's one deliberate exception. `GET /v1/ops/platform` additionally publishes `outbox_dead_letters` (depth, per-`job` breakdown, oldest age) from the SAME grouped aggregate that feeds the `outbox_dlq_depth` metric. | It is the widest-blast-radius control in the product and it was the least guarded: replaying does not flip rows, it RE-SENDS — real HMAC-signed webhooks into clients' own CRMs, real Sheets appends, real emails, across every tenant at once, from one unconfirmed POST. Scoping follows from the 100-row cap rather than from taste: unscoped, an operator recovering one client's CRM webhooks out of a backlog of dead-lettered emails replays 100 emails, reads `replayed: 100` as success, and leaves every webhook parked. The depth is on the existing route because `ops:manage` is a MUTATING permission, so a NEW GET declaring it would have to be added to `ADMIN_CONSOLE_GETS` — an allowlist whose own test warns that entries are how it becomes a hole. **Breaking, not grandfathered** (D-45's precedent): the runbook's curl and the console moved in the same change, and the runbook had twice stated something true when written and false when read. Per-TENANT scoping is refused rather than approximated — `outbox_messages` has no `tenant_id` and the ids sit unindexed inside the JSONB payload, so it is a migration under hard rule 8, and the console says so instead of letting an operator assume the scope bounds whose data moves. |
| D-59 | **A chain that appends inside a transaction is serialized BY that transaction, not by a lease** | The `audit_log` hash chain moves from BACKEND-PATTERNS §5's Redis `SET NX PX` mutex to `pg_advisory_xact_lock(hashtextextended('audit:chain', 0))`, and the Redis head cache is **deleted** — the head's only home is the last row of `audit_log`. `at` is stamped `clock_timestamp()` rather than `now()`, `(at, id)` is indexed (`ix_audit_log_chain`, built CONCURRENTLY), and `GET /v1/ops/audit/verify` walks the WHOLE log by default, returning `entries_checked` / `complete` / the `at` range beside the verdict. §5 and §7 are amended in place; `credit_ledger` already worked this way (`billing/service.py::lock_tenant_credits`), so this is one way per problem rather than a new one. | The Redis lock was not merely mis-tuned, it was **not consulted**: the `SET NX` result went into a variable read only by the `finally` that released it, so a writer that lost the race proceeded anyway. Repairing that would not have been enough. Correctness needs no second writer to read the head between our read and our COMMIT, and the entry is appended in the CALLER's transaction (§7's own choice) — a window of unbounded length, so any TTL is either too short (a 4s commit outlives a 3s lease and the next writer chains onto the same head) or long enough that a crashed holder parks every audit write behind it. The cached head fails the same way one level down: published inside the transaction a ROLLBACK erases it, published after COMMIT a crash in the gap loses it, and either leaves the next writer forking off a head that does not exist — a **durable** break in the one artefact whose entire value is being unbreakable. Validating the cache against the table is the query the cache existed to avoid. An advisory xact lock is released by COMMIT *or* ROLLBACK, the two events that decide whether the row exists, so its lifetime is exactly the window needing protection; there is no TTL, and no second system whose outage forks the chain. `now()` was the same class of bug in the timestamp: it returns TRANSACTION START, so two overlapping transactions could write entries whose `at` ordering contradicted their chain order and `verify_chain` would report tamper on an untampered log. The scope change is the visible half: `limit=1000` on `ORDER BY at ASC` verified the OLDEST thousand, so on any real log the green tick covered the entries nobody was worried about and said nothing about last night — and the console rendered it as an unqualified pass. **Cost, stated plainly:** audit writers now queue for the REMAINDER of the holder's transaction, not just its INSERT, which is inherent (releasing earlier lets the next writer read an uncommitted head) and is why `write_audit` belongs late in a transaction; a holder that then blocks on another audit writer deadlocks, which Postgres detects and raises — loud and recoverable, versus a silent fork. |
| D-60 | **Inbound A/B attribution is a question of fact, not a bucket we may draw afterwards** | `workers/pipeline.py::_record_arm_the_engine_ran` attributes a call to a script arm if and only if `ExecutionSnapshot.engine_agent_ref` names an ARM's own engine agent — the ref `service.publish_variant` minted for it — and attributes nothing when it names the agent's. `agents/assignment.py::arm_of_engine_ref` is that lookup, and it writes through the same `record` (same transaction as the `calls` row, `ON CONFLICT (call_id) DO NOTHING`) as the dial path, so there is still exactly one kind of assignment row and no call can change arms. `experiments.results_for` stops asserting `attributed_directions = ("outbound",)` and READS the directions off the assigned calls, and a two-way agent's coverage note now prints how many completed inbound calls in the window are in neither arm. `start` still refuses an inbound-only agent. No new table, so no migration. | The ROADMAP called this "an assignment lookup at inbound call creation", and that framing is the trap: the lookup it implies is `assign()`, which DRAWS a bucket. Outbound may draw because we choose to dial and can dial the arm we drew. An inbound caller dialled the client's number, which answers with the agent (FLOWS §3), and heard whatever the live prompt pointer said — so a bucket drawn when the call row is created names an arm nobody spoke, and reports the caller's real conversion under it with nothing in the data to show what happened. **The two-speed publishing machinery does not rescue it either**: the live version often EQUALS the control's, but attributing inbound on that basis puts 100% of a non-randomised traffic stream into one arm, and Newcombe's interval — which assumes two randomised binomials — would then be describing the direction mix rather than the script. What IS available is unambiguous: every arm is its own engine agent with its own ref, so the engine's answer to "which agent object ran this call?" is the arm, with no inference at any step. That fact is recorded wherever it appears, in both directions, which is why the same change repairs an OUTBOUND defect nobody had noticed: `dispatch_call` records only when its own INSERT wins the `engine_call_id` conflict, so an engine webhook arriving before our transaction committed created the `calls` row first and left a call that demonstrably ran arm A carrying no arm — a silent under-count of one side of the comparison. **Deliberately NOT built:** inbound randomisation via the engine's incoming-call webhook (selecting the agent per call is an UNVERIFIED vendor capability — D-31/D-32 discipline says that is a gate in OPERATIONS §2, not a premise), and a `unattributed_inbound` API field (the count reaches the reader through `coverage_note`, which the prompt screen already renders; a field no screen reads is the half-wiring this entry exists to remove). |
| D-61 | **Hard rule 6 is enforced at the SPAN EXPORTER, and the hand-called hook D-49 kept is deleted** | `observability.redact_trace_payload` — the hook CLAUDE.md's hard rule 6 names, which D-49 deliberately KEPT as a shape after Langfuse config was removed — is gone. Every span now leaves through `_RedactingSpanExporter`: attributes re-sanitized, exception events reduced to `exception.type` + `exception.escaped`, `Status.description` replaced by the exception type, span name through `redact_text`, and a span whose redaction RAISES is dropped rather than exported. Sentry loses `traces_sample_rate` and gains `before_breadcrumb=scrub_breadcrumb`. The OTel/error-tracker correlation the old hook justified moves to a real caller: `scrub_event` stamps `tags.otel_trace_id`. | **D-49's premise stopped being true, and the audit that checked it found a LIVE leak rather than a tidy-up.** D-49 kept the hook because no real enforcement point existed; it recorded a shape, and a shape enforces nothing. Every call site in this repo was already clean — no `set_attribute` / `add_event` / `record_exception` exists outside `observability.py` — which is exactly why this survived. The PII came from three fields the OTel SDK writes ITSELF: `start_as_current_span` defaults to `record_exception=True` and `set_status_on_exception=True`, so any exception escaping any span writes `exception.message`, `exception.stacktrace` (a span EVENT, which `sanitize_attributes` never sees) and a status description. Reproduced against the live in-memory exporter with a transcript in full; the production vector is `str(IntegrityError)`, which embeds `[parameters: {'p': '+91…'}]`, reached by a duplicate-lead insert in `pipeline.lead_upsert`. Fixed at the exporter and not the call sites because `record_exception=False` at four sites fails at the fifth — a guard you must remember is the one that fails when the tree grows fastest (D-29's own words). Message and stacktrace are DROPPED rather than `redact_text`-ed, on the same fail-closed doctrine as the attribute allowlist: `redact_text` catches phone-shaped runs and length, not a caller's name or a Telugu sentence; span, ids, ERROR status and exception type all survive, so the signal does. Two Sentry gaps came with it: `traces_sample_rate` was running a SECOND, unfiltered tracing pipeline whose transaction events `before_send` never sees (sentry-python#1226), and breadcrumbs are built by the logging integration from `record.getMessage()` BEFORE our formatter redacts. **Latency (hard rule 3) is zero on the ack path by construction** — the exporter runs inside `BatchSpanProcessor`'s own thread, never the request thread; measured at 9.12us/span anyway, with voice-runtime's disabled-tracing cost unchanged at 2.07us. The hook is deleted rather than left beside the automatic one because two ways to redact one thing is the accumulation CLAUDE.md forbids, and the hand-called one is the way that gets forgotten. **A Langfuse restoration therefore inherits a requirement, not a hook**: it must export through this pipeline, because a direct SDK client would be a second unfiltered path — which is precisely what `traces_sample_rate` turned out to be. |
| D-62 | **An A/B arm's counts are resolved BY DIRECTION, because one of them can hold calls nobody dialled** | `VariantResult.dialled` / `VariantOut.dialled` are replaced — no alias, D-45/D-58 precedent — by `outbound_dialled` (calls we PLACED into the arm, any status) and `inbound_attributed` (how many of `attributed` arrived inbound). `attributed` keeps its meaning and stays the denominator of `rate` and of Newcombe's interval; the statistics are untouched. `_counts_sql` filters each count on `calls.direction`, and the prompt screen renames the column to "Dialled (outbound)" and folds the mixture into `rateReading` itself, so no rendering of a rate can drop the sentence saying part of its denominator was never split. | D-60 gave `call_variant_assignments` a second writer, and `dialled` was `count(*)` over that table — so an inbound call an arm's own line answered was published to a client under a column meaning "calls we placed for you". A direction-neutral RENAME would have made the label true and left both numbers it feeds wrong. **The connect-rate diagnostic is an outbound question**: the gap between placed and completed is how a telephony fault is told apart from a script fault, and an inbound call arrives already connected, so folding it into both sides moves the ratio towards 1 — a diagnostic that fails in the direction of looking healthy. **And the count that can actually be a MIXTURE is `attributed`, not `dialled`**: outbound calls are randomised by `assignment.bucket_of`, inbound ones are not drawn at all (D-60), so two arms mixing the populations in different proportions are not the two randomised binomials Newcombe assumes. `coverage_note` already said so in prose; a number is what lets the screen qualify the specific rate it is rendering, and what a future decision to restrict the analysis set would be made on. **Deliberately NOT done: restricting the comparison to outbound.** That is the statistically cleaner estimator and it is a change to what the product measures — it would silently discard conversions D-60 chose to record as facts, and it should be made when this field shows the mixture is non-zero in practice, not ahead of the evidence. Today's provisioning answers inbound with the agent, so `inbound_attributed` is 0 on every live experiment. |
| D-63 | **The onboarding setup fee is billed through the invoice, and "once" is a unique index rather than a reader's `if`** | `plans.setup_fee` stops being a column nothing reads. New append-only, RLS'd `one_time_charges` table (migration c7e1a4b90d63) with `UNIQUE (tenant_id, kind, ref)`; `billing/charges.py` writes the setup charge with an unconditional `INSERT … ON CONFLICT DO NOTHING` and `billing/invoice.py` reads the month's charges as ordinary lines. The charge is stamped to the tenant's ONBOARDING month (the IST month of `organizations.created_at`) and carries the amount AS BILLED plus the `plan_id` that quoted it. A NULL, zero or negative `setup_fee` writes nothing and prints nothing. The `UNWIRED_BASELINE` entry is deleted; `tests/setup_fee_test.py` holds the four ways it could double-charge. | An invoice here is a DERIVED statement (D-46's whole premise), so "charged on the first invoice and never again" cannot mean "the first time this code runs" — regeneration is normal. It has to be a durable fact the render re-reads, which is a ledger row, and the ledger is where the idempotency belongs: `WHERE NOT EXISTS` in front of an insert is the check-then-write hole that put duplicate pairs on `credit_ledger` (f9c2b41a8e57), so the guard goes IN the write (BACKEND-PATTERNS §5) and the second of two concurrent generations blocks on the index entry and writes nothing. **The advisory lock the credit ledger uses was considered and rejected**: that ledger must READ its predecessor to compute `balance_after`, so only a lock covers its critical section; here there is nothing to read and a unique index also binds a future writer who forgets the lock. **The month is the onboarding month, not "whichever statement was opened first"** — the latter makes July's invoice depend on whether August's was rendered before it, which is the same defect D-46 closed one level up. Its cost is stated rather than hidden: a tenant whose onboarding month is never rendered is never billed (the row is written whenever that month IS rendered), and there is no proration to split a fee across a plan change because D-46 has none to build on. **The amount is frozen in the row** because `plans` is mutable and a re-derived fee could change a statement a client already paid. **Deliberately NOT built:** an `invoices` table (still only needed when a statement acquires state we cannot derive — sent/paid/overdue), a signup-time side effect (nothing in the product creates a plan row, so there is no such moment, and a fee recorded at signup and rendered elsewhere is two sources for one number), and any reversal SURFACE — a waiver is a compensating row under its own `ref`, which the invoice already prints. |
| D-64 | **The setup fee is issued by a scheduled job, and rendering an invoice writes nothing** | `billing/charges.py` splits into `issue_setup_fee` (the only writer) and `one_time_charge_lines` (a pure read); `billing/invoice.py` no longer writes, and no longer reads `organizations.created_at`. New `apps/workers/billing.py::issue_one_time_charges`, a DAILY arq cron registered in `workers/settings.py` at 02:05 worker-local with an explicit `max_tries`, issues every owed-but-unrecorded fee. Migration e3f9c2a71d84 adds `unbilled_setup_fees(charge_kind, charge_ref)`, a SECURITY INVOKER plpgsql scan modelled on `dispatch_scan` (D-57): it loops organizations and `set_config('app.tenant_id', ...)` per tenant, so `plans` and `one_time_charges` answer under that tenant's own policies. The unconditional `INSERT ... ON CONFLICT DO NOTHING` and the unique index of D-63 are untouched. | D-63 named its own gap and this closes it: the fee was recorded when a statement was RENDERED, and nothing in this product renders statements on a schedule, so a tenant whose first invoice nobody opened was never charged — and the only caller was a GET, i.e. a read with a side effect. **`POST .../issue` was considered and rejected**: a button is still a human, which is the same defect one layer down, and it would add admin API surface with no `apps/web` caller. Moving the write into `issue_setup_fee` buys the read/write split without an endpoint. **Daily rather than monthly** because the fee is owed the moment an operator attaches a plan quoting one, and a monthly run would leave a tenant's own in-progress statement without its setup line for up to 31 days and then grow one. Crucially THE SCHEDULE DOES NOT PICK THE MONTH — `issue_setup_fee` derives it from the tenant's `organizations.created_at` via `ist_billing_month`, so the stamp is identical whichever night the tick runs; that matters because arq evaluates `cron()` against the worker's LOCAL clock and a billing decision must never depend on a container's TZ. **`arq.cron()` defaults `max_tries` to 1 and `WorkerSettings.max_tries` does not apply to a function carrying its own**, so the retry ladder CLAUDE.md mandates would have been silently absent; it is passed explicitly and verified against a real `arq.worker.Worker` schedule. **The judgement call worth a reviewer's attention:** the cross-tenant scan runs under `admin_session()`. `engine_agent_routes` — what `campaign_dispatch` and `retention` use to avoid it — is a SUBSET of this population (a tenant can owe an onboarding fee having never published an agent), and c7e4b19d3f52's rule is "a superset here and a subset never"; the alternative was a third `RLS_EXEMPT_TENANT_COLUMNS` entry making every client's commercial terms globally readable. `app.admin` widens `USING` on `organizations` only, `plans` and `one_time_charges` stay fenced inside the loop, and every write happens in an ordinary `tenant_session`. The fencing is load-bearing and tested, not asserted: dropping the per-iteration `set_config` turns 6 of 22 tests red. |
| D-65 | **A state transition answers three questions, not one** | New `apps/api/db/transition.py::transition_status` — CAS first and unconditionally, then, only on the losing path, a write-free SELECT to name which zero-row fact it was: row absent = 404, already in the target state = SUCCESS with no second write, any other state = 409 `invalid_status_transition` NAMING the state actually found. `campaigns/service.py::set_campaign_status` and `kb/service.py::approve_source`/`reject_source` delegate to it and return whether THIS call moved the row; `admin/routes.py` writes its audit row only on a real transition. No new error codes. | All three cases previously collapsed into one 409, which lied in two directions. It told an operator reviewing a queue that an already-approved source "is not awaiting approval" when the outcome they wanted had already happened — and a repeat click wrote a SECOND audit row. And it answered 409 for an id belonging to ANOTHER TENANT, i.e. reported that a row exists; RLS makes those rows invisible, so 404 is both the correct answer and the one that does not confirm the id. Already-in-state is a success per RFC 9110 §9.2.2, and the repeat deliberately writes NOTHING so `approved_by`/`approved_at`/`rejection_reason` stay the FIRST reviewer's — an approval is attributable, and a double-clicked button must not rewrite who signed off. The shape is not new: `ingest/service.py::set_active` and `integrations/service.py::deactivate_endpoint` already did exactly this for BOOLEAN flags (which have no "different state" branch); this generalises that one to a status column rather than inventing a second way. `set_campaign_status` also stopped INTERPOLATING its from-statuses into the SQL as string literals — they are bound parameters now; the values are internal constants, so this was one refactor away from mattering rather than exploitable today. **No compliance gate was added or removed**, on the resume path or any other: a campaign can sit paused for a week, so a gate at THAT moment proves nothing about the moment it dials, and the dial-time check is the enforcement (`campaign_dispatch_audit_test.py` pins it). **Left for its own slice:** `launch_campaign`'s own CAS still raises a vague `frm`, campaign pause/resume still write no audit row at all, and lead status / DLT template status / experiment conclude were not audited for this pattern. |
| D-66 | **A tenant's commercial terms are RECORDED, never edited — and the absence of them is a state an operator must clear, not a row we invent** | New `billing/terms.py` writes `plans` INSERT-only, resolving what a row supersedes through the EXISTING `plan_in_effect_sql` rather than a second ordering. `GET/POST /v1/admin/tenants/{id}/commercial-terms` and the Commercials screen; onboarding still writes no plan row, and the GET returns a named `state` (`none` / `unpriced` / `lapsed` / `set`) the screen renders as a refusal. Loosening a hard cap additionally requires `ops:manage` AND the `X-Confirm-Action` header. Migration a1c4f70b9e28 adds `ck_plans_window_ordered` (`NOT VALID` then `VALIDATE`, per b1d5c8e73f04). | Nothing in this product wrote a plan row: the only writer of `plans` was the client cap upsert, so invoices, margin, caps and D-64's setup-fee cron all sat on a row an operator inserted BY HAND. **Seeding a default row was rejected** for two reasons that are stronger than the convenience: inventing prices is forbidden, so a seeded row could only carry NULLs — and an all-NULL row is EXACTLY equivalent to no row for every reader (`read_caps`, `usage_summary`, the setup-fee cron), so it buys the appearance of configuration and nothing else; and it would destroy the distinction `warn_no_plan_in_effect` depends on, which logs only when rows exist and none is in effect, making every new tenant look permanently misconfigured. **Recorded-never-edited** follows D-46: an in-place UPDATE re-prices a statement a client already paid, and the sabotage that proved the test suite holds this made next month's price start charging today. **Backdating is refused** for the same reason. **Churn deliberately does NOT close the plan window** — a window closed at churn leaves the final month unpriced and renders the last invoice at ₹0.00. `overage_rate_value` stays settable and NULL, because the retail number is a founder decision and the SURFACE was never what blocked it. |
| D-67 | **A suspended tenant stops dialling, which it did not before** | `compliance/service.py::account_stopped_blocker` refuses `suspended` / `churned` / soft-deleted, wired into `check_dispatch` immediately after the big red switch, and into `launch_blockers` under the same rule name. `POST /v1/admin/tenants/{id}/status` and the Lifecycle screen give the column its first writer, through the shared `db/transition.py` discriminator (D-65). Inbound is untouched. | `organizations.status` had a five-value CHECK constraint, was READ by the health board's ended-account filter, and was written by nothing — no suspend, reactivate or churn path existed anywhere. The part that made this a compliance defect rather than a missing button: **`check_dispatch` never read it.** That is the one gate every outbound path calls, so an account an operator believed suspended would have kept placing calls — and there was no way to suspend one to find out. Wiring it at the gate rather than per-caller covers the campaign tick, the call-this-lead button, the lead-callback webhook and WhatsApp escalation in one place, and fails CLOSED on an unreadable row. Inbound stays live because a suspended account's customers still dial the client's number and hanging up on them is a different decision. |
| D-68 | **Admin-realm MFA is enforced in `verify_token` from Clerk's `fva` claim, and `X-Confirm-Action` is KEPT beside it** | Clerk's session token carries `fva` = `[firstFactorAgeMinutes, secondFactorAgeMinutes]`, where `-1` means the factor was never verified for this session. `core/auth.py` refuses an admin-realm token whose `fva[1]` is `-1` (`mfa_required`) or whose claim is unreadable (`mfa_claim_missing`); absent fails CLOSED. The frontend `AdminMfaGate` explains the refusal and is keyed on the problem CODE, never the status. TRD §2 and SEC-COMP §5 now describe what exists. | Both docs called admin MFA MANDATORY and no MFA code existed anywhere in the tree — a documentation claim guarding the platform halt and the outbox replay. **Placement is the load-bearing choice**: in `verify_token`, not `current_admin` and not per-route, so that "an admin token" and "an admin token that passed MFA" are the same object — there is no `VerifiedToken(realm="admin")` in existence that skipped it, which covers ~60 route declarations and every future dependency by the act of calling the verifier at all. **The sabotage is the evidence that this is not theatre**: with the backend check removed and the UI gate fully intact, the frontend suite stayed 4/4 GREEN while four backend tests went red, including the big red switch actually going through. **`X-Confirm-Action` is retained, not superseded**: MFA answers WHO holds this session, proved once for 12h; the header answers WHICH ACT they meant on WHICH TARGET, proved per request. A fully MFA'd session is exactly what a tab left open on an unlocked laptop is. **Clerk reverification (a FRESH second factor bound to high-risk actions) is deferred** — it needs a browser reverification flow this repo does not have, and gating the 3am halt lever on a prompt nobody can answer is how a control gets switched off. Named in OPERATIONS §8, not silently skipped. Citation honesty: `clerk.com` is blocked by this environment's egress proxy, so the reference that could actually be READ is the Clerk Go SDK declaring `FactorVerificationAge [2]int64 \`json:"fva"\``. **An operator must still enable and require a second factor in the admin Clerk application** — the API can only refuse, it cannot make Clerk offer a factor. |
| D-69 | **Deploy path: one image, component-scoped `vps-deploy.sh`, expand-then-swap migrations** | `Dockerfile` (one image, three services), `compose.prod.yml`, `scripts/vps-deploy.sh`, `infra/nginx/` (including the `rate-zones.conf.template` the drift guard had been DEFERRING), a gated `.github/workflows/deploy.yml`, and `runbooks/deploy-failed.md`. | `docs/DEPLOYMENT.md` made `scripts/vps-deploy.sh` THE deploy mechanism and the file did not exist; there was no nginx config, no Dockerfile and no deploy job — everything in this repo was undeployable. **Migrate → then swap, never auto-downgrade**: hard rule 8 (no drop in the release that stops writing) IS the statement "old code runs against new schema", so the migrate→swap window is safe by construction while the reverse window is not. An automatic downgrade could drop a column already written to, turning a failed deploy into data loss. **Hard rule 3 is honoured by a reviewable path→component map plus `--no-deps`**: `apps/api/crm/**` deploys api and workers and does not touch voice-runtime; `core/**` and `packages/shared/**` DO deploy it, because it imports them — stated rather than hidden. Swap order is workers → api → voice-runtime last. Migrations run FROM THE DEPLOYED IMAGE, resolving a real self-contradiction in the doc (§2 "Python is NOT needed on the host" vs §4.7 running `uv run alembic` on the host) and guaranteeing the revision set that runs is the one in the artifact being shipped. **Blue/green was rejected for now**: a third-party rollout CLI plugin on the production host is a supply-chain surface (hard rule 9) and needs its own decision; the recreate gap is named and the reconciliation poller (D-31) is what absorbs it. Zero third-party GitHub Actions in the deploy workflow, deliberately — the runner IS the target host. Four independent gates, and `VPS_DEPLOY_ENABLED` unset means merging this changes nothing. **Closing the drift deferral ARMED a comparator that had never been able to fire**: it now refuses a rate-zone disagreement between the doc and the template, naming both and stating that the edge enforces the template. |
| D-70 | **voice-runtime derives the client IP from ONE header written by ONE trusted hop, and refuses when it cannot** | `engine_intake.client_ip` returns `str | None`. Outside `APP_ENV=local` it reads exactly one header — `CF-Connecting-IP` — and only when the socket peer is a trusted-proxy CIDR; `X-Forwarded-For` is no longer read at all. Absent, blank, a list, a `host:port`, or an untrusted peer all yield `None`, which is a 401 plus `webhook_source_rejected`. nginx now WRITES `CF-Connecting-IP` from `$remote_addr` (`calevate-proxy.conf`). | The previous code preferred `cf-connecting-ip` and otherwise took the **leftmost** `X-Forwarded-For` entry. Leftmost is caller-controlled by construction — MDN's XFF page says a security use must only trust addresses added by a trusted proxy, and RFC 7239 §8.1 says the same of `Forwarded` — and Cloudflare APPENDS rather than replaces. This is the ENTIRE authenticity control for an unsigned engine (hard rule 3, TRD §5), so it was one misconfiguration away from accepting a forged source. It was safe only because the origin lock made `CF-Connecting-IP` always present, which is a property of the edge rather than of the code. **The fix also closed a hole nobody had named:** the origin lock allows `127.0.0.1` for deploy health polls, so an on-box process could POST a forged `CF-Connecting-IP` at the hooks vhost — nginx was passing the header through untouched. Having nginx write it means the value can only come from the hop we trust. **What the deployment must now guarantee**, stated because the code depends on it: `real_ip_header CF-Connecting-IP` + `real_ip_recursive on` + the CF ranges; the `proxy_set_header`; and nothing reaching the container except through that nginx. Adding a second proxy changes the hop count and is a CODE change in the same commit. Failure is now a visible 401 storm plus poller catch-up rather than silent acceptance. XFF splitting was removed, so the ack path got cheaper, not dearer. |
| D-71 | **The subject-access export is MODELLED, and a guardrail exception is now per-FIELD rather than per-route** | `POST /v1/compliance/subject-export` returns `SubjectExportOut` (six nested models, all `extra="forbid"`), validated BEFORE the audit write so a document that fails validation is never disclosed. `scripts/check_redaction_exposure.py`'s `ALLOWED_ROUTES` values became `RawDisclosure(reason, fields)`: `fields=None` keeps the old whole-path skip for the three existing entries, a named set keeps the model walk ON. New `GET /v1/compliance/deletion-requests` returns the tenant's erasure register — ids, status, timestamps, `subject_ref`, and no phone numbers. | The export returned `dict[str, Any]`, which meant it was not a field the guardrail judged safe — it was a field the guardrail **could not see at all**, on the one endpoint in this product whose payload is an entire named human being. `deletion_routes.py` had already made this argument about its own `proof`; this applies it where it mattered most. Making the exception field-scoped is the part worth keeping: a whole-path skip means the next field somebody adds to that response is unexamined forever, which is how a guardrail quietly stops guarding. Proven rather than asserted — adding `to_e164` to a call model is reported, and narrowing the field set makes both phone fields reappear. The register exists because a filed request was reachable only by its opaque id, so a client who closed the tab lost the handle on an in-flight legal obligation with a statutory clock on it. `subject_ref` (a hash) is included and the number is not, because a register of erasure requests that lists everyone's phone number is the opposite of data minimisation. |
| D-72 | **Lead lenses: one column registry for screen AND export; unknown columns degrade, unknown filters are REFUSED** | `crm/columns.py` is the single registry, resolved by both `list_leads_page` and `export_leads_csv` — there is no longer a header constant in the export. Facets are built from the agent's `extraction_schemas.fields` (enum fields only, `MAX_FACET_FIELDS = 8`), counts follow every other filter and ignore the facet's own. New `lead_saved_views` table (a7e2c40d9b53), private per user, resolved against the live schema on read. | SURFACES §2 calls this the parity floor and there were ZERO hits for `saved_view` or `bulk_` in the tree. The registry matters beyond tidiness: the screen and the file each had a hand-maintained column list and **they disagreed** — the file carried `source`/`created_at`, the screen carried `owner`/`updated_at`. Mirroring is now structural rather than promised. **The asymmetry is the safety decision:** an unknown COLUMN key is dropped and reported (it narrows, and screen and file drop it identically), but an unknown FILTER key is a 422 — dropping it would WIDEN the result set, on the one route that emits unmasked numbers. Saved views prune stale filters visibly at read time so a client is never silently handed a wider set. **Shared views are deferred with the real question named**: the hard part is who may edit a view three colleagues have open, not storing a flag, and the table has no `visibility` column so there is no shared row to get a permission wrong on. Jira's answer to stale field references is that filters BREAK plus an integrity checker; resolving against the current schema on read and saying what was pruned beats that for free. **Deliberately NOT built:** bulk actions and further inline edit — a different question with its own guardrails, and half of it would have been the half-wired defect. |
| D-73 | **The QA report is computed ONCE and rendered twice; the 5% sample is a keyed hash with its seed and frame stored** | `scripts/qa_report.summarize()` produces `calevate_shared.qa_report.QaReport`; the CLI's Markdown, the stored row and the client-realm API response are all renderings of that one object, and `apps/api/quality/service.py` counts nothing. New `qa_reports` + `qa_call_samples` (d5b8a2c60e17). The weekly draw orders by `md5(seed || call_id)` with `seed = "<tenant_id>:<week_start>"`, storing seed, rank, `population` and `target`; `cron(draw_qa_samples, weekday={0}, ...)` with an explicit `max_tries`. | SURFACES §2 promised the report in-app and it existed only as `make qa-report`; SURFACES §1's sampling queue did not exist at all. **The anti-fork rule is the whole point**: a QA report that disagrees with itself is worse than none, so the harness (~110 model calls, unrunnable in a request handler) stays the one computation and the route renders a stored object. The test parses the numbers back OUT of the CLI's Markdown and compares them with the live route — the two documents a client can hold, not the API against a function it calls. **A sample nobody can re-derive is not evidence.** `random()` and `TABLESAMPLE` are both rejected in-code (unauditable; page-sampling correlates with arrival time); the stored seed and rank mean anyone can re-run one expression and get the same list, and storing the FRAME is what makes "we sample 5%" falsifiable rather than a slogan. The seed changes weekly so no call is permanently unlucky, and `UNIQUE(tenant_id, call_id)` means a re-run inserts nothing. Realms are split deliberately: the report is CLIENT-realm (its value is that the client reads it) and the queue is ADMIN-realm (it holds OUR verdict about THEIR call). The reviewer sees `get_call(raw=False)` — the same function and argument the client's own screen uses — with no raw variant on the admin router at all, asserted by a test that enumerates the router's paths. |
**OPEN, surfaced by D-74 and deliberately NOT decided there:** `docs/SURFACES.md` §2b lists the
voice on the IMMEDIATE lane and `LANES` says so, but `set_agent_voice` does not publish — so no
agent obeys the lane table, and the client screen renders "voice" under "Applies straight away".
Reconciling it means either auto-republishing (which re-voices a live line on an unrun ear test,
against OPERATIONS §2 gate 3) or amending an authoritative doc. Separately, `publish_variant`
sends the agent's CONFIGURED voice to experiment arms, so starting an experiment can move part of
the traffic while the mirror still reports "republish required" — over-reporting, which is the
safe direction, and pre-existing. Both need a decision; neither blocks anything today.

| D-74 | **A voice has TWO answers — configured and live — and `republish_required` is now measured rather than assumed** | Migration c8b3f14e7a29 adds `agents.live_tts_voice` / `live_tts_provider`, mirroring `live_prompt_id` (a4e7b2c95d18). `publish_agent` is their single writer and records what it ACTUALLY SENT, in the same UPDATE as `engine_agent_ref` and after the vendor call, so a vendor failure rolls the mirror back with it. `GET /v1/agents/{id}/pending` gains `voice.configured` / `voice.live` / `republish_required` / `headline`; the admin picker pre-selects the configured voice and renders **Callers hear now** beside **Configured**; the client agents card shows the voice callers hear. | The picker shipped able to SET a voice and unable to DISPLAY one, and closing that naively would have replaced a missing fact with a wrong one. `set_agent_voice` writes our row and deliberately never touches the engine, so a published agent is configured for one voice and SPEAKING another until the next publish — and `SetVoiceOut.republish_required` was computed as `published`, an assumption that is right the first time and wrong every time after, including when an operator re-selects the voice the engine is already running. **Exposed on `pending`, not on `AgentOut`**: `AgentOut` has nowhere to put the second answer, a single `tts_voice` there renders as "the voice" and is wrong on exactly the agents where it matters, and putting both there would move two-speed publishing onto a read that knows nothing else about it. `pending` already answers configured-vs-live for the script and the call cap; the voice is the third instance of one question, not a new one. It is also the read the admin screen already makes, so pre-selection costs no extra request. **Client-visible, and that was checked rather than assumed**: `list_voices` is already client-realm, and D-36's ladder is a PRICE ladder — `overage_rate` vs `overage_rate_value` bill the rungs differently and `usage_events.meta.tts_tier` records which rung each call ran on, so a client billed by rung must be able to read the rung. **No backfill, deliberately**: `live_tts_voice := tts_voice` would tell a client their callers already hear the new voice, which is the exact false claim the column exists to prevent. NULL means "nothing recorded as sent" and resolves to `republish_required` — one harmless extra republish, self-healing on the first publish. **Voice is NOT added to the `pending` change list**: that list is what Apply and Undo act on, and `undo_staged` does not touch these columns — listing it would put an Undo next to a change it cannot undo. |
| D-75 | **`GET /v1/integrations/events` is a MODEL, and it carries the Sheets capability** | New `EndpointOptionsOut { events: list[str], sheets_delivery_available: bool }` replaces `dict[str, list[str]]` on the same path. The screen reads both from one request; the Sheets form is not offered at all when the capability is false, and the frontend's `lookup()` walk is deleted. | An untyped response generates a bare index signature, so `events` was the one read in that module TypeScript could not check — the same defect class as D-71's `dict[str, Any]`, and fixed the same way. **`events` stays `list[str]` rather than narrowing to the `EventName` literal**, which is the call worth recording: the union is what THIS BUILD can put in a request body, the server's list is what the RUNNING DEPLOYMENT offers, and those are different facts. Narrowing makes the gap unrepresentable, deletes the screen's "this account can also receive X, which this console cannot subscribe to yet" branch, and turns a deployment that adds an event into a 500 out of response validation. **The capability is a rendering hint and never the check.** It follows `KycRecordOut.number_purchase_available` exactly, and the route's own `sheets_delivery_available()` remains the authority — pinned by a stale-capability test that reads the capability as true, flips the setting, and asserts the create is still refused with zero rows written. The screen's optimism is allowed to be wrong; it is not allowed to be the gate. **The hard failure on a malformed body was KEPT** and re-documented as a §52 boundary rather than deleted as a typing workaround: a 200 missing `sheets_delivery_available` would otherwise render "Sheets is not switched on for your account" — our ignorance printed as one of the server's two answers. |
| D-76 | **Bulk lead actions ship TWO named selection scopes, and a partial failure is never a green tick** | `POST /v1/leads/bulk` (`leads:write`, audited, `MAX_BULK_LEADS = 500`). The header checkbox selects THE PAGE; an escape hatch — offered only when rows are off-screen — selects every lead the filters match, and the request carries `scope: "ids" | "filter"` with NO default (omitting it is a 422). The response echoes the scope and returns `requested / changed / unchanged / failures[]` where `changed + unchanged + len(failures) == requested`. `unchanged` is a SUCCESS bucket. Above 25 rows the confirmation is typed and WHAT YOU TYPE IS THE COUNT. | Z deferred this deliberately, and the reason it was worth its own slice is that the facets it built made "select all" genuinely ambiguous: the filtered query can be far larger than the page. The research settles it as two NAMED scopes rather than one guessed meaning (PatternFly's page-scoped header checkbox, Helios' "where the selection is made implies a different scope", Gmail's banner as the learned interaction), and the confirmation must describe the set that will ACTUALLY be acted on — hence `expected_count`, and a cap that REFUSES rather than truncates. **Typing the count rather than a fixed word is a deliberate departure** from this repo's `ERASE`/`HALT`: a fixed word proves you meant the button, but the risk here is the NUMBER, and you cannot type "140" without reading it. **Partial success is 200 with per-item results, not 207**: this repo already answers "processed, and the answer is a refusal" with 200 (`CallLeadOut.status == "blocked"`), and problem+json is reserved for "the request failed". **Bulk delete was considered and REJECTED**: `deleted_at` only hides a row while DPDP erasure runs through `compliance/deletion.py` (request row, outbox job, worker reaching calls, turns, extractions, storage and the engine, then a certificate). A Delete button here would teach a client they had answered an erasure request when they had not. **Undo rejected too**: both actions are their own undo, and an undo token would be a second stateful path to the same write. |
| D-77 | **Lead status and assignment go through the shared discriminator, and `transition_status` learns `visible_where`** | `update_lead` ran a blanket `UPDATE … SET status`; it now uses `db/transition.py::transition_status` (D-65), and assignment gets the same three-answer shape with an `IS DISTINCT FROM` CAS. The primitive gains `visible_where`, applied to BOTH the CAS and the discriminating SELECT. | Three real defects, not a tidy-up: a second click wrote a second `status_change` TIMELINE ROW claiming a change that never happened; a no-op edit bumped `updated_at`, which is this table's sort key, so the client's list re-ordered under them for nothing; and a soft-deleted lead's 404 was a COINCIDENCE of the row not matching rather than a statement about visibility. `visible_where` has to cover both halves and that is the subtle part: applied only to the CAS, a soft-deleted row would fall to the discriminator and answer **409 naming a status the caller may not know it has** — which both leaks the row's state and contradicts the 404 the same id gets everywhere else. The bulk path inherits all of it, which is what makes "3 of 10 were already hot" report as `unchanged` rather than as failure. |
| D-78 | **Per-tenant feature flags are plain config rows; the default lives in code and absence IS the default** | New `apps/api/flags/` and `tenant_feature_flags` (3a91c7e04d58, RLS + policy in the same migration). Resolution is platform default (code, `FLAGS[name].default`) → per-tenant override (row); NOTHING seeds a row for any tenant. Write is `admin:tenants` with audit-on-real-change; read is `org:read`. The cache is THE REQUEST — memoised on `Session.info`, keyed by tenant id — so a flip takes effect on the very next request everywhere, with no window. | CLAUDE.md mandates the shape ("feature flags via plain config rows, not a flag SaaS") and SURFACES §1 asks for the surface; there were zero hits for `feature_flag` in the tree. **What it does NOT replace, argued rather than assumed, because this repo already has four flag-shaped things:** platform config like `self_serve_signup_enabled` (one value per deployment, read where no tenant exists); the big red switch (an incident control, globally scoped, step-up confirmed, with a three-layer cache tuned under a dispatch tick); plan tiers (dated, invoice-derived, contractual — collapsing them would let a beta toggle change a bill); and **the build-time constants `PROVIDER_CREATES_ORDERS` / `LEAD_RETRIEVAL_IMPLEMENTED` / `PROVISIONING_IMPLEMENTED`, where migrating would actively cause an incident** — a row cannot make unwritten code exist, so flipping one would enable a lie rather than a feature. The recommendation is that none of the four move. **A flag may never gate a compliance control**, stated in the registry and pinned by a test — hard rule 5's "no bypass for testing" with better manners. **No cross-request cache, deliberately**: a flag that takes thirty minutes to turn off is a different product from one that takes effect now, and if a flag ever gated something safety-relevant, staleness would be a safety question rather than a performance one. **No step-up**, because releasing an account for outbound dialling takes none and a flag is a smaller act; a flag that needs a step-up header is the wrong mechanism for what it is doing. Nothing is gated on a flag in this change — introducing a mechanism and rewiring behaviour onto it are two changes — and the console prints **"Nothing reads this flag yet"** beside any flag whose `consumed_by` is null. |
| D-79 | **A missed campaign occurrence is SKIPPED, never caught up; a repeat repeats the START, not the dialling** | `schedule_recurrence` stores weekday + IST wall-clock time under the existing `start_at` key, so no migration is owed. An occurrence fires only within `RECURRENCE_CATCHUP = 1h` of its own instant; past that it is abandoned, audited (`campaign.recurrence_skipped`), alerted, and the schedule advances to the next occurrence AFTER NOW. `_claim_occurrence` locks on the occurrence key, so the identity is the occurrence and not the clock. `complete_or_rearm` returns the campaign to `scheduled` WITHOUT resetting contacts. | This is Kubernetes CronJob's `startingDeadlineSeconds` answer, and the argument is stronger here because the jobs ring strangers' phones: a worker down for three days must come back to ONE upcoming run, not three at once, or the recovery is itself a compliance incident. The asymmetry with the one-time `GRACE = 24h` is deliberate — a lost one-time start is lost outright, a lost occurrence is followed by another. **A repeat repeats the START, not the dialling**: re-dialling subscribers who were already reached would be an unrequested repeat call, which is a different act from the one the client scheduled. **Weekday + time, never day-of-month**, so month-end ambiguity and the RFC 5545 skip/clamp divergence cannot arise. **A recurrence outside platform calling hours is REFUSED at creation** (unlike a one-time 22:00 start, which is a lawful one-off intent deferred to 09:00): a standing instruction silently reinterpreted is a schedule the client cannot read. **The gate is on every occurrence path** — `_fire_recurrence` reaches `running` only through `launch_campaign`, which opens with `launch_blockers`, and two tests pin it including a structural one. **`launch_campaign` keeps its own CAS and borrows only the DISCRIMINATOR**: `transition_status` treats already-in-target as success, and launch is NOT idempotent — it scrubs DNC, stamps `launched_at`, writes an append-only `campaign.launched` row and counts a start. Pause and resume now write audit rows, only on a real transition. |
| D-80 | **`conclude` names the experiment it is ending, and is never redirected onto another one** | `ConcludeExperimentIn.experiment_id` is REQUIRED; `_LATEST_SQL` is gone from the write path (it remains the READ's header). `transition_status(visible_where="agent_id = :aid")` scopes both the CAS and the discriminating SELECT, so a cross-agent or cross-tenant id is 404 rather than a 409 naming a status the caller is not entitled to. | The endpoint concluded "whatever is running on this agent", so **a retry arriving after a LATER test had started ended the later test** — losing a running experiment and promoting an arm nobody chose. **A body field, not a path segment**: the mutation is AGENT-shaped (it promotes a version onto the agent, moves the agent's disclosure line, republishes it, and the audit row's `object_id` is the agent), so `.../experiments/{id}/conclude` would name the experiment as the object of an agent-shaped act. The id rides as a PRECONDITION, which is the shape `ApplyIn.expected_version` already established. **`If-Match` was rejected on a fact about the resource**: the results representation changes with every completed call, so an ETag over it would refuse a good conclude because a phone rang while the operator was reading — and a precondition that fails for reasons unrelated to the hazard trains people to retry blindly. **Optional-with-a-default was rejected outright**: the only caller that would take the default is the stale request. A mismatched id is answered ABOUT THE TEST IT NAMED — 200 `changed:false` if that test already ended the way asked, 409 naming the ending if it ended another way, 404 if it names nothing visible. The existence of a later test is not a fact about this request. **The sabotage is what shows the D-65 branches were never enough**: restoring the agent-fallback turned exactly one test red while all 27 others passed. |
| D-81 | **A control the server permits is not hidden by the screen; a control the screen exposes states what the server will do with it** | The schedule and repeat forms move out of the launch panel's `ready` branch to a sibling guarded on `check.data` (a verdict answered, either way). Three stated consequences replace the hidden control: beside the form while arming, on the armed card BEFORE the first attempt, and from `schedule_blocked_rules` after it. | Scheduling deliberately runs NO gate at arm time — `schedule_campaign` and `schedule_recurrence` each do one CAS and import nothing from compliance — because the gate runs at FIRE time, every time, which is what makes a lapsed registration weeks later still refuse (D-79). The screen was therefore inventing a rule the server does not have: a client whose blockers are outstanding TODAY could not arm a start for next Tuesday, by which time they may well have cleared them. **The forms stay unreachable while the check is LOADING or FAILED**, deliberately: a consequence stated from a verdict we do not have is §52's defect rather than its remedy. **A gap closed on the way past**: an armed-but-unattempted schedule previously said only "Starts Monday, 10:00 IST", so a doomed schedule's first evidence would have been calls that never happened. The forecast is guarded on `status == "scheduled"` specifically, because a RUNNING campaign's `launch_blockers` reports its own status as a blocker — true, and not a statement about the next occurrence. **Cancel was checked rather than assumed** and did NOT have the same defect: both cancel controls already live outside the launch panel. That is now pinned by tests, because a schedule you cannot cancel BECAUSE you acquired a blocker would be the worse version of this defect. |
| D-82 | **Money reaches a wallet through a screen, and the confirmation is the payment reference itself** | New `/admin/tenants/{id}/credits` over the existing `POST|GET .../credits` — no backend change; the routes were already idempotent-by-UTR under an advisory lock with the audit row in the same transaction. The ledger renders above the form, the reference is typed TWICE, and a replay renders a neutral "Already recorded — nothing was credited" panel naming the entry that already exists. | `runbooks/topup-payments.md` instructed an operator to hand-construct an authenticated, MFA'd admin API call off a bank statement, and that was the ONLY way money reached an account. **The typed confirmation is the reference, not a fixed word** — the house idiom is a typed word naming the act (`HALT`, the tenant id in `spendCapConfirmation`), but a fixed word fails twice here: it becomes muscle memory, and it does not check the one field where the error is unrecoverable. The reference is different every time AND is the double-keying check. The amount is echoed in grouped INR in the button label, so a misplaced decimal is the last thing read before the click. **The duplicate-reference warning is one-directional**: a match warns, an absence never reassures — the screen holds the newest 50 entries while the server checks the whole ledger, and a console that said "not a duplicate" would be asserting something it cannot know. **`credit_ledger` is append-only (hard rule 4), so the screen surfaces the compensating-entry path rather than letting an operator discover there is no undo.** It also states plainly what has no tool: `reconcile_credit_ledger` handles DUPLICATES only, so a credit to the wrong tenant or the wrong amount has no remedy in code and must be escalated. **Found and NOT fixed** (reported): SURFACES §1 promises "credit adjustments (compensating entries, never edits)" and nothing implements it — `POST .../credits` refuses a negative amount with a remediation pointing at a tool that does not exist. |
| D-83 | **The invoice's supplier identity is CONFIG, and the document refuses the tax-invoice framing rather than printing an invalid one** | New `billing/gst.py` (Rule 46 particulars, place of supply per IGST s.12(2), the CGST+SGST / CGST+UTGST / IGST split); four `GST_SUPPLIER_*` keys plus `GST_SUPPLY_SAC`, no defaults and no placeholder GSTIN. With any of them absent the document renders **PROFORMA INVOICE**, says it cannot support input credit, and names the exact env vars blocking it. New client-realm `GET /v1/billing/invoice` — a second ROUTE, never a second computation. | The document charged 18% GST while carrying no supplier or recipient GSTIN, no HSN/SAC, no place of supply and no entity address, so a B2B client could not claim input credit against it. **The flat 18% was structurally wrong, not cosmetically**: CGST, SGST and IGST are three separate credit ledgers, so tax charged without naming the head cannot be claimed AT ALL — and the old shape could not express the Union-Territory-without-a-legislature case (CGST+UTGST) at all. **The refusal is a legal position**: CGST s.32 prohibits an unregistered person from collecting tax, so with no GSTIN there is no tax invoice to issue. **Zeroing the tax was considered and REJECTED** despite being the literal reading of s.32: one forgotten env var would then silently under-bill every client by 18%. A missing config key changes what the document CLAIMS, never what the client OWES. **Recipient GSTIN comes from the VERIFIED `kyc_records` row**, not a new `organizations.gstin` column, because that column's only writer lives in a module this slice could not touch — it would have shipped as a column nobody fills. A human having checked it against the registry is also the stronger fact. **Open and pinned rather than faked**: the serial `CAL-202608-0192f0aa` is 19 characters against Rule 46(b)'s 16-character cap and is deterministic rather than consecutive. A consecutive series needs a stored issued-invoice registry, which conflicts with D-46's derived statement, and truncating trades a length breach for a collision. A test fails the day the scheme changes. |
| D-84 | **The untyped-2xx sweep, third time: a model where the response MEANS something, a 204 where it never did** | `POST /v1/lead-sources/{id}/test` gains `LeadSourceDryRunOut`; the two intake receivers and the unschedule route gain models; four constant acks (`pause`, `resume`, `consent-provenance`, `DELETE /v1/dnc/{id}`) become **204**; `/v1/numbers/purchase` becomes `NoReturn`. | Same defect as D-71 and D-75 one level down, and this time on a handler with a NORMALISED CALLER PHONE in scope — not a field the guardrail judged safe, a handler it could not see. **A constant ack is not a candidate for a model**: four of these returned a fixed string saying only what the URL already said, and modelling a constant satisfies both tools while teaching the next reader nothing. 204 is what this repo already does for idempotent transitions. **`NoReturn` on the purchase route** because the handler always raises — the old `dict[str,str]` was the shape of a contract for a body that cannot exist, and `NoReturn` makes mypy force the author to declare a real one the day provisioning lands. **`IngestAckOut` on a duplicate returns nulls, not `dispatched: false`** — `false` beside `duplicate` reads as "nobody was called", the one thing it must not mean. Meta's handshake stays `text/plain` because it echoes `hub.challenge` verbatim and a JSON model would break the subscription. Two sabotages are now permanent detection tests, and the second proved the guardrail's walk reaches `/hooks` routes and not only `/v1`. |
| D-85 | **Impersonation mints a short-lived DELEGATION grant, and the grant is not a credential** | `POST /v1/admin/impersonation-grants` mints an HS256 JWT: `aud="calevate:impersonation"`, `sub`=tenant id, `act={"sub": admin id}`, `jti`, 15-minute expiry, presented as `X-Impersonation-Grant` beside `X-Impersonate-Org`. `verify_grant()` checks presence, signature, audience, shape, expiry, actor and subject in ONE call. Five refusal codes, all 403, none degrading to a plain admin session and none widening one. New `IMPERSONATION_GRANT_SECRET`, failing closed outside `local`. | D-22's "session start audit-logged" was decorative: the endpoint minted nothing, the console never called it, and `X-Impersonate-Org` alone opened a tenant — so `admin.impersonation_started` was absent for every real session. Reads WERE audited, so D-22 was not violated; the session-start half simply did not exist. **RFC 8693's `act` claim is the exact shape**, and the reason is precise: a token CARRYING `act` has delegation semantics while one without it has impersonation semantics — which is D-22's own rule written by someone else first, since D-22 forbids acting-as to avoid dual attribution. So the feature called impersonation deliberately gets a delegation-shaped credential. Token exchange, `may_act` and nested `act` chains were read and refused; chaining is refused in code (an impersonating session may not mint). **No revocation list and no grants table, because the grant is NOT a credential**: it never travels in `Authorization` and does nothing alone, so every request still carries the operator's own MFA-gated admin token and still re-reads `admin_users`. Revocation lag is ONE REQUEST, not one token lifetime — and the docstring names what would invalidate that argument. The TTL is sized against audit-ledger volume, not against revocation. **A dedicated secret rather than a subkey derived from `audit_chain_secret`** (NIST SP 800-108 was read and rejected): that secret rotates with a drill, and coupling a routine credential rotation to a drill means it never happens. **Found and NOT fixed**: `audit_chain_secret` still falls back to the guessable constant `local-dev:{app_env}` in EVERY environment including prod, so a deploy that forgot it has an unverifiable chain. |
| D-86 | **Every HMAC key resolves through one ladder, and a present-but-short key is refused exactly like an absent one** | `resolve_hmac_key()` in `core/settings.py` with `MIN_HMAC_KEY_BYTES = 32`, used by the audit chain, the idempotency scope fingerprint and the D-85 grant. Staging/prod refuse to sign or verify without a real key and `/healthz/ready` is red until it is set; the derived constant survives only under `local`. `AUDIT_CHAIN_SECRET_RETIRED` verifies (never signs) the pre-rotation era, and `ChainVerifyOut.entries_under_retired_key` publishes how much of the log rests on it. | D-85 found and did not fix it: `audit_chain_secret` fell back to `local-dev:{app_env}` in EVERY environment, so a prod deploy that forgot the variable signed its append-only ledger with a constant printed in this repository — anyone who could read the source could forge a row that verifies, and the console said "chain intact". **Length is part of being configured**: to a caller, a 6-byte secret and no secret are one condition ("no usable key here"), so failing closed on absence while accepting a weak key would guard the easier half of one mistake. 32 bytes because three authorities converge there and the strictest governs — RFC 2104 §3 (a key under the hash length "is strongly discouraged"), NIST SP 800-107 Rev. 1 §5.3.4 (128-bit absolute floor), RFC 7518 §3.2 (HS256 key ≥ hash size). **The retired key is a verification-only input, never a signing one**, so a rotation cannot be quietly un-done by an operator who sets the wrong variable. **The weakly-attested count is NOT a component of `ok`** and is rendered beside both verdicts: those rows are intact, and what they lack is attestation STRENGTH — a distinction that only matters when the log is exported as evidence, which is exactly when a caveat living in a runbook reaches nobody. **Idempotency got its own secret rather than sharing the chain's**: the chain's rotates on a drill, and a rotation of the fingerprint key makes in-flight `Idempotency-Key`s miss their stored record, so a client retry re-executes. |
| D-87 | **A credit adjustment is a compensating entry against a NAMED ledger row, keyed on (entry, amount) and gated on the direction** | `POST /v1/admin/tenants/{id}/credits/adjustments`. Idempotency key `adjust:<corrects_entry_id>:<amount>`, content-addressed and enforced by the EXISTING `ux_credit_ledger_tenant_reason_ref` (an index, not a reader's `if` — D-63). The ceiling is the target entry's remaining magnitude, cumulative across corrections (`reversed_amounts`, published as `LedgerEntryOut.reversible_inr`). `allow_negative=True`. Step-up `X-Confirm-Action: adjust_credits:<entry_id>` on the DEBIT direction only. | Hard rule 4 says the wrong row stays where it is because it IS the evidence, so the only correction an append-only ledger allows is a new row with the opposite sign — and the product promised adjustments (`credit_ledger.reason = 'adjustment'` since M1) with no way to make one. **A caller-minted key was rejected**: the failure this must survive is a SECOND CLICK, which mints a second key. The cost is stated in the code — two genuinely distinct corrections of the same amount against the same entry collapse to one key, and the second reads as "already corrected, nothing moved", which is the safe direction when money is leaving. **The balance may go negative, and refusing would be the worse answer**: a wrong credit that was partly spent cannot be fully reversed otherwise, and a ledger that keeps claiming credit the client never had is the condition this route exists to end. Because that must not be silent, `stops_dialling` carries the DIAL GATE's own predicate, evaluated inside the write's transaction rather than re-derived — a second copy of "does an empty wallet stop calling" is how the console and the gate tell a client two different stories. **No superadmin gate**: `core/rbac.py` reserves that for unbounded switches, and this act is bounded (≤ what the named entry moved) and itself correctable — making the operator who erred wait for a superadmin keeps a wrong ledger wrong for longer than the risk justifies. **Known residual, pinned by a test**: `reversible_inr` nets one level, so correcting a correction does not restore the original entry's ceiling. Netting properly is an alternating sum over a chain with a cycle guard on a money path, and it buys nothing — the chain never dead-ends, because the next correction goes against the newest entry. **Still absent**: under-crediting (₹5,000 recorded for a ₹50,000 UTR) has no path, since reversing the entry does not free the reference. |
| D-88 | **Cartesia is rejected as an ORCHESTRATOR and retained as a speech-vendor candidate; D-31 stands, and its Cartesia rationale was stale** | TRD §10 row rewritten from evidence: Cartesia Line's LLM is fully BYOK (LiteLLM, 100+ providers, `model=` + `api_key=`) while **STT and TTS are not** — Ink 2 and Sonic 3.5 are the product and the SDK exposes no swap interface. So Line cannot host D-36's Sarvam stack, and its $0.06/min is a BUNDLED rate that is not comparable to Bolna's BYOK platform fee. New TRD §10.4 prices both paths at four volumes. | The teardown's "low-latency English-first TTS" is now false and had to be corrected rather than left: Sonic 3 covers the top 9 Indic languages including Telugu, the Blue Machines AI partnership (~Feb 2026) targets India-RESIDENT processing for BFSI/healthcare, there is a Bangalore office, and deployment spans cloud-regional, on-prem/VPC and air-gapped with SOC 2 Type II / HIPAA / PCI L1. A stale rationale is how a settled decision gets re-litigated badly. **The decisive criterion is not price**: our compliance layer assumes an Indian carrier relationship (PE/TM, DLT templates, 140/160 series, DNC before dispatch), and Line's telephony is Cartesia numbers, imported Twilio, or Voximplant — none of which yields a DLT-registered Indian number. **Where Cartesia WOULD win is recorded honestly**: its Scale tier ($0.014/min, models included) beats Bolna+Sarvam on variable cost at S4 volumes, gated on the India telephony gap and a Telugu ear test against Sonic we have not run. ⚠ **This row originally added "an unpublished enterprise commitment" as a third gate — that was WRONG and is withdrawn; Scale is published and self-serve at $239/mo annual. See D-94, which supersedes the economics here.** **The useful residue**: Bolna is BYOK on speech, so Ink/Sonic are a candidate INSIDE Bolna against Bulbul — a config change at our adapter boundary, and an option D-36 did not list. **Sources are secondary**: `docs.cartesia.ai` and `www.cartesia.ai` are unreachable from our build environment; the BYOK finding rests on Cartesia's own Line SDK README on GitHub, which is first-party. |
| D-94 ⚠SUPERSEDES the economics in D-88 | **Build for the orchestrator switch; do not build the switch. Three pre-committed triggers, and telephony is the one that decides it** | TRD §10.5. We build the capability descriptor on the `VoiceEngine` port, make the conformance suite PROVE a capability claim rather than trust it, and run the system in tests against a capability-restricted engine that dictates speech and has no built-in KB — the Cartesia shape — without writing a Cartesia adapter. Re-evaluate only when ALL THREE hold: sustained volume >12,500 min/month, Line BYOC SIP from an Indian DLT-registered carrier confirmed **in writing**, and Sonic Telugu passing the same ear test Bulbul passed. | **Two corrections to D-88, both in Cartesia's favour, recorded because a comparison that only flatters the incumbent is not evidence.** (1) Scale is **not** an unpublished enterprise negotiation — it is published and self-serve at **$239/mo annual** ($2,868/yr ≈ ₹252,384) or $299 monthly, dropping Line to **$0.014/min** with models included, 8M model credits and $299 prepaid Line minutes. The commitment is a known ₹21,032/month. (2) That moves the crossover to **≈12,300 min/month** (₹1.71/min variable advantage ÷ the plan fee), earlier still — ≈6,450 — once the Bolna path needs Sarvam Pro for rate limits, which the Cartesia path does not because it buys no Sarvam speech. At S3 (20,000 min) Cartesia Scale is **₹3.79/min against our ₹4.30, a saving of ₹23,168/month**. **There is also an upper bound nobody quotes**: Scale caps concurrency at 10 slots ≈ 26,400 min/month, so the window is ~12,300→26,400 and above it the price is Enterprise and genuinely unknown again. **The founder's BYOK instinct is HALF right and the halves matter**: our Sarvam LLM key does work on Line (LiteLLM lists Sarvam as a provider; Sarvam serves an OpenAI-compatible endpoint at `api.sarvam.ai/v1`), so D-36's free-per-token LLM leg survives a move — but Line's TTS config takes a Cartesia `voice_id` and a Cartesia `model` with **no provider field**, so Saaras and Bulbul do not, and those are the two legs carrying D-36's residency argument. **The decisive unknown is not price**: whether Line accepts BYOC SIP from an arbitrary carrier. If it does, an Exotel/Vobiz-class Indian carrier can front it and the exit is live; if it does not, the whole window is unreachable and the price analysis is moot. It is cheap to ask and it gates everything, so it is asked first. **No Cartesia adapter is written**, on this repo's own doctrine that an adapter written against an imagined API is worse than none because it looks finished — and their docs are unreachable from our build environment, so anything written today would be invention with a version number on it. |
| D-95 | **Core config and secrets are managed from `admin.calevate.tech/ops`, in Postgres, under envelope encryption — and the KEK never enters the database** | Full spec: `docs/PLATFORM-CONFIG.md`. Two tables, not one: `platform_settings` (plaintext, readable, revertible) and `platform_secrets` (AES-256-GCM ciphertext + wrapped per-version DEK, append-only, joins the hard-rule-4 family). `PLATFORM_KEK` in the environment; `PLATFORM_KEK_RETIRED` unwraps but never wraps, mirroring D-86. Resolution order `env → db → default → refuse`, with six bootstrap keys (`APP_ENV`, `DATABASE_URL`, `ALEMBIC_DATABASE_URL`, `PLATFORM_KEK`, `PLATFORM_KEK_RETIRED`, `REDIS_URL`) that a new guardrail forbids ever reading from the store. Sentinel-key polling propagates a change to all four processes in <10s with no restart and no DB read on voice-runtime's request path. | The founder's requirement is legitimate — rotating a vendor key must not need an SSH session — and CLAUDE.md's "no secrets in the database" is equally binding. Envelope encryption satisfies both: the database holds ciphertext and wrapped keys, and the key that opens them is somewhere else. **Two tables rather than one `is_secret` boolean**, because that boolean is one bad default away from writing an API key in plaintext and nothing would notice. **`env` beats the database deliberately** — it breaks the bootstrap deadlock, keeps a broken store from bricking the deployment, and is the 3am override when the console itself is what is broken. **No read-back route exists or will**: a console that can display a credential leaks every credential through one screenshot, so a compromised session buys write access, not exfiltration. **A `/test` route dry-runs a candidate value against the vendor before storing it**, which is what separates this from a new outage source — a wrong key is refused at the screen instead of at the next call. **Vault, OpenBao, Infisical and Doppler were read and rejected**: Vault's BSL plus a seal/unseal lockout risk for a solo operator; OpenBao and Infisical are second stateful deployables on a one-VPS estate (CLAUDE.md prefers Postgres first); Doppler and cloud KMS give up the India residency D-36 was chosen for. The KEK provider is isolated in one module so OpenBao or a KMS is a later swap, not a rewrite. **The trade is recorded rather than discovered**: today stealing every vendor credential needs VPS access, and afterwards one compromised admin session is enough — accepted against mandatory admin MFA (D-68), a distinct `platform:secrets` permission, no plaintext on any route, hash-chained audit rows, and the env override as the way to take control back. **Residual and unmitigated**: an admin session can REPLACE a key with an attacker's own, pointing our engine at their account; detection (an alert on every `platform.secret_set` in production), not prevention, is the answer. **Six ENV-ONLY bootstrap keys, and that is a count of `check_bootstrap_keys.BOOTSTRAP_KEYS` rather than a count of lines in a file** — `.env.example` carries eight, and the two extra ones are not a leak (D-185). `OBJECT_STORE_ENDPOINT` and `OBJECT_STORE_BUCKET` are required `Settings` fields with no default, so `Settings()` cannot construct — and therefore the store cannot be reached — without them; they are console-MANAGED (source `env`, read-only, when set) rather than console-only, and they are locators, not credentials. The object-store CREDENTIALS (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`) are deliberately absent from `.env.example` and always were, because botocore reads them itself and nothing here passes them to boto3. `tests/env_example_bootstrap_floor_test.py` boots `Settings` off exactly that key set and off every 7-key subset, so the floor is measured rather than asserted. The earlier wording here — "`.env` drops from ~54 keys to 6" — was a file count, and a file count is the drift class D-103/D-105 exist for. |
| D-93 | **Each voice-engine adapter DECLARES an `EngineCapabilities` descriptor, and the conformance suite makes it impossible for that declaration to lie** | Per-leg `SpeechControl` (`ours`/`engine`) for stt/tts/llm; `campaigns`, `knowledge_base`, `transfer`; `number_series: frozenset` (per SERIES, because the launch gate matches on it); `webhook_auth`. **No field carries a default** — a new adapter answers every question in writing rather than inheriting today's engine's answers by omission. Every site that hard-coded Bolna's answer now asks the one selector; an absent capability raises `EngineCapabilityAbsentError` (a `ProblemError` subclass carrying `capability` as a FIELD, not seven problem types for one condition). Four conformance subjects: `fake`, a capability-restricted `fake`, `bolna`, and a real `cartesia` adapter. | The audit found nine Bolna-shaped assumptions outside the engine package, and three were live defects rather than tidiness: **`voice-runtime/engine_intake.py` hard-coded `if engine == "bolna"` with NO `hmac` branch at all**, so adopting a signing engine meant editing the latency-critical receiver in lockstep with the adapter — exactly what hard rule 3 forbids; **the KB publish path assumed an engine-side KB unconditionally**, discovering otherwise three calls in, *after* withdrawing the live version, so a KB-less engine took knowledge down to report it could not replace it; and **the two shipped adapters silently DISAGREED** — `bolna.transfer`/`provision_number` raised while `fake` cheerfully succeeded and returned a number, with nothing able to detect the divergence. **The suite refuses `campaigns=True` outright** because the port has no campaign method, so the claim is unfalsifiable and would sit among six enforced claims borrowing their credibility. **`transfer` was redefined mid-slice** to mean "will `VoiceEngine.transfer` work" rather than "does the vendor have transfer": Cartesia has a real `transfer_call`, but agent-initiated mid-call, and a `True` would have put an escalation control on the console that refuses every time a caller asks for a human. **The first sabotage run caught only one of two lies** — a TTS claim walked through because `models_readable=False` excused everything; that hole is closed (an adapter claiming a leg is `ours` must read that leg back) and the re-run caught both. **TTS answer**: the voice catalogue stays our Sarvam data and `voice_selection_capability()` decides whether it may be OFFERED — the closed case renders as a product fact, not an error, while still showing what callers hear. |
| D-89 | **An under-credited payment is RESTATED against the same reference: you type the TOTAL the bank moved, never the difference** | `POST /v1/admin/tenants/{id}/credits/restatements`. Ref `restated:<payment_ref>:<corrected TOTAL>`; `meta.payment_ref` links back, and `PAYMENT_REF_SQL` groups on `COALESCE(meta->>'payment_ref', ref)` so the wallet still shows ONE bank transfer. New `PaymentOut` (`credited_inr` summed server-side across every row of that payment, `entries > 1` iff restated). Confirmation header ALWAYS, echoing the amount. | The workaround was a second top-up under an annotated reference (`UTR-123-part2`), which puts two references on one bank transfer and silently breaks the reconciliation the reference exists for. **The operator types the total, not the difference**, because at 2am with a bank statement open the total is the number in front of them and the difference is arithmetic they can get wrong; the amount to credit is derived server-side from what the reference credits today, which the screen shows beside the field. **Confirmation on every restatement, unlike the adjustment's direction-bound rule** — this route has one direction, no ceiling above it, and it credits the client, so there is no safe direction to leave unguarded; the header carries the amount so one captured for ₹50,000 cannot be replayed for ₹500,000. Restating twice credits once; restating to a HIGHER total credits only the new difference. **Found the hard way:** the sabotage that proved the link-back test was left in the tree and reached a commit — the `meta` key had been renamed and never restored, which would have silently unlinked every restatement from its payment. |
| D-90 | **Meta lead retrieval is implemented, so a Facebook form submission can actually be called back** | `LEAD_RETRIEVAL_IMPLEMENTED` flips to `True`. The Graph read `GET /{leadgen_id}?fields=field_data` behind the existing `LeadRetriever` Protocol, with `field_data` → flat-map normalization feeding the EXISTING consent gate rather than bypassing it. | A leadgen webhook is a change notification carrying no name and no phone; without the Graph read every authenticated Meta lead died at `meta_lead_retrieval_unavailable`, so the product's headline promise — someone fills your form, the AI calls them in seconds — could not happen at all. The consent answer arrives inside the very `field_data` we can now read, which is why it flows INTO `require_form_consent` (`no_consent_field_configured` still distinct from `no_form_consent`): a lead we may not call is taken and refused, never dialled. |
| D-91 | **WhatsApp goes direct to Meta Cloud API, not through an Indian BSP** | `apps/workers/whatsapp_cloud.py` implements `whatsapp.WhatsAppTransport`. The seam is unchanged: `whatsapp.py` keeps the messages, the gates, the delivery records and the retry ladders, and nothing about "was this person allowed to be messaged" is redefined in the vendor module. Runs in a worker; no request handler reaches Meta. | ROADMAP §6 carried no decision, so the seam shipped and refused to guess (the module said so). Ranked on what actually DIFFERS for a two-person pre-revenue company with one client (R4), the first question decides it: **can we get a WABA without an enterprise sales process?** Cloud API is self-serve through the Meta Developer Console — Business Verification raises limits, it is not required to send at all — where the BSP layer inserts a sales motion and a margin between us and the platform for a capability we would be reselling anyway. |
| D-92 | **The backup tree stops being a hypothesis: the half that needs no cloud account now runs on every developer's machine** | `scripts/restore_drill.py` — seed, dump exactly as `dump-offsite.sh` dumps, encrypt with the same `age` invocation, into object storage, back out, decrypt, restore into a scratch database, then PROVE the restore rather than trust an exit code. A `SABOTAGE=` mode corrupts the dump, drops an RLS policy or disables an append-only trigger so the drill can be shown going red. | `infra/backup/README.md` §9 and the drill runbook both say the tree is unapplied and no wal-g command has ever run, while OPERATIONS §8 treats "backups verified" as a pre-launch gate that today means a file exists. **The coverage statement is the load-bearing part**: wal-g, R2, the offsite provider, the libsodium key, the secrets-manager age identity, the systemd timers and `rclone` are NOT tested and are each named individually, because a harness that ran the cheap half and printed PASS would manufacture precisely the false confidence `backup-health.sh` was written to prevent. Verification reuses `check_rls_coverage`, `check_ledger_immutability` and the audit-chain verifier rather than writing second implementations — a restore that silently drops policies is a data breach dressed as a recovery. |
| D-96 ⚠AMENDS D-95 §5 | **The KEK label is a FINGERPRINT of the key material, and `get_settings()` itself is the resolution point** | `kek_id` is SHA-256 of the key, domain-separated and truncated to 31 bits — not an operator-maintained counter. Unwrap is trial decryption over the ring (KEK + retired). `get_settings()` keeps its signature and its `@lru_cache` and layers `env → platform_settings → code default` underneath; `core/platform_config.py` owns the IO and the dependency runs one way, so there is no cycle. Sentinel bump is a STATEMENT-level database trigger, not an application-side write. | **The counter fails in the one way that cannot be recovered from.** It is correct only while somebody remembers to bump it on every rotation; forget once and new rows carry the OLD generation, the rewrap job skips exactly the rows that need it, and the NEXT rotation renders them permanently unreadable — silent, unrecoverable loss gated on human memory. A label derived from the key cannot disagree with reality. Sound because it is a LABEL and not a control: GCM's tag is a 128-bit MAC, so trial decryption rejects a wrong key at ~2⁻¹²⁸ and a mislabelled row still opens. **Condition attached at review, and it is what keeps the scheme safe**: the rewrap must never use the fingerprint to decide which rows to process — every row is unwrapped and re-wrapped, or the counter's failure mode returns wearing a hash. **`get_settings()` as the resolution point** means ~200 existing call sites pick up console config with no edit and the read path stays IO-free (hard rule 3) — one way to read a setting rather than two, which is the migrate-don't-accumulate rule applied to the most-called function in the repo. **The sentinel is a database trigger** because an application-side bump is correct only while every writer remembers, and the spec's own acceptance test is a `psql` edit at 3am. **Validation uses `TypeAdapter(field.rebuild_annotation())`, not `.annotation`** — the bare annotation would accept `otel_traces_sample_ratio = 5.0`, store it, and drop the next deploy; constraints must travel with the type. The managed set is DERIVED from the `Settings` model minus env-only keys minus credential-shaped names (36 of 56 managed, 18 credentials excluded automatically), so a new field is governed the day it is added rather than the day someone remembers a second list. **Cold start with no snapshot serves loudly** — `platform_config_never_loaded` as a state distinct from `degraded` — because refusing to boot would put an empty table in front of the phone system, and the bootstrap six are env-only regardless. |
| D-97 ⚠TWO DELIBERATE HARD-RULE DEVIATIONS, both argued | **`platform_secrets` joins the append-only family with a BOUNDED mutation, and `/test` calls a vendor from a request handler** | (a) `platform_secrets_forbid_mutation` permits an UPDATE only when every column except `dek_wrapped`, `dek_nonce`, `kek_version` and `retired_at` is byte-for-byte unchanged, and RAISEs on everything else including every DELETE. One file-scoped `BOUNDED_MUTATIONS` entry in `check_ledger_immutability`, not an exemption; the database trigger stays the enforcement. Seven ledgers now. (b) `POST /v1/ops/secrets/{key}/test` makes one authenticated vendor request from the handler, 8s timeout, reading `response.status_code` and nothing else. | **(a)** The blanket `calevate_forbid_mutation` cannot work here: a KEK rotation must re-wrap HISTORICAL DEKs in place, or the retired key can never leave the environment and "the rotation completed" is never true. **The ciphertext is the evidence and is immutable; the wrapping is operational state and is not.** The rejected alternative — splitting the wrapping into a second table so the blanket trigger could stay — is recorded in the migration: it puts a join on every credential read and splits one fact across two rows, and the trigger instead enumerates four mutable columns POSITIVELY, so a fifth cannot be added without editing both the migration and a test that derives the immutable set from the LIVE schema. **(b)** CLAUDE.md bars calling providers from request handlers, and the whole value of `/test` is an answer BEFORE the value is stored: deferring to a worker means storing the candidate first — the outage this exists to prevent — or inventing a job the console polls. Admin realm, `platform:secrets`, manually triggered, one request. The rule protects tenant-facing and latency-critical paths; this is neither. **Engine isolation survives** because the probe reads a status code and never a body, so no vendor shape escapes `apps/api/engine/`. Four outcomes are never collapsed — `accepted`/`rejected`/`unreachable`/`no_probe` — because "we could not check" is a different sentence from "the vendor said no", and all three probes carry `verified=False` since no live refusal has been observed from this build (OPERATIONS §2 marked assumption, surfaced to the operator rather than hidden behind a tick). **§10's residual risk is now WIRED, not merely accepted**: every secret write fires `platform_secret_set` naming key and actor, in every environment. **Found by the tests, not by review**: `AESGCM.decrypt` raises `ValueError` rather than `InvalidTag` when the nonce is malformed, so one bad row would have escaped the background refresh and frozen every process's configuration instead of refusing one credential. **Three of sixteen sabotages found tests that asserted nothing** — including a confirmation test comparing the header against the function that produced it, and a bootstrap test asserting `local == local`. |
| D-98 | **The Razorpay order-creation adapter exists, and "an adapter exists" stays a different fact from "this box can take a payment"** | `PROVIDER_CREATES_ORDERS` is now True — a claim about CODE. `PaymentCapability.creates_orders` is False on every deployment with its own reason `no_api_secret`, carried separately from `reason` so "why can I not pay" and "why is there no order" stay distinguishable in an alert, and never pulling `available` down. `POST /v1/orders` pinned to `/v1`. Receipt content-addressed over `(tenant, amount, 15-minute window)` per D-87, served through `claim_idempotency` per `call_back`. New `GET /v1/billing/topups/capability` publishing two booleans and no reason code (D-75's shape). | **A money bug found on the way, and it was correct only by accident.** The route converted rupees with `int((amount * 100).to_integral_exact())`, which LOOKS like it refuses a fraction of a paisa — but `to_integral_exact` only *signals* `Inexact`, and the default decimal context has that trap off, so it silently rounded half-to-even. It was safe solely because Pydantic's `decimal_places=2` rejected 3-dp input upstream: one validator away from a missing paisa. `inr_to_paise` now refuses rather than rounds, and is the only conversion in that direction. **The order is created SERVER-SIDE so `notes.calevate_tenant_id` reaches the provider by construction** rather than by hoping a browser checkout attached it — the webhook resolves the tenant from that key and from nothing else. **Idempotency composes the two existing answers rather than inventing a third**: D-87 supplies what the key IS (content-addressed, because a caller-minted key is defeated by a second click), `call_back` supplies how a repeat is SERVED. Two departures are stated in code: a 15-minute window, without which a client who paid ₹2,000 today could never pay ₹2,000 again; and the claim commits BEFORE the network call, so a loser gets `idempotent_request_in_flight` instead of a database lock held across a call to a payment provider. **No checkout widget was built**: `checkout.js` is a third unverified vendor surface and a supply-chain decision (hard rule 9), and the browser callback is never the source of truth — the signed webhook is. No Razorpay SDK entered the lockfile. **A defect on the screen this closed**: `PAYMENT_PROVIDER` is unset by default, so the top-up form was offered on every deployment and could only ever answer `payments_not_configured` — a control whose single possible outcome was a red notice. **Research promoted an existing UNVERIFIED claim**: their published SDK confirms the webhook signing scheme our receiver already implements, read at source; the header NAME stays unverified, because the SDK is handed the signature and never names where it came from. |
| D-99 | **Two coverage floors closed to near zero, and the gate-removal sabotage is the evidence that matters** | `dial-path` 97 → 1 (agents/service 6→0, crm/routes 57→0, ingest/service 2→0, campaign_dispatch 15→1, workers/dispatcher 17→0). 29 tests, four new files, no source touched. | The D-21 button and the callback had **never been exercised over HTTP**, on the surface whose whole rule is that no dial skips the gate. The sabotages are the point: removing `if not decision.allowed` from `call_lead` turns a DNC number into a placed call and two tests red; removing it from `call_back` gives `assert 'queued' == 'blocked'`; dropping `AND a.deleted_at IS NULL` from `_load_agent` means a soft-deleted agent dials, because the vendor's object outlives our row and that predicate is the only thing between it and a caller. Also pinned: a reserve that swallows the pool dials nobody AND alerts; a shared pool is spent once; a campaign paused between the scan and the budget read is not dialled; a campaign cancelled mid-tick is not written back to `completed`. **The one remaining unit is unreachable and argued rather than contorted** — `slots = min(slider, tenant_budget)` cannot be zero given `CHECK (concurrency BETWEEN 1 AND 10)` and the `tenant_budget <= 0` break above it, so a test pins the CONSTRAINT that makes the argument true and fails if it is ever relaxed. |
| D-100 ⚠NUMBER NEVER ALLOCATED | **Reserved and burned — no decision was ever recorded under this number** | Nothing. The log skips from D-99 to D-101; a draft claimed the number, the work landed under D-101, and the number was never reclaimed. Uncited: `grep -rn "D-100"` returns this row and nothing else, so no citation dangles. | **A hole in the sequence is an invitation to a collision, and the collision is the failure mode `duplicate_decision_ids` exists for.** The likeliest way two rows end up sharing a number is two sessions independently picking "the next one"; the second-likeliest is one session spotting a gap and treating it as free. Both readings of a duplicated number look right, which is why the guard calls it worse than a dangling reference. Recording the gap costs a row and removes the ambiguity permanently — the alternative, backfilling D-100 with a decision that was actually made under a different number, would put a lie in the log to make it look tidy. |
| D-101 | **The ops console stops lying about when a change takes effect, and a change in flight can no longer be lost or half-applied** | `applies` becomes five values enumerated per key with its reason, guarded by `check_config_applies`; writes are conditional on a per-key sequence-backed ETag under a per-key advisory lock; an identical value is a true no-op; `settings_scope()` pins `Settings` once per request in both HTTP deployables; every managed field is bounded on the field itself; and the SIGTERM handler stops replacing uvicorn's. | **The finding that outranks the rest was not about the console at all.** `_install_signal_handlers` destroyed the drain it documented: `Server.serve()` enters `capture_signals()` and *then* runs the lifespan, so our handler replaced uvicorn's `handle_exit` with one raising `KeyboardInterrupt`. Measured with a real server and a sleeping request — before: `ESCAPED KeyboardInterrupt`, no in-flight line at all; after: `INFLIGHT 200` then exit 143. The exception escaped `asyncio.run`, so `Server.shutdown()` never closed sockets and waited, and the lifespan's `finally` never flushed. **On `hooks.calevate.tech` that made every deploy an abort of whatever webhook was in flight, on an at-most-once feed with no retry (D-31)**, and `stop_grace_period: 30s` had nothing to give its seconds to. DEPLOYMENT §4b's "in-flight work finishes instead of being killed" is true for the first time. **Sixteen of fifty-five keys were mislabelled**, worst first: `usd_inr_rate` said `live` but `BolnaEngine.__init__` copies it and `get_engine()` caches the adapter, so a new rate reached `get_settings()` in seconds and never reached the conversion stamped into `usage_events` — money, labelled the worst possible way; `bolna_api_key` had no classification at all, so **a rotation from the Secrets panel never reached the code placing calls**, and the symptom reads as "the vendor is rejecting our key", sending an operator to Bolna's dashboard instead of a restart; `db_pool_size` claimed `on_restart`, which is worse than a wrong label because it *promises a restart applies it* — `db/session.get_engine` builds from a bare `Settings()` before the store is reachable, since reading the store needs a connection from the pool it would size, so a stored value can never apply and the write is now refused. A fifth value, `unclassified`, is the fail-safe: a field added tomorrow is managed the day it is added, so it arrives unclassified, uneditable and refused — the safe default is "you cannot edit this yet", never "we assume it works". **The ETag is per-key and sequence-backed, not the fleet sentinel**: the sentinel moves on every key, so with 36 fields two operators on *different* fields is the normal case and B editing `alerts_email` would invalidate A's edit to the price — false conflicts teach people to retry without reading, which is last-write-wins with a round trip in front. A global sequence rather than a per-row counter, because a counter restarts at 1 after a revert and a token read *before* the revert would match the row that replaced it. A version column alone is insufficient — two writers reading revision N both pass — so `set_value` takes a per-key advisory lock before the read. **The idempotency comparison is on typed values, not JSON bytes**: a test the author expected to pass failed and was right, because `9.5` and `"9.50"` are one `Decimal` and two strings, so a byte comparison would bump the whole fleet because a form formatted its number differently. **Torn reads were tested rather than reasoned** — 2,000 races against a flipper found zero mixed pairs, the snapshot does swap atomically — but a unit of work is not one read, and two `get_settings()` calls in one request could straddle a refresh: new rate with old price, which on a money path is a *wrong* number, not a stale one. **A bound had to be taken back and produced a rule**: an exact `min_length=15` on the GSTIN broke a test that feeds 14 characters on purpose, where `parse_gstin` already degrades to a proforma — a model-level bound caps blast radius and must never be stricter than a consumer that already fails gracefully. The migration also found a latent bug: `ALTER DEFAULT PRIVILEGES` covered tables only, and this schema had never had a sequence, so the first console write on a fresh deploy would have failed `permission denied for sequence`. |
| D-102 | **The greppable-constant device gets its reverse check: prose that STATES a capability constant's value is compared against the constant, and CI fails when the doc understates what was built** | New section 5 of `scripts/check_docs_drift.py`. The registry is DISCOVERED, not listed — every module-level `NAME = <bool literal>` under `apps/`, `packages/`, `scripts/`, `alembic/`, `tests/` found by AST, which today is exactly the five honesty constants (`PROVIDER_CREATES_ORDERS`, `LEAD_RETRIEVAL_IMPLEMENTED`, `PROVISIONING_IMPLEMENTED`, `CLOUD_API_CONFIRMED_AGAINST_LIVE_WABA`, `ENGINE_REPORTS_TTS_MODEL`) and nothing else. Prose is judged **only** where it joins the name to a boolean in the present tense (`= False`, `is False`, `is now`, `is still`, `stays`, `remains`, `flips to`) with nothing between them but backticks and asterisks; a sentence that merely NAMES a constant claims nothing and is not judged. Scanned everywhere the claim actually lives: markdown, Python docstrings and comment runs, and the console's JSDoc. Second half catches the rename hole — a value claim about an UPPER_SNAKE name the tree defines nowhere. Negative controls in `tests/capability_claim_guard_test.py`. | **Every error a readiness audit found ran the same direction: the capability was BUILT and the doc still said missing.** `PROVIDER_CREATES_ORDERS` had been True since D-98 while BUILD-LOG, ROADMAP §5, ROADMAP §7 and OPERATIONS §7 all said no checkout could be opened; `LEAD_RETRIEVAL_IMPLEMENTED` had been True since D-90 while SURFACES §2 said "still NOT built and not to be claimed". The existing device only ever guarded OVERclaiming — `UNWIRED_BASELINE` may only shrink, the pilot scorecard derives its verdict — and understating is not the safe error: it is how a shipped checkout gets rebuilt, and how a session re-derives a decision (`docs/` is authoritative, so a reader believes the doc over the code). **Adjacency, not proximity, is the load-bearing choice.** `whatsapp_cloud.py` writes "the same device `payments.py::PROVIDER_CREATES_ORDERS` and `ingest/meta.py::LEAD_RETRIEVAL_IMPLEMENTED` use — and it stays False", where the "it" is a third constant and both named ones are True: any window-based rule reports that line twice and is wrong twice. **Present tense only**, because BUILD-LOG is a chronological log whose older sections correctly record states that have since changed — "was False" is history, "is False" is a claim about this tree. **A whole class was deliberately given up rather than approximated**: BUILD-LOG's "WhatsApp … no vendor adapter, because no decision picks a BSP" was false in both halves (D-91 picked Meta Cloud API; `whatsapp_cloud.py` is the transport) and names no constant, so no matcher can decide it. A grep-shaped guard over that class was written for the credential rule the same week, flagged three files that merely NAMED a credential, caught neither real offender, and was thrown away — half of a decidable question beats all of an undecidable one. What closes the other half is not a cleverer matcher but a constant: a capability worth a paragraph is worth a greppable boolean, and the moment WhatsApp's landed, the prose about it became checkable here. |
| D-103 ⚠COMPLETED by D-104 (the two defects it reports below are now closed) | **The set of engine names has exactly two definitions, both in `calevate_shared`, and an AST scan fails the build on a third** | `config.EngineName` (the `Literal` pydantic validates `ENGINE=` against) plus the new `config.SELECTABLE_ENGINES = frozenset(get_args(EngineName))` answer *what may be selected*; `engine.WEBHOOK_AUTH_BY_ENGINE` answers *what has an authenticity story*. `apps/voice-runtime/engine_intake.py` loses its own `EngineName` and its character-identical copy of `WebhookAuthMethod`; `KNOWN_ENGINES` becomes `frozenset(WEBHOOK_AUTH_BY_ENGINE)` — the same table `verify_source` authenticates from, so "known to the receiver" and "has an authenticity story" are one answer. The receiver's last hardcoded vendor name goes with it: `if engine == "fake"` becomes `if method == "none"`. `tests/engine_name_drift_test.py` AST-walks `apps/`, `packages/shared/src/` and `scripts/` for any tuple, list, set, dict-key or `Literal[...]` spelling two or more engine names outside the two homes, deriving its vocabulary from the homes themselves, and asserts `SELECTABLE_ENGINES ⊆ WEBHOOK_AUTH_BY_ENGINE`. | **The drifted copy did not cause a security hole; it caused a blind spot, which is why nothing caught it for a wave.** Cartesia deliveries were already refused twice over — `WEBHOOK_AUTH_BY_ENGINE["cartesia"] == "hmac"` takes the branch that returns `ok=False`, and `cartesia.py::verify_webhook` fails closed with `SIGNATURE_UNIMPLEMENTED_REASON` — and neither refusal can be softened by a change to the other. What the stale `Literal["bolna", "fake"]` did was collapse the label: `_refuse` reports anything outside `KNOWN_ENGINES` as `"unknown"`, so on `ENGINE=cartesia` every delivery 401s and every 401 is attributed to a stranger probing the URL rather than to our own unimplemented verifier. That is the exact failure `_refuse`'s own docstring argues against for Bolna ("goes SILENT… indistinguishable from a quiet night"), one step worse, because a misattributed graph sends the incident somewhere else. **`WEBHOOK_AUTH_BY_ENGINE` and not `SELECTABLE_ENGINES` is the receiver's source**, because `fake-restricted` is deliberately unselectable and is the only engine declaring `hmac` — the receiver must answer for it identically or the single test exercising the signature branch would exercise a different code path from production. **The subset assertion runs in the direction that hurts**: a selectable engine missing from the auth table answers `unknown engine` on every delivery it will ever send. **A THIRD copy was found and is reported rather than fixed**: `apps/api/agents/models.py::ENGINES = ("fake", "bolna")` renders `ck_agents_engine_enum`, and `admin/service.py::_default_engine` writes `get_settings().engine` into that column on the tenant-birth path — so `ENGINE=cartesia` fails client creation with a raw `IntegrityError`, not a named refusal. It needs a migration, so it is pinned as a strict `xfail` naming the fix and listed in `KNOWN_OPEN_COPIES`, which is equality-asserted: fixing the constant fails the test and forces the entry's deletion, so the list cannot rot into a permanent exemption. Same shape one layer up, also reported: `core/settings.py::runtime_config_missing_keys` names `BOLNA_API_KEY` under `engine == "bolna"` and has no `cartesia` clause, so `/healthz/ready` is green on a credential-less Cartesia deployment — the fix is to ask `engine_availability()`, which already answers `no_engine_credentials:cartesia`, rather than grow a second per-vendor `if`. **The guard is a scan, not a pin**: pinning the copy we found would have been satisfied by this commit and silent on the next one, and the copy in `agents/models.py` — the one with a database constraint behind it — is precisely the copy nobody thought to look for. |
| D-104 | **The third engine-set copy is gone, and `/healthz/ready` stops answering for Bolna on a deployment that is not running it** | `agents/models.py::ENGINES` becomes `tuple(sorted(SELECTABLE_ENGINES))` and migration `d7b1c48a2e93` widens `ck_agents_engine_enum` to admit it; the strict `xfail` and the `KNOWN_OPEN_COPIES` entry D-103 left are deleted, forced by that dict's equality assertion. `runtime_config_missing_keys` loses `if cfg.engine == "bolna"` and asks the adapter instead, via a new `VoiceEngine.credential_env_keys` and `engine.missing_engine_credential_keys()`. `get_engine` is split: `build_engine(cfg)` is the branch, `get_engine` is the cache over it. The `str` widening and the two `getattr`s in the factory — vestigial since D-93 — are gone with it. | **Two Cartesia-shaped defects, one root: a fact about a vendor written down outside `apps/api/engine/`.** The CHECK constraint was the worse of the two, because a constraint is not advisory: `admin/service.py::_default_engine` writes `get_settings().engine` at tenant birth, so on `ENGINE=cartesia` the first thing a new client does — exist — failed with an `IntegrityError` out of Postgres, naming a constraint whose text disagreed with the setting that produced the value. The second was quieter and reaches further: readiness named `BOLNA_API_KEY` under one hardcoded vendor clause, so a credential-less Cartesia box reported itself fit for traffic on the probe an orchestrator uses to decide whether to send it any. **The obvious patch — a second `if` — is the shape that produced the bug**, and it would also put "Bolna needs BOLNA_API_KEY" in a core module, which hard rule 2 reserves to the engine package. So the adapter answers both halves: `holds_credentials()` for the verdict, already the single authority `engine_availability` derives from, and `credential_env_keys` for the NAME, because "not ready" without the key to set is a red light with no next step. **`credential_env_keys` names only what `holds_credentials` gates on**: `CARTESIA_FROM_NUMBER_ID` is required to dial out and NOT to reach the vendor, so listing it would hold a whole deployment down for a control that refuses cleanly on its own. **`build_engine` had to exist before readiness could be correct** — `get_engine` caches on the engine NAME, so a caller passing its own `Settings` gets an adapter built from a different one, and readiness answering about a configuration that is not deployed is worse than readiness not answering; a test warms the cache and asserts the answer still comes from the settings handed in. **The migration's downgrade refuses rather than reverses when it cannot**: narrowing the CHECK with a `cartesia` row present would either be rejected by Postgres mid-flight or "succeed" by deleting client agents, so it counts first and raises BEFORE dropping the constraint — a refused downgrade must not leave the table unconstrained, which is the one outcome worse than either direction. **The schema half needed its own test and the model half could not have caught it**: `ENGINES` deriving from `SELECTABLE_ENGINES` makes the two agree in Python while the database keeps whatever the last migration wrote, so a fourth engine shipped without a migration would leave the model permissive, the constraint narrow, and the failure deferred to a client's first insert — the new test reads `pg_get_constraintdef` and parses it rather than string-matching, because an equality test on rendered SQL fails on spacing and teaches the next reader to loosen it. Sabotage confirmed all five: hand-retyping `ENGINES` (2 red), restoring the hardcoded clause (3 red, including a grep that fails on any vendor key named in `core/settings.py`), narrowing the live constraint behind the model's back (1 red, and ONLY the schema test saw it), pointing readiness at the cache (3 red), and an adapter that refuses for want of credentials while naming no key (2 red). |
| D-105 | **Post-call extraction was aimed at a Sarvam model Sarvam has retired, and pilot gate 1 would have configured a live agent with it** | `SARVAM_DEFAULT_LLM = "sarvam-105b"` and `SARVAM_RETIRED_LLMS` live beside `ModelConfig` in `calevate_shared/engine.py`; `workers/extraction.py::SarvamExtractor` and `scripts/pilot/gates_api.py`'s pilot `ModelConfig` both import the first. `tests/sarvam_model_identifier_test.py` AST-scans `apps/`, `packages/shared/src/` and `scripts/` for any retired identifier as a string CONSTANT (not prose), asserts the default is not itself retired, and asserts the pilot gate IMPORTS rather than spells. TRD §7's extraction paragraph corrected. | **Two literals, both `"sarvam-m"`, both correct when written, and the vendor moved.** Sarvam's changelog reports that `sarvam-m` is deprecated and a Chat Completions request naming it FAILS — so extraction was pointed at a model that no longer answers, and the pilot gate would have provisioned a real agent on a real telephone number with a dead LLM. **The cost model made it worse than a 400**: TRD §10 has priced the LLM leg at ₹0.00 on "Sarvam 105B, free per token" since D-36, so the code and the unit economics disagreed about which model was running, on the one leg whose entire contribution is that it is free. **This is the third time this repo has paid for a constant it did not have** — D-103's engine set, D-104's credential names, and now a model identifier — and the shape is identical each time: no single home, so the correction has one place to land and two places to be missed. **Evidence standing is REPORTED, NOT READ and says so at the constant**: `docs.sarvam.ai` is refused by this environment's egress proxy, so this is two independent search summaries of the changelog agreeing, never a page fetched here. Shipped rather than gated because a wrong identifier fails LOUD — the vendor 400s and `extraction.py`'s error ladder records it — which is the safe direction. **`sarvam-30b` is in the retired set too**, and that is the detail that argues for the set existing: it was `sarvam-m`'s own migration target and has since been retired in turn, so a fix that had simply moved to the 30B would be broken again already. **The scan reads AST constants, not source text**, because the correction has to be EXPLAINED somewhere and a regex would flag the paragraph explaining it. **The pilot-gate assertion is the one that would have been skipped**: `scripts/pilot/gates_api.py` reads like a fixture and is not — it drives a real Bolna account — and an in-place edit there passes the retired-name scan while reintroducing the defect on the next retirement, so it is asserted separately. |
| D-106 | **The one test that could validate the object-lifecycle policy had never run, and when it ran it failed** | `test_minio_accepts_and_returns_the_policy` becomes three: the rules MinIO implements are round-tripped, the excluded rule's exclusion is RE-MEASURED every run, and MinIO's silent drop of `AbortIncompleteMultipartUpload` when combined with another action is pinned. `MINIO_UNSUPPORTED_RULE_IDS` is asserted from both directions. `infra/object-lifecycle/policy.json` is UNCHANGED. | **It skipped on every run for as long as it has existed** — the guard is `_endpoint_reachable("localhost:9000")` and MinIO was not up, so "1 skipped: local MinIO not running" was the line nobody read. Bringing the container back for an unrelated reason ran it, and it failed: MinIO rejects `PutBucketLifecycle` for our fourth rule with `InvalidArgument`. **The cause is a documented MinIO limitation, not a defect in the policy** — MinIO's own S3-compatibility page and `minio-go`'s lifecycle package both state `AbortIncompleteMultipartUpload` is unsupported there; production is Cloudflare R2 (DEPLOYMENT §1), which implements it. Every shape was tried against `RELEASE.2025-09-07T16-13-09Z` before concluding that — `Filter: {"Prefix": ""}`, `Filter: {}`, no filter, the legacy top-level `Prefix`, a non-empty prefix — and all five are rejected. **So the policy stays and the CLAIM changes.** Deleting a real growth control so a local emulator goes green is weakening the thing to fit the test; what was actually wrong was the test's sentence, "the document survives a real S3 implementation's parser", which was never true of this document and had never been executed to find out. **The finding that outranks the failure is the SILENT one**: fold the abort action into a rule MinIO can implement and it answers 200, stores the rule, and discards the action — measured, and the stored rule comes back carrying `Expiration` alone. That is the obvious way somebody makes this red test go away, and it yields a green suite, a 200 from the store, and a lifecycle policy where the multipart growth control does not exist; a loud rejection is recoverable, a silent drop is discovered by a storage bill. Both are now sabotage-verified: emptying the exclusion set fails the round-trip test, and folding the action into `policy.json` fails the exclusion test. **The exclusion cannot rot**: it is re-measured on every run against the live store and fails the day MinIO implements the action, telling the reader to delete it. **`infra/README.md` §2 predicted exactly this class** — "R2, DO Spaces and MinIO each implement a subset of S3's lifecycle grammar" — which is now a measured instance rather than a warning. **CI could not see any of it, and now can**: the workflow declared only `postgres` and `redis`, so all three checks would have skipped there exactly as they did locally — a guard that skips everywhere is not a guard. MinIO joins CI as a `docker run` STEP rather than a `services:` entry, because it needs `server /data` and GitHub Actions service containers accept no `command`; the step polls `/minio/health/live` and FAILS the job if the store never comes up, since a silent timeout would restore the skip and with it the whole defect. |
| D-107 | **The national DND half of the gate becomes a recorded scrub RUN, not a loaded list, and `dnc_list.scope='global'` gets its first writer** | Migration `a1c8e40f27b9`. `preference_scrub_runs` — tenant-scoped, FORCEd RLS, append-only — records provider, the platform's reference, `scrubbed_at`, `expires_at` (23:59:59 IST of the run's day) and **counts only, never a phone number**. `national_dnd_scrub_missing`/`_expired`/`_incomplete` refuse **promotional** campaigns in `launch_blockers` AND `dispatch_blockers`. `scope='global'` is redefined as an absolute platform-wide suppression, written only by the step-up-confirmed `POST /v1/ops/dnc/global`; the RLS `WITH CHECK` gains one branch requiring the session to carry NO `app.tenant_id`. `campaigns.dnc_scrubbed_at` is stamped in the same statement as the CAS to `running`. | **THE BRIEF WAS WRONG AND THE RESEARCH IS WHAT CHANGED THE DESIGN.** I asked for a loader for a list of "tens of millions of numbers". The NCPR (formerly NDNC) **cannot be downloaded**: every access provider's DLT documentation carries the same sentence — *"the preference and consent database will not be accessible to telemarketers due to secure controls and use of blockchain technology on the DLT platform"*. A Registered Telemarketer SUBMITS a list to the operator's scrub facility and receives a reference, a report of COUNTS rather than numbers, and a verdict valid until 23:59:59 that day. So the producer I specified was a producer for a feed that does not exist, and the right primitive is a RUN with an expiry, not a list. **Loading NCPR rows into `dnc_list` was rejected for a second reason that outranks the first**: preference is category-scoped, so `check_dispatch` would read those rows as absolute and refuse **lawful transactional** traffic — wrong rather than conservative. **The gate is scoped to promotional only**, because under full DND every category is blocked except service-implicit while transactional is delivered regardless, so gating those would suppress calls the subscriber is entitled to receive. **It is not a self-inflicted outage**: performing a scrub needs the same RTM relationship that produces `platform_state.tm_id`, and `tm_registration_missing` already refuses every tenant's campaign until that exists — the blocker's precondition is already a precondition. **The blocker is also on the DISPATCH tick, not only launch**, because the window closes at midnight IST while a campaign keeps dialling. **`UNIQUE(tenant_id, phone_e164)` never constrained global rows at all** — NULLs are distinct in a unique index — so the global scope had no uniqueness for as long as it has existed; a partial unique index now supplies it. **The scrub timestamp SEC-COMP §67 has promised all along did not exist**; there are now two, and the "one list or two" distinction was previously invisible from every screen. **Three of seventeen sabotages found defects in the agent's own work**: `submitted` taken from the caller passed first because the fixture had equal counts, a log-line leak passed because `JsonFormatter.redact_text` masked it (strengthened to pin the call site's `extra` field set), and one guard turned out redundant against RLS and was reported honestly rather than quietly kept. **The protocol also caught a real reversibility bug**: the downgrade dropped the trigger but not its function, so `downgrade`→`upgrade` failed `DuplicateFunction` — a downgrade that cannot be followed by an upgrade is not reversible (hard rule 8), fixed and proven over three full cycles. **Recorded, not fixed**: the `scrub_ref` is taken from an operator and never queried back, so the gate proves an accountable assertion rather than a performed scrub — the test asserts no HTTP client is imported into `preference_scrub.py`, so the entry cannot outlive the defect. Closed by R-01, an RTM relationship with an access provider. |
| D-108 | **The money path had one rate card in two places, one rounding rule enforced nowhere, and a screen that called a paying client unpriced** | `MONEY_Q` derives from `models.MONEY.scale` and `rates.ROUNDING` is the one mode (`service.ROUNDING` re-exports it). `terms.PRICING_COLUMNS` + `CEILING_COLUMNS` PARTITION `TERM_COLUMNS` and `states_pricing` reads the former; `_record` reads by name, not position. `margin_for_tenant` sums `_tier_totals` instead of carrying its own copy of the cost expression. New `check_docs_drift` §4b diffs TRD §10.1's TTS rate card against `TTS_INR_PER_10K_CHARS` and against the doc's own second table. Guards: an AST scan for `.quantize()` with no explicit mode, a term-column partition check, a rate-card differ, a margin-definition check — measured at 0 false positives each. | **The live bug was in the WRITE path for money.** `workers/pipeline.py::_unit_price` stamps `usage_events.unit_cost_paid` and called `.quantize()` with no rounding mode, inheriting the process-global `decimal` context — mutable, and ROUND_HALF_EVEN by default. Reachable, not theoretical: a ₹0.0180 telephony leg over a 360-second call is exactly ₹0.00005/second, which half-even stores as `0.0000`, rounding the whole leg out of the margin panel and out of a closed month's `spend_used`. The "pass it EXPLICITLY, never inherit" doctrine existed in two docstrings and was enforced by nothing. **The second defect is the D-102 shape wearing a price**: `PlanRecord.states_pricing` read a hand-written list of price columns that `overage_rate_value` was never added to when D-36's second rung landed — so a plan quoting only the value rate, which is precisely the row a founder writes the day that price is decided, reported as stating no price and the console rendered "No price agreed … They are still invoiced nothing" over a plan billing ₹5.50/min. Verified arithmetic: 100 value minutes → ₹550 billed, screen says ₹0. The same lie survived on the history table, which had one "Rate / min" column. **The lead about `rate_zone_drift` was wrong and the agent said so**: that check reads nginx `limit_req_zone` directives — requests per second, no rupees anywhere — so NOTHING in the tree had ever compared a price against the doc stating it. **A tautology was caught by sabotage, in the agent's own new test**: `assert SERVICE_ROUNDING is ROUNDING` stayed GREEN under the sabotage, because `decimal.ROUND_HALF_UP` is an interned string that compares identical between any two modules naming it; replaced with an AST scan for module-level rounding-mode definitions, with the reason recorded where the next person reaching for `is` on a decimal constant will meet it. **Five ambient-rounding sites outside the slice** (`engine/fake.py` ×2, `scripts/pilot/fidelity.py` ×3) are recorded as an equality assertion so fixing one fails the test and forces the entry out. **A pre-existing coverage gap, found by sabotage rather than by reading**: `commercial_terms_test.py` tests that `overage_rate_value` is settable and unset, and 22 of its tests stayed green while `states_pricing` was blind to it. |
| D-109 | **The one latency budget this repo names is measured for the first time, and it turns out to be a process-count question rather than a handler question** | Server-side ack on the in-call tool endpoint, real handler, real `verify_source`, real ARQ enqueue against real Redis, nothing stubbed: **p50 1.0ms · p95 1.4ms · p99 1.6ms at one call in flight**, 0 database round trips, 1 enqueue, 3 Redis round trips. Under concurrency the distribution is flat at every width — p50 6.3ms at 8 in flight, 48.5ms at 96, **143.0ms at 250** — so `latency ≈ in-flight ÷ ~1,750/s` and the whole 100ms is spent at ~175 concurrent in-flight tool calls per process. Distribution recorded in TRD §6.2 beside the number it feeds; CI asserts the MECHANISM (zero DB round trips, exactly one enqueue) rather than a wall clock. | **`CLAUDE.md` carved out this one exception and attached an instruction — "measure it" — and nothing did.** Three near-misses were checked and rejected: `probe_tool_call_budget` scores the right number but from an evidence file that does not exist and measures the end-to-end round trip instead; `voice_runtime_ack_budget_test` pins the WEBHOOK receiver at 500ms; `call_optout_test` asserts one sample against the wrong budget. **The endpoint the budget names does not exist** — D-33/TRD §6.2 keep T3 inside the engine's KB and a test fails the day one appears in voice-runtime — so what was measured is `POST /tools/v1/{engine}/opt-out`, the engine custom function that IS on the audio path and shares every layer a RAG endpoint would need before retrieving anything. That measures the question that actually decides §6.2's fallback: whether the server half of 100ms is affordable at all. **The verdict has two halves and collapsing them would be the lie**: at one call the budget is not close to blown (p95 is 1.4% of it); at production concurrency it is blown by our own server time alone, and D-32 records Bolna at 100 concurrent on Pilots with 250+ in production. **NOT a CI latency assertion, argued rather than assumed**: the margin at width 1 is 70×, so a bound loose enough to be stable would pass against a handler an order of magnitude slower, and this repo already argues that a wall-clock assertion on a shared runner measures the runner and gets deleted carrying its guarantee with it. **What is excluded is stated**: the network — the dominant term — is gate 8's, because vendor hosts 403 at CONNECT here. **The round-trip ledger is the durable half**: sabotage put a lazy `SELECT 1` in the handler and 42 other tests stayed green, including the import-surface guard. Also fixed on the sweep: KB `kind="url"`/`kind="file"` were ACCEPTED and silently ignored — 201 for a fetch never performed, chunks taken from whatever text happened to be pasted — now a named 422; an admin publish panel rendered NOTHING while its read was in flight, telling an operator nothing was awaiting publish about a question nobody had answered (§52); and FLOWS §7 promised four steps that do not exist, one of which — the regression smoke — CANNOT be built, because `VoiceEngine` exposes attach/detach/list and no way to ask the engine's KB a question. **A correction the agent made against itself**: it first claimed nothing pinned the diagnosis ordering, then found by sabotage that an existing lifecycle test reddens too — by coincidence, not by design — and recorded why the coincidence is not a guarantee. |
| D-110 | **The poller was not the guarantee of record, and a concurrent post-call pair double-billed into an append-only ledger** | `_already_completed` becomes `_pipeline_settled`, which asks whether the artefacts THIS snapshot implies (`_expected_artifacts`) exist rather than whether a `completed` call row does; the verdict is a `Literal` so `reconciliation_repairs` separates `missing_call` (the vendor's at-most-once delivery) from `unfinished_pipeline` (our own drop). `lock_call_writes` — the house `pg_advisory_xact_lock` idiom — wraps all three check-then-writes. `EXTRACTION_OWED_SQL` is the SQL form of `needs_extraction`, imported by both the poller's probe and the stall alarm. A systemic publish failure defers the outbox tick with per-attempt backoff instead of charging every message its poison budget. `dispatcher.STALL_AFTER_MINUTES` now IMPORTS `PIPELINE_STALL_AFTER`. | **D-31 calls the poller the guarantee of record and for one shape it guaranteed a status line and nothing else.** `ingest_engine_event` writes the completed call row and THEN enqueues the pipeline, so when the delivery arrived and the post-call job was lost, `_already_completed` saw a completed row and skipped: measured at **ten consecutive ticks reporting `repaired=0` with artefacts still 0,0,0,0** — no lead, no transcript, no usage row, no bill, unrecoverable forever, because Bolna never redelivers and the execution ages out of the 30-minute window. The only trace was a stall alarm that reports and never repairs. **The snapshot is what makes the question answerable with no new column**: the engine's own record says whether the execution had a transcript and a cost, so an absence stops being ambiguous — which is why this needed no migration, and why it is strictly stronger than the `calls.pipeline_completed_at` marker first considered, since a stage marker reports "the pipeline ran" and a run that silently produced nothing satisfies it. **The concurrency defect is worse than a race because the ledger is append-only**: `asyncio.gather` on two `_meter` calls for one call produced **10 usage rows for a 5-row call and 1.5833 minutes counted into `spend_state` as 3.1666** — a permanent double charge AND a spend cap armed at half the tenant's real allowance, guarded only by an ARQ job id, which is a Redis convention with a finite window rather than a database fact. **A 50-second Redis outage dead-lettered the entire outbox** (10s cron × 5 attempts, no backoff), and recovery was a step-up-confirmed operator replay at whatever hour it happened; tolerated downtime goes 50s → 300s and poison messages still reach the DLQ untouched. **The stall alarm's population was false positives**: a silent call on an agent with no extraction schema owes no extraction, and the alarm counted it twice an hour for a day. **Two of fourteen sabotages found defects in the agent's own tests**, which is the argument for doing them: removing the fan-out locks changed nothing because `_meter` took the same lock four stages earlier and staggered the runs — the race was being run by a different lock — and one assertion read `0 == 0` because the counter it checked was never bumped. **The concurrency suite carries a negative control** that stubs the lock to a no-op and asserts the double-bill reappears, so a future edit that accidentally serialises the harness fails loudly instead of leaving the other tests silently vacuous. **Two vendor assumptions are named rather than assumed**: whether the execution listing paginates (a truncated window is calls the poller never sees at all) and whether `GET /executions` can lag the vendor's own webhooks, which decides whether the 10-minute grace is generous or tight. Both bound the guarantee this row strengthens and both belong to pilot gate 6. |
| D-111 ⚠SECURITY, amends D-107 | **Any tenant could DELETE a global DNC row, because `WITH CHECK` is not evaluated on DELETE** | Migration `e4f2a86b13d7` adds a RESTRICTIVE `FOR DELETE` policy on `dnc_list` whose predicate is the same sentence as the existing `WITH CHECK`. The permissive `FOR ALL` policy is untouched. Asserted with raw SQL at the table, not through the route, plus the opposite direction (an ops session can still lift a suppression). | **Found by writing the coverage the ratchet demanded, not by reading the code.** D-107 raised `compliance-gate` from 9 uncovered units to 21; covering `remove_global_entry`'s "RLS refused the delete" branch meant constructing a tenant session that could see a global row but not delete it — and the test would not go red, because the delete SUCCEEDED. Measured as the app role against the real schema: `SET app.tenant_id = '1111…'; DELETE FROM dnc_list WHERE phone_e164 = '+9198…';` → **`DELETE 1`**. One client lifting a regulator instruction, or our own permanent refusal, for every other client on the platform. **The cause is a Postgres semantic that is easy to read past**: `USING` decides which rows a DELETE may remove and `WITH CHECK` applies only to INSERT and to the NEW row of an UPDATE, so an asymmetric policy that deliberately opens `USING` to `tenant_id IS NULL` — which D-107 had to, or a nationally suppressed number would not be visible to the tenant scrubbing against it — opens DELETE with it. **UPDATE was checked rather than assumed** and is correctly refused (`new row violates row-level security policy`); `dnc_list` is also the only table in the schema whose `USING` mentions `tenant_id IS NULL`, confirmed against `pg_policy`, so the blast radius is one table. **A RESTRICTIVE policy rather than four per-command policies**: the permissive rule is load-bearing for SELECT and correct for INSERT/UPDATE, so rewriting three working rules to fix one would make "can a tenant see a global row" a four-policy question; RESTRICTIVE is ANDed and scoped to the one over-permitted verb. **The application guard was not the fix and could not have been**: `remove_global_entry` already carried a `rowcount != 1` refusal written in the belief that RLS refused the delete, and D-107's own sabotage S9 reported that guard "redundant" on the strength of the SELECT's behaviour — correct about the SELECT, wrong about the DELETE. Hard rule 1 says RLS is the enforcement, and an `if` in one function constrains neither psql, nor the next caller, nor the next function. |
| D-112 | **`usage_events` gets the natural key its two sibling ledgers already had, and an unguarded double-metering aborts instead of double-charging** | Migration `b8d3f47c2a19`: partial unique index on `(tenant_id, call_id, unit_type)`, excluding `unit_type = 'other'` and grandfathered on `created_at >= '2026-08-15 10:14:00+00:00'`. Both `KNOWN_AMBIENT_ROUNDING` (five sites) and `KNOWN_OPEN_RELIABILITY_GAPS` (four entries) are now empty. | **A lock is a convention; an index is a fact.** D-110 closed the double-billing race in-process with `lock_call_writes`, and that was the right first move, but the ledger is append-only so a row that slips past the lock is a permanent wrong number. **The exclusion is the finding**: `billing.service.record_tier_correction` — hard rule 4's compensating-entry mechanism, the ledger's ONLY repair path — appends `unit_type = 'other'` against the same `(tenant, call)`, and two ops refs correcting one call legitimately produce two rows. A key over all unit types would have turned the repair mechanism into a 500, so the predicate enumerates the five types `pipeline._meter` writes, POSITIVELY, following `ux_credit_ledger_tenant_reason_ref`'s precedent. **`ON CONFLICT` is deliberately unused**: `DO UPDATE` fires the append-only trigger, and `DO NOTHING` would hide a partial interleaved write. What a retry does now: the guarded path is untouched, and the unguarded path aborts — and because `_meter` writes the five rows, the wallet charge and the `spend_state` upsert in one transaction, the abort rolls back the whole second metering and ARQ retries into a clean no-op. A permanent double charge becomes one failed attempt. **The grandfather column departs from the sibling and the reason is argued in the migration**: `credit_ledger` keys on `occurred_at`, but `_meter` stamps `occurred_at` from `snapshot.ended_at`, so a poller repair of yesterday's call would land OUTSIDE an `occurred_at` cutoff — precisely the path with the weakest lock discipline — while `created_at` is monotone and actually means "written after this migration". **Sabotage proved the cutoff was necessary rather than cautious**: dropping the index made six tests red, and the index could not then be rebuilt, because the unguarded negative control had left permanent duplicates behind. |
| D-113 | **The outbox stops spending a message's whole retry budget on a receiver that is restarting, and the console stops saying "nothing is dead-lettered" in the middle of an outage** | `mark_outbox_failed`'s retry branch REPLACES the claim lease with the backoff instead of clearing it; `OUTBOX_SYSTEMIC_BACKOFF_*` becomes `OUTBOX_RETRY_BACKOFF_*` because it now governs both failure kinds, with the SQL interval extracted so the two writers cannot drift. `deferred` is published on `DeadLetterQueue`/`DeadLetterQueueOut` from the SAME grouped aggregate as the depth (one instant, `FILTER` clauses). | D-110 fixed the systemic branch and left the per-message one, where clearing `locked_until` made a message with budget left re-claimable instantly — five attempts inside fifty seconds against a receiver that would have been back in sixty. **The console half is the one an operator meets**: it truthfully reported zero DEAD LETTERS while messages sat deferred, which reads as "nothing wrong" at exactly the moment something is. **Two of the agent's own fixes had no test until sabotage asked for one**, and writing them found more: the all-clear lives in TWO places, and only the green notice box had been fixed — `deadReason`, the sentence physically beside the replay button where an operator actually reads, still said "Nothing is dead-lettered" mid-outage. The second, `deferred` hardcoded to `0`, sailed through the existing probe because that probe only checks the FIELD EXISTS; the new test asserts a delta and includes recovery, so the gauge is proven to come back down. **The registry's own equality was restructured**: it required `PROBES == KNOWN_OPEN`, so closing a gap deleted the probe that had just proved it could detect the defect. It is a superset assertion now — entries must have probes, and probes outliving their entries are regression tests that must keep answering False. Three of the four fired during sabotage. |
| D-114 ⚠BILLING CORRECTNESS | **Any two clients onboarded within 65 seconds of each other shared an invoice number, permanently and in every month** | The serial's tenant suffix was `tenant_id.hex[:8]`; it becomes `blake2s(tenant_id.bytes, digest_size=6)`. Rule 46(b)'s remaining gap is pinned by a test asserting its two arms — length and consecutiveness — separately. | **Found by writing a test for something else.** Tenant ids are uuid7, whose first 48 bits are a millisecond timestamp, so `hex[:8]` is `ms >> 16` and advances once every **65.5 seconds**. Not a birthday risk — deterministic collision, and it reproduced on the first two tenants a test created back to back. The old test could not see it because it only ever asserted the serial's LENGTH. The replacement derives from the whole id rather than from a claim about uuid7's layout (~2e-7 at 10k tenants). **On Rule 46(b) itself**: verified by search (REPORTED, NOT READ — `taxinformation.cbic.gov.in` is egress-blocked; five secondaries quote clause (b) identically), and the rule adds a phrase worth having — *"in one or multiple series"*. **Nothing is out of compliance today**, and that is the honest framing: Rule 46 binds a REGISTERED PERSON, there is no GST registration, `supplier.is_registered` is false everywhere and the document says `proforma`, which 46(b) does not govern. What exists is a scheme that would be non-compliant the day registration lands. **The serial was deliberately NOT shortened**, because a 16-character hash satisfies length, still fails consecutiveness, and would have deleted the only record of the gap. **The design is written into `invoice.py` and one founder decision is named**: whether a stored `issued_invoices` registry is accepted against D-46, since a consecutive series is stateful by definition and "the 123rd invoice of this year" cannot be recomputed from ledgers — `build_invoice` cannot mint it behind a GET (D-64). Not a sequence: `nextval` is non-transactional, so a rollback leaves a permanent gap, and there is no financial-year reset short of scheduled DDL at midnight on 1 April; instead `UNIQUE (financial_year, serial_no)` allocated `MAX+1` under an advisory lock on the FY inside the issuance transaction, gapless because a rollback un-allocates, with the year in the key so the boundary needs no cron. |
| D-115 ⚠DPDP, the worst defect found this week | **Filing an erasure request made the recording permanently undeletable, and every test was green** | Migration `9c1d3e7a05f4`: `recording_erasure_holds` (tenant-scoped, FORCEd RLS, cross-tenant zero-rows test, deliberately NOT append-only because `erased_at` is stamped on completion), plus `ttl_days > 0` and `UNIQUE (tenant_id, data_category)` on `retention_policies`. `_erase_recordings` destroys the bytes when past the floor and writes a hold with `erase_after` fixed from the CALL's own clock when inside it; the sweep's hold arm runs OUTSIDE the policy loop. `_sweep_objects_in_batches` replaces a hand-written loop across three callers. The erasure certificate distinguishes destroyed / scheduled-with-a-date / proof-predates-this, and never renders absent as zero. | **`execute_deletion_request` nulled `calls.recording_url` and every existing test asserted that column, so the suite was green on a function that deleted nothing.** The column holds an object KEY, and nothing in the repository had ever deleted a recording object — the sweep's own comment claimed the bucket lifecycle rule did it, while SEC-COMP §4 already recorded that rule as a bucket-wide 2555-day GROWTH CEILING that "CANNOT follow the retention policy". Two consequences, and the second is the one that matters: a caller who never asked to be forgotten had only the POINTER expire on their tenant's policy while the audio survived ~7 years; and a caller who DID ask was left worse off than one who did not, because clearing the pointer destroyed the only handle on the key and the sweep selects `WHERE recording_url IS NOT NULL`. **Filing a DPDP erasure request made the recording permanently unreachable by any deletion path we have.** That is not either side of the open "does erasure beat the retention floor" question — it is the failure to have asked it. **The legal reading is encoded where it is enforced, not in a doc**: DPDP §12(3) lets a retention obligation DEFER an erasure, and §8(7) makes keeping the data past that obligation a breach in its own right, so "we cannot delete this yet" is lawfully a SCHEDULE and a schedule must be a row — which is what the holds table is. The 2025 Rules' Third Schedule and its 48-hour pre-erasure notice were checked and do NOT apply (they ride on e-commerce/gaming/social-media thresholds), recorded in SEC-COMP §4 so nobody re-derives it. **The recording floor's authority does not check out and was NOT unilaterally changed**: TRAI's 90 days is the opt-out cooling period before a sender may seek fresh consent, and the two-year commercial-record archive is Unified Licence clause 39.20 binding LICENSEES — Calevate is a telemarketer, not one. The floor errs towards keeping data, which is safe against destroying evidence and unsafe against §8(7), so it is a registry entry for counsel rather than a guess. **Sabotage caught a defect in the agent's own work of exactly the predicted shape**: its first export fix asserted against a query the test had typed out itself rather than calling `build_subject_export`, so breaking the real code left it green — and the fix was unreachable anyway, because an erasure nulls `from_e164`/`to_e164` in the same statement that creates the hold, so no held call could ever match the export's phone predicate. Rebuilt keyed on `subject_ref`, the handle D-44 preserves for exactly this. **Three constraint sabotages could not be reverted afterwards** — the rows the tests inserted while the constraints were absent are precisely the rows the constraints reject — which is a second proof that they were doing work. |
| D-116 | **Every egress path asserted on the bytes that leave, and the formula guard's fifth surface was the one it missed** | `tests/crm_egress_redaction_test.py` drives ten doors with TWO real-shaped Indian mobiles — the lead's own, and a different one spoken inside the call — asserting in both E.164 and bare-national spelling, each paired with a positive "the surface returned something" guard so an empty page cannot pass. Fixes: `sheet_header` now goes through `_cell`/`_disarm`; `lead.updated` is emitted from the PATCH and the bulk action; `export_leads_csv` returns `LeadExport(csv, row_count)`; a BOM on the browser blob only. | **The Sheets HEADER bypassed the formula guard while the CSV export's own comment cited that same writer as the "EVERY CELL, HEADER INCLUDED" rule it was copying** — the guard held on four surfaces and leaked on the fifth, which is the shape this slice existed to look for. **`lead.updated` was subscribable and had no producer**: it was in `EVENT_TYPES`, in the endpoint form as "A lead's details change", and in `DEFAULT_SHEET_COLUMNS` since D-23, with zero emitters — a client could subscribe and wait forever. Wiring it forced `set_lead_name` to grow a CAS, because the blanket `SET name` always reported success, so re-saving an unedited row would have posted to a client's CRM and re-sorted their table. **The export's audit row counted `csv_body.count("\n") - 1`**, which over-counts by exactly the rows whose name or note contains a newline — `QUOTE_ALL` keeps it in-cell — so the compliance record of how many people's data left was wrong precisely for the awkward rows. **The missing BOM is a Telugu-first product detail**: Excel decodes a markless `.csv` in the legacy code page, so every Telugu name arrived mojibake; added to the browser blob only, keeping the API response clean UTF-8, the same one-hazard-two-renderings split `spreadsheet_safety.py` already makes. **A guardrail blind spot is now documented rather than assumed away**: `check_redaction_exposure` walks OpenAPI, so CSV bytes, webhook bodies and Sheets rows are structurally invisible to it, and the runtime suite that does cover them is named in the script. **Three of the agent's own tests passed for the wrong reason and it caught all three**: the audit-count sabotage went GREEN because no test existed; `Blob.text()` runs the spec's UTF-8 decode and STRIPS the BOM, so a character-level assertion was green either way (switched to `arrayBuffer()`); and the webhook test claimed to cover the producer while being handed an already-redacted summary, so the producer got its own real-pipeline test. It also initially swept `\t` out of CSV cells, which IS the OWASP mitigation, and corrected to sweep executable leaders only. |

| D-117 ⚠COMPLIANCE | **A lead ad fill that never asked permission was refused at the front door and dialled by the button on the leads screen an hour later** | `ingest.service._record_dial_consent_declined` writes a `consent_ledger` row on both refusal branches — `purpose='callback'`, `status='declined'`, `consent_source='web_form_optin'` — keyed on the PHONE. `check_dispatch` grows a `no_consent` rule reading the LATEST row for `(tenant_id, phone_e164, 'callback')` and refusing on any status outside `granted`, via `DIAL_REFUSING_CONSENT_STATUSES` derived from the ledger's own vocabulary. | **The front door was already right and it did not matter.** `ingest_lead` correctly saves and refuses to dial a Meta lead-ad fill on a form with no opt-in question — but the refusal lived only in a `lead_events` row of type `note`, which the timeline renders as prose for a human and which no dial path reads. So `POST /v1/leads/{id}/call` dialled that person with the ENTIRE compliance gate passing, because the gate had nothing per-person to ask. The CRM slice proved it by test before recording it, which is why it is a fix and not a hypothesis. **The write is keyed on the phone, not the lead**, because the ledger's unit is a person and the same person arrives twice as two lead rows from two forms — a refusal attached to lead #1 would be silently undone by lead #2. **ABSENCE IS NOT A REFUSAL**, and that asymmetry is the whole design: most dialable numbers have no ledger row at all (typed in by staff, imported from a CSV, a caller who rang US), so reading silence as `declined` would refuse every one of them and be met as a total outbound outage rather than as a rule. **The status set is subtractive** — everything except `granted` blocks — so a status added to `CONSENT_STATUSES` tomorrow refuses by default and has to be argued INTO the allowed set, which is the safe direction on a compliance gate. **`callback`, not `marketing`**: this gate governs whether we may telephone this person, and `messaging` has its own gate on the WhatsApp path — a person may accept a call and refuse a message, and both answers are theirs. **The schema pushed back and was right**: `ck_consent_ledger_granted_consent_carries_evidence` refused a bare `granted` row in the supersession test, so a grant must say how it was obtained while a decline needs no evidence beyond the absence the timeline already records. **A methodological note worth more than the fix**: the first sabotage round reported the same two failures for four different breaks, and one of those breaks had not even applied — the source files had been reverted underneath the run, so the baseline was never green. Four identical results from four different causes is the signature of a test suite proving nothing, and it was caught only by noticing the uniformity. The round was redone with each edit's application verified by grep, a green baseline asserted either side, and the four breaks now produce four distinct signatures. |
| D-118 | **The onboarding wizard could hand over an account whose agent had never reached the engine, and when it did reach it, it could carry nine words of English** | `admin/intake.py` stops carrying its own copy of the `prompt_versions` INSERT and calls `prompts.insert_prompt_version` with `apply_live = not script_staged`; `IntakeOut` gains a required `staged_behind_script`. Publishing an agent with no script is `agent_has_no_script` (422) and writes nothing. A "Voice platform" panel performs the FIRST publish. Both client-realm invitation refusals move into `admin.service.create_invitation`, with admin-realm list/revoke added so the refusal cannot lock an operator out. `inviteLink()` is the one spelling of `/invite`. | **Four defects, and three of them made the product LIE rather than fail.** `agents.live_prompt_id` was never set by the wizard even though `agents/service.py` states the invariant "`live_prompt_id IS NULL` can only mean the two pointers agree" and NAMES `admin/intake.py` as a writer upholding it. Consequences in opposite directions: a freshly onboarded, freshly published agent told the CLIENT "Script v1 is waiting to go live" **forever**, about the script the engine was actually running — because `publish_agent` sends `COALESCE(live_prompt_id, system_prompt_id)`; and re-submitting the intake over a staged edit minted the new version as a DRAFT and then published the version the applied pointer still named, so an operator updating a client's prices saw `prompt_version: 3` while the engine kept v1. **Publishing an agent with no script shipped `"You are a helpful receptionist."`** — the wizard mints the agent row at step 1 before any prompt exists, and `_to_config` fell back to a hardcoded English string. The route answered **200 `status: live`**, wrote the routing row, and put nine words of English with no hours, no prices and no business name on a Telugu clinic's line. **Nothing anywhere performed the first publish.** The route was mounted and tested and had zero callers in either realm; every other publish path is a RE-publish guarded on the agent already being live (`apply_to_live` → `if row.is_live`, `set_call_cap`/`recompile_t0` → `status == 'live' AND engine_agent_ref`). A founder could run the entire wizard, invite the owner and hand over an account whose agent had never reached the engine — the half-wired-feature defect at the scale of a whole customer. **The admin invite path enforced neither of the client realm's two refusals**, so pressing Create-invite twice minted two live owner credentials for one address with only one revocable; the fix moves both into the single statement that mints the row, so both realms inherit them. That fix then created a 72-hour lockout for an operator whose token was lost — the only revoke was client-realm and the owner cannot sign in yet — which is why admin-realm list/revoke landed in the same change rather than as a follow-up. **Two sabotages found defects in the agent's own tests**: one asserted only a button's absence, which an empty card also satisfies (fixed to assert a skeleton), and the `/invite` path had nothing pinning it at all — the very defect that made every invite link a 404 for eight days. **Two sabotages could not be made red from app code and are recorded honestly rather than dropped**: `calevate_app` is NOBYPASSRLS and cannot `SET ROLE`, so those tests document a database-enforced guarantee rather than an application one. |
| D-119 ⚠SECURITY | **A blank impersonation header was an undesigned third state that set `impersonating` without a grant, a permission check or an audit row — and the RBAC registry passed vacuously on a permission that does not exist** | One `_impersonation_slug()` shared by all three dependencies; blank or whitespace is `422 impersonate_org_blank` naming the header; `Principal.impersonating` is derived from the RESOLVED TENANT, so it cannot be true without an authorised, audited entry. `assert_policy_registry_complete` validates every declared string against `get_args(Permission)` and against `GRANTED_PERMISSIONS`. `current_principal` refuses a request carrying the impersonation header with `403 impersonation_not_available_here`. | **`X-Impersonate-Org: ` with an empty value satisfied `header is not None`**, so `current_admin` set `impersonating=True` while the falsy slug skipped the `admin:impersonate` check, the grant requirement AND the `admin.impersonation_read` row. `Principal.impersonating` is what every mutating dependency, `can_mutate` and the console's grey-out logic read as "a grant was verified and this read was audited" — and it could be true when none of that had happened. `current_any` disagreed with `current_admin` about the same header and fell through to the CLIENT verifier, so a header problem answered "This token is not valid for this realm"; whitespace reached the directory lookup and answered `404 Organization not found`, a 404 about a client for a defect in a header. **The registry defect is the more instructive one**: `permission_meta("agents:reed")` beside `requires("agents:reed")` satisfied every existing check, because **two copies of one typo agree with each other** — declared equals enforced, an identity resolves. `role_has` then answered False for every role the database enums allow, so the route was a 403 for the entire population: a dead endpoint that reads as a guarded one, with no happy-path test to go red. mypy catches the literal spelling but does not run at boot, cannot see a permission built from a variable, and never looks at the route table. **Three client-realm mutations refused a valid view-as session with the wrong sentence** — correct refusal, wrong explanation, handed to someone whose credential is fine, escalating to whoever owns Clerk instead of whoever owns the console; the new check is on the HEADER rather than the token, because deciding "this is an admin's token" would create a second definition of which realm a request belongs to. **The mutation sweep is the test that would have caught all of it**: every one of the 66 routes declaring a mutating permission, driven with a real minted grant, each required to refuse with one of exactly two known codes — 63 by D-22's read-only rule and 3 by the realm boundary — with a floor assertion so the split cannot drift toward "D-22 is enforced by accident of which realm a route happens to declare". **Guarantees named as database-enforced rather than application-enforced**: `ROLE_PERMISSIONS` is one flat dict and nothing in Python stops a client `owner` and an admin role sharing a name and merging their permission sets — what prevents it is `ck_memberships_role_enum` and `ck_admin_users_role_enum`, so a colliding role cannot be STORED, and the test says plainly that its sabotage is an `ALTER TABLE … DROP CONSTRAINT` rather than a `.py` edit. Seven sabotages, seven distinct signatures, each verified landed by grep with a green baseline asserted either side. |
| D-120 ⚠TELUGU-FIRST | **Every non-Latin business name became `/c/client`, a closed account could be handed a key that burned itself on redemption, and the wizard's audit row was a second transaction** | `slugify` returns `""` and `derive_slug` refuses with `slug_not_derivable` (422) naming the field, on the wizard and on self-serve signup, with both screens asking before the POST. `admin.service.assert_account_open` is asked at BOTH the mint and the burn of an invitation — 404 for an absent tenant, 409 `account_closed` for churned or soft-deleted — and the refused redemption rolls back so the link stays redeemable. The lifecycle switch answers 404 for a soft-deleted client via the one `service.tenant_exists` definition. The wizard's audit row, the invite mint and the revoke all move into the birth transaction through the `on_created` hook self-serve signup already used. | **The slug defect is the one that should never have survived on a Telugu-first product.** `slugify` folded Telugu and Devanagari to the empty string and substituted a constant, so the FIRST such client silently took the immutable slug `client` and the second got `slug_taken` naming a slug nobody had typed. That is the default path for this product's market, not an edge case. Transliteration was considered and rejected for a stated reason rather than an aesthetic one: no ASCII-folding library is installed, and adding one to the tenant-birth path is a hard-rule-9 supply-chain decision — so the product ASKS. **The invitation defect is a complete broken journey rather than a bad status code**: inviting into a churned or soft-deleted tenant answered 201, the invitee accepted with 200, burned their single-use token, received a `memberships` row, and was then refused "You are not a member of this account" on their first request, because `core/auth.py` filters `deleted_at IS NULL AND status <> 'churned'`. Every step succeeded and the outcome was a person with a spent credential and no account. **`suspended` is deliberately still invitable** and pinned by a test: suspension stops outbound dialling, not access, and it is exactly when you add the person who will pay. **The audit gap is the quiet one**: a client account — or a live owner credential — could exist with nothing recording who created it, because the wizard wrote its audit row in a second transaction while self-serve signup had already solved this with the `on_created` hook. One mechanism now, not two. **Two of the agent's own tests were flaky and were found because the baseline was re-asserted between sabotages** — a shared signup-quota source IP and a non-unique 39-character slug — which is the argument for that discipline rather than for trusting a green run. Nine breaks, nine distinct signatures. **Two gaps recorded rather than fixed, neither vendor-blocked**: `organizations.deleted_at` has seven readers and no writer anywhere in `apps/`, so tenant-level erasure has no execution path (`deletion_requests` is per data subject); and `current_identity` answers 401 to a verified Clerk token whose Svix mirror has not yet landed — the ordinary race on signup and invite-accept — where a browser reads 401 as "sign in again", which does not help. |
| D-121 | **`publish_agent` never called `get_agent`, so "live" was four claims about the engine all derived from one fact about ourselves: our HTTP call returned without raising** | Migration `c1f6a94d2b07` adds `agents.live_verify_state` (four values, CHECK) and `live_verified_at`. New `agents/verification.py` — `judge()` / `verify_publish()` with verdicts `applied` / `not_applied` / `unreadable` / `unreachable`, scoring the disclosure line SEPARATELY from the script. `publish_agent` takes a row lock, reads back after the write, REFUSES on a proven mismatch (`engine_publish_not_applied`, transaction rolls back), records unproven states without rounding them up, and carries `AND deleted_at IS NULL` with a rowcount refusal. Same for `publish_variant`. `engine_drift_for()` + `GET /v1/agents/{agent_id}/engine-state`. A conformance clause over all four adapters. | **D-64 built the entire verification surface and nothing called it.** `get_agent` went onto the Protocol carrying the sentence "a 2xx says the vendor took the bytes, not that the agent is running them", along with `carries_prompt_marker`, `holds_speech` and three `*_readable` tri-states — and no production code path ever invoked any of them. So `status='live'`, `engine_agent_ref`, `live_prompt_id` and `live_tts_voice` were four assertions about the vendor derived from one fact about us. **The disclosure line is scored separately from the script** because an engine that keeps the script and drops the greeting passes a script-only check while breaking hard rule 5 — the one property here with a legal consequence — and a dictated-TTS engine is not scored on a voice it never took, because a check that fails on every publish is a check operators learn to ignore. **The orphan turned out to be manufactured by the ordinary case**: `publish_agent` was a read-then-write over `engine_agent_ref` with a vendor round trip in the middle and no lock, so two concurrent publishes both saw "no ref", both created, and one vendor agent became unaddressable and undeletable — proved with a double minting a fresh id per create (FakeEngine's deterministic ref hides it) plus 50ms of vendor latency to make the coroutines genuinely interleave. **The publish UPDATE named the id alone**, so a delete landing between `_load_agent`'s filter and that statement resurrected a soft-deleted agent to `live` AND gave it an `engine_agent_routes` row — the same class as the finding that `AND a.deleted_at IS NULL` in `_load_agent` was the only thing between a deleted agent and a caller, one statement further down. **`unreadable` and `unreachable` deliberately do NOT block**, argued at the module head rather than assumed: on a create the vendor write has already happened, so refusing there trades a recorded uncertainty for a guaranteed orphan plus the same uncertainty. **Reconciliation is a READ**: it re-publishes nothing, because overwriting someone's emergency console edit is a decision with a blast radius, and it is the only instrument that can see the two drifts a publish-time check structurally cannot — a vendor-dashboard edit, and a publish that failed on our side AFTER the vendor committed. **Two of the seven sabotages forced test fixes rather than passing**: the concurrency break was GREEN first time because `asyncio.gather` with no yield inside the vendor window ran each transaction in turn and measured nothing, and the resurrection break reported only `DID NOT RAISE` until the assertions were moved out of `pytest.raises` so they say what the missing refusal actually left behind. **Three gaps recorded with live probes**: `create_agent` is not idempotent on a lost response (the remedy is an idempotency key, and whether Bolna's `POST /v2/agent` honours one is unknown from here — a guessed header shipped as a guarantee is the D-31/D-32 mistake); there is no `delete_agent` on the Protocol, so an orphan cannot be compensated; and drift reconciliation is on-demand only, which is NOT vendor-blocked and closes with one ARQ cron. |
| D-122 | **`organizations.deleted_at` had nine readers and no writer anywhere, so tenant-level erasure — the last step of offboarding — had no execution path at all** | Migration f3a71c9e26b4 adds `tenant_erasure_requests` (RLS'd, one open request per tenant), the CHECK `ck_organizations_deleted_implies_churned`, and an exclusive second owner on `recording_erasure_holds` (`num_nonnulls(request_id, tenant_erasure_id) = 1`). New `compliance/tenant_erasure.py` + `tenant_erasure_routes.py`: `POST /v1/admin/tenants/{id}/erasure`, `admin:tenants` declared, `ops:manage` checked and a step-up bound to the tenant id, filing the row, the outbox job and the audit entry in ONE transaction. `workers/retention.execute_tenant_erasure` reuses `_erase_recordings`, `_erase_delivery_bodies` and the per-subject statements over a keyset walk of the tenant, and writes `deleted_at` LAST, beside the certificate. | **The column was load-bearing and unreachable**: `core/auth.py` (membership resolution AND the impersonation slug lookup), the dial gate, `tenant_exists`, `assert_account_open`, the admin directory, the health board, `quality/service.py`, `quality/sampling_routes.py` and `workers/qa_sampling.py` all branch on it, and nothing in `apps/` could set it — a whole column of behaviour that read as shipped. **The readers disagreed with each other and the fix was an invariant, not a vote**: some filter `deleted_at` alone, some `status` alone, some both, which is only safe if `deleted_at IS NOT NULL => status = 'churned'`. Without it an 'erased' but `active` tenant would be in `core/auth.py`'s refusal set and the health board's LIVE set at the same instant. Enforced in the DATABASE because a writer is a property of this week's code — the test says so and names `ALTER TABLE … DROP CONSTRAINT` as its only real sabotage. **A second erasure MECHANISM was refused and a second SUBJECT was insisted on**: `deletion_requests` is one data principal's DPDP §12 right, keyed by `phone_e164`, client-realm, with a CHECK forbidding an open row without a number — so tenant rows there would have shown up in a client's own register, and the fan-out alternative would have minted one §12 certificate per caller for a request nobody made. The worker, the object-store helpers, the outbox and the hold schedule are shared; only the table, the precondition and the tenant walk are new. **`recording_erasure_holds` needed a second owner or the bug it was built for came straight back**: clearing `calls.recording_url` destroys the only handle on the audio and the sweep selects `WHERE recording_url IS NOT NULL`, so a tenant erasure without a hold row makes the recording permanently undeletable — the exact defect migration 9c1d3e7a05f4 closed on the other path. **Hard rule 4 was a design constraint, not an obstacle**: every append-only ledger survives byte for byte, including the `consent_ledger` rows that carry the caller's number, and the certificate SAYS so rather than letting a document that lists what it cleared imply it cleared everything. **The certificate had to outlive its subject**: every other admin route filters `deleted_at IS NULL`, so a status read built the ordinary way would 404 at the instant the erasure it certifies succeeded — none of these routes goes through `tenant_exists`. Six sabotages, six distinct signatures, green baseline re-asserted either side; the seventh guarantee (the invariant) and the eighth (RLS) are database-enforced and say so in the test. |
| D-123 | **D-121 recorded three gaps and called two of them vendor-blocked; only one was. `delete_agent` was a published Bolna endpoint nobody had looked up, and the drift sweep was one cron** | `VoiceEngine.delete_agent` on the Protocol, IDEMPOTENT BY CONTRACT, implemented in all four adapters (`absent_is_success` on `_request` converts a 404 into the satisfied postcondition) with a conformance clause that deletes one of two agents, proves it gone through `get_agent`, proves the other survived, and deletes twice. `agents/service.py::_orphaned` became `await _reclaim_orphan(...)`: the compensator now DELETES the vendor object inline before the publish raises, and falls back to the old ERROR line naming the ref when the delete itself fails. Migration `d4b8e1c73f05` adds `drift_state` / `drift_checked_at` / `drift_detected_at` to `engine_agent_routes` with a five-value CHECK and a partial `(engine, drift_checked_at NULLS FIRST) WHERE active` index. New `agents/reconciliation.py` (`claim_drift_batch` / `record_drift` / `read_engine_drift`) and `workers/engine_reconciliation.py`'s `sweep_engine_drift`, an ARQ cron at :07/:37 with `max_tries=WORKER_MAX_TRIES`. `GET /v1/ops/platform` carries `engine_drift`; the admin ops console renders it as a read-only panel. OPERATIONS §2 gate 2 grew a `delete_agent` sub-check that runs on a THROWAWAY agent, and `runbooks/agent-engine-drift.md` is the alarm's procedure. | **A GAP CALLED VENDOR-BLOCKED SURVIVES BECAUSE NOBODY RE-CHECKS IT.** `no_delete_agent_on_the_protocol` said an unverified vendor behaviour is not a capability (D-31/D-32) and named a Bolna account as what closes it — and Bolna publishes `DELETE /v2/agent/{agent_id}` in its reference, 200 `{"message":"success","state":"deleted"}`, with a note that it destroys the agent's batches and executions. One search closed most of it. What genuinely remains unknown is narrower than the entry claimed: what a REPEAT delete answers. That is a MARKED ASSUMPTION in both real adapters (assumed 404, falsified by a 400) plus gate 2's third sub-check, not a silent premise. **Cartesia's is weaker and says so** — no public agent-delete documentation exists, which is itself the finding. **THE COMPENSATION IS INLINE AND NOT THROUGH THE OUTBOX**, because the ref lives only in the frame: an outbox row would roll back with the transaction that is failing and take the last copy of the id with it. **A HUMAN'S SOFT-DELETE DELIBERATELY DOES NOT REACH `delete_agent`** — the vendor's delete destroys call history that is a DPDP retention obligation, and the orphan's subject is an agent minted seconds ago that has never taken a call. **THE SWEEP'S RECORD LIVES ON `engine_agent_routes`, NOT ON `agents`**, and that is structural: `agents` is FORCE-RLS'd, so a staleness-ordered global work queue and a cross-tenant ops summary are both unaskable from a tenant session — the alternative was the first RLS exemption on `agents`, versus reusing the routing bridge's existing one, and the three columns hold a five-value verdict and two timestamps with no prose (hard rule 6). **IT SKIPS THE REDIS LEASE `campaign_dispatch` NEEDS**, because a 120s budget plus one read inside a 1,800s interval cannot overlap — and that licence is checked at import (`_assert_the_tick_fits_its_interval`) rather than asserted in a comment. **`unreadable`/`unreachable` ARE NOT THE ALARM**: an alert that fires when a vendor is briefly slow is muted before it ever catches a real dashboard edit, so the console publishes them as a separate count. **`oldest_checked_at` IS ON THE PAYLOAD BECAUSE THE PANEL WOULD OTHERWISE LIE BY OMISSION** — if the cron dies every count freezes and `out_of_sync: 0` reads as all-clear forever. **Two tests were green for the wrong reason first**: the lost-response drift wrote its new prompt in a committed transaction of its own, so our row and the engine ended up agreeing and the sweep correctly reported `applied` — it only became a divergence once the setup went through `apply_to_live`, which pushes to the engine INSIDE the transaction that moves the pointer; and every `checked=N` count was whatever routing rows previous suites had left in the shared database until the sweep's scope was pinned with `active`. **`create_agent` idempotency stays OPEN and is the one real vendor block**: whether `POST /v2/agent` honours an idempotency key is unknown from here, and a guessed header shipped as a guarantee is exactly the D-31/D-32 mistake. |
| D-124 ⚠SECURITY | **A verified Clerk token whose Svix mirror had not landed yet was answered 401 "This account is not provisioned.", so signup and invite-accept — the first thirty seconds of every client's and every colleague's life in the product — put people in a sign-in loop with no exit** | New `apps/api/core/clerk_identity.py` owns the ONE resolution of "which user is this token": `resolve_mirrored_user`, called by both `current_identity` and `_load_client_principal`. On a mirror miss it reconciles from Clerk's Backend API (`GET /v1/users/{id}`) through `mirror_clerk_user` — the SAME upsert the Svix webhook now delegates to — and only when Clerk cannot be reached does it raise the transient `identity_mirror_pending` (503, `retryable: true`, `Retry-After: 2`). `apps/web`'s transport waits that code out with bounded backoff (0.5/1/2/4s) instead of bouncing to sign-in. A deactivated row is 401 before any reconcile; a Clerk 404 is 401, not an endless 503; the admin realm is never reconciled. FLOWS §2's "the mirror must land first" ordering claim is deleted. | **Clerk's own guidance is the evidence and it names our design as the anti-pattern**: "Webhooks are asynchronous and eventually consistent… Do NOT rely on webhook delivery as part of a synchronous flow such as onboarding… For data the user just created, read it from the Clerk session token or call the Backend API directly" (github.com/clerk/skills, `features/clerk-webhooks/SKILL.md`, read 2026-08-15; clerk.com is unreachable from this build host). **Minting the row from the token's CLAIMS was rejected as a security defect, and that is the whole reason the Backend API is used instead**: `accept_invitation` binds an invitation to `users.email`, so that column is an authorization input — a row built from an `email` claim whose verification status the token does not state would let someone who added a colleague's address to their own Clerk account redeem the colleague's key. Pulling Clerk's User object places EXACTLY the trust the webhook already places and no more, which is why the two paths share one function rather than resembling each other. No new table, no migration and no fourth reliability mechanism: the upsert's `ON CONFLICT (clerk_user_id)` is what makes a late webhook and two concurrent reconciles collapse to one identity — proven by a test whose stubbed Clerk call blocks on a barrier until both callers are inside it, and which times out rather than passing if the pair is serialised. |
| D-125 | **Two events a client could subscribe to and one lead a client could see were all unproducible — `campaign.completed` had no producer, and a Meta lead refused for want of a token stopped being recoverable the moment Meta gave up retrying** | `campaign_dispatch.emit_campaign_completed` fans `campaign.completed` out IN the transaction `complete_or_rearm` writes the terminal status in, with aggregates only (`campaign_id`, `name`, `contacts_total`, `contacts_reached`, `completed_at`) and a matching `DEFAULT_SHEET_COLUMNS` layout; a re-armed repeat produces nothing. `POST /v1/lead-sources/{webhook_id}/meta/redrive` (`org:manage`, audited) selects that source's `webhook_inbox_events` rows failed on `meta.REDRIVABLE_REASONS` and calls the SAME `_absorb_leadgen` with a `LeadNotification` rebuilt from the row and the ROW'S OWN `payload_hash`, so the claim, the capability selector, the consent branch and the compliance gate are production's. `IngestActivityItemOut` gains `event_key`, `lead_source_id` and a server-derived `recoverable`; the Meta setup card counts what is waiting and offers the button. Both entries in `tests/crm_egress_known_gaps_test.py` deleted, both probes kept. | **The registry's own equality is what made this a fix rather than a rediscovery** — the entries could not survive their defects, and the kept probes now fail by the original names if either regresses. Three things the design work had not foreseen and the build found. **(1)** A `LeadNotification` rebuilt from an inbox row carries the `leadgen_id` and no provenance, so hashing it presents a different digest for the same key and `claim_inbox_event` correctly refuses it as a doctored replay — the row's stored hash has to be passed, and the price is that a recovered lead's `leads.data` names the id that fetched it and not the ad it came from, because the reliability inbox stores no vendor payload and hard rule 2 says it must not start. **(2)** The re-drive could STRAND the lead it exists to rescue: a Graph timeout mid-re-drive is recorded as `meta_graph_unavailable`, which is correct on the live path where Meta's own ladder brings the notification back, and fatal here because the recorded reason IS the candidate predicate — so `_restore_redrivable_reason` puts the original refusal back, guarded on `status = 'failed'`. **(3)** `campaign.completed` was the specimen two Sheets tests used for "an event with no column order", so giving it a producer left that refusal reachable by no event at all; the tests now monkeypatch the layout away and a third asserts the stronger claim — every event in `EVENT_TYPES` has one. A frontend sabotage also caught a test of mine passing for the wrong reason: the waiting-lead count was proved only against rows that were all recoverable, so the flag was not load-bearing until a non-recoverable row for the same source joined the fixture. |
| D-126 | **The debug archive for raw vendor payloads was a personal-data store no erasure could enumerate — fixed before its first caller, not after** | `storage.payload_key` now writes `engine-payloads/{tenant}/{call}/{engine}-{execution}.json` instead of `engine-payloads/{engine}/{date}/{execution}.json`; `archive_payload` takes the tenant and call as REQUIRED arguments and states the write order it needs (commit `calls.engine_payload_ref` FIRST, then PUT); `retention._erase_engine_payloads` deletes every object under the erased calls' prefixes and clears the refs on both the per-subject and the tenant erasure path, counted in each proof's `actions`; `storage.delivery_body_keys_for` became `keys_under` so one listing serves both stores. No migration and no schema change — the column already existed and nothing writes it — and NO change to `infra/object-lifecycle/policy.json`: the rule's `engine-payloads/` prefix is unchanged, which `tests/object_lifecycle_test.py` asserts rather than assumes, so nothing in `infra/` (which has never been applied to any bucket, README §5) was touched or needed to be. The registry entry in `tests/dpdp_known_gaps_test.py` is DELETED, which its equality assertion required. | The archive carried the caller's number and the transcript under a key naming neither tenant nor subject, so a DPDP §12 request could not reach it and no `retention_policies` category expires it — and it was safe only because the function had no caller. **Remedy (b) over remedy (a):** deleting the capability was the cheaper change but the wrong one, because TRD §5, SURFACES §5 (persist-then-ack), DEPLOYMENT §1 and DATA-MODEL §3 all describe archiving raw payloads as the deliberate escape valve for hard rule 2 — vendor payloads live in object storage precisely so they never land in typed columns — and Bolna is unsigned and at-most-once with the poller as truth (D-31), which is exactly the reconciliation an operator needs the vendor's own document for. Deleting a documented capability is a product decision, and the two-step of hard rule 8 does not license the drop just because step one is already true. The arm is gated on a call carrying a reference, following `_erase_delivery_bodies`' own argument that no erasure should depend on the object store being up when there is nothing to enumerate; the gate is sound only because the write order above makes an object-without-reference impossible, which is why that order is a contract in the docstring and not a preference. **What remains open and is nobody's to code around:** the archive still has no retention category (the enum is `recording|transcript|lead|consent_log`), so anyone who does NOT file an erasure is covered only by the bucket's 90-day rule — giving it a category is a DPA commitment plus a documented-enum change, reserved in SECURITY-COMPLIANCE §4 beside the backup clause. |
| D-127 | **Dashboard AI runs on Gemini through Vertex AI `asia-south1` and NOWHERE ELSE — supersedes D-36's LLM leg for the post-call, USER-TRIGGERED surface only; D-36's in-call stack (Saaras STT · Sarvam 105B · Bulbul v3) stands, and so does Sarvam on the first extraction pass** | Records the founder's decisions of 15 Aug 2026 (PLAN-HARDENING-AND-GEMINI §1, G-1…G-8) as one binding row. **G-1 — the endpoint, and it is the whole decision**: `asia-south1-aiplatform.googleapis.com` with `locations/asia-south1` in the path; the AI Studio / Gemini Developer API (`generativelanguage.googleapis.com`, which is what `apps/workers/extraction.py:55` reaches today) is DISQUALIFIED, not deprioritised. **G-2**: the dashboard assistant is given tenant-authored config plus REDACTED call data — `text_redacted`, masked summaries — and never raw PII. **G-7**: the FIRST post-call extraction stays Sarvam, permanently, for the reason under "why" below; Gemini serves only what a user asks for AFTERWARDS (re-summarise, reshape, ask-about) over the redacted copy. **G-3/G-4/G-5**: one Calevate-owned GCP credential, cost absorbed, metered per tenant into `usage_events` with its own unit types, overage charged as one `credit_ledger` row with reason `usage` past an included monthly quota — and at the ceiling the feature BLOCKS and opens a modal naming the cost, debiting nothing until a person accepts, that acceptance being itself auditable. **G-6**: on failure — unconfigured, over quota, provider down — one place decides, and it either falls back to Sarvam or refuses with a message the user can act on; a fallback is ALWAYS disclosed in the response and on the screen, because a silent one changes output quality with nobody told. **G-8**: autofill is a suggestion, never an act — human approval (client or operator) is required and it never reaches the publish path unapproved. Enforced before any Vertex code exists by `scripts/check_model_residency.py` in `make guardrails` (PLAN Part 12): no global Google model host in any URL literal under `apps/`, `packages/`, `scripts/`; every `*-aiplatform.googleapis.com` literal carrying `asia-south1`; the region resolvable only from a `Final` constant and never from a console-editable `Settings` field; and the `locations/…` segment interpolating that constant and nothing else. SECURITY-COMPLIANCE §4's cross-border row is rewritten around that guardrail and names Google Cloud (`asia-south1`) as a sub-processor in the client DPA. | **The founder's instruction — "Gemini for everything outside the live call" — reads at a glance as moving extraction, and moving extraction would have shipped raw caller PII to a second processor.** Verified at source before this row was written, not taken from the plan: `apps/workers/pipeline.py:749` computes `redacted = redact(turn.text)` and `:750` appends `turn.text` — the RAW turn — to the string `_persist_transcript` returns at `:773`, which `:541` binds and `:572` hands to `extract_call`. One line apart, deliberately, because a CRM "callback number" field needs the actual digits and an extractor reading `[REDACTED]` returns nothing worth storing. So the raw-PII pass stays sovereign and everything downstream of it runs on Gemini over the redacted copy — G-7 is not a compromise, it is the only split that keeps both halves true. **The guarantee D-36 bought survives; its ARGUMENT does not, and pretending otherwise is how a residency posture rots.** D-36 reasoned "Sarvam is sovereign, therefore no transcript text leaves India". That sentence stops being available the moment a Google endpoint is in the product, and the replacement is narrower and has to be enforced rather than asserted: **the endpoint is regional**. Which is precisely why the guardrail lands BEFORE the client that could violate it — a region is one string, the difference between `asia-south1` and `us-central1` is nine characters, and nothing in a code review reliably catches nine characters. **The AI Studio API is disqualified on two independent grounds, either alone sufficient**: it offers no data-residency guarantee (global host, no region in the URL, `locations/global` on the Vertex side of the same door), and on the free tier Google states it uses submitted prompts, uploads and responses to improve its products, **with human reviewers able to read them** — which for a Processor holding an Indian SMB's callers' transcripts is not a tradeoff, it is a disclosure we could not make. Vertex `asia-south1` (Mumbai) processes and stores in-region for GA generative features and does not train on paid usage, and serves the Gemini Flash / Flash-Lite class we need. ⚠ **Evidence standing is REPORTED, NOT READ and says so here rather than in a footnote**: `docs.cloud.google.com`, `modelavailability.com` and `docs.litellm.ai` are all refused by this environment's egress proxy, so the endpoint format, the regional model availability and the AI Studio terms rest on multiple independent search summaries agreeing — never a page fetched from this repository. It is the D-105 posture for the same reason: a wrong host fails LOUD (a 404 or a 401 from a host that does not serve our project), which is the safe direction, and confirming it is a five-minute task on the day the GCP project exists — it is an OPERATIONS §2-class vendor gate, not an engineering unknown. **The region is deliberately NOT a `Settings` field**, and that is the half a reviewer would wave through: `platform_config.managed_fields()` computes the console's editable set from `Settings.model_fields` minus the bootstrap keys minus credential-shaped names, so a field called `vertex_location` would be console-editable the day it is declared, and a residency posture invertible from a web form at 3am is not a posture. Same doctrine `check_bootstrap_keys` applies to `APP_ENV` (D-95 §4), enforced the same way. **What this row does NOT decide**: which model extracts better. The public benchmarks answer a different question (OCR, Hindi GEC) and nobody has published Telugu code-mixed call transcript → structured CRM fields; this repo owns the right instrument in the golden-transcript fixtures and `scripts/eval.py`, and that run is blocked outside this repo on a Sarvam key and egress, named as such rather than scheduled. |
| D-128 | **The readiness probe stopped naming the credentials this deployment has not installed, and the OpenAPI surface stopped being served in prod** | `core/health.py` splits the VERDICT from the DETAIL: `/healthz` and `/healthz/ready` answer `{status, service}` with an unchanged status code to everybody, and disclose `degradation_mode`, `checks`, `queue` and `fields[]` only to a caller who proves `ops:manage` through the existing `core.auth.requires` ladder — injected by `create_app` as `build_health_router(service, detail_gate=...)` rather than imported, so `core.health` stays inside voice-runtime's pinned import surface. What is withheld from the wire is written to a `health_not_ready` WARNING instead. `create_app` sets `docs_url`/`redoc_url`/`openapi_url` to `None` when `app_env == "prod"`, and `PUBLIC_PREFIXES` loses `/openapi.json`, `/docs` and `/redoc`. `tests/health_disclosure_test.py` drives both directions of every pair. | **`fields[].field` was `runtime_config_missing_keys` verbatim** — the NAMES of the credentials not yet installed (`BOLNA_API_KEY`, `CLERK_ADMIN_SECRET_KEY`, `AUDIT_CHAIN_SECRET`), unauthenticated, exempt from the in-app rate limiter, and proxied from both `api.` and `hooks.` — a targeting oracle that is loudest during exactly the window before real keys land. **The origin lock is not the alternative it looks like**: `calevate-origin.conf` is already included by the api vhost and what it admits is every Cloudflare edge range, i.e. the whole internet on a proxied zone, so the endpoint was ALREADY behind it — no nginx template was changed, because an edge `deny` would also refuse the authorised operator. **`/healthz` published the identical `checks` dict**, so gating readiness alone would have been a mitigation defeated by a second URL — the same shape as the one-route `/docs` workaround in `integrations/routes.py` that this change's other half removes. **Deleting the detail was not an option**: during `db_down` nobody can authenticate, so `ops:manage` reaches no one and the log line is the operator's only copy; it joins the key names into a string because `logging.redact_mapping` renders a list extra as `[N items]`, which would have logged the COUNT of missing credentials and none of their names. **voice-runtime deliberately gets no gate at all** — sabotage proved that wiring one drags `httpx` and `apps.api.compliance` onto a live-call service's boot graph and fails the import-surface guard (hard rule 3), so its readiness detail goes to the log and nowhere else. **"nothing loses a capability" was verified, not trusted**: `gen:api` reads the committed `apps/web/src/lib/api/openapi.json` off disk and `check_openapi_fresh` calls `app.openapi()`, which `openapi_url=None` does not remove — both asserted, and `check_openapi_fresh` stayed green at 145 paths because the response annotation is still `dict[str, Any]`, the honest declaration for a body whose keys depend on the caller's permission. Twelve sabotages, each with its own signature; the one that mattered most was a test asserting the log line off the LogRecord rather than through `JsonFormatter`, which would have passed for a line shipping `[1 items]` — found and fixed rather than reported. |
| D-129 | **The outbound webhook URL was a tenant-controlled SSRF into our own network, and registering one was the only unaudited egress act** | New `apps/api/integrations/egress_guard.py`: `assert_public_http_url` RESOLVES the host and refuses loopback, link-local, private, multicast, reserved, unspecified and non-globally-routable answers — unwrapping IPv4-mapped, 6to4 and Teredo and refusing the NAT64 prefixes outright — plus any port but 80/443. It runs at BOTH registration (`integrations/routes.py`) and again immediately before every POST (`service.deliver`), which returns the refusal as a PERMANENT `DeliveryResult` rather than raising, so it lands on the client's own delivery screen with our own code and never climbs the retry ladder. `APP_ENV=local` relaxes loopback ONLY, port rule included; RFC 1918 and link-local stay refused on a laptop. Both create routes now write `integration_endpoint.created` in the SAME transaction as the INSERT, recording the HOST (or the spreadsheet id), never the URL. | `routes.py:280` validated `HttpUrl` and nothing more while `service.py:534` POSTs a signed body carrying a lead's NAME AND PHONE NUMBER to whatever it names, so `http://169.254.169.254/latest/meta-data/` was registrable and `GET /v1/integrations/deliveries` published the status code and error type back — an internal port scanner with a UI, and a PII egress channel into the private network. RESOLVING rather than pattern-matching is what makes `http://2130706433/`, `0177.0.0.1` and `0x7f000001` refusals with no special case anywhere: glibc parses them with the same legacy rules the connection would. The connect-time re-check is the half that matters, because the endpoint row outlives the lookup and the tenant owns that name's DNS. Measured on this interpreter, `is_global` alone is NOT the test — IPv4 multicast, IPv6 multicast and the NAT64 well-known prefix all report `is_global == True` — and CVE-2024-4032 moved those properties in 3.12.4, so every category is asserted explicitly rather than delegated. IP PINNING was considered and deliberately NOT taken; the residual window (our `getaddrinfo` to httpx's) and the trigger to close it are stated in the module. Sixteen sabotages, each with its own signature; the sharpest was dropping `follow_redirects=False` from the REQUEST, which re-sent the signed lead to 169.254.169.254 carrying the guard's approval of the first hop. |
| D-130 | **The one admin-realm route in the client path space moved into it, and two guardrail allowlists that had drifted off the route table were pinned back to it** | `PATCH /v1/agents/{agent_id}/voice` becomes `PATCH /v1/admin/tenants/{tenant_id}/agents/{agent_id}/voice` — tenant out of the body and into the path, `SetVoiceIn` down to one field, web client and screen test moved in the same change, no alias left behind — and `route_shape_test` grows `test_no_admin_realm_route_lives_outside_the_admin_path_space`. The D-22 sweep's five exemptions in `authz_audit_test` become module-level `READS_SHAPED_AS_POSTS`, each with its reason, under the staleness assertion its sibling `impersonation_reads_test` already had. `loadshed.ALWAYS_ALLOWED_PREFIXES` drops `/v1/auth`; new `tests/loadshed_exemption_test.py` asserts the census — every exempt prefix names a live route and records which of three admissible reasons (observability, operator reachability, provider callback) it is exempt for, and no publicly reachable write survives a shed unless it is a callback. BACKEND-PATTERNS §6 and SURFACES §2c updated to match. | **All three are one defect: a rule matched by STRING against a route table nothing re-checked.** The voice write was `realm="admin"` sitting under `/v1/agents` with its tenant in the body — the only such route in the app, confirmed by walking the live table rather than by eye — so it took the client rate-limit profile instead of `/v1/admin`'s, its audit row could not be read from the path, and it was the shape the next author would copy with a rationale attached. Moved with **no deprecation window**, deliberately: hard rule 8's two-step doctrine is about columns and readers that deploy apart, and an admin-realm route has exactly one reachable caller — this repo's own console, generated from this schema and shipped with it — so an alias would only keep the copyable wrong shape alive on the wrong limiter, which is the defect rather than a migration path (`POST /v1/agents/{agent_id}/publish` was moved the same way for the same reason). The D-22 sweep skipped five paths by string with nothing checking they still existed: rename one and the entry stops matching silently, and the next route to land on that path inherits an exemption written about a different endpoint — the hole its sibling file had been guarded against for a year. And `ALWAYS_ALLOWED_PREFIXES` exempted `/v1/auth` "so sign-in survives maintenance", which named a route **this API does not have** — Clerk owns sessions — while the one route the prefix did cover was `POST /v1/auth/signup`, a four-table write that let a platform in `emergency` or `maintenance` keep manufacturing tenants it had just declared it could not serve. Nothing was observably broken, because `signup.py` carries its own mode check and refuses first; that is exactly why it survived review, and why the fix ships as a census rather than as a behaviour patch. `POST /v1/invitations/accept` — the other write a caller with no organization can reach — was never exempt, which is the consistency this restores. Six sabotages, each with its own signature; the sharpest was renaming an exempted route AND updating the allowlist to its new path while leaving the old entry behind, which is the realistic drift and which only the new staleness assertion catches. |
| D-131 | **One client IP for the whole platform, a limiter identity that survives a restart, and every route under a named limit with a tenant dimension and a cost weight** | `calevate_shared/client_address.py` is now the ONE `client_ip`/`is_trusted_peer`/`TRUSTED_PROXY_CIDRS` — promoted out of `apps/voice-runtime/engine_intake.py` with `app_env` as a PARAMETER, because the import-linter contract forbids `calevate_shared` importing `apps` and hard rule 3 forbids voice-runtime importing `apps/api`; the engine receiver, the in-call tool route, `core/auth.client_request_ip` (renamed from `_request_ip`) and `tenancy/signup_routes` all call that one function. `core/middleware.py` gains `PUT` in `allow_methods` and `If-Match` in `allow_headers`. New `core/ratelimit.py` owns named `LimitProfile`s, a `*`/`**` pattern table resolved by SPECIFICITY rather than declaration order (one matcher answers for a concrete path and its route template, so the pre-routing and post-auth halves cannot disagree about which limit applies), the ONE fixed-window counter — `consume(..., fail_open=)`, with the signup quota passing `fail_open=False` instead of keeping its own copy of the INCR/EXPIRE pair — and a `blake2s` identity fingerprint. `RateLimitMiddleware` keeps only the dimensions decidable before routing and authentication; the per-TENANT dimension is charged at the first instant the tenant is VERIFIED, in `core/auth.charge_tenant_quota`, memoised on the ASGI scope. `/hooks` splits into `webhook_ingest` (keyed on the path's `webhook_id`), `webhook_payment` and `webhook_identity`; 22 cost rules weight 24 routes as `costly`/`bulk_read`/`bulk_write`; all 171 routes resolve a named profile and `tests/rate_limit_census_test.py` fails both on a route with no rule and on a rule with no route. Four suites: `cors_contract`, `client_address`, `rate_limit_census`, `rate_limit_behaviour`. | **`PUT` was missing from `allow_methods`, so five routes were unreachable from any browser** — `PUT /v1/billing/caps`, `PUT …/feature-flags/{flag}`, `PUT /v1/ops/config/{key}`, `PUT /v1/ops/secrets/{key}` (the vendor-credential install path this whole plan exists to prepare for) and `DELETE /v1/ops/config/{key}`, which sends `If-Match` — and NEITHER suite could see it, because the web tests mock `fetch` and CORS is enforced only by a browser; the guard is therefore a walk of `iter_api_routes` against `allow_methods` plus a regex over `client.ts`'s own request headers, not a list somebody typed. **`_request_ip` was one of 83 copies of `request.client.host if request.client else None`**: the audit named one, the tree holds 83, and 80 of them still stamp the PROXY's address into `audit_log.ip` where SEC-COMP §5 asks for the caller's — a mechanical migration to `client_request_ip`, left undone in this change only because it spans ~30 files being edited concurrently, and recorded in that function's docstring with its grep so it cannot be mistaken for a design. **The signup quota's `SIGNUPS_PER_IP_PER_HOUR = 30` was a cap on the PLATFORM**, since behind Cloudflare + nginx every request's socket peer is our own edge. **`hash(auth)` is salted per process (PEP 456) and prod runs two workers**, so one token held N buckets, the effective limit was N× the declared one, it moved on every restart, and the 32-bit truncation put ~77k live tokens at even odds of spending a neighbour's budget. **`requires()` calls `current_principal`/`current_admin` DIRECTLY rather than through `Depends`**, so FastAPI's per-request dependency cache does not cover it and any route carrying both `requires(...)` and `tenant_of` resolves a principal twice — measured, not theorised: without the scope memo `GET /v1/agents` costs two units of tenant budget and the effective ceiling depends on which dependencies a route happens to declare. **The signup quota's fail-closed branch was documented and untested** and now has a test that fails when the policy flips. **A degraded edge is now alerted rather than silent**: a trusted peer with no `CF-Connecting-IP` outside `local` breaks nothing visibly — every `audit_log.ip` goes NULL and every anonymous caller shares one bucket — so it fires the same `alert()` path the receiver uses for the identical condition. **Two of my own tests passed for the wrong reason and were fixed rather than reported**: the limiter suites drew bucket subjects from RFC 5737's 254-address /24 against hour-long windows, so a later run inherits an earlier run's count, and a production 60s test window can roll mid-flood and silently reset the counter — replaced by call-unique RFC 3849 IPv6 subjects, a unique trusted peer per call and an hour-long test window, verified stable over eight consecutive runs in one minute. Fourteen sabotages, each with its own signature; the one worth naming is that removing the per-tenant limit from the PRODUCTION profile fails only the census test, never the behaviour tests — which patch the profile — so configuration and mechanism are guarded by different files on purpose. |
| D-132 | **§52 made decidable for the shapes that were hiding in it: the guard grows a failure-state gate, and the nine sites it found are closed — one of them a tenant erasure offered over a read that never landed** | `tests/surfaceStatesGuard.test.ts` gains three rules on top of its original two, all off the same `X.data`-still-includes-`undefined` mechanism: `Boolean(q.data?.flag)` treated as `?? false`; a manufactured literal ANYWHERE (not only in a JSX child); and a POSITIVE truthiness test that decides whether a CONTROL is rendered, following one hop through a `const` that still carries the `undefined` and treating an early-returning `if`'s fall-through as its else. The last two are gated on "nothing in the declaring function ever reads this query's `isError`/`error`/`status`", mutation envelopes are excluded, and the fixture grows five banned and six safe shapes. `EXEMPT` is now empty. Fixed: the admin lifecycle erasure panel (skeleton + `ProblemNotice` above the `filed.data?.[0]` branch); the lead-sources agent picker (refusal in place of the `<select>`, and the save blocked); the leads table (`readOnly` and `canCall` moved onto `useWriteAccess`, `unavailable` names which read did not answer); the call-detail follow-up card (loading and failed branches beside the eligible one); the dashboard's "Spend this month" tile (its own three-state ladder); the integrations payload offer (`useWriteAccess`, plus a new `WriteAccess.unknown` so "we could not check" can be said without printing a refusal at every staff reader); and both shell counters, whose `?? 0` made a dead API look like an all-clear. `BLOCKER_COPY` keeps precedence over the server's `reason` and gains `tests/campaignBlockerCopy.test.ts`, which reads the compliance gate's rule strings and fails naming any key that no longer matches one. | **The guard's own "not checked" list was an exact map of where the defects were**, and the narrowing that produced it was correct when written and wrong now: flagging every literal fallback measured 22 hits/~20 correct, the failure-state gate got it to 12/2, and it was rejected because the slice that wrote it owned none of those screens — an EXEMPT list of ten is a treadmill. **The treadmill was a property of the backlog, not of the rule**; this slice owns the screens, so the gate goes in and the list comes out empty. Re-measured with the gate plus two narrowings §52 itself supplies — positive polarity only (`if (!q.data) return <refusal/>` is the CORRECT spelling, and four screens fail closed on `q.data !== undefined`), and a branch that withholds a CONTROL rather than a sentence — it lands at **9 hits, 9 real**. The worst was `admin/.../lifecycle`: `useTenantErasures`'s `isLoading` and `error` were read nowhere, so during the read AND after it 503s the screen stated that no erasure had been filed and offered an irreversible tenant-wide DPDP erasure that may already have been running. **On `BLOCKER_COPY` the two offered fixes are not equivalent**: inverting precedence trades a hypothetical drift for a certain regression, because `launch_blockers` writes for an operator ("Campaign is running, not draft.", and `PURCHASED_LIST_REASON` cites "policy, SEC-COMP §3" at a client) and the three DLT-entity entries exist precisely to say whose desk it is on — the table is a translation keyed by the server's identifier, and only the KEY can go stale silently. The end state is neither fix and needs no test: type `BlockerOut.rule` as a `Literal[...]` union so `openapi.json` carries the enum and `tsc` checks it, exactly as `LIST_PROVENANCE_COPY` already is — a BACKEND change, reported not made. **Two of this slice's own tests proved nothing and were fixed**: the mutation-vs-query exclusion stayed green when neutered, because the fixture's mutation branch held a `<p>` rather than a control, and the guard's whole-tree scan was quadratic per component (13s, timing out under the parallel suite and starving two unrelated files into failure) until the refusal answer was memoised per declaration. Eighteen sabotages, each with its own signature; the one worth naming is that reverting a refusal a screen still mentions elsewhere is invisible to the guard and caught only by the screen's own test — the gate is per-query, not per-branch, and that is the price of not computing branch dominance. |
| D-133 | **Twenty-one routes had no HTTP-layer test, and driving them found three route wrappers where the database was the only refusal** | Five suites — `kb_review_routes_test`, `dlt_admin_routes_test`, `agent_publishing_route_wrappers_test`, `campaign_route_wrappers_test`, `admin_read_route_wrappers_test` — drive all 21 at the wire: the realm or the under-privileged role, the happy path with the RESPONSE MODEL'S FIELDS read rather than its status code, the `audit_log` row with its redacted summary, and the cross-tenant id. Four defects fixed. `campaigns.service.add_contacts` answered 422 `campaign_not_draft` — "Contacts can only be added before a campaign is launched" — to an id no campaign of the caller's holds; it now answers 404, the shape its three sibling readers of that column already had. `POST …/dlt-templates` and `POST …/kyc` answered **500 `internal_error`** to a mistyped tenant uuid, and `POST …/numbers` answered **409 `number_taken`** to one; all three now ask `service.tenant_exists`, the predicate whose own docstring exists for this. | **The census was re-derived rather than taken from the audit, and the audit's 27 is now 21**: three of its entries had gained HTTP tests in this wave (`endpoints/sheets`, both `leads/views` mutations), and my own first pass produced three false positives because its regex could not match a path built as `f"…/{source['id']}/rotate-secret"` — a quote inside an f-string hole. A route census by grep is a census with a spelling bug in it; the corrected script is the number reported. **The three tenant-uuid defects are one shape, not three bugs**: every one of these routes names its tenant in a PATH an operator copies off a console URL, and nothing stood between that string and an INSERT carrying it as a foreign key. The `number_taken` one is the worst of the three despite being the only non-500: `provision_number` maps EVERY `IntegrityError` to that code, which is correct for the UNIQUE index it was written for, so a typo told an operator to go looking for the other account holding a number nobody holds. **Requirement "403 under impersonation" was deliberately NOT written 18 times**: `realm_boundary_test::test_no_route_declaring_a_mutating_permission_is_reachable_while_impersonating` already DRIVES every mutating-permission route under a real minted grant, so per-route copies would be a second way to answer one question — each new file names that sweep instead, so the next reader does not add them. Five sabotages, five distinct signatures (`422 != 404`; `404 != 422` plus a pre-existing suite going red; two different `ForeignKeyViolation`s; `409 number_taken != 404`), each restored under `sha256sum -c` with a green baseline either side. |
| D-134 | **The Vertex client, one availability policy, and twenty-four doc claims bound to a constant instead of to prose** | `apps/workers/extraction.py`'s `GEMINI_CHAT_URL` pointed at `generativelanguage.googleapis.com` and authenticated with `params={'key': ...}`; both are gone. `VertexGeminiExtractor` posts to `vertex_generate_url()` — an f-string that fills the host prefix AND the `locations/` segment from ONE frozen constant, `calevate_shared.engine.VERTEX_LOCATION`, beside `SARVAM_DEFAULT_LLM` where this repo keeps model identity — with an OAuth2 service-account bearer, `gemini-3.1-flash-lite` (`GEMINI_DEFAULT_LLM`, GA 7 May 2026 and Google's named replacement for the 2.5 family that retires 16 Oct 2026, BRD R-04), and a Vertex `responseSchema` built from the agent's own `ExtractionSchemaSpec` so valid JSON is a MODEL-side guarantee rather than `_first_json_object`'s fence-stripping — which stays for Sarvam, whose API publishes `json_object` but no schema. `check_model_residency`'s dated allowance for `extraction.py` is DELETED, which its own `stale_allowances()` required. **G-6 is one function**: `assist_capability()` answers with a DISCLOSED Sarvam fallback (`fallback_reason` + the sentence the response and the screen must carry) or a refusal with a remediation, on authored reasons `no_credential` / `quota_exhausted` / `provider_unavailable` / `ai_studio_key_disqualified`, in `PaymentCapability`'s shape. `run_assist()` re-runs `redact()` on its input and REFUSES text that still matches, so G-2 is structural. New `Settings`: `gcp_project_id` (plain config, GCP's own 6-30 pattern) and `gcp_service_account_json` (secret-shaped, so `platform_secrets` seals it) — and NO region field, which `check_model_residency` check 3 enforces by name. `GEMINI_EXTRACTION_DEFAULT is False` is minted as the greppable form of G-7 and now binds eleven prose sites across CLAUDE.md, AGENTS, README, TRD (×3), BRD R-04, BUILD-LOG, OPERATIONS, settings.py and the evidence set through `check_docs_drift` §5. | **The branch that had to go was not the URL, it was the selector.** `get_extractor()` returned a Gemini client whenever a Gemini key was configured and a Sarvam key was not — and its caller is `workers/pipeline.py`, which hands over the RAW transcript (`turn.text`, one line after `redacted.text`, deliberately, because a CRM callback-number field needs the digits). So ONE absent environment variable sent raw caller PII to a second processor, which is G-2 inverted by configuration rather than by code. Fixing the host and leaving that ladder would have shipped a correctly-regioned endpoint receiving data it may never see. Gemini is now unreachable from that function at all; a Google-only deployment extracts with `OfflineExtractor`, which is the intended direction and is a state no environment is in (`SARVAM_API_KEY` has been a `/healthz/ready` requirement since before D-127). **`google-auth` was resolved, its lockfile diff read (hard rule 9), and declined** — `google-auth 2.56.3` pulls `cryptography` (already here) plus `pyasn1-modules`/`pyasn1`, two pure-Python packages with no install hooks, so the supply-chain diff was honest and not the objection. Three other things were: this repo ALREADY implements the RFC 7523 JWT-bearer flow in `workers/google_sheets.py` and a library doing the same job is the second implementation, not the first; `google-auth`'s refresh is SYNCHRONOUS and its shipped transports are `requests`/`urllib3`, neither of which is in this tree and either of which would put a blocking call in an arq event loop; and `pyjwt[crypto]` is already a declared dependency. So the flow was EXTRACTED to `apps/workers/google_oauth.py` and `google_sheets.py` migrated onto it in the same change — one implementation, keyed now on `(client_email, scope)` because one account serving two scopes was the one behaviour the move had to ADD (a token minted for `.../auth/spreadsheets` handed to Vertex is a 403 that names neither the scope nor the cache). `apps/workers` also gained the `pyjwt[crypto]` declaration it had been importing on `apps/api`'s: two lines in the lockfile, zero new packages. ⚠ **Evidence standing on the model: REPORTED, NOT READ.** `docs.cloud.google.com` is refused by this environment's egress proxy, so `gemini-3.1-flash-lite`'s GA date, its price and its retirement lineage rest on independent search summaries agreeing, and **that `asia-south1` SERVES it is NOT confirmed** — an OPERATIONS §2 gate on the day the GCP project exists, not an engineering unknown, because the failure is a 404 from a host that unambiguously serves our project and `assist_capability()` answers `provider_unavailable` with a disclosed Sarvam fallback meanwhile. **Not done, with the reason**: `llm_tok_in` — a `usage_events` CHECK-constraint value with no writer anywhere — is still there. Removing it means rewriting `ck_usage_events_unit_type_enum`, which migration `e1a7c93d5b02` (D-127 G-3's dashboard-AI unit types) is rewriting in this same wave and whose comment already names Part 18 as its owner; a second migration on the same constraint would collide in the alembic DAG and in the constraint body. Hard rule 8's step one (stop writing it) is trivially satisfied — nothing ever did — and step two belongs to the next migration that touches that constraint. |
| D-135 | **The adversarial pass: RLS is the whole of tenant isolation and every route proved it, but three limits the platform declares were not limits — a bucket the caller could rename, a body cap one header walked past, and a blocking socket read on the event loop that any stranger could aim** | `core/context.bearer_token` becomes the ONE reading of `Authorization` — leaf module, beside the header names, for the reason stated there — and `RateLimitMiddleware._caller` keys on it instead of on `fingerprint(<raw header>)`. `BodyLimitMiddleware` now COUNTS the streamed body as well as reading `Content-Length`, refusing mid-request by handing the app `http.disconnect` and dropping anything it sends afterwards, so the 413 is the caller's whole response. `verify_token`'s JWKS lookup moves to `asyncio.to_thread` and `PyJWKClient` gets `timeout=JWKS_FETCH_TIMEOUT_S` (5s) instead of PyJWT's 30. `tests/adversarial_pass_test.py` pins all three plus the IDOR sweep: 34 `{id}` routes in the client path space, driven with a NEIGHBOUR's real row ids and a valid session, required to answer 404 `not_found` and never 403, 422 or 2xx. | **What held is the headline and it was verified by taking it away**: with `ALTER ROLE calevate_app BYPASSRLS` the same sweep leaks 29 routes at once — reads another tenant's leads, calls, transcripts and campaigns, and DELETEs their lead source, their webhook endpoint and their members — so tenant isolation is the database's and nothing else's, which is the property that survives a refactor and the answer PART 6 asked for empirically. Also held under attack, with reproductions: `alg:none`, RS256→HS256 with the public key as the HMAC secret, an expired RS256 token and a wrong-realm token are all one indistinguishable 401; admin `fva[1]==-1` and an absent `fva` are 403s with different codes; CSV formula injection is disarmed in every cell including the phone column; deeply nested JSON is a 400 on the API path as well as on voice-runtime; the signup quota keys on the verified Clerk id and the resolved address, so header rotation buys nothing; no `text()` interpolation anywhere reaches user input; and email header injection is refused twice over (`EmailStr` at the boundary, `EmailMessage` raising on a linefeed). **The three that were real share one shape** — a control that names a number the caller can change. The limiter's bucket was a hash of the raw header, so `Bearer x`, `bearer x` and `Bearer x ` were three budgets and padding made it unbounded: one authenticated client could spend the whole `bulk_read` allowance as many times as it liked, which is the profile whose comment says it exists because "this is the shape data exfiltration takes". The body cap read `Content-Length`, and `Transfer-Encoding: chunked` declares none — the argument that nginx backstops it was checked and is false, `client_max_body_size` on the api vhost is **25m**, twelve times the app's 2 MiB. The JWKS fetch is `urllib.request.urlopen` (READ AT SOURCE, PyJWT 2.13.0) called straight from an `async def`, and `get_signing_key` refetches whenever the `kid` is unknown while never memoising the failure — so varying one field of an unsigned JWT buys one blocking fetch per request, and `/healthz*` has no limit profile at all because a probe must answer during an incident. Measured: 1s of JWKS latency was 1s in which the process served nobody. **The fix nearly broke hard rule 3 and the guardrail caught it**: `bearer_token` was written in `core/auth`, which `core/middleware` would then have imported — and `core/middleware` is on voice-runtime's pinned import surface while `apps.api.compliance`, which `core/auth` pulls, is FORBIDDEN on it. It lives in `core/context` for that reason, which is also where it belongs. **Handed back rather than fixed, both documented as deliberate**: `kb/service.preview` and `agents/prompts.list_prompt_versions` answer 200 `[]` for an id this tenant cannot see, where `kb.service.approve_source`'s own docstring says the honest answer is 404 — no data leaks (RLS empties both), but a client cannot tell "this source has no chunks" from "that is not your source", and `tests/kb_isolation_test.py:122` pins the weaker contract on purpose. |
| D-136 | **Six half-wired compliance routes get the affordances they were designed for, and the eval harness gets a provider dimension that refuses rather than scores zero** | **PART 9.** New ops screen `/admin/ops/dnc` (nav entry, a11y-swept) drives all three global-DNC routes: paste-and-suppress with the `suppress_number_platform_wide` header, the masked list, and a PER-ROW release confirmation that names the number and takes RELEASE typed against that row. `MAX_NUMBERS_PER_ADD`/`DNC_LIST_LIMIT`/`parsePastedNumbers` are IMPORTED from the client-realm `dnc.ts` rather than restated, and `WriteFailure` (the step-up-skew panel) moved out of `ops/page.tsx` into `admin/ops/writeFailure.tsx` with its old caller migrated in the same change. New client screen `/c/[slug]/settings/alerts` and a new operator panel on `/admin/tenants/[tenantId]` drive the three WhatsApp alert routes; the panel needed a read that did not exist, so `whatsapp_optin_routes` gains `GET /v1/admin/tenants/{tenant_id}/whatsapp-alerts` (`admin:tenants`, the owner's state, no number). `runbooks/dnc-complaint.md` §6 now names the screen and keeps the curl as the console-down fallback, and gains §8 — the full NCPR scrub procedure, which is the ONE route of the six that stays curl-only. **PART 16.** `scripts/eval.py` grows `--provider` (repeatable) over a `PROVIDERS` registry built on `vertex_extractor()`/`SarvamExtractor`/`OfflineExtractor`, a per-FIELD scorecard (`right`/`missed`/`wrong`/`invented`/`withheld`, never one aggregate), `--evidence` writing `docs/evidence/extraction-provider-scorecard.md` through the pilot's own redaction sweep, and exit code 2 when a named provider has no credential. Default runs are byte-for-byte unchanged, so `make eval-ci` still measures what it measured. | **The routes were not undocumented, they were unreachable, and the two halves needed opposite answers.** The global-DNC surface is an act we can perform TODAY and `dnc-complaint.md` §6 had an operator hand-assembling a step-up header against production while answering a regulator — the exact defect the ops console already closed for the load-shed mode and the dead-letter replay, so it is closed the same way. The WhatsApp routes are stronger still: the API returns `current_notice_text` so a SCREEN can render the exact wording a client is agreeing to, and refuses a stale `notice_version` — a route designed around a console that did not exist. The preference scrub is the honest (b): it records a run performed on an access provider's DLT platform, which needs the Registered Telemarketer relationship R-01 does not yet have, and the console has no campaign identity to hang it on (`/admin` serves no campaign list) — a uuid typed into a form is the curl's failure mode in a nicer font. **Two defects found by building the affordances.** `record_for_client` was WRITE-ONLY: no admin-realm read existed (the client GET deliberately answers no subject state to an impersonated session), so an operator could record a month-old onboarding form over last week's withdrawal, on an append-only ledger where the correction is another row and the alerts went out in between. And `GlobalEntryOut.removable` is always `false` — it is `is_removable()`'s answer about CLIENTS — so gating the ops Release control on it, the obvious reading, would have left the only surface that can lift a global suppression unable to. **On the eval side the trap was the opposite of a bug**: `get_extractor()` falls back to the offline heuristic by design, because a post-call pipeline that failed for want of a key would lose a lead — inheriting that in a comparison would print offline output under a `sarvam` heading, in a scorecard feeding a residency decision. Named providers therefore resolve to nothing rather than to something else, and the AI Studio `GEMINI_API_KEY` cannot produce a gemini column at all (D-127 G-1). The committed artefact says in its own body that it cannot move the first post-call extraction to Gemini whatever the numbers say. **Still blocked outside this repo, named**: a Sarvam API key and egress for task #87's credentialed run; R-01 for anything to record in §8; a Meta WABA (D-91) before an alert can be delivered at all, which is why the client screen withholds the grant while `delivery_available` is false and never withholds the withdrawal. |
| D-137 | **A dashboard assist was unmeterable, unbillable and unbounded at once: the metering key excluded it by predicate, the only ledger row it could have written meant something else, and one Calevate-owned key had no ceiling in either direction** | New `usage_events.ref` + unit types `ai_assist_ktok_in`/`_out` + a SECOND partial unique index `ux_usage_events_tenant_unit_ref` on `(tenant_id, unit_type, ref)` where `ref IS NOT NULL AND call_id IS NULL` — disjoint from `ux_usage_events_tenant_call_unit`'s `call_id IS NOT NULL`, so neither shadows the other (migration `e1a7c93d5b02`, whose downgrade REFUSES rather than dropping idempotency keys off an append-only ledger, and which was run down and back up before it shipped). `billing/ai_quota.py` is the whole policy: a rupee ceiling per tenant per IST month by plan tier, displayed as BOTH rupees and an "about N assists" estimate the server derives; `require_ai_assist` is the ONE gate; `record_ai_assist_usage` is the ONE writer, idempotent on `ref` via `ON CONFLICT … DO NOTHING` with the index predicate repeated verbatim; overage is ONE `credit_ledger` row, reason `usage`, ref `ai_assist:<YYYY-MM>`, bought only after a person accepts an echoed amount on `POST /v1/billing/ai-quota/extra` (`org:manage`, audited in the same transaction); and `platform_ai_spend` is the brake on our own key that no per-tenant ceiling can see. `/c/<slug>/ai-assist` is the screen and the G-5 dialog. `_tier_totals` now excludes the AI units so our absorbed cost cannot reach a client's spend, their margin panel or a closed month's statement. | **All three exclusions in the old key were real and each alone was fatal, verified against `pg_indexes` and not against the migration that claims them**: `call_id IS NOT NULL` is a PREDICATE, so an assist row is not in that index at all — not merely unprotected by NULL semantics, which matters because the remedies differ (a NULL inside an index is reachable with `NULLS NOT DISTINCT`; a row excluded by predicate can only ever be covered by a different index); the unit list omits `llm_tok_in`, which is anyway a column with no writer and therefore a finding a new writer would erase rather than fix; and `llm_tok_out` already means "one call's LLM leg" at `qty = 1` (`pipeline.py:1201`) because the engine bills legs with no token count. **The unit is `ktok` and that is a money decision, not a naming one** — `unit_cost_paid` is NUMERIC(12,4), so the smallest expressible price is ₹0.0001 per unit of `qty`, and a Gemini-class input token at ~₹0.000008 would have stored as 0.0000: per-token metering would have counted every assist as free and the ceiling would never have moved. **Two things only the build found.** (1) `usage_events` is shared with the call meter, and `_tier_totals` sums EVERY row for the month grouped by `meta.tts_tier` — so an assist landed in the unattributed bucket and reached three client-facing figures including a closed month's `spend_used_inr`, which is Calevate's absorbed cost printed on a client's own spend; the exclusion is spelled negatively, from the one constant, because a positive list of call units would silently drop `number_rental` out of a client's cost. (2) A platform-wide total is UNASKABLE in app code — `usage_events` is FORCE-RLS'd, an untenanted session sees zero rows, and hard rule 1 forbids the admin DB role — so the brake had to be its own untenanted counter, keyed by month rather than a column on `platform_state`, whose single row every dispatch tick reads. **The overage is a fixed BLOCK, once per tenant-month, and the shape is forced by hard rule 4**: a row that grew with usage would be an UPDATE the `calevate_forbid_mutation` trigger refuses, and per-request billing is what G-4 rules out in words — a block is also the only shape whose modal can state a TRUE figure before the click. Its cost is stated on the screen rather than hidden: the unused part is not refunded and does not carry over. **Sabotage, ten breaks, ten distinct signatures**; the one worth recording is that moving `lock_tenant_credits` to AFTER the read it protects still produced ONE ledger row — `find_entry_by_ref` takes the lock itself — so an outcome-only race test was green and the ordering assertion is what caught it. **Not done, and named**: the metering writer and the gate have no production caller until a user-triggered dashboard-AI ENDPOINT exists (PLAN Part 15's surface; D-134 landed the Vertex client and the extractor, not a console assist route), and a MANAGED tenant at the ceiling is refused with their account manager named rather than offered a wallet debit, because a retainer client's invoice is derived and there is no priced AI-overage line to derive — closed by a founder pricing one, not by code. |
| D-138 | **The last outbound path fetching a third party's address with no guard, found by the pass that D-129 made possible** | `copy_recording` vets `source_url` through `egress_guard.assert_public_http_url` and follows redirects BY HAND — `follow_redirects=False`, bounded by `RECORDING_REDIRECT_LIMIT`, re-vetting every hop. `EgressRefusedError` becomes `StorageUnavailableError` so the pipeline records it against the call. | **The URL is the VENDOR's**, off an engine payload or a poller snapshot — not ours, not a tenant's, and the category this repo trusts least everywhere else (hard rule 2 keeps vendor shapes out of typed columns for exactly this reason). It was the ONE remaining path fetching an externally supplied address with no check and `follow_redirects=True`, so a vendor response naming `169.254.169.254` — or merely redirecting to it — would have been fetched by a worker inside our network and written to our bucket. **Vetting only the first URL is not a guard**: the redirect is the whole bypass, which is why `integrations.service.deliver` already set `follow_redirects=False` and why the hop loop re-runs the check rather than trusting httpx to chase a `Location` nobody judged. The bound exists because an unbounded chain is a worker sitting in a redirect loop until its 60s timeout — a job slot held hostage by a third party. Three sabotages, three distinct signatures: vet-only-the-first-hop reddens the redirect test alone (`the redirect target was fetched`); removing the guard reddens two; unbounding the loop reddens the chain test. The assertion that carries the class is `asked == []` — a refusal AFTER the request is not a refusal, because the response body is the thing an attacker wanted. Found by D-135's adversarial pass, which could only look for this because D-129 had established what the doctrine was. |
| D-139 | **`audit_log.ip` recorded our own nginx on every audited route: the fourth field of SEC-COMP §5, honest-looking and wrong 80 times** | All 80 handlers writing `ip=request.client.host if request.client else None` inline now call `core.auth.client_request_ip(request)`, which asks whether the peer is a trusted proxy before believing a header. New `scripts/check_audit_ip.py` in `make guardrails` AND in CI fails on any `request.client` read under `apps/api` outside that one function, with 7 negative controls in `tests/audit_ip_guard_test.py`. | **D-131 fixed the definition and left the callers, and said so** — its docstring carried the grep as a worklist and the reason it must not read as a design, because a 30-file read-modify-write against five concurrent agents is how a previous session lost every tracked edit. This is that worklist closed. **The count was the finding**: the audit that produced the plan named ONE call site (the impersonation dependency); the real number was 80 across 26 files, so the compliance record was defective platform-wide rather than at one dependency. **The guard is the deliverable, not the sweep.** A worklist in a docstring is a promise kept by whoever reads it; a check is a guarantee, and the next author reaching for `request.client` is now stopped by the build rather than by whether a reviewer remembered a paragraph. It bans the PEER READ rather than the `ip=` spelling, because the next author will spell it differently and what is actually wrong is consulting the peer instead of the predicate. **The allowance is bound to the FUNCTION, not the file** — a control proves that moving the read one function down in `core/auth.py` is still refused, which would otherwise have made that file a hole — and it **fails if `client_request_ip` stops reading the peer at all**, because a green check over an absent guarantee is worse than a red one. AST rather than grep, so the prose in `core/auth.py` explaining this very rule is not a finding (the lesson `check_model_residency` learned about its own docstring). `apps/voice-runtime` is out of scope deliberately: it reads the peer to decide whether the peer is trusted, which is the same argument-not-answer distinction the allowance rests on. |
| D-141 | **The missing-existence-check class was not exhausted: two more 500s, a refusal about the wrong thing, three cheerful 200s, and the one destructive step-up in the repo that named no subject** | `tests/absent_tenant_answer_test.py` is the CENSUS rather than a fifth one-off: every route the app serves under `/v1/admin/tenants/{tenant_id}` and `/v1/ops/tenants/{tenant_id}` is driven with an id no organization holds and must answer 404 `not_found`, with a completeness assertion that fails on a route absent from its own table and a staleness assertion on the one exemption. Seven fixed. `POST …/dlt-registration` and `POST …/first-campaign-review` answered **500 `internal_error`** off their tenant FKs; `GET`/`POST …/whatsapp-alerts` answered **422 `alert_optin_no_owner_with_a_number`** ("add a mobile number to the owner's profile"); `GET …/margin`, `GET …/commercial-terms` and `GET …/invitations` answered **200** with a ₹0 margin card, `state: none` and `[]`; `GET …/agents/{agent_id}/prompt` answered **200 `[]`**, closed in `prompts.list_prompt_versions` through the same `_agent_state` the two writes beside it use. `DELETE /v1/ops/dnc/global/{entry_id}` gains `release_globally_confirmation(entry_id)` — runbook §6, `opsDnc.ts` and both suites moved in the same change. `route_shape_test` grows the CONVERSE of D-130's rule (nothing in the admin path space may admit a client principal), and `agent_voice_test`'s `in (401, 403)` becomes an exact answer with a positive control. | **D-133 fixed four instances of a class and the class kept producing**, because the fix was per route: `record_dlt_registration`'s own docstring says it is "the same family, same permission and same shape" as the two that got the guard, and it was missed for a reason worth naming — it is an `ON CONFLICT` upsert, which reads like a write that cannot fail on a key. The whatsapp pair is D-133's `number_taken` defect in another module: a real refusal, correct for the state it was written for, sending an operator to edit a profile in an account that is not there. **The three 200s are the worst of the seven and the reason the census had to be a census**: none of them is an empty payload anyone would notice — a ₹0 margin, "no terms have ever been set" and "no invitation is outstanding" are the payloads a REAL new client produces, and each sat beside a POST on the same screen that already answered 404 for the same id. **On the step-up**: `core/stepup.py` says the header exists so "the confirmation captured for one action cannot be replayed against another", and every subject-bearing string in the repo binds its subject — except this one, whose recorded reason (hard rule 6 forbids a phone number in a header) is sound for the sibling POST, whose subject is a list of numbers, and does not apply to a DELETE whose subject is a uuid already in the path. It is the act that re-permits dialling somebody who asked not to be dialled, and the runbook printed a curl an operator could re-run against a different id. **On D-130's weakened voice test, measured rather than argued**: downgrading `VoiceSetter` to `realm="client"` leaves the behavioural test GREEN, because the handler also takes `AdminSession` and `core.deps.admin_db` depends on `current_admin` — a real second lock, but one that means no request can distinguish the declaration from the session dependency, so the declaration is now asserted off the route table (71 routes, all clean today). **On D-132's branch dominance, re-measured**: removing the per-query gate gives **43 hits**, several of which are the LADDER ITSELF (`campaign-review:152`, `prompt:939`, `[tenantId]:554`) — an ungated rule would flag its own remedy, and the correct spelling here renders the refusal as a SIBLING of the data branch, so there is no dominance to compute. Left open, with the number written into the guard. Ten sabotages, ten distinct signatures. |
| D-142 | **The one vendor fact D-127 rests on got re-searched and the answer moved the wrong way: `asia-south1` is very likely NOT serving the model we ship, and four things that were true-ish became true** | **The gate first.** `calevate_shared.engine.GEMINI_MODEL_CONFIRMED_IN_REGION: Final = False` is minted beside `GEMINI_DEFAULT_LLM` — a ninth capability constant, so the eight prose sites claiming Gemini 3.x runs in Mumbai now bind something `check_docs_drift` §5 can fail on. It is promoted from a footnote in OPERATIONS §2's D-31 blockquote to **numbered hard gate 14** with its own pass/fail and its own wrong-fix warning, restated in DEV-SETUP §1 where a person reads BEFORE they spend an hour in the GCP console, and re-scoped in BRD R-04. `VertexGeminiExtractor._log_refusal` makes the 404 answer itself: `vertex_model_not_served_in_region` carries region, model, project, the flag READ (not quoted) and the remedy, and every other non-2xx logs `vertex_request_refused` with the STATUS — the whole diagnosis used to reach the log as the word `HTTPStatusError`, with 401/403/404/429/503 indistinguishable. The body is still never logged (Google's error bodies quote the request, and the request is a transcript). **Three more seams closed in the same pass.** (1) `google_oauth`'s bearer cache is keyed `(client_email, private_key_id, scope)` and its deadline moved to `time.monotonic()`; the superseded slot is dropped on the replacement's first successful mint, so a rotation cannot grow the dict. `platform_config`'s `AppliesRule(LIVE)` on `gcp_service_account_json` loses its caveat paragraph because the caveat is gone. (2) `build_vertex_response_schema` de-duplicates `propertyOrdering`. (3) `run_assist` takes `quota_exhausted` and returns `AssistResult.usage` (`TokenUsage`, thinking tokens folded into output), which are D-137's two missing wire-ends. `check_model_residency`'s self-exemption shrinks from the whole file to `_host_definition` — a template that IS one of the two banned host strings. New `tests/google_oauth_test.py` (11), plus 11 in `vertex_extraction_test` and 3 in `model_residency_guard_test`. | **The re-search did not merely fail to confirm; it pointed the other way, and that is a different fact.** D-134 recorded `asia-south1` serving `gemini-3.1-flash-lite` as unverified because the docs are egress-blocked. Searching again on 16 Aug 2026 (`docs.cloud.google.com`, `firebase.google.com`, `discuss.google.dev`, promptfoo, litellm and modelavailability.com are ALL still refused, so this is REPORTED, NOT READ) produced a consistent picture that nobody had assembled: independent summaries place the 3.x family on the GLOBAL endpoint and the `us`/`eu` multi-region REP endpoints, one reports it absent even from `us-central1`, Google documents that "some models are only available in the global region, especially on initial release" — and what search DOES place in Mumbai is the 2.5 Flash / 2.5 Flash-Lite class, *the family BRD R-04 retires on 16 Oct 2026*. So the gate has a calendar on it and R-04 is no longer neutralised for the dashboard leg. **What survived, and it is the leg that matters**: `asia-south1` IS in Vertex's own ML-processing-location table with Tokyo, Singapore, Sydney and Seoul, so the REGION's residency property stands and only the MODEL is in doubt — which is why the wrong fix (`locations/global`, on which Google says the caller cannot control or know where processing happens) is named in the gate, in the 404's log line and in SEC-COMP §4 rather than left for somebody to discover at 3am. **Two exemptions were checked rather than assumed, and they answered differently.** The residency guard's docstring filter was DEAD when Part 12 shipped — its author sabotaged it and the CLI stayed green — and D-134's own Vertex client made it live: `extraction.py:292` explains the AI Studio ban by naming the host, so disabling the filter now reports that explanation as the offence, and a test pins exactly that. The self-skip was a REAL hole and is now two literals: the guard was the only module in `apps/`, `packages/` and `scripts/` where a `us-central1` URL passed, which is a poor place for the single hole because a guardrail is edited by whoever is relaxing it. **`gemini_api_key` is not mid-deprecation and never was**, which its own comment said twice in contradictory directions ("stop reading it (done — nothing in the tree does)" three lines above "it is read in exactly one place"). A field that is READ has not had hard rule 8's step one taken, so there is no step two to schedule and the sentence promising a later release was a schedule wearing a rule's clothes. It is kept permanently: it is the only input separating "no AI credential" from "the WRONG KIND of AI credential", and deleting it would send an operator who did install a Gemini key to go and check their typing. **Two tests were caught passing for the wrong reason before they shipped.** `httpx.MockTransport` with a sync handler never suspends, so four `gather`ed token requests ran strictly in sequence and the race test observed one exchange — it would have observed one against a cache with no concurrency safety at all; one `await asyncio.sleep(0)` in the fake is the difference. And a "no vendor body in the log" assertion greping for the words `invalid`/`message` matched `LogRecord`'s own machinery, so the fake now returns a distinctive marker. The race itself is recorded rather than fixed: N cold callers mint N valid tokens, the cache converges, and a per-key `asyncio.Lock` in a module-level dict costs a lock created on one event loop and awaited on another. A 401 mid-flight deliberately does NOT evict — the margin means a bearer is never handed over older than 55 minutes, so a 401 is a statement about the KEY, and with the key id in the cache key installing a new one now self-heals on the next call. **Not done, and it is one line in another agent's file.** `llm_tok_in` — a `usage_events` CHECK value with no writer, ever — is still there. D-134 deferred it on a migration collision that is now OVER (`e1a7c93d5b02` has landed, so a new revision is a child and not a conflict). The removal is exactly: delete the string from `CLIENT_BILLED_UNIT_TYPES` in `apps/api/billing/models.py` and rewrite the paragraph above it, plus one child migration that rebuilds `ck_usage_events_unit_type_enum` without it, freezing its unit list as literals at that revision the way `b8d3f47c2a19` and `e1a7c93d5b02` both do. `apps/api/billing/models.py` belongs to another agent in this wave, and the constant and the constraint cannot move separately — `tests/ai_quota_test.py` holds them equal — so it is reported here rather than half-applied. |
| D-143 | **The guard was vetting a name the transport was never going to use — two IDNA standards read one URL as two hosts, with no race to win — and three more things a third party was choosing for us** | `egress_guard.assert_public_http_url` now ends by asking the TRANSPORT where it would go: `httpx.URL`'s parse of the same string must name the same host bytes and the same effective port, or the destination is refused as `webhook_url_ambiguous` (published in WEBHOOKS.md §1.6). A URL httpx will not parse at all is refused by the same clause, which also stops `httpx.InvalidURL` — NOT an `httpx.HTTPError`, so no caller catches it — escaping as an unhandled exception. `UnicodeError` joins the fail-closed resolver clause. `VettedDestination` gains `url`, and `deliver` and `copy_recording` request THAT rather than their own argument, so what was judged and what is sent cannot drift. `copy_recording` streams with `MAX_RECORDING_BYTES` (= `CALL_CAP_MAX_S` x 32 kB/s, enforced on the running total, with a `Content-Length` pre-flight) inside `RECORDING_FETCH_DEADLINE_S = 120s` over the WHOLE fetch. `bootstrap._ops_detail_gate` short-circuits on a missing credential and admits at most `_DETAIL_GATE_CONCURRENCY = 2` authorisations at once, logging `health_detail_gate_saturated`. `integration_endpoint.created`'s `events` is joined, not a list. 22 new tests across the three suites. | **The parser differential is not the TOCTOU window D-129 weighed pinning against — it is worse, because it needs no race.** `socket.getaddrinfo` IDNA-encodes a `str` host with the STDLIB codec (`Modules/socketmodule.c::idna_converter`, IDNA 2003); httpx uses the `idna` package (IDNA 2008 / UTS-46). Measured on this interpreter: `fass.example.com` is what our lookup vets, `xn--fa-hia.example.com` is what httpx connects to. Owning one name and pointing the other at `127.0.0.1` is a deterministic SSRF, and re-judging pinning is what surfaced it. Pinning STAYS rejected — the residual is now a test (`the caller connects by name and never by `VettedDestination.addresses``) rather than only a paragraph. **The full outbound enumeration D-135 asked for, done**: 18 egress sites in `apps/`+`packages/`. Exactly TWO fetch an externally supplied address — the tenant webhook (D-129) and the vendor recording (D-138) — and its claim that `copy_recording` was the last unguarded one is CONFIRMED. Eleven are hardcoded vendor constants (`api.bolna.ai`, `api.cartesia.ai`, `api.razorpay.com`, `api.clerk.com`, `graph.facebook.com` x2, `sheets.googleapis.com`, `oauth2.googleapis.com`, `api.sarvam.ai`, the three `secret_probes` URLs); Bolna's pagination `next` link is already pinned to its own base host; the Sheets path interpolates a tenant id that `parse_spreadsheet_ref` re-validates on READ as well as on write; `apps/voice-runtime` makes NO outbound HTTP call at all. The remaining five (S3 endpoint, SMTP, OTLP, Redis, JWKS host) are operator configuration behind `platform:config`/`platform:secrets`, a different trust class, and are named rather than guarded. **D-128 put an authentication attempt on the two routes carrying `PROFILES["exempt"]`**, and D-135's own `_signing_key_for` docstring names the consequence — an unknown `kid` forces an un-memoised JWKS refetch, "and on `/healthz*` there is not even a rate-limit profile in front of it". That fix moved the fetch onto a `min(32, cpu+4)` thread pool shared with the whole process; moving a hazard to a scarcer resource is not bounding it, and the MOUNTING was the half left open. **Re-judged rather than inherited**: the `[phone]` masking of a Drive id stands (roughly one registration in a million, and `object_id` plus `outbound_webhooks.url` hold it in full), but `events` was collapsing to `"[1 items]"` on 100% of rows — the same trap `core.health` had already dodged in the same wave, so the audit line for "who pointed our leads outward, and where" recorded the COUNT of what started flowing. **Checked and found solid**: CI does run MinIO and `exit 1`s if it is unhealthy, so the 3 skips are local-only; all 174 routes but 10 declare a permission and every `/v1/ops/*` is `ops:manage`/`platform:*`; the 15s delivery worst case sits under a 300s `job_timeout` with no lease or tick assuming otherwise; the conftest resolver double is unreachable from `apps/` (now a test). **Two of my own tests were caught passing for the wrong reason before they shipped**: a 64-char-label test that never reached `getaddrinfo` because the conftest double answers `.example` without it, and a byte-cap test that went GREEN with the cap deleted because the `Content-Length` clause refused first — the body is undeclared now, and the two messages are distinct. Twelve sabotages, twelve distinct signatures. |
| D-140 | **The dashboard-AI money path was four unfalsifiable constants, a metering key a browser could have chosen, two clocks disagreeing about which month a rupee belongs to, and a brake that only announced itself by refusing somebody** | The prices are now ONE sourced table (`ASSIST_LIST_PRICE_INR_PER_KTOK` — `gemini-3.1-flash-lite` at $0.25/$1.50 per 1M, ₹95.66/USD) and `AI_ASSIST_NOMINAL_INR` is DERIVED from it times a stated reference assist and margin; it evaluates to the ₹0.50 that was typed, so the value is unchanged and now checkable, and `tests/ai_quota_test.py` holds all four product terms inside bands that fail on a misplaced decimal point. `record_ai_assist_usage` accepts only `new_assist_ref()`'s `assist:<uuid7>` and logs a replay at WARNING instead of returning silently. The platform counter is bumped for the month the ROW landed in, read back from the insert's `RETURNING`, never the API process's clock. `purchase_ai_overage` refuses inside `LAST_SALEABLE_MINUTES` of the IST month (`ai_extra_month_ending`, new `plans.ist_month_end`, which `month_pricing_instant` now shares) and its three hand-written refusals collapse into the ONE `EXTRA_REFUSAL` table that `extra_unavailable` and `require_ai_assist` also read. `record_ai_assist_usage` announces 80% and 100% of the brake through `alert()` on the bump that CROSSES the line. `UNIT_TYPES` is now derived from `CLIENT_BILLED_UNIT_TYPES` + `PLATFORM_ABSORBED_UNIT_TYPES`. `FieldScore.record`'s bare `getattr` is documented as load-bearing and pinned. | **The metering key was an off switch pointed at ourselves.** `ON CONFLICT ... DO NOTHING` means a `ref` seen before meters nothing, moves no quota and moves no platform counter — and `billing/models.py` invited a browser to supply it ("the caller mints one `ref` per request and re-sends it"), so one constant string would have bought unlimited assists on Calevate's credential, past a ceiling that could no longer move, invisible to the brake. Double-click protection deduped AFTER the provider was paid, which hides the spend rather than saving it. **Two clocks, and the halves never reconcile**: `occurred_at` is Postgres's `now()` and the counter was keyed by the API process's `datetime.now(UTC)`, so ordinary NTP skew at an IST boundary guards a month that spent nothing while the month that spent it goes unguarded. **The 23:59 sale is two bugs in one**: a block that expires in seconds, and a roll between `read_ai_quota` and `record_entry` that debits ₹500 under a key next month's read does not look for — the client is down the money, holds no allowance, and is offered the block again; an hour-wide refusal makes the interval unreachable where a re-read would only have detected it. **The order the docstring claimed was already wrong**: `extra_unavailable` put `not_prepaid` ahead of `already_purchased`, so a tenant moved to `managed` after buying was told their plan cannot buy a thing they had already bought. **The brake's own release note was true and insufficient** — a `transient` ProblemError really is a 503 and really does fire `alert()` (verified in `core/errors.install_error_handlers`), but only when a client is refused, and nothing anywhere read `platform_ai_spend`, so "how close are we" needed psql. **The order-of-magnitude check found the numbers sound and the prose stale**: the ktok argument was computed at ~₹0.000008 a token when the real figure is ₹0.0000239, and the claim that a per-token price stores as zero is true of input and NOT of output, which rounds to ₹0.0001 — not zero, and still 30% of the price discarded, which is the case a reader would have assumed was fine. **Fourteen sabotages, fourteen distinct signatures, and one of them exposed a test of mine that passed for the wrong reason**: `record()`'s guard is `getattr`, not `slots=True`, so the `slots` I had added earned nothing and its rationale was false — reverted, and the test now fails on the `getattr(self, verdict, 0)` edit that would actually break it. **NOT CLOSED, and this is the one that matters**: the metering writer and the gate still have no production caller. An assist endpoint cannot meter honestly because `Extractor.run()` returns the parsed JSON and DISCARDS `usageMetadata`/`usage`, so no token count exists anywhere outside `apps/workers/extraction.py`; writing an estimate into a column named `ai_assist_ktok_*` would be worse than no caller. The join is one field on `AssistResult` away and it belongs to whoever owns that file. |
| D-144 | **A principal resolved twice wrote the D-22 ledger twice, a stranger could opt out of the per-caller limit with one header, and the suite's own limiter buckets were shared with every other run on the box** | `core/auth` memoises the resolved principal on the ASGI scope, keyed by REALM, so `requires()` — which cannot go through `Depends`, because the permission is a closure argument — reuses the first answer instead of re-resolving; `charge_tenant_quota` keeps its own narrower memo and the docstring now says which invariant each one owns. `RateLimitMiddleware._caller` splits into `_address_subject` and `_subjects`: a token still gets its own bucket, but MINTING one costs a unit of the address bucket the same request would have used with no `Authorization` header, reported through a new `Decision.first_in_window`. `install_middleware` refuses a wildcard CORS origin at boot and moves `CorrelationIdMiddleware` OUTERMOST. `check_audit_ip` gains the ASGI spellings of the peer read (`scope["client"]`, `scope.get("client")`, and both through `request.scope`) plus a second function-bound allowance. `signup._consume` stops hashing its subjects. `tests/conftest.py` gives every test its own limiter key prefix. Three suites added (`principal_resolution`, `middleware_refusal`, two census sensibility tests + a `fingerprint` caller guard), two stale prose blocks and DEPLOYMENT §7a made true. | **The double resolution was not only a cost: with the Redis dedupe marker unavailable, `_record_impersonated_read` fails towards recording — correctly — and one impersonated request therefore wrote TWO identical `admin.impersonation_read` rows** into an append-only ledger whose whole readability argument is that one presence is one row (measured: 2, not 2 in theory). **The `Bearer <random>` bucket D-131 called the owner's call is closed by charging MINTS rather than requests** — charging the address per request is the obvious alternative and it fails `test_a_tenants_users_share_a_tenant_budget`, which is the NAT'd-SMB case the tenant dimension exists for, so the sabotage that proves the fix also proves the rejected design. **A census cannot see whether a limit is SENSIBLE**: `Rule("/v1/leads/export.csv", "admin_api")` matches a route, ties with nothing and resolves cleanly at 300/min instead of 6 — so a cost weight must now be strictly tighter than the family it overrides, computed by removing the rule and re-resolving. **The same blindness in the CORS suite**: every assertion there checks that what we serve is ALLOWED, and `*` allows everything — Starlette pairs it with `allow_credentials=True` by echoing the caller's Origin, and `bootstrap.py`'s "a wildcard is never acceptable here" was a comment with nothing behind it. **§3's "echo on response" held for every response except the three the stack produces itself**: the 413, 429 and 503 carried no `X-Correlation-Id` even when the caller sent one, `trace_id: null`, and no `request` log line — the three refusals support is most often asked about were the three with nothing to trace them by. **D-139's guard knew one spelling of the peer read while a second was already in the tree it was guarding** (`core/middleware.py` reads `scope.get("client")`), which defeats the stated rationale that the next author will spell it differently; `apps/workers` stays out of scope, now verified rather than assumed — it has no `Request`, no scope and no `write_audit` call. **The signup quota's "not a directory of who signed up from where" was contradicted one module over**: an unkeyed digest of an IPv4 address encodes a 32-bit space, the limiter writes the same address in the clear, and SEC-COMP §5 keeps it permanently in `audit_log.ip` — so `fingerprint` is now for the one live credential and a test pins its single caller. **The suite was measuring its neighbours**: `ASGITransport` peers at `127.0.0.1`, so ~4,000 tests and every concurrent run shared one bucket against a `bulk_read` ceiling of six — 53 failures became 1, all of them 429s that passed standalone, verified over eight consecutive runs in 27s and against a heavy suite running concurrently. **Still open, in another batch's files**: `test_the_last_owner_rule_survives_two_owners_demoting_each_other` is timing-flaky under load independently of this change (interleaved A/B: 5/8 failures before, 7/8 after) — the loser's principal can resolve after the winner commits, which is a legitimate 403 the test does not admit. |
| D-145 | **A test that read a file anyone could overwrite, and a model chosen because the region would not move** | **The ratchet's parser test stopped reading `REPO_ROOT/.coverage`.** `TestReportParsing` now reads the `real_run` fixture's own scratch measurement — a genuine `coverage run` this file already builds for `TestRefusalReachesTheExitCode`, so the property the test existed for (parse REAL coverage.py output, not a fixture that can drift) is kept at zero extra cost, and its `pytest.skip` on a fresh checkout is gone with it. `load_report`, `manifest_path`, `unvouched_run` and `blind_spots` lose their `data_file=None` defaults, so the well-known path is now reachable from exactly two places: `main()`'s argparse default (which run is scored) and the plugin's uninstrumented-run fallback (which stale file is invalidated). Two new controls: `test_a_partial_coverage_at_the_repo_root_cannot_fail_it` plants a REAL one-module `coverage run` over the live `.coverage` and restores it byte- and mtime-for-mtime, and `test_no_reader_defaults_to_the_repo_root_data_file` pins the signatures. **And the dashboard AI ships on `gemini-2.5-flash`.** `GEMINI_DEFAULT_LLM` moves off `gemini-3.1-flash-lite`; `gemini-2.5-flash` leaves `GEMINI_RETIRED_LLMS` (it cannot be both banned and sent) and both jobs that entry was doing land elsewhere rather than being dropped — the literal ban becomes `test_no_shipped_module_spells_the_gemini_default`, and the DATE becomes `GEMINI_DEFAULT_LLM_RETIRES: Final = date(2026, 10, 16)` plus `test_the_shipped_gemini_model_has_runway_left`, which turns CI red on 16 Sep 2026 with the remedy in the failure message. OPERATIONS §2 gate 14 is rewritten and gains **dated gate 14b** (replace the identifier before 16 Oct 2026, three steps, three wrong answers); BRD R-04 stops being a risk that was routed around and becomes an obligation that is owed; CLAUDE.md, AGENTS, README, TRD, SEC-COMP §4, DEV-SETUP and BUILD-LOG follow. the residency flag was deliberately NOT flipped. | **The coverage coupling had a mechanism, and it is worth knowing because it will outlive this fix**: `coverage`'s source walk skips any directory without an `__init__.py`, and D-18 makes `apps/voice-runtime` hyphenated, so a partial run's data file contains no line of hard rule 3's service — `voice-runtime-ack` matches zero files and the assertion that fires is `every area must match measured files`, on a surface the diff never touched. Reproduced deliberately before fixing anything: one `coverage run -m pytest <one file>` at the repo root, and the test went red. **Two other options were argued and rejected.** `COVERAGE_FILE` redirection moves the collision rather than removing it and costs three commands plus CI; teaching the TEST to recognise a whole-suite run is the gate's job and the gate already does it far better (`unvouched_run` pairs the data file with a manifest) — a test cannot, because inside the suite the newest manifest describes the PREVIOUS run, so the honest version would skip exactly when a partial file was present. **Planting the poison in the real tree was measured, not assumed**: `coverage run` erases its data file at start and holds no handle until the save at exit, so a file written and removed mid-suite cannot reach the gate's own measurement. **On the model, the search moved the other way from D-142 and the flag still did not.** Independent summaries now place the 2.5 class in Mumbai with ML processing (2.5 Flash, 2.5 Pro, 2.5 Flash-Lite) and still place no 3.x model there; a Google developer-forum thread is titled asking for anything in `asia-south1` more capable THAN Gemini 2.5 Flash. That is the good news D-142 lacked — and it is the same class of evidence D-142 refused, so the residency flag was left where D-142 put it: `docs.cloud.google.com`, `discuss.ai.google.dev`, `modelavailability.com`, `innfactory.ai`, `gcloud-compute.com`, `openrouter.ai` and `pricepertoken.com` were each ATTEMPTED from here and each returned EGRESS_BLOCKED, so this is REPORTED, NOT READ, and one 200 from `generateContent` is what closes it — blocked on a GCP project and nothing else. **The founder's trade, stated: a live 16 Oct 2026 deadline bought in exchange for a model the only permitted region is reported to serve**, because D-127 does not move the region and a 404 is not better than a deadline. The runway is 30 days rather than more for a reason worth recording — the decision landed 61 days out, so any runway of 61+ is red on arrival, and a gate that is red on arrival is one somebody deletes. **Not done, and it is another agent's file.** `apps/api/billing/ai_quota.py` still prices the assist at 3.1 Flash-Lite. The exact change is `ASSIST_LIST_PRICE_INR_PER_KTOK` `in` 0.0239 → **0.0287** and `out` 0.1435 → **0.2392** (2.5 Flash at $0.30/$2.50 per 1M, ₹95.66/USD), which carries `reference_assist_cost_inr()` 0.33475 → 0.50230 and `AI_ASSIST_NOMINAL_INR` ₹0.50 → ₹0.75 by derivation. The PRICE is the one fact here that was genuinely READ, not reported: LiteLLM's `model_prices_and_context_window.json` on `raw.githubusercontent.com` (unblocked) gives `gemini-2.5-flash` under `vertex_ai-language-models` as 3e-07 in and 2.5e-06 out, and gives 3.1 Flash-Lite the $0.25/$1.50 the current table was built from, so it is the same source of truth. **Two of that file's own bands then fail, and one of them is a product finding rather than a test to edit**: the output leg loses only 16.4% to per-token rounding against an assertion of 25% (the claim that this ledger must count in thousands still holds — input loses 100% — so the threshold moves and its comment is rewritten with both real numbers), and a ₹25 trial allowance now buys **49.8** assists against a floor of 50, i.e. at 2.5 Flash prices the trial tier no longer clears its own enough-to-form-an-opinion bar. That is `AI_QUOTA_INR['trial']`, a price, and a founder's to move. **Eight sabotages, eight distinct signatures**, including the one that proves the doc binding is live: flipping the flag to True without touching prose fails `check_docs_drift` §5 on all TEN sentences at once, so the day gate 14 answers 200 they move together or not at all. |
| D-146 | **The seam: a dashboard assist a user can actually reach, and the price table caught up with the model it meters** | **PART 1 — the join.** `POST /v1/calls/{call_id}/assist` (`org:manage`, client realm, `costly` limiter) is the production caller D-137 and D-140 both named as the thing that did not exist: SUBJECT → GATE → RUN → METER in one handler, with each arrow argued in `apps/api/crm/assist.py`. New `crm/assist.py` owns the two halves that are not a route: `load_assist_source` builds the model's input from `transcript_turns.text_redacted` — the RAW column is not a parameter of its query and is not named in the file — and `meter_assist` turns `AssistResult.usage` into `record_ai_assist_usage`'s two `ai_assist_ktok_*` rows at `ASSIST_LIST_PRICE_INR_PER_KTOK`. `Idempotency-Key` is REQUIRED (400 + problem body, draft-ietf-httpapi-idempotency-key-header-07 §2.4) because a repeat is a second payment to Google and D-140 moved dedupe here specifically so it happens BEFORE the provider is paid. `CallAssistOut` is three required fields — `summary` (through `crm.service.redacted_summary`, entered in `check_redaction_exposure.KNOWN_SAFE_FIELDS` and its pin), `disclosure`, `metered` — and NOTHING is persisted. The call detail screen grows `AssistCard`; `AcceptChargeDialog` and `extraUnavailableSentence` MOVE out of `c/[slug]/ai-assist/page.tsx` into `components/aiExtraDialog.tsx` with that page migrated in the same change, so the ceiling opens the ONE wallet dialog from both entry points. `tests/call_assist_test.py` (20) + `apps/web/tests/callAssist.test.tsx` (10). **PART 2 — the price.** `ASSIST_LIST_PRICE_INR_PER_KTOK` moves to `gemini-2.5-flash`'s $0.30/$2.50 per 1M (₹0.0287/₹0.2392 per ktok), carrying `reference_assist_cost_inr()` ₹0.33475 → ₹0.50230 and `AI_ASSIST_NOMINAL_INR` ₹0.50 → ₹0.75 by derivation; `AI_QUOTA_INR['trial']` ₹25 → ₹40; `billing/models.py`'s ktok paragraph and three worked examples in `ai_quota.py` recomputed. | **The two halves were one call apart and neither could be exercised, which is why both delivering agents put the same line at the top of their report.** `run_assist`'s redaction guard — the one thing standing between this product and D-127 G-2 — was DEAD: nothing called the function, so the branch that refuses unredacted input had never run against anything but a test. This route is what makes it live, and the sabotage that matters is the one that proves it: swapping `text_redacted` for `text` in one SQL string turns TEN tests red, and the FIRST of them is not the assertion about bytes — it is `run_assist` refusing its own caller with `assist_input_not_redacted`. The guard catches the mistake the route was most likely to make, from inside, exactly as its author predicted. **`AssistResult.usage is None` was the decision this join had to make and it has two causes that must not meter the same.** A Sarvam fallback is free (D-36) and writes nothing; a GEMINI answer Vertex did not count is money we spent and cannot account for, so it writes nothing AND fires `alert("CORE_LOGIC", "ai_assist_unmeterable")` — the per-tenant ceiling and the platform brake are both blind to that spend, and an operator can act on "the meter has stopped" where they cannot act on a plausible invented number. Metering it as `qty = 0` was the tempting third answer and is D-140's own objection with the sign flipped: an assist that certainly burned thousands of tokens recorded as zero thousand is a fabricated quantity, and it would ALSO move `requests_used` (`COUNT(DISTINCT ref)`), so the rupee ceiling that blocks and the request count a person plans around would start disagreeing about the same month. **Three departures are argued rather than assumed.** (1) The model is called from a REQUEST HANDLER, which CLAUDE.md forbids — the rule protects the latency-critical path and a blocking worker, and this is neither (every hop is awaited on an asyncio loop); what it really costs is one pooled Postgres connection held across the vendor round trip, because `app.tenant_id` is transaction-local so the transaction cannot be committed and resumed. Bounded by `EXTRACTION_TIMEOUT_S` and the `costly` profile, and taken because the alternative — 202-and-poll — needs somewhere to PUT the answer, which is a new store of transcript-derived prose (D-126's class) that erasure and retention would then have to enumerate. (2) The assist writes NOTHING to `calls` or `call_extractions`: the stored record is the raw-transcript pass, which is the only one that can capture a callback number, and overwriting it with a redacted-pass reading would degrade the lead and rewrite history a client may have exported. (3) `org:manage`, because a POST that spends money must be in `MUTATING_PERMISSIONS` (D-22) and the whole AI surface is already owner-scoped — `billing:read` to see the allowance, `org:manage` to buy more. A new `calls:assist` was rejected on `bulk_leads`'s ground: its holders would be exactly `leads:write`'s. **`quota_exhausted=quota.at_ceiling` is a wire that carries a constant today and says so**: `require_ai_assist` RAISES at the ceiling rather than answering "no", which is G-5's block, so the parameter is False on every path that reaches it — written as the READ rather than as a literal `False`, which would be a promise about another module's control flow made in this one. **On the price, the evidence standing is better than anything else Google-shaped in this repo and that is the finding**: the LiteLLM price file was FETCHED from `raw.githubusercontent.com` and PARSED LOCALLY (3e-07 / 2.5e-06 for `gemini-2.5-flash` under `vertex_ai-language-models`), because an earlier fetch SUMMARY of the same 1.7 MB document returned a different vendor's Flash-Lite row and would have under-priced every assist eightfold — a summariser answering about the wrong row is indistinguishable from an answer. **A 50% price move in one model decision then broke two bands, and only one of them was a test's fault.** The output leg now loses 16.4% to per-token rounding against a 25% floor: the ktok argument SURVIVES (input still loses 100%, so half of every assist would meter free) but it is getting thinner as output prices rise, so the threshold moved to 15%, the input leg is pinned separately as the load-bearing half, and the comment says the next drop is a re-argument and not another lowering. The other was real: ₹25 bought 49.77 assists against a floor of 50, so at 2.5 Flash prices the trial tier no longer cleared its own "enough to form an opinion" bar — the test was right and the constant was wrong. ₹40 buys 79.6 (1.6× the floor) and stays above it until an assist costs ₹0.80, a 59% rise — more than the 50% just absorbed — which matters because two more moves are dated (16 Oct 2026 retirement, 1 Jan 2027 price cliff) and a value tuned to clear 50.0 exactly is one revision from being back here. What a trial tenant SEES is 53, not 79.6, because `requests_included` divides by the margin-loaded nominal; both clear 50, which is what makes the screen and the gate agree. **Nine sabotages, nine distinct signatures**, including one that only the frontend suite could catch (deleting the disclosure block leaves every backend assertion green) and one that shows the fixture rot this change exposed: `aiQuota.test.tsx` claimed 200 assists for a ₹100 allowance, which no server can now answer, so three fixtures were recomputed. **NOT DONE, and it is one line in another agent's file.** `apps/workers/extraction.py`'s `ASSIST_QUOTA_ENFORCED: Final = False` is now FALSE-in-the-code and true-in-the-world: its own comment says "no route calls `require_ai_assist`, hands its verdict to `run_assist`, and meters `AssistResult.usage` back" and names this endpoint as what closes it. One route now does all three. Flipping it is `extraction.py:978` plus `tests/vertex_extraction_test.py:412`, both owned by the agent editing that file in this wave, and a greppable honesty boolean is exactly the thing two agents must not edit at once. |
| D-147 | **The receiver measured two waits it did not bound, went silent on the two exits it did not measure, and cached duplicates by a key that let anyone bypass the cache** | `apps/voice-runtime` gains bounds and accounting where it had measurement and prose. **`_BODY_DEADLINE_S = 2.0`** wraps `_read_bounded`: the comment above `_DURABLE_DEADLINE_S` claimed Postgres was the only unbounded wait on the ack path and the body read was the other one — a trickled body was answered **202 with `X-Ack-Ms: 1506.1`**, correctly measured, correctly alerted, bounded by nothing. `ClientDisconnect` out of `request.stream()` is caught and answered 400 `body_incomplete`. **`measured()`** funnels every non-ack exit through the same header and metric: the inbox's 409 and any 5xx shipped with NO `X-Ack-Ms` and no `webhook_ack_ms` sample at all. The Redis fast-path key becomes the unit of work `{engine}:{execution_id}:{raw_status}` — the same transition the inbox claims — with **`_body_digest(raw)` as its VALUE**, so a replay whose bytes differ is absorbed AND counted (`record_webhook_replay_divergence`). `_ack`/`_refuse`/`_read_bounded` take an **`AckMeter`**, so the in-call tool endpoint records `tool_ack_ms` instead of diluting the receiver's series (closes `tool_endpoint_budget_test`'s `KNOWN_OPEN_ACK_METRICS`). `engine_intake` tries each of the three execution-id spellings instead of `a or b or c` then one type check, and `_keyable` strips surrounding whitespace. New `tests/voice_runtime_deploy_independence_test.py`; `voice_runtime_import_surface_test` drives the tool endpoint and every error branch, and pins what the ALERT DELIVERY THREAD acquires. | **The old body-in-the-key was not a control, and that was verified rather than argued.** A doctored re-delivery driven against the pristine receiver returned 202 `duplicate`, ZERO alerts, an unchanged `payload_hash` and three Postgres statements: the inbox is handed `body_hash({engine, execution_id, raw_status})`, a pure function of the key, so its "same key, different hash ⇒ doctored replay" branch is a tautology at this endpoint and cannot fire on a body change (deliberate, D-40 era). All the old key bought was routing that delivery to a database with nothing to say — 15 Postgres statements for five replays carrying one varying integer, i.e. a round-trip amplifier for anyone past the source check — and ~6.4ms of CPU at the megabyte cap re-serialising the payload to hash it, on the one event loop D-55 puts at ~250 acks/s. Hashing the raw bytes costs ~0.64ms and now DETECTS the divergence the old shape only paid for. **`ClientDisconnect` was an availability hole, not a cosmetic 500**: it fired `alert()` under `ROUTE_HANDLER:unhandled_exception`, and `_admit` suppresses a fingerprint for 15 minutes — one half-open POST per quarter hour kept the receiver's real crash alarm permanently silent. **`proxy_request_buffering off` on the `hooks.` vhost is why the edge does not cover the body read**: nginx streams, and `client_body_timeout` bounds the gap between reads, not the total. **Re-measured on a shared box, indicative not deterministic**: receiver 4.5ms p50 single-flight, 881ms p50 at 250 in flight (budget crossed near ~140, flat distribution = D-55's convoy); tool endpoint 0.6ms p50 single-flight and 103ms p50 at 175 concurrent, confirming D-109's ~175 estimate; a 1000-wide refusal storm costs 0.1ms p50 and zero round trips. Deploy independence proven by serving from a database 48 migrations behind head. **Still open and now visible instead of invisible**: `alerting._deliver` imports `apps.workers.transport` into this process on first alert delivery, which D-49 recorded as kept clean by being lazy — deferring it moved it out of the boot graph, not out of the process. |
| D-148 | **The post-call pipeline's own re-drive could be blocked by the vendor, and two of the ingest job's five steps had no failure policy at all** | `ingest_engine_event` now wraps every stage in the ONE policy `_abandon_ingest` owns (payload parse included, as a `validation` ProblemError mirroring `_post_call_target`), instead of only the engine fetch and the enqueue; the recording copy is skipped when `calls.recording_url` already holds our key (`_copy_recording_once`, with its own stage span); `storage.recording_key` dropped the `datetime.now(UTC)` month segment and is a pure function of (tenant, call); `_meter`'s and `_unit_price`'s docstrings were corrected against the code and against `billing.service`. `tests/pipeline_partial_failure_test.py` walks all eleven stage boundaries — kill at stage N, re-drive, compare the whole artefact census against a clean run — with an in-file negative control that doubles the lead timeline on purpose. | Measured on a real arq worker: a transient blip in `_resolve_agent`, `_upsert_call` or `mark_inbox_processed` got **1 attempt, 0 alerts, and an inbox row left `processing`** — which `claim_inbox_event` answers `duplicate` to for the whole `CLAIM_LEASE`, so the vendor's at-most-once retry was swallowed by a claim with nobody behind it and only the 10-minute poller could recover the call; that is D-31's ladder-and-guarantee inverted. The recording copy ran on every re-drive, so a repair for a MISSING EXTRACTION had to clear a vendor S3 link with no documented expiry first — an expired one failed step 1 before step 3 was reached, every hour, forever. And a second copy across a month boundary (crash between the PUT and the `recording_url` commit, or a replay of a call that ended on the 31st) landed under a second key that the retention sweep and the DPDP erasure both work from `recording_url` and cannot enumerate — a caller's voice surviving a certificate that says it was destroyed. Also pinned: a zero-length call reconstructs ₹0.20 of a ₹1.0000 charge through `SUM(qty * unit_cost_paid)` while `spend_state` takes the whole amount — `_unit_price` claimed the money never disappears, `billing.service._spend_used` always said otherwise, and the closable half is a reader in `apps/api/billing`. |
| D-149 | **The dial gate held; the batch behind it did not — a pause, a narrowed window and a withdrawn consent all leaked past the claim** | Three defects on the one path with a legal consequence, all of them in the gap between the CLAIM and the DIAL. (1) New `campaigns.service.campaign_dialable_now`, asked per contact inside the per-dial transaction, re-reads the campaign's status and its own narrowed `calling_hours` — `status = 'running'` is in the WHERE clause, so paused, cancelled, completed, deleted and another tenant's id all fail closed as one absent row. (2) `compliance.service.PERSON_LEVEL_REFUSALS` names the refusals that are facts about a PERSON (`dnc`, `no_consent`); the new `_refuse_contact` in the dispatcher reads it instead of hardcoding `"dnc"`, and is now the ONE writer of both the contact settlement and `record_compliance_block`. (3) `check_compliance_invariants.SCAN_ROOTS` gains `scripts/`, with the pilot harness's two engine reaches exempted BY NAME and re-verified every run. Tests: pause-mid-batch, window-closed-before-the-dial, withdrawn-consent-settles-and-the-campaign-completes, every-blocked-dial-is-labelled, halt-holds-while-Redis-is-unreachable, and the whole `CONSENT_STATUSES` vocabulary refusing by derivation (structural + behavioural). Runbooks `campaign-stall.md` §8 and `calls-stopped.md` corrected. | **`_dispatch_for_campaign` commits its claim and then dials one contact at a time, and `_run_tick` picks every tenant's campaigns before any of them dials — so "this campaign may dial" and "this contact is dialling" are separated by a whole dial phase, bounded only by the 300s job timeout.** The big red switch survived that gap because `check_dispatch` re-reads it per contact; the campaign's own two switches did not. A client pressing pause — or FLOWS §5's complaint-spike auto-pause, whose entire point is stopping fast — rang every contact already claimed, and a campaign narrowed to 09:00-12:00 dialled at 12:05, because `calling_hours` may only NARROW the platform window and the platform check therefore cannot see it. Separately, D-117 added `no_consent` to the gate and nothing told the dispatcher it was terminal: the contact went back to `pending` with its attempt REFUNDED every thirty minutes forever, the ladder could never exhaust it, and the surviving `pending` row meant `complete_or_rearm` never fired — so a person who withdrew permission held their campaign open, and its `campaign.completed` event, indefinitely. The metric half was a documentation lie in the opposite direction: `runbooks/campaign-stall.md` §8 sent an operator to `compliance_blocks{rule=...}` for a stalled campaign and the per-dial refusal never emitted one. And the census that claims to enumerate every way this repo can ring a phone walked `apps/` only, which is how `scripts/pilot/` — the ONE program here that dials a telephone on purpose, twice — was invisible to it; the harness is legitimate (no DB session by construction, a single `--to`, `--yes-place-real-calls-and-spend-money`, a hard `--max-calls`, and a refusal to run against production-shaped config), and now it is written down instead of unenumerated. |
| D-150 | **A figure quantized twice arrives twice — the invoice printed a line that did not multiply out — and a call finishing could un-press a client's own stop button** | `billing/service.allocate_paise` (largest-remainder apportionment) makes `_tier_totals` return ONE set of paise-exact rung minutes summing to the month's total; `usage_summary` derives `minutes_used` from them instead of running a second aggregate, so nothing downstream re-rounds a minute. `overage_rungs` is now the ONE place a rung's money is computed and BOTH the usage panel and `build_invoice` call it — which deletes `invoice._reconcile_overage` rather than keeping it. `_ZERO_PAISE` keeps a floored zero two decimals wide on the wire. `usage_summary` reads the IST billing month ONCE and hands it to `read_spend_counters` and `_spend_used`. New `caps.lock_tenant_spend_state` serializes all three writers of `spend_state.capped`, the meter's upsert included (one line in `workers/pipeline.py::_meter`). `tests/money_walk_test.py` (16) walks a rupee end to end on awkward decimals, forces the cap interleave at a barrier, and PINS two open findings; `invoice.py` records what Rule 53 and CGST s.34 do to the designed serial registry. | **The invoice was wrong by six paise on a line a client checks by hand.** 300.3s premium + 299.7s value is 5.005 + 4.995 minutes — exactly 10.00 — and `to_paise` on each rung published 5.01 + 5.00 = 10.01 against an `overage_minutes` of 10.00. `_reconcile_overage` then bent the LAST invoice line to make the total right, printing "5.00 min at ₹3.75/min ... ₹18.69". Three docstrings claimed the parts add up (`split_overage`, `tier_usage`, `invoice`) and none of them did; `tests/billing_audit_test.py` already asserted "every line multiplies out" and passed throughout, because its fixture bills whole minutes at ₹8.00 — **a rounding defect cannot show itself against round numbers.** Deriving the last part by subtraction is right for two parts and wrong for three (three buckets of 0.005 min give `total - a - b = -0.01`, a negative minute count), which is why the primitive is largest-remainder and why its "total is not the parts' total" guard is proved by a test rather than left unreachable. **The cap race reproduces on demand and a row lock cannot close it**: the meter's upsert reads the ceiling from `plans` and writes `spend_state`, so under READ COMMITTED it blocks on the row, unblocks with the new counters and the OLD ceiling (postgresql.org/docs/16/transaction-iso.html §13.2.1 — an updating command sees concurrent effects on rows it is updating, not on other rows), and overwrites the `capped` a client's stop button had just armed, mid-runaway-campaign. The module docstring's "three writers share one expression" argued definition and never inputs. **Two findings pinned, not fixed, both cross-territory and both about the same gap between quoted and charged**: a self-serve wallet is debited at `cost.total_inr` — OUR supplier cost — while the runway on the same screen prices at `self_serve_inr_per_min`, so the margin on every self-serve minute is zero and `self_serve_inr_per_min` has exactly one reader; and `usage_summary.spend_used_inr` publishes that same supplier cost to the CLIENT ("Used so far", `/c/<slug>/usage`) three paragraphs below the rule forbidding it, which also means a client's own rupee cap is denominated in our cost rather than their bill. **Also**: `tier_usage` had no caller outside tests (CLOSED by D-152), and CBIC's own pages stay unreachable from this network, so Rule 46(b)/53 and s.34 remain REPORTED, not READ. |
| D-151 | **The ledgers were immutable against the two verbs the check asked about, and erasable by the two it did not; and an exemption written for a READ was applied to every verb** | Migration `a2e9f31c605d` gives all eight `APPEND_ONLY_TABLES` a statement-level `BEFORE TRUNCATE` trigger and promotes every blocking trigger to `ENABLE ALWAYS`; `check_ledger_immutability` now verifies TRUNCATE cover and ORIGIN-vs-ALWAYS, not just UPDATE/DELETE presence. Migration `c4b70e928a1f` puts a FORCEd write policy on `engine_agent_routes` (global `FOR SELECT USING (true)` kept, so the session-less webhook lookup is unchanged) and the registry's exemption reason now says it buys reads only. `05bba2f3c19c`'s downgrade drops the cluster-wide `calevate_app` role only when `pg_shdepend` shows no other database depends on it. New `tests/ledger_truncate_immutability_test.py` and `tests/migration_reversibility_test.py`; `rls_sweep_test` gains a cross-tenant INSERT probe over all 42 tenant tables, a self-row-hijack probe, and a pin on what an exemption buys. | Measured, not inferred, on a migrated database: `TRUNCATE audit_log` emptied the compliance hash chain and `SET session_replication_role = replica; DELETE FROM consent_ledger` emptied the consent ledger — a row trigger cannot see TRUNCATE, and an ORIGIN trigger stops firing under a plain SET that needs no DDL and leaves no schema diff, which is what `pg_restore --disable-triggers` emits. Separately a session scoped to tenant A could UPDATE, DELETE and RE-TENANT tenant B's `engine_agent_routes` row, silencing or stealing another client's inbound calls: the same bug `e4f2a86b13d7` had already fixed on `dnc_list`, on the only other table with the same shape. And `alembic downgrade base` could not complete at all on a cluster with a sibling database, because a per-database migration was dropping a cluster-wide role. What held: all 63 revisions walk head->base->head with a byte-identical schema, all 42 tenant-isolated tables refuse cross-tenant SELECT/INSERT/UPDATE/DELETE and the self-row hijack, and `calevate_app` holds no TRUNCATE, no CREATE, no SET ROLE and cannot set `session_replication_role`. |
| D-152 | **The coverage ratchet charged for a `Protocol` declaration and called it a pragma** | `check_coverage_ratchet.Measurement.excluded` becomes `suppressed`, counted by `_suppressed_lines`, which reads the source and counts only lines carrying a no-cover comment; the failure detail says "suppressed by a no-cover comment" instead of naming a pragma that may not be in the file. D-29's doctrine is unchanged — a suppression still counts as uncovered, and the only routes down are still a test or a `RAISED_BUDGETS` waiver. Sharpening it took two areas below their floors and left four real suppressions in guarded surfaces, all four then REMOVED rather than accepted: two `.first()`-plus-unreachable-`None` reads became `.one()`, one opt-out validation and one post-CAS re-read got the tests they always could have had. `tier_usage` — a D-36 reporting surface D-150 pinned as having no caller — is nested under `tiers` on `GET /v1/admin/tenants/{id}/margin`, where it partitions that card's `cost_inr`, and the admin panel renders it. | coverage 7 excludes `...`-bodied stubs and `if TYPE_CHECKING:` blocks BY DEFAULT and sweeps the blank lines around each excluded clause in with them. Nobody wrote those exclusions, so charging for them made a four-line `Protocol` cost four units in a hard rule 3 surface — three of them whitespace — under a message that sent the reader hunting for a comment the file did not contain. `VoiceEngine` is written in exactly that shape, so the guard was pricing the repo's own typing idiom and the cheapest way to satisfy it was an untyped callable. The new tests measure coverage's real defaults from an actual run rather than from a changelog, because the behaviour is version-dependent and a guard built on a remembered version is a guard that silently changes meaning on upgrade. Mounting `tier_usage` also exposed why `pnpm -C apps/web typecheck` had not caught five stale margin fixtures: each carried `as Margin`, which asserts the shape rather than checking it — the casts are gone. |
| D-153 | **A recording link that expired before its own audio ended, and a player that could not say so** | `crm/routes.recording_link_ttl_s` derives the presigned window from the call's metered `duration_s` — twice the duration, floored at `RECORDING_LINK_FLOOR_S` (300s) and ceilinged at `RECORDING_LINK_CEILING_S` (2 x `CALL_CAP_MAX_S` + the floor) — rather than handing every recording the flat `PRESIGN_TTL_S`. `recording_key_for` becomes `recording_ref_for`, returning a `RecordingRef(key, duration_s)` read in ONE query so a retention sweep cannot land between the two. `RecordingLinkOut` gains `duration_s` (required on the wire, nullable in value). The client screen replaces `<audio controls>` with `components/callAudioPlayer`: speed, +/-10s, a range-input seek bar, and a single re-mint on `error` that restores the playback position; every transcript turn carrying `start_ms` becomes a seek target and the turn being spoken is highlighted, bounded by the NEXT turn's start. | **Measured, not inferred: every link was signed for 300 seconds and `CALL_CAP_MAX_S` is 3600.** A twenty-minute call played for five minutes and stopped. S3 answers an expired signature with a 403 whose body is XML, which a browser surfaces as a bare `MEDIA_ERR_NETWORK` on the element and nothing else — so the owner's reasonable conclusion was that we had recorded only the first five minutes of their call, and no log anywhere would have contradicted them. Raising `PRESIGN_TTL_S` was rejected: it is shared with every other presigned artefact and the signature IS the credential, so widening it globally hands a longer window to objects that need seconds. Deriving per call keeps the window proportional to the only thing that justifies it, and the ceiling is a named constant so the widest window this route can open is readable rather than derived. `start_ms` had been on every transcript turn since the pipeline was written and NOTHING read it — the column, the API field and the seek it exists for were three-quarters of a feature for as long as the screen has existed. **Retention is unchanged and is not ours to shorten**: TRAI's 90-day floor is enforced by a DB CHECK and by `RECORDING_FLOOR_DAYS`, so a shorter recording policy is unlawful rather than merely aggressive. |
| D-154 | **The frontend's test fixtures stop asserting what they are supposed to prove: 106 casts onto generated wire types removed, and a guard so the 107th cannot land** | `tests/wireFixtureGuard.test.ts` bans any type assertion whose target resolves to a declaration in `src/lib/api/schema.d.ts`, in `tests/` AND in `src/`, with `tests/fixtures/wireFixtureShapes.ts` as its negative control (four banned shapes, six safe look-alikes). `satisfies T` and a plain annotation are the sanctioned spellings; DOM casts, hand-written types like `ApiProblem`, `as const`, and a deliberately off-contract literal carried as `unknown` all stay legal. The sweep removed 106 assertions across 42 files (55 of them `as unknown as T`), and the compiler then reported 34 errors that had been sitting behind them: three `Agent` fixtures with **no `disclosure_line`** (hard rule 5's field, on the outbound agents the campaign screens dial with), `CampaignSummary.created_at` missing from four, `CampaignProgress.concurrency`/`launched_at`/`status` from five, twelve client-realm `Me` fixtures carrying an `organization.plan_tier` that `OrganizationOut` does not define while omitting the `status` it requires, and `Dashboard.minutes_used_month` — a decimal-as-string under hard rule 7 — set to `null`, which its schema forbids. Also closed: `admin/.../prompt`'s call-cap box, whose `min`/`max` come from `/v1/agents/lanes` and silently became unbounded when that read failed, now says so. | **The assertion did not make the fixture right, it made it UNCHECKED**, and an unchecked fixture drifts from the wire in silence because nothing else in the system ever compares the two. None of the 34 errors broke a test — that is why review never caught them, and it is the reason the count matters more than any single instance: a fixture that lies in the direction the screen ignores produces a green suite and a false belief about what the screen has been shown. **90 of the 106 sites already had a type annotation underneath the assertion**, cancelling it: `as T` makes the initialiser's type `T`, so the annotation beside it checks nothing. Stripping revealed a second failure mode the sweep had to fix by hand — 11 sites had NO annotation, so removing the cast traded a false check for no check, and that residue is exactly what this guard cannot see and says so. **`as unknown as T` is strictly worse than `as T`** and was the majority spelling: a single assertion still rejects a literal with no overlap, a double rejects nothing. Two narrowings were forced by the negative control rather than by review: an indexed access (`"x" as AttentionItem["kind"]`) resolves to a bare union with no symbol in `schema.d.ts` and needed a syntactic branch, and it is the highest-damage shape of all — it asserts that a closed union already contains the value whose ABSENCE is the premise of the test around it, and would keep compiling on the day a kind is deleted for real. **The reverse direction was investigated and is genuinely closed**: the API sets no `response_model_exclude_*`, so every field is always serialized, and openapi-typescript 7 emits `default`-bearing properties as REQUIRED — so a Pydantic default does not become an optional TS property, and the 25 remaining optional-but-non-nullable fields are all read through `??`/`?.`, which absorb the `null` the server actually sends. |
| D-155 | **The log stream, the error tracker and the alert fingerprint each carried a hard-rule-6 hole that the module's own docstring said was closed** | Five closures, one per hole. (1) `JsonFormatter` now redacts `record.getMessage()` and the traceback, not only the extras — a `%`-arg or f-string message went out verbatim, and `tests/pii_logging_sweep_test` pins BOTH the formatter and the tree-wide discipline that every log message is a static event token. (2) `redact_exception` replaces `redact_text` on the traceback: frames are kept in full, every exception MESSAGE is withheld, and the whole thing is bounded by line count instead of by a 200-character head cap. (3) `redact_mapping` fails closed on TYPE — a tuple, set, Pydantic model, dataclass or `bytes` used to skip every branch and be rendered by `json.dumps(default=str)`, i.e. its `repr()`. (4) `scrub_event` scrubs `exception.values[].value` (withheld, same marker), `logentry` and event-attached `breadcrumbs`, and drops `request.cookies`; `_redact_span` scrubs span LINK attributes. (5) `install_error_handlers` fingerprints the catch-all alarm on the exception CLASS (`unhandled_exception:ValueError`), and `alerting` NAMES the codes its token bucket dropped instead of only counting them. Plus an ISO-8601 date joins the uuid and the hex digest as an identifier shape held out of the phone pass. | **A docstring is not a control, and every one of these had a docstring.** `observability` dropped `exception.message` off every span while `scrub_event`, forty lines away, shipped the identical string to Sentry — and `str(exc)` is where `pydantic.ValidationError` renders `input_value=`, i.e. the extraction payload. **A length cap is not a control either**, which `route_shape_test` already said about this exact class: whether a traceback's message fell inside 200 characters depended on how deep the stack happened to be, so the old code leaked the message on short stacks and destroyed the diagnosis on long ones — a 334-character twelve-frame traceback was cut off inside frame three, leaving the only durable record of a 500 naming three frames of ASGI middleware. Withholding the message costs the constraint name on a DBAPI error; CPython renders the `raise`'s SOURCE line, which recovers most of it, and no redactor on this path can recognise a caller's NAME or a Telugu sentence — proved by writing the permissive version first and watching `Ravi` reach the log stream. **The type fall-through was latent, not live**: a probe over the whole suite saw 20,377 records with extras and zero non-scalars, which is exactly the state in which a hole is invisible until the first `extra={"turn": turn}`. **The catch-all fingerprint is D-147's defect as a class rather than an instance**: `_admit` suppresses `stage:code` for fifteen minutes, so one code shared by every crash meant the first class to fire silenced the rest — `ClientDisconnect` was caught at its one call site, and the next cheap exception would have done the same thing. **The bucket's blindness is the same shape**: `webhook_source_rejected`, `tool_source_rejected` and both bad-signature codes are unauthenticated and free, and while dedupe stops a stranger holding a fingerprint down, it does not stop them emptying the six-token burst at a moment of their choosing — "12 other alerts were dropped" gave the operator no way to learn that one of them was `postcall_pipeline_stalled`. |
| D-156 | **Key moments in a call — precomputed once at storage time, and NOT embedded in the audio** | `apps/workers/moments.py` derives markers from what the post-call pipeline already holds: each extracted value anchored to the EARLIEST transcript turn whose text contains it (`start_ms`, persisted since the pipeline was written and read by nothing until now), plus the opt-out detector's `turn_idx`. Stored as a nullable `moments` JSONB on `call_extractions` (migration `f8c1d47a90e3`) — a column, not a table, because moments are 1:1 with a call and `call_extractions` already enforces that, so they inherit its FORCEd `tenant_isolation` policy instead of needing a second one. All three erasure statements in `workers/retention.py` clear `moments` with `data`. `CallDetailOut.moments` is REQUIRED on the wire and its `label` moves on the same raw/redacted switch as the transcript. `merge_moments` reserves room for model-authored highlights and caps only the model half. | **The audio is the wrong place and the research says so.** ID3v2 `CHAP`/`CTOC` is a real standard that Apple Music, Spotify and foobar2000 all ignore, and browsers do not read at all; WebVTT `<track kind="chapters">` is spec'd but every major browser ignores `<track>` entirely on an `<audio>` element. So embedding buys nothing for our own player while costing a container rewrite, a stored copy that is no longer byte-faithful to the vendor's, and — worst — chapter titles quoting the caller INSIDE an object that can only be deleted, never redacted (hard rules 5/6). **Cost is genuinely zero, for a reason worth stating precisely**: D-35 verified from the live rate card that Sarvam's LLM is free per token and that *the binding constraint is RATE LIMIT, not price*, so the free design is one that adds no REQUEST — the derived half adds no model call at all, and the model half must ride the extraction call that already runs. **The derived half exists because it cannot be wrong**: `anchor_of` returns a turn's own offset or None, never an approximation, and no fuzzy matching — a panel whose timestamps are sometimes right teaches people to distrust the ones that are exact. There is exactly ONE model kind (`highlight`) rather than a taxonomy, because D-36 records Sarvam's Telugu extraction quality as UNMEASURED and rendering an unscored classification as fact is a claim we cannot support until task #87 runs. |
| D-157 | **Hard rule 2 was a claim about imports, and the archive its erasure arm guards had no writer — both are now measurements** | `ExecutionSnapshot.raw_document: bytes | None` carries the vendor's own document out of `get_execution` (`apps/api/engine/document.engine_document`, capped at `MAX_ENGINE_DOCUMENT_BYTES` = 1 MiB, refused rather than truncated), and `pipeline._archive_engine_document` is the production caller `storage.archive_payload` never had: it commits `calls.engine_payload_ref` and THEN PUTs, which is the order D-126 made a contract. `archive_payload` becomes `async` over `asyncio.to_thread` and takes `document: bytes` instead of `payload: dict`. `ExecutionListing.complete` loses its default and gains a validator: an incomplete listing must name a reason, a complete one may not carry one. The fake engine now reports `page_cap_reached` for a truncation it can PROVE. `tests/engine_audit_test.py` grows a repo-wide AST scan for vendor keys, a classify-every-adapter-key clause, a stale-ban clause, an allowlist for the receiver, and a driven hard-rule-6 measurement; the conformance suite grows the archive clause and two saboteurs; `tests/engine_payload_archive_test.py` is new. No migration and no schema change — the column has existed since `05bba2f3c19c`. | **The import-linter contract proves nothing about a dict key.** It forbids naming a vendor adapter in an import; `payload["telephony_data"]` needs no import, arrives in a worker, and is invisible to it. Measured instead: **no shipped module outside `apps/api/engine/` reads a vendor-only key today** — every hit in the tree is a test stub building a vendor payload on purpose — and the scan now keeps it that way. The previous check existed but was scoped to two receiver files and banned four words (`transcript`, `recording_url`, `from_number`, `to_number`) that are also OURS, so it could not be widened; the receiver gets a positive allowlist instead, which also catches `duration_s` — our own word, and just as wrong there. **The archive's producer is the seam D-126 left open by design** ("the arm is built before the producer, because after the producer the unreachable objects already do"), and the type is the whole design: a `dict` would have handed a worker the vendor's field names, so what crosses is bytes, `exclude=True` so no `model_dump` into a span or a job payload can carry a caller's number, `repr=False` so no exception message can. **`complete: bool = True` was the defect its own docstring warned about** — "never the fallback for 'we did not look'" — since `ExecutionListing(snapshots=rows)` asserted it by omission; `EngineCapabilities` had already settled that doctrine three fields at a time. **Twenty-one sabotages, twenty-one distinct signatures, and one of them found a hole in a clause I had just written**: the Cartesia adapter archiving its own `ExecutionSnapshot` dump passed the new conformance clause, because the document still parsed and still differed per call — only a structural check (its keys are not a subset of our model's fields) catches it, and that check had been in the pipeline test instead of the contract, where exactly one adapter was covered by it. Ten adapter-file sabotages across bolna/cartesia/fake were each caught by the suite. **Not done, and named**: the rest of `workers/storage.py` is still synchronous boto3 called from async workers (`store_delivery_body`, `keys_under`, `delete_objects`, `copy_recording`'s PUT) — a real defect, not this slice's, because moving it touches `retention`, `outbound_webhooks` and two route modules at once; and the archive still has no `retention_policies` category, which SECURITY-COMPLIANCE §4 reserves as a DPA commitment plus a documented-enum change. |
| D-158 | **Our knowledge base and Bolna's copy only ever agreed at publish time, and a knowledge base can go months between publishes** | `apps/workers/kb_reconciliation.sweep_kb_drift` — hourly at :23, 15 agents a tick, stalest first — reads `list_kb(agent_ref)` back for each live agent and compares it with `kb.service.recorded_handles_of_agent`, the SAME set the publish gate consults. Verdicts land on `engine_agent_routes.kb_drift_state/_checked_at/_detected_at` (migration a7c31e05b8d4) and ride `GET /v1/ops/platform` as `kb_drift`, beside `engine_drift`. Six values: `in_sync`, `unaccounted`, `missing`, `divergent`, `unreadable`, `unreachable`. | `_reconcile_engine_state` asks "is the engine holding a copy no row of ours names" ONLY on the publish path, and a KB is the object here with the longest gap between writes — a client pastes a price list once and does not touch it for months, which is exactly how long a vendor-console edit or a publish that committed at Bolna and rolled back here stayed invisible. **BOTH DIRECTIONS**: an engine copy we cannot account for (the agent reads unapproved text to callers under the client's own PE registration — the FLOWS §7 gate bypassed entirely) and a handle we recorded that the engine no longer lists (the client silently lost knowledge, and the next publish dies on `kb_detach_failed`). **IT IS A READ**, D-121's argument with more force: the repair a KB drift invites is `detach_kb`, an irreversible DELETE at the vendor of a document our tables by hypothesis cannot describe — unattended, that destroys the only copy of text somebody added by hand. Output is a record plus an alert; `test_the_sweep_only_ever_reads` pins the call inventory at `['list_kb']`. **THE HONEST LIMIT**: an empty listing is ambiguous between "the documents are gone" and "the vendor's KB list does not carry the `agent_id` the adapter filters on" — pilot gate 8's `kb_list_carries_agent_linkage`, OPEN, and an external blocker (a Bolna account nobody has used against `/knowledgebase/all`). So an empty listing for an agent we believe holds documents is `unreadable` unless another agent in the same tick listed something, which is the positive control `scripts/pilot/knowledge.py` uses; reporting it as `missing` would put a fleet-wide false alarm on a schedule. **BOUNDS** are 15/tick (not 25 — `bolna.list_kb` pulls the WHOLE account listing per agent), 180s of wall clock, and hourly rather than half-hourly, with the tick's worst case asserted at import to fit inside its interval so no Redis lease is needed. Separate columns from D-123's because `drift_checked_at` IS that sweep's work queue and sharing it would make each sweep starve the other. The sweep takes `pg_try_advisory_xact_lock(kb:publish:<agent>)` around a read-verify-read bracket: the detach-then-attach ordering guarantees a window where the engine holds fewer documents than our rows record, so without it every routine price-list update would be reported as a divergence. `max_tries` explicit at the `cron()` call site (arq defaults it to 1). |
| D-159 | **Every object-store call in the tree now leaves the event loop, and a scan keeps it that way** | `store_delivery_body`, `read_delivery_body`, `keys_under`, `delete_objects` and `copy_recording`'s upload all move onto `asyncio.to_thread`, the pattern `archive_payload` established in D-157. Nine call sites follow: `integrations/routes.py` (one), `workers/retention.py` (five), `workers/outbound_webhooks.py` (one, which makes `_retain_body` a coroutine), and two test fixtures. `keys_under` runs its WHOLE pagination in one hop rather than one hop per page, and `delete_objects` all its chunks, because both already contract to be re-driven as a unit on failure. `presigned_url` stays synchronous and is named in the guard's allowlist: S3 presigning is a local HMAC with no round trip. `tests/storage_event_loop_test.py` is new. | **D-157 named this as the defect it was not fixing** — "the rest of `workers/storage.py` is still synchronous boto3 called from async workers" — and the worst instance was not in a worker at all: `read_delivery_body` is reached from a FastAPI request handler, so a synchronous `get_object` froze the API loop for EVERY tenant's request, not one job. arq's worker is one loop across all jobs, so the rest stalled siblings. **The test file is two halves that fail for different reasons.** Five behavioural tests run a store that sleeps SYNCHRONOUSLY (an `asyncio.sleep` fake would yield and pass against the very bug under test) while a witness coroutine counts how often the loop got to run: a threaded call yields ~20 ticks, a blocking one yields 0. Two structural tests are AST scans — every `_client()` call must sit inside a function handed to `to_thread`, and every network-touching function must be `async def` — and they exist because the behavioural half can only cover functions somebody remembers to add, which is exactly how four functions kept the old shape after D-157 fixed one. **Three sabotages, three distinct signature sets**: reverting the read reddened both halves at `0 >= 5` and named the line; reverting `keys_under` reddened three clauses including "not `async def`, so a caller cannot tell from the call site that a network round trip happens here"; and calling an existing `_delete()` helper directly instead of through `to_thread` — the regression that LOOKS correct — was still caught by the scan. **Rejected: aioboto3.** A second object-store client library for a property `to_thread` already delivers, on a path measured in a handful of calls per call record. |
| D-160 | **Alert suppression and the alert budget were per PROCESS, and production has run multiple processes since D-55** | `apps/api/core/alert_admission.py` moves the whole admission decision — repeat window, per-fingerprint suppression counts, and the token bucket — into ONE atomic Redis Lua script keyed by SERVICE. `alerting`'s module globals stay as a per-process pre-filter whose only remaining job is keeping one process from flooding its own bounded queue. Called from `_drain`, never from `alert()`, so the caller's thread still does nothing but a log record and a `put_nowait`. `_forget` gains its shared twin. `tests/alert_multiprocess_test.py` proves the property with REAL subprocesses; `tests/voice_runtime_event_loop_test.py` pins the two deployment facts the ack budget rests on. | **This was shipped, not hypothetical.** `compose.prod.yml` runs `--workers=${VOICE_RUNTIME_WORKERS:-4}` and `--workers=${API_WORKERS:-2}` — uvicorn workers are PROCESSES — so production had four independent 15-minute windows and, worse, four independent token buckets: a deliberate `ALERT_BUDGET_PER_HOUR` of 20 was silently 80. The duplicate pages are the visible half; the multiplied budget is the half an inbox cannot diagnose, because the symptom is "the rate limit does not seem to work" rather than an error. **FAIL OPEN, ABSOLUTELY**, because `alerting`'s docstring makes a load-bearing promise that the alert path survives the failures it reports — half these codes fire when infrastructure breaks. Unreachable, slow or unreadable Redis all return send, so an outage costs deduplication and never an alarm; the docstring was corrected rather than left saying "touches no Redis". **The clock is the caller's wall clock, not `redis.call('TIME')`** — TIME is the right tool for skewed hosts and the wrong one here, where every sharer is a `--workers=N` sibling on one VPS (D-25/D-26) and TIME buys nothing while costing a window no test can hold still. The marker STORES its instant and the window is decided by comparing it; the TTL is garbage collection only, because deciding by `EXISTS` would govern one window with two clocks. **uvloop needed no install** — `uvicorn[standard]` already ships it and `--loop auto` selects it — but nothing asserted either, and D-55's 250 acks/s was measured WITH uvloop, so trimming the extra (which also pulls httptools/watchfiles/websockets this service does not want) would have silently invalidated the whole worker table. Five sabotages, five distinct signature sets; the per-process revert reddened with "4 of 4 worker processes delivered the same alert" and "10 distinct alerts against a service burst of 6", and dropping the shared `_forget` was caught by a test that already existed. |
| D-161 | **A marketing homepage with real motion, and not one new claim** | `app/page.tsx` is rebuilt as a full landing page — hero, animated call-to-row figure, capabilities, the Indian-compliance section, a data section and the two doors — with `components/marketing/motion.tsx` (Lenis smooth scroll + GSAP `useGSAP`/ScrollTrigger) and `components/marketing/callDemo.tsx`. Three dependencies added: `gsap@3.15.0`, `@gsap/react@2.1.2`, `lenis@1.3.26`. `globals.css` gains a `:has([data-marketing-root])` override so the document can scroll. | **The honesty doctrine survived the redesign, and its guard proved it three times.** `publicLanding.test.tsx` asserts almost entirely NEGATIVE properties — no price, no customer count, no uptime or accuracy figure, no self-serve door when the flag is off — and it caught three real regressions in the new page: a rupee figure in the demo dialogue, the word `free` in a line of dialogue, and, most instructively, MY OWN disclaimer caption "not a measurement of accuracy" tripping the ban on that word. All three were reworded; the guard was not widened. **MOTION IS AN ENHANCEMENT AND THE PAGE IS FINISHED WITHOUT IT**: content renders visible and GSAP animates FROM a displaced state, so a failed bundle, a crawler, or `prefers-reduced-motion` all yield the same readable page — the `opacity: 0`-in-CSS alternative ships a permanently blank page when JS fails. **Supply chain, against hard rule 9 and the July 2025 incident**: all three packages have ZERO install-time lifecycle scripts and ZERO runtime dependencies, so pnpm never prompts and `allowBuilds` is untouched — verified byte-for-byte, since the incident's kill chain was a `postinstall` that pnpm correctly blocked and a human then allowed. **The dashboard pays nothing**: measured at build, `/` is 228 kB first load and `/c/[slug]/calls` is unchanged at 187 kB, because the libraries are imported only by client components under `components/marketing`. **Scroll ownership** is the one global change: `html, body { overflow: hidden }` is load-bearing for the `fixed inset-0` shells, so rather than relax it the override is scoped by `:has()` on the marketing root and is structurally unable to reach /c or /admin. **A jsdom `matchMedia` stub was needed** for the reduced-motion read, and adding it made the axe sweep MORE capable — it immediately surfaced a genuine pre-existing `heading-order` defect (an `<h4>` inside a `Card` whose title is an `<h2>`) on the admin tenant page, fixed in markup rather than exempted. |
| D-162 | **`outbox_messages.queue` read as routing and routed nothing; step 1 of a two-step retirement** | `enqueue_outbox` loses its `queue=` keyword and writes the module constant `reliability.service.OUTBOX_FLEET` (`"default"`). Six call sites drop the argument; `TENANT_ERASURE_QUEUE` and `DELETION_QUEUE` — two constants whose only value was `"default"` — are deleted. The COLUMN stays (hard rule 8 forbids dropping in the release that stops writing it) and `claim_outbox_batch` still selects it, so a second fleet has something to filter on. | **It was a second encoding of a fact already on the row.** Callers passed `"notifications"` or `"default"` as a pure function of `job`, `dispatch_outbox` published WITHOUT the value, and `WorkerSettings` sets no `queue_name` — so every message landed on arq's single default queue whatever the column said. The hazard is comprehension, not capacity: a column that reads as routing makes an operator believe notifications are isolated from CRM deliveries when the two are sharing one worker's ten slots, and that belief costs most on the night it matters. **HONOURING IT WAS THE OTHER FORK AND IS REFUSED FOR NOW.** arq routes by `enqueue_job(_queue_name=...)` consumed by a worker whose `queue_name` matches, and one worker consumes exactly one queue — so honouring the column means a SECOND DEPLOYABLE (container, deploy step, CI job) for a platform with no clients, against "monolith module before new service", and passing `_queue_name` today with no worker on that queue would silently stop every notification. **What closes this entry**: either the column is dropped in a later release, or a second worker fleet arrives and `dispatch_outbox` starts passing the value it already reads. Whichever happens, the code no longer claims the second one is already true. |
| D-163 | **The AI disclosure and the recording notice stop sharing a column, and both become per-agent toggles — while the truthful ANSWER becomes unfalsifiable** | Migration `f4a1d0b6e29c` splits `agents.disclosure_line` into `ai_disclosure_line` + `recording_notice_line` (both NOT NULL, non-blank) and adds `ai_disclosure_enabled` + `recording_notice_enabled` (NOT NULL, default TRUE) on inbound and outbound agents alike. `calevate_shared.engine.compose_opening_line` is the ONE producer of `AgentConfig.opening_line`, which may now legitimately be EMPTY. **The half that is not negotiable moved out of reach at the same time**: `TRUTHFUL_ANSWER_DIRECTIVE` is a `Final` in the portability contract with no writer anywhere, `compose_engine_prompt` appends it AFTER the client's script on every adapter with an explicit precedence sentence, `verification.judge` scores `TRUTHFUL_ANSWER_MARKER` on the read-back and REFUSES a publish that lost it, the half-hourly drift sweep alarms if it disappears later, the conformance suite requires it of every adapter, and `check_compliance_invariants` §6 fails the build if it ever becomes a field, a parameter or a second binding. Client-facing switch is `PATCH /v1/agents/{agent_id}/disclosure` (`org:manage` — the client owner's, which no admin or impersonating session holds against a tenant, D-22), applying immediately with an in-transaction republish; every flip writes an `audit_log` row whose ACTION names the toggle and the direction, because `write_audit` does not persist summaries and "who switched this off" must survive in the hash-chained ledger. **No `consent_ledger` row**: that register holds a data principal's consent, and a Fiduciary changing its own notice practice is not a caller consenting to anything. | **WHY.** SEC-COMP §2 has always stated two invariants under two regimes — AI identification (TRAI/UCC) and a recording notice (DPDP §5/§6 notice-and-consent) — and one column made them one switch, so a client could have both or neither. The seeded Telugu line is the defect written down: `"Namaskaram, idi {business} AI assistant. Ee call record avutundi."` is two obligations in one `TEXT NOT NULL`. Competitively, `docs/evidence/outpero-teardown-aug2026.md:276` records that Outpero makes disclosure optional and pushes the legal obligation to the client (AUP §4); the founder's decision is to match the flexibility and NOT the honesty gap — ours is the only one of the two where the agent cannot be scripted into claiming to be human. **THE RISK ACCEPTED, WITH DATES AND SOURCES (searched 17 Aug 2026; the primary PDFs are unreachable from the build environment, so these are search-summary citations and are marked as such, per the `GEMINI_MODEL_CONFIRMED_IN_REGION` doctrine).** (a) TRAI has **not notified** an AI-disclosure mandate as of this date: the 13 Mar 2026 draft Third Amendment to the TCCCPR (`trai.gov.in/sites/default/files/2026-03/Draft_CP_13032026_0.pdf`) mandates AI/ML spam detection by ACCESS PROVIDERS, and a mandatory AI-disclosure-at-call-start for senders is the widely reported likely OUTCOME of the accompanying consultation, not current law. If and when it is notified, `ai_disclosure_enabled` must be forced TRUE for `promotional`/commercial traffic and this entry is superseded. (b) The "₹10 lakh penalty" figure that circulates with these rules is the TCCCPR financial disincentive on **access providers** for misreporting UCC counts (₹2/5/10 lakh by instance, Feb 2025 amendment, PIB PRID 2102413) — it is not a telemarketer penalty; a telemarketer's real exposure is TSP enforcement (usage suspension at 5 complaints in 10 days, blacklisting on repeat) and the client PE's own liability. (c) The recording notice is DPDP, not the IT Act: a participant recording their own call is the *R. M. Malkani* (1973) one-party baseline, so switching the in-call notice off moves the client's DPDP §5 notice obligation to their own privacy notice — it does not discharge it, and the DPA must say so. (d) MeitY's IT Amendment Rules 2026 (G.S.R. 120(E), notified 10 Feb 2026, in force 20 Feb 2026) require labelling of synthetically generated information but bind **intermediaries**, not a telemarketer placing a call — considered and not engaged. **WHAT IT COSTS.** "We disclose everywhere anyway" stops being true by construction, so the EU AI Act Art. 50 row and the DPA both become conditional on a client's setting; `calls.disclosure_played` reads NULL rather than `true` for agents with the notice off, which thins the QA evidence for those clients; and the legacy `agents.disclosure_line` is now written and not read on the publish path — **step 1 of a two-step deprecation (hard rule 8)**. **WHAT CLOSES / REVERSES IT.** Step 2 is dropping `disclosure_line` in a later release. The DECISION is reversed by TRAI notifying the mandate (force the AI toggle on for commercial traffic) or by counsel ruling that the in-call recording notice cannot be moved to the client's own notice. Neither touches §2.0: the truthful answer is not part of what is toggled and there is no version of this entry that makes it so. |

| D-164 | **The erasure certificate discloses the backup window, closing SEC-COMP §4's open question** | `ERASURE_LIMITATIONS` gains a ninth clause and `ERASURE_EXCEPTIONS` its index-aligned `backup` entry, with `BACKUP_WINDOW_DAYS = 35` as the single constant and a test pinning it across three places — `infra/backup/README.md` (where the number is actually decided), the published DPA at `/legal/dpa`, and the certificate. Outcome word is `expires_with_backup`, deliberately not `retained_as_record`. The clause states the window, says backups are never searched or edited to remove one person, and says the erasure is re-applied on restore, which is already a mandatory step in `runbooks/database-restore.md`. | **WHY.** SEC-COMP §4 had reserved this as a legal call — "adding a sentence to a notice that clients hand to data principals is a commitment, not a code change" — and that framing was right when it was written. It stopped being the live question the moment the client DPA published the 35-day window: the commitment was then already made IN WRITING to the controller, while the certificate forwarded to the *data principal* still omitted it. Disclosing to the client and withholding from the subject is the wrong way round, and the legal audit recorded the asymmetry as F-7 (`docs/LEGAL-SURFACE.md`). Closing it added no new promise — it matched one already given. **WHAT IT COSTS.** The certificate now names a number that must track the ops runbook; the test is what stops those drifting, and it fails until all three agree. The WORDING is still counsel's — the whole `/legal` set carries `{{PENDING LEGAL REVIEW}}` for the same reason — and if counsel rewrites the clause, this entry stands: what was decided is that the fact is disclosed, not the sentence that discloses it. **WHAT REVERSES IT.** Counsel advising that a backup-retention clause misstates our position under DPDP §8(7), or the backup window changing — the latter is a one-constant change plus the runbook, which the test enforces jointly. |
| D-165 ⚠SUPERSEDES D-37's authentication leg; D-37's "our Postgres is the system of record" half STANDS and is what makes this cheap | **Clerk is replaced by a first-party auth module we own — designed in full, proved by a slice, cut over behind a flag.** (The number skips two: D-163 and D-164 were both claimed by branches in flight while this was being designed, and two rows sharing one id make every citation of it ambiguous.) | `docs/AUTH-MIGRATION.md` is the design AND the acceptance criteria — a 26-row capability inventory (§1) naming every behaviour Clerk performs today, where it moves to, and the test that proves it. Shipped with it: migration `e9a4c1d70b52` creating `auth_credentials` + `auth_sessions`, both platform-level (identity crosses tenants) and both FORCE-RLS'd **deny-by-default** under `app.auth`, a GUC only `db/session.credential_session()` sets — so a tenant session, an admin session, a user session, an invite session and a bare untenanted session all see zero rows. `apps/api/authn/`: Argon2id (`m=19456,t=2,p=1`, OWASP's second listed configuration) over an HKDF-derived pepper, and opaque server-side sessions with issue / verify / rotate / revoke, family-wide reuse detection, and per-realm idle+absolute bounds. `verify_session` OWNS its transaction — reuse detection's family revocation is the write the refusal would otherwise roll back, and the second-session fix would be a pool-exhaustion lever on a branch an attacker reaches on demand. 49 tests at 100% statement and branch coverage of the new package, with a negative control per security property, the reuse-durability one driven red against a deliberate rollback before it was kept. **Mounted on no router**, deliberately. | **RESIDENCY IS THE DRIVER AND IT IS NOT A LEGAL VIOLATION BEING CLOSED — say the smaller true thing.** Hosting is moving to an Indian VPS, and SEC-COMP §4 can already claim every model endpoint is pinned to an Indian region because a guardrail fails the build otherwise. Clerk is the hole in that sentence: identity data — every operator's and every client user's email, name, sign-in events, addresses — sits outside India. DPDP §16 permits transfer except where restricted, so Clerk is lawful; what it is, is a named foreign sub-processor in every client DPA whose incident is our reportable incident, while the rest of the stack is being pinned. Removing it shortens the disclosure and makes a DPDP erasure one database instead of two. **THE LANGUAGE CONSTRAINT IS THE DESIGN CONSTRAINT.** The prior implementation to learn from (raghava-organics, ~8,200 lines) is Node/TypeScript and CLAUDE.md forbids a second backend language, so it is a REQUIREMENTS CHECKLIST, not a drop-in: its frontend hooks port nearly verbatim and its reuse-detection design is adopted, while its `bcryptjs` (two different cost factors, and bcrypt over 256-bit refresh tokens in a per-row loop) is refused with a citation rather than by reflex. **Opaque sessions over JWTs** because revocation must bite on the next request and a denylist is a session table with worse failure modes. **The pepper is HKDF'd from `PLATFORM_KEK`** because the two obvious homes both fail OWASP's "not stored with the hash" test — `platform_secrets` is the same database, and a ninth bootstrap env var breaks the stated rule of `.env.example`. **The realm boundary is the dangerous part** and gets four independent mechanisms, of which one is structural: the realm is inside the token's hash domain, so cross-realm confusion is arithmetic rather than a WHERE clause somebody remembers. **WHAT WOULD REVERSE THIS**: hosting NOT moving to India, or a Clerk India-resident data-residency tier under contract — either removes the driver and leaves only cost, which at zero tenants does not justify owning credential storage. **WHAT BLOCKS IT**: an email transport account (external), the founder's answers to AUTH-MIGRATION §9, and the nginx hostname split, which was "worth doing" under Clerk's JWKS separation and is a PREREQUISITE under cookies. **WHAT WE LOSE AND ARE NOT PRETENDING OTHERWISE**: bot detection, breached-password checks, device management and SOC 2 on the identity layer (§1 C-23…C-25, §7). |
| D-166 | **Restore-drill evidence EXPIRES, and the check that says so is structurally incapable of producing it** | `scripts/check_drill_freshness.py` reads `docs/evidence/restore-drill-<YYYY>-Q<N>.md` — runbook §9's quarterly record — and refuses when the newest one is more than `MAX_QUARTERS_BEHIND = 1` quarters old, is post-dated, carries the template's unfilled `**PASS \| PARTIAL \| FAIL**` line, has no verdict, or records **FAIL**. It is in `make guardrails` and in CI. **The second property is the finding this entry exists for and it is enforced three ways**: the module may import only `ALLOWED_IMPORTS` (no `os`, `shutil`, `subprocess`, `tempfile`, and no path to `scripts.restore_drill`); it may call nothing in `FORBIDDEN_CALLS`; and `check_this_module_cannot_write` walks the file's own AST on every run, with `tests/drill_freshness_guard_test.py` re-deriving the import rule INDEPENDENTLY, planting writers to prove the audit sees them, and snapshotting `docs/evidence/` around a real run. The freshness clock is read off the FILENAME, never an mtime. `restore-drill-local-*.md` — what `make restore-drill` writes into the same directory — is counted, named, and never evidence. Today's verdict is **NOT RUN** (exit 0): no quarterly record exists, so nothing has expired, and the check prints that sentence plus "OPERATIONS §8's backups verified is untick" on every single build rather than passing quietly. | **WHY.** OPERATIONS §6 schedules the drill quarterly and §8 makes a committed record the evidence behind "backups verified"; nothing read those records, so a Q1 PASS would have gone on reading as proof in Q4, about infrastructure since rebuilt and re-credentialed. **AND WHY THE SECOND PROPERTY OUTRANKS THE FIRST.** The reference implementation this idea came from (`docs/evidence/raghava-deploy-teardown.md`) has the expiry check and not the property: `dr-stale-drill-check.js` demands a "production-like" artifact, `dr-ephemeral-pack.js:35-45` writes `status: 'ok'` into that artifact while touching no database, and their CI runs generator then validator in one job — a gate that cannot fail, leaving behind a file that reads as proof a drill happened. Ours is the same idea with the loop cut: the producer (`scripts/restore_drill.py` + a human on real infrastructure) is a different program, and the validator is built so that making it the producer is a diff a reviewer sees. **WHAT IT COSTS.** A guardrail whose green state today is "NOT RUN", which is a state somebody has to keep reading; the mitigation is that the line is printed unconditionally and names the untick gate. And a filename convention is now load-bearing: a quarterly record misnamed is reported, not silently ignored. **WHAT CLOSES IT.** The first quarterly drill — blocked outside this repo on a scratch host, an R2/offsite credential and the age identity (runbook §1), which is the same blocker `infra/backup/README.md` §9 already carries. **WHAT REVERSES IT.** A change to the ops calendar's cadence, which moves one constant; or a decision that PARTIAL should not reset the clock, which moves `ACCEPTED_VERDICTS`. Neither may be resolved by letting the check write. |
| D-167 | **The deploy pipeline's four host-side gaps close together: a disk ladder, a certificate reload hook, a daily hygiene timer, and one argument-free root grant** | (1) `scripts/deploy/docker-reclaim.sh` replaces the deploy's single 3GB refusal with an escalation — tier 0 always (our compose project's stopped containers, dangling images, build cache capped at 3GB), then below `RECLAIM_PURGE_FLOOR_GB` = 8, in order and stopping at the first rung that clears the floor: whole build cache -> per-commit images beyond the newest -> every unreferenced image; below `RECLAIM_REFUSE_FLOOR_GB` = 3 it refuses, before anything is built, migrated or swapped. (2) `--deploy-hook "systemctl reload nginx"` moves into the `certonly` command itself in DEPLOYMENT §9.5a step 3, with step 5 stating why and two pass conditions. (3) `infra/hygiene/` + `scripts/deploy/host-hygiene.sh`: a systemd timer, unprivileged, idempotent, holding the deploy's `flock`. (4) `infra/privileged/`: `calevate ALL=(root) NOPASSWD: /usr/local/sbin/calevate-nginx-apply ""` and nothing else; `install_nginx` loses all six of its self-composed `sudo` commands and stages into a fixed directory the root script validates. DEPLOYMENT gains §11 and renumbers §4's sequence (a step was inserted at 5). | **Each one is a defect the reference host already paid for, read off `docs/evidence/raghava-deploy-teardown.md` §9 rows 3, 15, 21 and 22.** (1) Their own comment records the deadlock — a post-build prune only runs after a SUCCESSFUL build, so a near-full disk kills the build and wedges every later deploy; ours walked into it from the other side, refusing at 3GB and printing commands for a human. **The rung ORDER is the design**: their version reaches for `image prune --all` first, throwing away the rollback artefact to save a build cache, which is exactly backwards on the day it matters. 8 is kept from their host because it is the only one of the two floors ever observed against a real build; ours has been run on no host at all. (2) `certonly` never touches nginx — that is why §5 forbids `--nginx` — so without a hook a day-60 renewal writes a new certificate that nginx never reads, and the fuse ends 30 days later on a live edge. `--deploy-hook` rather than `renewal-hooks/deploy/` because it fires once per renewed lineage and we have exactly one lineage covering four names; **a plain `certbot renew --dry-run` does not run deploy hooks**, so the pass condition is `--run-deploy-hooks` (present in certbot 5.7.0, absent in 2.6.0 — the introducing release is NOT verified here) or a hand reload. (3) Their cleanup runs from `/etc/cron.daily`, deletes the runner's `_work` under a job in flight, and prunes host-globally on a multi-project host. Ours does none of the three, and the lock is `flock` rather than a pid file because the pid file's cleanup is precisely what does not happen when an OOM takes `next build`. The journal is CAPPED in journald rather than vacuumed daily, which is what removes the last privileged step and lets the timer run as the deploy account. (4) Their grant `NOPASSWD: /usr/bin/rm -rf /var/lib/docker/containers/*` is unrestricted root deletion of any path, because sudo matches arguments as one concatenated string and the wildcard spans `/` and words — held by the account that runs every merged pull request. Inverting it cost the deploy its six privileged commands: the whole backup/test/restore dance moved into the root script, where it belongs, and the deploy's `sudo -n true` probe had to become `sudo -n -l <path>` because a correct policy REFUSES `true` — the probe would have failed a deploy for being configured properly. **WHAT IS UNVERIFIED**: none of this has run on a host (external blockers: a Hostinger VPS, root on it, a domain and issued certificates — teardown §9.1 items 1, 2, 4, 9), `systemd-analyze verify` and `visudo -c` have never been run against these files, and the `--reserved-space` / `--keep-storage` question is answered by asking the binary rather than by pinning a flag that is wrong on some engine either way (moby/moby#50120). **WHAT REVERSES IT**: a host with a different disk moves the two floors, which are environment-overridable constants; nothing else here is a preference. |
| D-168 | **A deploy can now be refused for what its environment SAYS, not only for which keys it has** | `scripts/check_deploy_env.py` is a new preflight that reads the environment the new image will actually run with and reports EVERY problem at once, never a value: the two DSNs must name one database on one host through TWO ROLES; `REDIS_URL` and both DSNs must name hosts a container can reach (`host.docker.internal` per D-26, `redis` by service name); `PLATFORM_KEK` is checked by calling `envelope.build_ring`; a retired key equal to its active generation is refused; the three HMAC secrets must be pairwise distinct and at least `MIN_HMAC_KEY_BYTES`; `AUDIT_CHAIN_SECRET` may not be the constant this repository publishes (D-81); and any value still byte-identical to `.env.example`'s, or carrying placeholder text, is refused. Console-managed keys declared in the environment WARN rather than refuse. Wired at `vps-deploy.sh::verify_bootstrap_env`, in the new image, before migrations and before any swap. `check_env_parity` gains a fifth direction (`preflight_contract_failures`) so the gate can never demand a variable nothing reads, and the Dockerfile finally copies `scripts/` and `.env.example` into the image. | **WHY.** `docs/evidence/raghava-deploy-teardown.md` §2.3 found this to be the one place the other project is genuinely ahead of us: it refuses on `DATABASE_URL` vs `POSTGRES_DB`, on `REDIS_URL` not embedding `REDIS_PASSWORD`, on `JWT_REFRESH_SECRET === JWT_SECRET` and on leftover placeholder text, and we checked PRESENCE and nothing else. Every one of those failures produces a deploy that swaps cleanly and breaks afterwards, which is the class the ordering in §4 exists to prevent and the one thing that ordering could not see. **THEIR CHECKS ARE NOT TRANSLITERATED.** We have no `POSTGRES_DB` and no `REDIS_PASSWORD`; what we have is `DATABASE_URL`/`ALEMBIC_DATABASE_URL` (one database, two roles — hard rule 1's RLS rests on the app role being NOSUPERUSER NOBYPASSRLS) and a redis with no published port, so the equivalent refusals are agreement between the DSNs and reachability from inside a container. Their half-configured-integration refusal is deliberately NOT copied: ours live encrypted in the console store (D-95), so an absent vendor key is what a CORRECT host looks like and `/healthz/ready` already owns that question. **THE SEAM WAS BROKEN BEFORE THIS AND NOBODY COULD HAVE KNOWN**: `vps-deploy.sh` already invoked `python -m scripts.deploy_revision_check` and `python -m scripts.seed` against an image that has never contained `scripts/`, so both would have died with `No module named 'scripts'` on the first real deploy. **WHAT IT COSTS.** The host-side `grep`s in `preflight()` stay, because DEPLOYMENT §2 says Python is not on the host and a pre-build refusal is worth keeping — the split is by WHEN a question can be answered, not two implementations of one check, and both files say so. Every refusal is declared in `REFUSAL_CODES` and `tests/deploy_env_preflight_test.py` fails if one of them has no negative control, because a check that cannot fail is worse than no check. **WHAT REVERSES IT.** A topology change: containers reaching Postgres by some route other than `host.docker.internal`, or a redis published on a host port, would make the two reachability refusals wrong — and each is one named check with one test, not a rule spread through a shell script. **NOT VERIFIED IN AN IMAGE**: the container registry is unreachable from this build environment (403 on both ghcr.io and docker.io blobs), so the Dockerfile change is proven only to the extent that `.dockerignore` admits both paths, which was checked against a locally cached base image. |
| D-169 | **An observability readiness LADDER, and configuration validity is where it stops — reachability becomes OPERATIONS §2 gate 15** | `apps/api/core/observability.observability_readiness` returns a `ComponentReadiness` per component with three rungs — `skipped` (not configured: clean skip, printed rather than silent), `ready`, `misconfigured` (named, by field) — and `scripts/check_observability_ready.py` runs it in `make guardrails` and CI. **sentry**: DSN shape (scheme, public key, host, numeric project id, no query), `sentry_sdk.utils.Dsn` when the optional group is installed, the SDK's presence at all, and `RELEASE_VERSION` still `dev` off `local`. **otel**: endpoint absolute/http(s)/no query, not already ending in `/v1/traces` (the exporter appends it), sample ratio not 0.0, SDK + OTLP-HTTP exporter installed. **langfuse**: no settings field exists (D-49), so the rung asks the TREE — an import, a dependency or a `langfuse`-shaped field — and fails on arrival, because the v3 SDK is its own OpenTelemetry pipeline and a direct client is a second, UNFILTERED exporter carrying the extraction prompt. On top, two syntax-decidable rungs hold hard rule 6's export filters in place: `sentry_sdk.init`'s `before_send`/`before_breadcrumb`/`send_default_pii`/`max_request_body_size` and the absence of `traces_sample_rate`, and every span processor in `init_tracing` being fed `_RedactingSpanExporter`. The same predicates run at BOOT (`observability_component_misconfigured`, codes only, never values), and `init_observability` now survives a malformed DSN instead of dying on `BadDsn`. Exit 2 = refused, when no `Settings` can be built at all. | **WHY.** Sentry initialised whenever a DSN was present and the ops console accepts one (D-95/D-101, `sentry_dsn` is `on_restart`), but nothing anywhere verified that error reporting reached anything: a typo'd project id, a `sentry-sdk` left in its dependency GROUP, a collector endpoint pasted from the signal-specific env var, or a 0.0 sample ratio each produce a process that reports nothing and says nothing — and every one of them is type-valid, so `check_config_applies`' bounds cannot see them. The middle rung is the whole point; off is already loud and on-and-working needs nothing. Worse, a malformed DSN did not degrade at all: `sentry_sdk.init` raises `BadDsn`, only `ImportError` was caught, and one wrong character in a console field would have stopped voice-runtime booting — contradicting this module's own docstring on the machine whose job is answering a telephone. **WHY THE LINE IS DRAWN AT CONFIGURATION.** The reference's `otel-readiness-check.js` probes the collector with a HEAD request; we cannot and must not fake that from a build container, and a check that "verified" a vendor by parsing a string is exactly the silent premise D-31/D-32 exist for. So delivery — a Sentry event accepted, a collector answering, a human seeing either — is **gate 15** in OPERATIONS §2, beside gate 14, on our own accounts. **WHAT IT COSTS.** Two rungs read `observability.py` by AST, so moving that code moves this check with it; both say so loudly instead of passing when they cannot find their subject. And a green run means "nothing here can be shown to be broken", never "errors are arriving" — the check prints that distinction every run. **WHAT REVERSES IT.** Gate 15 passing does not retire the ladder (configuration drifts after a gate is ticked); restoring Langfuse needs its own entry and the OTLP export path, which is what the langfuse rung refuses without. |
| D-170 ⚠SUPERSEDES D-165's cutover framing and its TOTP leg | **The first-party auth module is finished, mounted and is now the ONLY authentication this product has — and its second factor is an emailed one-time code, not TOTP.** | Migration `b3d9f6a2c815` adds `auth_email_tokens` (verification / password-reset / invite / admin-bootstrap, purpose inside the hash domain) and `auth_otp_challenges` (six digits, ten minutes, five guesses on the row), both FORCE-RLS'd deny-by-default under `app.auth` exactly as `e9a4c1d70b52`'s pair; plus `users.email_verified_at`, `admin_users.email` (unique on `lower(email)`) and NOT NULL dropped from both `clerk_user_id` columns. `apps/api/authn/` gains `codes` (keyed hashing + code minting), `otp`, `tokens`, `subjects`, `throttle`, `cookies`, `service`, `invitations`, `bootstrap` and `routes` — **24 endpoints mounted** across `/v1/auth/admin/**` and `/v1/auth/client/**`, two independent routers from one factory whose realm is a closure constant rather than a runtime branch. `Settings.first_party_auth_enabled` defaults **TRUE** and is a kill switch, not a cutover flag. `apps/workers/auth_email.py` delivers every one-time secret through the existing outbox + transport, registered in `FUNCTIONS`. | **WHAT "MFA" MEANS HERE, because a reader will otherwise go looking for an authenticator app: a six-digit code emailed to the address on file, and nothing else.** TOTP, recovery codes, an encrypted `auth_mfa_secrets` table and a KEK-wrapped seed were all BUILT and then REMOVED on the founder's decision that the second factor should be "nothing more than entering an OTP" — removed rather than left unmounted, because a table nothing writes and a module nothing imports are the half-wired defect `check_wiring` exists to catch. **The honest cost, stated rather than discovered:** the strength of the admin realm's second factor is now the strength of the operator's mailbox. It stops a stolen password and does not stop a compromised email account, which a TOTP secret would. **What it buys:** one mechanism instead of three (secret storage, enrolment, recovery), no device to lose, no enrolment step between a bootstrap and a usable console — and it closes task #66, whose complaint was that admin MFA is called mandatory in two documents and exists nowhere. `MFA_REQUIRED_REALMS` is asserted equal to `core/auth.py`'s copy so the sign-in path and the verifier cannot disagree. **CLERK IS NOT A PEER AND IS NOT COEXISTING** — D-165 designed a flag-gated cutover beside a live vendor; the founder has since decided Clerk is removed entirely, so this is built as the system of record and the flag's remaining job is an incident switch. Clerk code paths are untouched and still green in this change; deleting them is the next slice, and what becomes dead is named in that slice's brief. **The five defects of the reference implementation are each designed out with a named test** (`tests/authn_codes_test.py`, `authn_enumeration_test.py`): bcrypt's 72-byte truncation (Argon2id, proved by two 128-character passwords sharing a 72-byte prefix), unsalted SHA-256 over a 6-digit OTP (HMAC under a `PLATFORM_KEK`-derived key that is not in this database, proved against a built rainbow table), `randomInt`'s exclusive maximum (the generator's image is asserted to include `999999` rather than sampled), a reset token outliving its account (one `Subject | None` resolver, no code path reads `users` directly), and `check-identifier`'s enumeration oracle (uniform status, body AND measured timing, with an unknown address consuming a real throttle budget against a derived pseudo-subject). **Two real bugs were found BY these tests and are worth recording**: `str.isdigit()` accepts non-ASCII digits, so a Devanagari code reached `hmac.compare_digest` and raised an unhandled 500 on an authentication route — in a Telugu-first product sold in India; and `created` is a reserved `LogRecord` attribute, so the bootstrap's own log line crashed it. **WHAT WOULD REVERSE THE OTP DECISION**: a client or a regulator requiring a phishing-resistant factor on the operator console, at which point TOTP (or WebAuthn, which is the better answer if it is being built fresh) is a migration written then. **WHAT IS STILL NOT BUILT**: the signed double-submit CSRF token (half of it is `apps/web`, out of scope; `Sec-Fetch-Site` + `Origin` + `SameSite=Strict` ship), breached-password and bot checks (D-165 §7 accepted these), and the frontend. |
| D-171 | **The first administrator arrives by emailed invite, and the bootstrap refuses to be a back door** | `scripts/bootstrap_admin.py` is rewritten: `--clerk-user-id` is DELETED and it takes `--email`, minting an `auth_email_tokens` row of purpose `admin_bootstrap` and mailing a single-use setup link through the existing transport. `apps/api/authn/bootstrap.py` holds the logic; `POST /v1/auth/admin/bootstrap/confirm` is the redemption half, declared on the admin router only so the client realm answers 404 rather than refusing. **TTL is 60 minutes**, a named constant in `tokens.TOKEN_LIFETIMES`. | **WHY A SCRIPT AND NOT A ROUTE, still**: a bootstrap ENDPOINT is a route that exists to be unauthenticated exactly once — a window an attacker races on a public host, and permanent code whose whole value is being unreachable. Whoever runs the deploy already holds database credentials. The REDEMPTION half is a route and is not the same hazard: it needs a 256-bit single-use token mailed to an address the deployer named. **THE TTL IS ARGUED, NOT INHERITED.** The reference implementation's `admin-newuser.mjs` uses `INVITE_TTL_MS = 10 * 60 * 1000`; ten minutes is defensible for an invite handed to somebody watching their inbox and wrong for a deployment bootstrap, where an expired link means NO WAY INTO THE PLATFORM at all — every admin route 403s until an operator exists. Sixty minutes covers a deploy window plus greylisting's classic 15-minute retry, matches what a password reset gets (both set a password on an existing account), and is bounded by the fact that an unconsumed link is the only way in, so one that outlives the deploy is a live path to the widest authority in the system. Re-running is the recovery and is safe by design. **IDEMPOTENCY IS "REFUSE", AND THERE IS DELIBERATELY NO `--force`.** Three states: no operator → create and mail; the named operator exists with no password → mail a FRESH link, retiring the previous one (a resend, not a second account — the reference's reactivation case); any operator anywhere already holds a password → refuse with `already_bootstrapped` (409). A flag that mints an unattached administrator from a shell is exactly the back door this is not, and it would be reachable by anyone who has ever held database credentials — a contractor, a restored backup, a compromised CI runner. The check and the insert share one transaction under `LOCK TABLE admin_users IN SHARE ROW EXCLUSIVE MODE`, because the invariant is "zero rows match" and there is no row to compare-and-swap on. **NO PASSWORD IS PRINTED, GENERATED OR DEFAULTED.** The reference's `seed-admin.mjs` logs `Admin created: ${admin.email} / ${PASSWORD}` — a hard rule 6 violation on its face that `check_redaction_exposure` would fail the build on, and a fixed password in a repository is a backdoor with a changelog entry. What is printed is the LINK; locally `ConsoleTransport` also logs the mail, so a developer needs no special path and mail delivery failing is NOT fatal (the mail credentials are themselves stored by an operator, in the console — a bootstrap that required SMTP would be a chicken-and-egg). **AUDITED**: `auth.admin_bootstrapped` at mint (naming the address, through `redact_mapping`) and `auth.admin_bootstrap_completed` at redemption — the most privileged act in a deployment's life leaves a record of when and to whom. The redemption endpoint additionally refuses an account that ALREADY has a password, so a leaked link from a finished deploy opens nothing even before it expires. **WHAT CLOSES THE REMAINING GAP**: an email transport account (external blocker, named in AUTH-MIGRATION §9 Q2) — until one exists the link is delivered by the operator reading it off their own terminal. |
| D-172 | **Raw SQL must be assembled from text we typed, and where a fragment is passed in the CALLERS are read** | `scripts/check_raw_sql.py` (in `make guardrails` and CI) walks every `text(...)` in `apps/` and `packages/` — 493 statements — and requires the expression to be *literal-derived*: a constant, a name resolving to constants across module and package boundaries, an f-string/`%`/`.format()`/`.join()`/comprehension whose parts all are, a call to one of our own functions whose every `return` is, or a call to a declared identifier validator (`_identifier`). When it reaches a function PARAMETER it does not give up and does not wave it through: it finds every call site of that function in the tree and applies the same rule to the argument, recursively. `exec_driver_sql` and `literal_column` are refused by name so a second door cannot open. `tests/` and `alembic/` are out of scope, stated in the docstring with the reason. Negative controls in `tests/raw_sql_guard_test.py`, including a two-step control that pins what the validator buys: a runtime table name is clean WITH `_identifier()` and reported WITHOUT it. | **The premise was written down and enforced by nobody.** `billing/plans.py::plan_in_effect_sql` interpolates its `columns` and `at` arguments straight into a SELECT and its own docstring says *"a caller that passes a user string is writing an injection, and no caller does"* — a true sentence about today's six callers and a promise about every future one, kept by review alone. `db/transition.py` interpolates a table name and a status column, which is correct because it validates them, and nothing checked that the validation stayed. **The blast radius is what makes this worth an AST walk rather than a grep**: hand-written SQL is how most tenant-scoped access here is expressed, and it executes as a role that is `NOSUPERUSER NOBYPASSRLS`, so a successful injection sits inside a session whose `tenant_id` GUC decides visibility — one `SET LOCAL` from every tenant's leads. That is hard rule 1 failing, not one account leaking. **WHAT IT COST.** Four resolver features the tree actually needed — relative imports, tuple destructuring, `enumerate` indexes, and separating the ITERATION question from the INDEXING question on a dict, because `transition_status` builds `{":__t_from0": state}` and splices the keys while binding the values, and a single verdict for that dict is wrong in one direction or the other. Two call-name indexes cut the run from 23s to 2.5s. **WHAT IT DOES NOT DO**: no taint tracking from request models — the rule here is *stricter*, refusing all runtime values including safe ones, and a narrower rule would have to be right about where data comes from while this one only has to be right about what a literal is. `SPLICE_ALLOWANCES` is EMPTY and is meant to stay so; an entry is a standing promise to re-read every future caller, which is the promise this file exists because nobody keeps. |
| D-173 | **The unauthenticated surface is ENUMERATED, because a path prefix is default-open** | `scripts/check_public_routes.py` (in `make guardrails` and CI) takes the routes `core/rbac.assert_policy_registry_complete` skips — the ones matching a `PUBLIC_PREFIXES` entry — and asks five questions of the live app: every one is declared in `UNAUTHENTICATED_ROUTES` with a reason; every declaration matches a live exempt route (the registry may only shrink); every `PUBLIC_PREFIXES` entry covers at least one live route; every MUTATING exempt route names the credential it verifies instead of a session, and that symbol is present in the module serving it; and no exempt route carries `x-calevate-permission`, which would read as protection in the OpenAPI schema and the generated client while the registry never looks at it. Ten routes today. Negative controls in `tests/public_routes_guard_test.py`, including one that mounts a route on the REAL app under `/v1/auth/` and takes it away again, plus a partition test proving the guarded and public sets cover the route table exactly once each. | **The strongest check in the repo opens with `continue`.** The RBAC boot assertion is genuinely excellent — declared AND enforced, the permission must be real and granted to somebody, and it refuses to pass if it checked zero routes — and the first thing it does is skip everything under a prefix. That is fine for a prefix holding one route and dangerous for one about to hold a module: the first-party auth module — designed and landing in a parallel slice, with Clerk still the live authenticator in `apps/api/core/auth.py` on this branch — adds login, refresh, logout, password reset, OTP request/verify and invite setup under `/v1/auth/`, and every one of them would have been exempt on the day it was mounted with nothing in the tree able to notice. **The evidence for that being the real trajectory is in `docs/evidence/raghava-platform-teardown.md` §2**: the reference repo's `route-discipline-check.js` has exactly this shape and its exemption list reached twelve hand-commented entries, none of them ever re-validated against a live route, beside a customer-side rule that is default-OPEN by prefix on the half holding customer PII. **WHAT IT COSTS.** Opening a route now costs a reviewed line instead of nothing, which is the entire point and is also the only thing anyone will feel. **WHAT IT DELIBERATELY IS NOT**: not a second implementation of the RBAC rule (one way per problem — the two partition the table and the test above proves it), not a judgement on whether a credential is STRONG (that is the module's own security tests; a static check answering from a name would be answering from a name), and not applied to `apps/voice-runtime`, whose whole surface is unauthenticated-by-design under hard rule 3. |
| D-174 | **The first-party auth SCREENS, and the nine defects of the reference implementation designed out one by one** | `apps/web/src/lib/authn/` — the realm factory (single-flight rotation, 3s result cache, rotation barrier), the cookie transport and its 429 rung, the RFC-9457 refusal vocabulary, and restore-on-mount with audiences, a 15s deadline, soft-vs-hard failure and a generation counter; `src/components/authn/` — sign-in + emailed code, set-password, reset-request, email verification, the shared session gate, the admin idle modal; and **ten screens** under `src/app/(auth)/auth/` — both realms' sign-in, forgot-password, reset-password and session/account pages, plus client invitation acceptance and the D-171 admin bootstrap. Two module-scoped instances, `adminAuthn` and `clientAuthn`, constructed once each with a literal. | **THE FACTORY IS WRITTEN ONCE AND THAT IS NOT REALM SHARING.** CLAUDE.md forbids the realms sharing session logic and AUTH-MIGRATION §3 says why — "a `realm` parameter on one shared module is one bad conditional away from presenting an admin credential on a client surface". `createRealmAuthn(realm)` has no such conditional: the realm is a **closure constant** fixed at import, a literal in every path, with no request-time input able to move a call between realms — the identical argument `apps/api/authn/routes.py::_realm_router` makes on the server side, and what comes out is two objects with two caches, two restore runtimes and two generation counters. The two SESSION modules are duplicated, neither importing the other, and `tests/authnSourceGuards.test.ts` pins all of it: the factory is called exactly twice, with literals, and each realm's module names exactly one instance. **A ROTATION BARRIER, WHICH THE REFERENCE DOES NOT HAVE.** §5.2's single-flight plus result cache is necessary and here insufficient: their refresh is a token exchange, ours calls `rotate_session`, which supersedes under CAS with **no concurrency grace window**, on the stated premise that rotation is "never a burst of background fetches". Presenting the superseded token is not staleness — it is `reuse_detected` and the whole family is revoked. So every call in a realm waits on an in-flight rotation before dispatching (bounded at three turns; a hang is worse than an ordering risk), and the transport deliberately has **no refresh-on-401 rung**: ours would be a second, uninvited rotation caller whose failure mode is every session the person holds revoked as theft. **THE NINE §5.7 DEFECTS, EACH WITH A TEST ASSERTING THE OPPOSITE.** (1) the restore deadline's `setTimeout` is cleared in a `finally` and touches no rotation state at all — theirs fires 15s later and nulls `refreshInFlight`, reintroducing the double-consume the single-flight exists to prevent; (2) **no enumeration oracle** — the server equalises status, body and wall-clock time, and the sign-in screens render a FIXED sentence chosen by problem code, so two different upstream bodies produce character-identical DOM and the typed address is never echoed; (3) no `devOtp` field, no `process.env` branch and nothing auto-filled, enforced by reading this directory's source; (4) one `useCountdown` keyed on an absolute deadline, and no hand-rolled interval survives anywhere in the surface; (5) the password is cleared the instant step one succeeds, which our backend makes possible because `/login` sets the cookie on the `otp_required` branch and `/login/otp/resend` therefore takes NO BODY — the cookie IS the short-lived challenge §5.7 asks for; (6) no credential is compared in the browser (the set-password confirmation compares two strings one person just typed and authenticates nothing); (7) no token is decoded and no claim is trusted — we hold none, and the idle modal accepts an extension only on the server's freshly re-read `SessionOut`; (8) `MIN/MAX_PASSWORD_CHARS` are read back out of `apps/api/authn/hashing.py` by a test and the hasher is asserted to be Argon2id; (9) unreachable and signed-out are different screens with different remedies, `status: 0` carrying the distinction. **TWO DEFECTS FOUND IN OUR OWN FIRST DRAFT, AND WORTH RECORDING.** The idle hook dismissed its warning on ANY activity, and `click`/`mousemove` are activity — so the pointer moving towards "Stay signed in" closed the dialog before it could be pressed, and pressing it closed the dialog on the same click that began the extension, hiding the outcome including the failure an operator most needs to see. The warning is now sticky and an explicit `resume()` restarts the clock: the standard shape (react-idle-timer's `promptBeforeIdle`, design.cms.gov's idle-timeout), right for the durable reason that extending a session is a decision and a stray mouse movement is not. And `signInMessage` indexed its copy table with a wire string, walking the prototype chain — routed through `lib/lookup.ts`, this repo's one answer to that class. **WHAT IS NOT BUILT, NAMED RATHER THAN DISCOVERED.** `Idempotency-Key` is sent by both reset forms and is **inert today**, because `/v1/auth/**` does not take the reliability dependency; the signed `X-CSRF-Token` has no server half to answer to (AUTH-MIGRATION §11). Both are backend changes this slice does not own, and the client-side one-in-flight-submit guard is what protects a user meanwhile. The screens stand BESIDE Clerk rather than replacing it: `apps/api/core/auth.py` does not yet read the first-party cookie, so `/admin` and `/c/<slug>` still authenticate through the vendor while these pages sign in, hold, extend and end a first-party session on their own. The day the verifier reads the cookie, the two providers move up into the two layouts and these become the session-management screens they already are. |
| D-175 | **An idempotency scope may never be attacker-choosable — audited, found CLEAN, and made checkable rather than left as a habit** | New `scripts/check_idempotency_scope.py` in `make guardrails` AND in CI, with 18 negative controls in `tests/idempotency_scope_guard_test.py`. Four properties: `reliability.scope_key` keeps its keyword-only `tenant_id: UUID \| None` / `user_id: UUID \| None` signature; `scope_key` is the only producer of a `claim_idempotency(scope=)` (through at most one local binding, which `billing/payment_routes.py` needs); its arguments are plain id references, never a call, a subscript or a literal; and no socket peer reaches ANY replay key — `claim_idempotency(scope=)`, `claim_inbox_event(provider=, event_key=)` or `enqueue_outbox_once(dedupe_key=)`. `check_audit_ip._is_peer_read` is promoted to `is_peer_read` and IMPORTED rather than restated. `enqueue_outbox_once` added to `reliability.service.__all__`. NO code path changed. | **THE FINDING WAS SOMEBODY ELSE'S, AND THE ANSWER HERE IS NO.** A teardown of a reference platform whose idempotency design is ours line for line — same `(scope, route, method, key)` tuple, same body hash, same 24h TTL — found its scope falling back to `anon:{fingerprint(request.ip)}` for an unauthenticated caller. Behind nginx or a CDN that is the EDGE's address unless proxy trust is exactly right, and the payoff is not a bad log field: it is caller A being served caller B's stored `response_payload`, before any tenant-scoped query runs, so no RLS policy is in a position to stop it. **We do not have it, and the evidence is exhaustive rather than reassuring**: `scope_key` has no fallback branch at all, and all four `claim_idempotency` call sites (`crm/routes.py` ×3, `billing/payment_routes.py` ×1) pass ids off a verified `Principal` or a tenant id resolved from a signature-verified payment. The other replay namespaces were audited to the same standard and are provider-issued, not caller-chosen: the inbox keys on Clerk's svix-id, Razorpay's payment id, Meta's leadgen id under a per-webhook-id provider, and the engine's `execution_id:raw_status`; the outbox's three `dedupe_key` spaces are built from uuid7 ids inside workers that never see a request. **THE DELIVERABLE IS THE GUARD, NOT A FIX** — CLAUDE.md's rule for an audit that finds nothing, and the reference project's fallback was also absent once. **THE TYPE IS THE FIRST LINE OF DEFENCE and the check's job is to keep it**: because both parameters are `UUID`, mypy strict already refuses `scope_key(tenant_id=request.headers.get(...))` at every call site in the repo — a header is a `str` — so widening one annotation would retire the guarantee everywhere at once with no call site changing, which is precisely the edit the signature property fails on. **WIDER SCOPE THAN `check_audit_ip` ON PURPOSE**: that guard asks "who is the caller?" over `apps/api`; this one asks "what may a replay key be derived from?" and must reach `apps/voice-runtime` and `apps/workers`, which hold inbox and outbox call sites it deliberately does not scan. **The `Idempotency-Key` header is deliberately NOT guarded** — it is the caller's to choose by contract, and is safe only because the scope beside it is not; a control pins that a guard banning it outright would be banning the feature. **What would reverse it**: an unauthenticated write that legitimately needs idempotency (a password reset, an OTP request) has no principal to scope by, and the answer is a server-derived subject, never an address — the guard refuses the address either way. |
| D-176 | **Our own twenty guardrails were audited against the rule D-166 wrote, and six of them could pass while seeing nothing** | `docs/evidence/our-gates-audit.md` asks three questions of every `scripts/check_*.py` — what makes it fail, whether anything on its own run path produces the evidence it reads, and whether it can pass vacuously — and records the answer per check with `file:line`. **Nothing in this repo has the reference's closed loop**: no check reads an artefact its own pipeline wrote (`check_openapi_fresh` compares a COMMITTED snapshot against a freshly built app, and `--write` is run by a human and by no gate; `pnpm -C apps/web gen:api` READS that file and never writes it). SIX vacuous passes were found and closed. `check_metadata_columns` printed `OK (61 tables agree in both directions)` against an EMPTY database, because `compare_metadata` reports an absent table as one `add_table` op rather than one `add_column` per column — it now REFUSES (exit 2) naming every table the models declare and the database lacks. `check_wiring`'s router half reported no offenders when `declared_routers()` found none, so a moved `apps/api` would print `OK (0 routers all mounted)`. `check_env_parity`'s third direction is an `rglob` search that yields nothing, without raising, over a directory that has been renamed. `check_config_applies` iterates a set DERIVED from `Settings` by exclusion, and four checks over an empty set are four green ones. All four now open with a `blind_spots()` section in the shape `check_docs_drift` and `check_web_env_parity` already had. **Two more were found by VERIFYING that list rather than trusting it**, and both had been cleared by READING in the first pass: `check_ledger_immutability`'s check 1 — hard rule 4's code half — returns `[]` from `check_sources(root=<empty dir>)` and prints `no mutating statements in app code` having read no code, anchored only by an exception registry whose correct end state is empty; and `check_observability_ready`'s langfuse rung asserts an ABSENCE while swallowing a missing scan root with `continue` and a missing manifest with `if manifest.exists()`, so a renamed `apps/` turns “no langfuse import anywhere” into “none anywhere I looked”. The first now has its own `blind_spots()`; the second raises `ObservabilityBlindError` and exits 2 REFUSED. And `test_every_guardrail_script_runs_in_both_gates` — the test that claims both gates run every check — was a substring search over the WHOLE Makefile and the WHOLE workflow, both of them heavily commented and several comments naming the script beside them; it now reads only recipe lines and only parsed `run:` scalars, with a control proving the reduction drops comments. An eighth finding of the same species: ENGINEERING-PRACTICES §2 — the page a reader goes to for what guards what — had a row for TWELVE of the twenty, and `test_every_guardrail_script_is_named_in_the_catalogue` now globs the directory so it cannot fall behind again. | **WHY.** D-166 landed the constraint — a check must be able to name what would make it fail, and that thing must not be produced by the run — and enforced it on the one check it shipped with. The reference repo's four instances (`docs/evidence/raghava-platform-teardown.md` §1.11, §3.1; `raghava-deploy-teardown.md` §5.3) are a PATTERN, not an accident, so the honest next move was to turn the rule on the other nineteen rather than to trust that ours were different. **WHAT THE AUDIT ACTUALLY FOUND.** Not the loop — the second shape, a gate whose evidence can be ABSENT and read as agreement. `check_metadata_columns` is the clean example: pointed at `sqlite://`, a database with nothing in it, `column_differences` returned `[]` and the check printed OK. That is the same failure as theirs from the other side, and it is the one a repo with committed, human-produced evidence is actually exposed to. **WHAT IT COSTS.** Four more sections that can fail for a reason that is not a violation, each with a floor (10 routers, 100 columns, 5 managed settings) that a real deletion could one day cross; the floors are an order of magnitude below today's counts for that reason, and every one names what it thinks went wrong. **WHAT CLOSES IT.** Nothing — it is a property, and the fourteen checks cleared are listed in the audit by name, each with the probe that cleared it, so the next reader inherits the evidence rather than the verdict. **THE PART THAT IS STILL INHERITED**: `check_coverage_ratchet` was read and not run (its plugin refuses a run whose stores were not empty), and `check_rls_coverage`'s empty-database refusal is reasoned rather than observed because the Postgres is shared — and findings 5 and 6 are exactly what reading-without-running costs, since both were cleared by reading before being run. **WHAT REVERSES IT.** A guardrail that legitimately has nothing to scan, which would move a floor to zero; that is a decision-log entry, never an edit. |
| D-177 ⚠SUPERSEDES D-37's authentication leg entirely; D-124 and the identity-mirror race are DELETED rather than superseded | **Clerk is gone. `core/auth.py` verifies our own session cookie, and the first-party module stops standing beside the vendor and starts being the authenticator.** | `core/auth.py` reads the realm's `__Host-` cookie (`authn/cookies.read_token`) and proves it with `authn/sessions.verify_session`; `VerifiedToken` becomes `VerifiedCaller` carrying `subject_id` — `users.id` on the client realm, `admin_users.id` on the admin realm — so both principal loaders look up by primary key and the vendor-subject translation disappears. **Deleted:** `core/clerk_identity.py`, `tenancy/clerk_webhooks.py`, `POST /hooks/v1/clerk` and its `webhook_identity` rate profile, the six `CLERK_*` settings and their `platform_config` rules, `jwks_url` / `missing_realm_separation_keys` / `_jwk_clients` / the `fva` claim reader, `Principal.clerk_user_id`, the Clerk-era `POST /v1/invitations/accept`, `tests/clerk_mirror_security_test.py`, `tests/identity_mirror_race_test.py`, `apps/web/src/lib/auth/**`, the three `(auth)` Clerk route groups and the `@clerk/nextjs` dependency. **Web:** both realm layouts mount D-174's providers and gates, `lib/api/client.ts` sends `credentials: "include"` and makes the bearer OPTIONAL, `lib/auth/mode.ts` survives as `lib/authn/mode.ts` with `clerk` renamed `session`, and `lib/authn/realmSessions.ts` is the one branch on that mode. `PyJWT` stays: `core/impersonation.py` signs the D-22 grant with it. | **THE DEV PATH KEEPS TWO INDEPENDENT GUARDS AND THE SECOND ONE IS NEW.** AUTH-MIGRATION C-21 requires them to survive; one of them was "this realm has no Clerk secret configured", which cannot. Its successor is `PLATFORM_KEK` being unset, and it is the SAME statement — *this deployment holds no real credential material* — but wider: the KEK peppers every password (`authn/hashing.py`), a prod host cannot omit it, and `/healthz/ready` reports it. So one mis-set `APP_ENV` can no longer switch the dev path on, which was the entire point of there being two. The subject also stops being caller-chosen: `dev:<realm>:<subject-uuid>`, and `dev:admin:anyone` no longer parses. **WHAT CREATES AND UPDATES `users` AND `admin_users` NOW, because deleting a mirror deletes a writer.** `users`: `authn/invitations._find_or_create_user`, on invitation redemption, in the transaction that also sets the password and creates the membership. `admin_users`: `scripts/bootstrap_admin.py` → `authn/bootstrap.py` for the first operator, and the ops console for the rest. `organizations` NEVER had a Clerk writer — the webhook answered `ignored` to every org event (D-10, admin-driven onboarding) — so nothing there moved. **The one writer with no successor is `user.deleted` → `users.deactivated_at`**, and that is correct rather than orphaned: an account is ours to end now, `POST /v1/members/{user_id}` removal and the operator surfaces set the column, and `authn/subjects.py` reads it as the client realm's whole liveness rule. **TWO ENDPOINTS BECAME ONE, and the collapse changed a security property rather than merely tidying.** `POST /v1/invitations/accept` compared the invitation's address against the address the caller had signed in with, and refused a mismatch (`invitation_wrong_recipient`). The first-party route takes the address FROM the invitation, so there is no comparison to get wrong — and no requirement that the redeemer already control a verified mailbox at that address. A forwarded link now yields an account for the invited address to whoever opens it first; what it buys is that an honest invitee whose mailbox differs from their old vendor account can no longer be locked out by a refusal they cannot act on. Single-use, hash-at-rest, 72h and CAS-burned are what bound the window, and they are untouched. `tests/member_invitations_test.py` carries that trade in the test that used to assert the refusal. **`mfa_claim_missing` AND `mfa_required` COLLAPSE INTO `second_factor_required`** — the code `authn/routes.py` already raised for the identical state. The pair existed because a custom JWT template could silently drop `fva`, so "we cannot tell" and "you did not" were genuinely different facts; `auth_sessions.mfa_verified_at` has no third state. It is `401 auth`, not `403 permission`: a half-MFA session is half-AUTHENTICATED, and the difference is what sends an operator to finish signing in rather than to ask somebody for a role. **`clerk_user_id` IS NOT DROPPED** (hard rule 8): both columns stop being written and stop being read — the ops console's "who set this key" fallback moves to the admin id, which is present for every operator rather than only Clerk-era ones — and both are recorded in `check_wiring.UNWIRED_BASELINE` with the DROP migration named as what closes them. **WHAT THIS CHANGE MAKES UNTRUE AND DOES NOT FIX**: `apps/web/src/lib/legal/{cookies,privacy,subprocessors}.ts` and the published DPA name Clerk as a sub-processor and as where passwords are held. Both are now false and both are DISCLOSURES — removing a named sub-processor from a client DPA is counsel's call, not a refactor, and the whole `/legal` set already carries `{{PENDING LEGAL REVIEW}}`. Named here so it is a deferral with an owner rather than a stale sentence. **WHAT WOULD REVERSE IT**: nothing short of a decision to buy authentication again; the Clerk applications' sessions are dead and a rollback is a restore, not a rollback (AUTH-MIGRATION §5 step 6 said so before it was run). |
| D-178 | **AUTH-MIGRATION §11's four named gaps, closed — three built, one retired, and one decided against with the guard left intact** | Migration `c7a1e93d40b8` (parent `b3d9f6a2c815`): `uq_users_email_lower` — `UNIQUE (lower(email)) WHERE deactivated_at IS NULL` on `users`, with a pre-flight that refuses BEFORE writing and prints colliding row ids and never addresses (hard rule 6) — plus a third `auth_otp_challenges.purpose`, `step_up`. New `apps/api/authn/stepup.py`; `core/stepup.py` becomes a `Depends(step_up_gate)` resolving a frozen `StepUp` whose synchronous `require` demands BOTH halves at all 15 call sites — a dependency rather than an inline await, because two of those sites hold an open `tenant_session` and `db/session.py`'s `max_overflow=0` is safe only while no path holds two. Two new endpoints, `POST /v1/auth/admin/step-up{,/verify}`, admin-realm-only by declaration. `authn/cookies.enforce_same_origin` stops exiting early on `Sec-Fetch-Site: same-site`; `core/middleware.CookieCsrfMiddleware` applies the same rule to every mutating request carrying a session cookie. `authn/invitations._find_or_create_user` moves to `ON CONFLICT`. Four new test files (49 cases); AUTH-MIGRATION §1 C-09/C-22, §2.2, §10.5 and §11 rewritten. | **STEP-UP (C-09) — BUILT, BECAUSE THE OBJECTION EXPIRED.** `core/auth.py` records why freshness was not enforced on the Clerk credential: Clerk models it as "reverification", this repo had no browser flow to raise the prompt, and "gating an incident lever on a flow that does not exist would be a control that gets switched off". D-170's emailed OTP IS that flow. So a dangerous mutation now needs INTENT (the `X-Confirm-Action` echo, which a stolen cookie satisfies trivially — the refusal prints the string on purpose) and PRESENCE (`mfa_verified_at` under five minutes). Five minutes because an operator working a runbook must not be challenged twice inside one task, and because a window at or above the 30-minute idle bound would be satisfied by every live session — a control that never fires. A request bearing no first-party session is NOT refused, and that is not the "passes when absent" hole: freshness is a property of a CREDENTIAL, the Clerk token has its own gates, and deleting Clerk leaves no other credential, so the branch becomes unreachable rather than permissive. **CSRF DOUBLE-SUBMIT — RETIRED, AND IT UNCOVERED A REAL HOLE.** OWASP's cheat sheet names five conditions for origin verification plus `SameSite` being sufficient without a token; we met four, and the one we did not — no shared registrable domain — was live: three consoles under `calevate.tech` mean any `*.calevate.tech` host issues `same-site` requests carrying the cookie, and `enforce_same_origin` returned before consulting the allowlist. `SameSite=Strict` does not stop it and `__Host-` stops a sibling SETTING the cookie, not the browser sending it. Checking `Origin` unconditionally is the same defence a token would have given against the same attacker, with no secret to distribute and no "absent" branch. The middleware exists so the sentence stays true of the whole API the day cookies authenticate the whole API. **`Idempotency-Key` ON `/v1/auth/**` — DECIDED AGAINST.** D-175's guard requires a scope derived from a principal or a tenant; the auth routes a double submit could hurt are the unauthenticated ones, which have no principal, so the only candidate scopes are the typed address and the socket peer — the second being D-175's finding verbatim. Weakening the guard to fit was not on the table. Each route's own single-use CAS is what answers the double submit, and it is a stronger statement than a replay cache: exactly one of two concurrent submissions wins at the database. The 422 the loser gets is correct — nothing can tell a double-click from a replayed leaked link. **`users.password_migrated_at` — VESTIGIAL.** D-170 makes first-party auth the only authenticator; there is no population of Clerk-era hashes to migrate and `auth_credentials.password_set_at` already carries the fact. **THE INDEX'S PREDICATE IS THE ONE DEPARTURE FROM `admin_users`** and it is what makes the two express one rule: "at most one LIVE account per address" is what both readers already assumed, and `admin_users` has no soft-delete to predicate on. Without it an owner who left and came back could never be re-onboarded. **WHAT REVERSES ANY OF IT**: a same-site attacker who can forge `Origin` (browsers do not permit it) would re-open the token question; an unauthenticated auth route that acquires a server-derived principal would re-open the idempotency one. |
| D-179 | **Four DPDP findings that were ours: the two stores with no retention clock, the breach procedure behind the 48-hour promise, and the caller notice a client cannot write without us** | Migration `c4d1f7b83e26` widens `retention_policies.data_category` to six with `engine_payload` (90 d, the number `infra/object-lifecycle/policy.json` already assigned that prefix) and `kb` (365 d, the transcript default). Two sweep arms: archived vendor documents page expired calls into the SAME `_erase_engine_payloads` the erasure uses; superseded and rejected knowledge versions are deleted, never the live one and never one still carrying an engine handle. `execute_deletion_request` now SEARCHES the knowledge base for the subject's number, digits-normalised, and reports the count — so `KB_OUTCOME` narrows from `not_searched` to `searched_not_erased` and both halves of the index-aligned register move with it. `runbooks/data-breach-notification.md` + `apps/api/compliance/breach.py` + `scripts/breach_notice.py` are the Rule 7 procedure and its renderer. `GET /v1/compliance/caller-notice` generates a client's caller-facing notice draft from their own extraction schemas, retention rows and D-163 announcement switches. | **WHY.** `docs/LEGAL-SURFACE.md` F-2, F-3, F-6 and F-8 are the four findings of that audit that needed nothing from outside this repository. F-2 and F-3 are DPDP §8(7) exposures that err in the dangerous direction — a copy of tenant personal data outside every policy a tenant can set, expiring only for the handful of people who file a §12 request. F-6 was a clock in a signed contract with no procedure behind it. F-8 is the one that is a product feature as much as a compliance fix: the itemised list Rule 3 demands IS the client's extraction schema, which only we hold, so "the notice duty is yours" was true and unhelpable. **WHAT IT COSTS.** Two more per-tenant numbers to honour, and a narrowing of the erasure certificate — which is the direction SEC-COMP §4 does not permit lightly, so it is earned by two mechanisms rather than by rewording. The KB search reads client content on every erasure, which is why nothing about it is logged. The caller-notice draft is a document a client publishes: it is marked DRAFT in the envelope AND in the text, every blank only they can fill is a visible `{{...}}`, and an agent with its AI announcement switched off changes what the draft says rather than being absorbed. **WHAT IS DELIBERATELY NOT DONE.** `campaign_contact` is still not a category — how long a client's own uploaded list is kept is a DPA commitment whose number is the founder's, and `tests/dpdp_known_gaps_test.py` keeps probing this constraint to hold that gap open. The erasure does not EDIT knowledge content: editing a live price list changes what the agent says on the next call and the platform holds its own copy, so the certificate names a manual step on both. The breach tooling sends nothing. **WHAT CLOSES THE REST.** The Board's own reporting channel (a lookup, nobody's blocker but nobody's yet) and counsel's review of the notice wording — both stated in the runbook's §7 rather than left to be discovered at 4am. |
| D-180 | **The host is a Hostinger India VPS — a founder decision, and the first thing that could make a residency claim true rather than aspirational** | Hosting for the site stack (web, api, workers, voice-runtime, Postgres, Redis) is a Hostinger VPS in India. It supersedes D-25's "Hetzner-class, India co-location NOT required" as to the PROVIDER and the REGION; it does not supersede D-25's reasoning, which was that co-location is not *required* for a stack outside the live-call path — that stays true, and this decision is the founder buying something D-25 said was optional. **Nothing about it is provisioned**: `infra/` still contains no host, no network and no DNS, and `terraform validate` has never run (`infra/README.md` §5). | **WHY.** F-1 is the founder decision the whole residency question hangs on, and it was open in both directions: the landing page's residency sentence had to be removed because nothing supported it, and `docs/BRD.md` went on telling sales to make the same claim. Choosing an Indian host is what turns that from a claim nobody can make into a claim that needs verifying. **WHAT IT DOES NOT SETTLE, and this is the part that matters more than the choice.** Our VPS is one of at least five places tenant personal data lives. Cloudflare R2 runs `AWS_REGION=auto` and offers no India-only jurisdiction; Bolna's recordings were OBSERVED on S3 `us-east-1` and its residency posture is still unpinned in the contract; Sarvam is all-India by D-36; Gemini is Vertex `asia-south1` by D-127; Resend and Sentry are operated outside India. So **the host being Indian does not make the data plane India-resident**, and until every one of those is answered in writing nothing in this repository may assert residency — the ban in LEGAL-SURFACE F-1 survives this decision rather than being lifted by it. What IS sellable today is unchanged: we name every sub-processor and where it runs, before signature. **WHAT IT COSTS.** `docs/DEPLOYMENT.md` §0's provider prose and its Hetzner-class latency worked example are now describing a machine we are not renting, and they are the next thing to correct; the ~150ms figure was an argument about European hosting and does not survive the move. |
| D-181 | **A dial gets a call row BEFORE the vendor can seize a line; an `Idempotency-Key` claim stops dying with the side effect it guards; and the recording audio and the leads search leave `calls:read` and the URL** | (1) `dispatch_call` commits an INTENT row in its OWN transaction before `start_outbound_call`, keyed on an `engine_call_id` we mint (`UNCONFIRMED_ENGINE_CALL_PREFIX = "local:"`), stamps the vendor's handle over it on a clean return, and raises `DialUnconfirmedError` when the answer is lost. The campaign dispatcher writes `campaign_contacts.last_call_id` inside that same transaction (`on_reserved`), settles an unconfirmed dial TERMINALLY rather than on the ladder, and `_reap_stuck_dialing` does the same for the case that outruns an `except` (a `CancelledError` through the dial). (2) The two CRM routes that spend money — `POST /v1/calls/{id}/assist` and `POST /v1/leads/{id}/call`, plus `/callback` which had the same shape — take `billing/payment_routes._create_order_once`'s three-transaction form: claim committed before the paid call, `fail_idempotency` on the arm where nothing was paid for, meter+audit+complete committed together afterwards. (3) `GET /v1/calls/{id}/recording` moves to `calls:read_raw`. (4) `search` moves off the query string to `POST /v1/leads/search` and `POST /v1/leads/export.csv` (and into `LeadBulkIn.search`); the GET shapes REFUSE it. (5) The campaign retry ladder is computed with `now() + make_interval(mins => :minutes)` like its two sibling writers. No migration: the calls table is unchanged. | **(1) The docstring promised the ordering the code could not have** — the INSERT bound `engine_call_id` to the value the engine returns, so the row could not precede the call. A lost response (read timeout at 10s, reset, proxy 502 after the vendor committed) left a ringing phone with no row: no metering, no wallet debit, and a campaign contact returned to `pending` with no `last_call_id` for `resolve_campaign_contact` to match — **a real person dialled a second time**. A namespaced local id was chosen over making `engine_call_id` nullable because NULL would mean both 'not yet' and 'lost', and every reader that joins on the column would need a branch it cannot resolve. The conservative classification is deliberate: only failures that PROVE no line was seized (`DIAL_NOT_PLACED_CODES` — 429 and the pre-flight refusals) keep the retry ladder, because the adapters collapse 4xx and 5xx into one `engine_rejected` and a wrongly-retried dial costs a second unsolicited call while a wrongly-abandoned one costs a contact a human looks at. A vendor-side idempotency key on `POST /call` would let us do better and is UNVERIFIED for Bolna, so it is not assumed. `lead_id` is stamped through the caller's session after the dial, not in the intent row: `ingest/service.py` creates the lead in the same uncommitted transaction it dials from, and an FK from another connection would wait on the transaction that is waiting on us. **(2)** `core/deps.db` rolls the request back on any exception, so a raise after `run_assist` erased the claim, the `usage_events` rows and the audit row while Google had been paid — and the retry with the same key paid again. This repo had already solved it in `billing/payment_routes.py` and argued why; adopting that shape removes the second way of doing it. On `DialUnconfirmedError` the claim is left `processing` ON PURPOSE: releasing it invites a second call to somebody whose phone may be ringing. **(3)** The audio is the SOURCE of the text the whole redaction apparatus protects; the route had the audit half of hard rule 5 and not the role half, while the raw transcript and the CSV export beside it were already `calls:read_raw`. **(4)** `search` is matched against `phone_e164`, and nginx's default `combined` format logs the request line — the hazard `POST /v1/dnc/check` is already a POST for. REFUSED rather than ignored on the GETs, because FastAPI drops an undeclared parameter silently and a dropped filter WIDENS the set, which on `export.csv` is the whole contact list leaving with full numbers. **(5)** The worker clock wrote a column the database clock reads. **Cost accepted**: a dial now briefly holds two pooled connections (the caller's plus a short-lived one, released before the engine round trip), which the audit's separate no-overflow-pool finding already describes for two other paths. |

## 7. Explicitly Deferred (do not build until pulled by a client or a gate)

White-label/reseller tiers · payments-in-call · voice biometrics ·
multi-agent teams · non-India telephony · GPU inference · Temporal · separate analytics DB.

**No longer deferred: self-serve signup.** D-34/D-39 pulled it in and it ships —
`POST /v1/auth/signup`, plus the top-up intent, the server-side order route and the
payment webhook (SURFACES §2c). Server-side order creation **is** built (D-98); this line
said it was not, for a wave after it landed. What holds a client back is the credential,
not the code: the intake flag `self_serve_signup_enabled` defaults to OFF and no Razorpay
API secret is configured on any deployment, so `PaymentCapability.creates_orders` answers
False with reason `no_api_secret`. "Shipped" here means the surface and the adapter exist,
not that a client can pay today.
| D-182 | **The connection pool is sized against the nesting the code actually has, no transaction is held across a vendor round trip, and the three waits that could not end now end** | Five things, one theme: a bound that was justified by an untrue claim, and four places a wait had no end. (1) `db/session.py` justified `max_overflow=0` with "no code path here holds two sessions at once (checked across apps/api and apps/workers)". Fifty functions hold two: FastAPI's `Depends(deps.db)` holds the request session for the whole handler, so `check_dispatch` → `get_platform_status` → `_read_durable` on a cold 5s memo is a second connection inside the first; every admin route holds the directory session outside and a `tenant_session` inside; and `dispatch_outbox` → `claim_outbox_batch` takes a second connection on purpose. `max_overflow` becomes `MAX_NESTED_CONNECTIONS - 1` = 1, and `scripts/check_session_nesting.py` walks call chains THROUGH THE IMPORTS (understanding `Annotated[..., Depends(db)]`) and fails CI at depth 3, in `make guardrails`, CI and the guardrail catalogue. (2) `outbound_webhooks.deliver_outbound_webhook` splits into read / vendor / write, so neither the POST to the client's CRM nor the object-store PUT happens inside a transaction. (3) `compose.prod.yml` gives `api` `stop_grace_period: 30s`, and `tests/worker_reliability_test.py` pins drain < grace for all three long-lived services rather than for `workers` alone. (4) `core/health.py` bounds both dependency probes and the queue read at 2s and answers `db_down`/`redis_down`. (5) `dispatch_outbox`'s catch-all narrows to `POISON_PAYLOAD` (`arq.jobs.SerializationError`, `TypeError`, `ValueError`), and `platform_config.refresh` moves its whole body inside the `try` its docstring already promised. | **The pool comment is the finding, not the number.** A capacity decision justified by a property the code does not have is worse than an unjustified one: with no overflow, sixteen depth-2 tasks hold all sixteen connections and each waits `_POOL_TIMEOUT_S` for a seventeenth — a self-deadlock, and the memo hides it until Redis is down, which is when the API is busiest with retries. Rejected: restoring the old invariant. It would mean resolving the platform status before every session opens and changing `check_dispatch`'s signature across six callers, to buy back a rule the framework itself breaks on every route — so the honest move is to state the real invariant and enforce it, which is why the guard and the pool now hold ONE number in two files with a test asserting they agree. One slot is not the single-use-connection treadmill `get_engine` measured either: that needed 186 fresh backends sustained above `pool_size`, and one slot cannot sustain anything. **(2) is the same fault seen twice** — correctness 4 and R-5 found it independently. Ten slow deliveries park ten of the worker's sixteen connections, the campaign dispatch tick starves, and the alarm that fires is `dispatch_tick_overrun`: the operator is pointed at the dispatcher for one client's slow CRM. `pipeline._copy_recording_once` and `campaign_dispatch`'s claim/dial split already reason about this; the delivery worker did not. **(3) was measurable from the file**: Docker's documented default grace is 10s and the api declared none, so a 25-second drain met SIGKILL at ten on every deploy and the lifespan `finally` D-101 restored never ran — the fix undone one layer up by a key nobody wrote. **(4) A health endpoint that hangs is worse than one that fails**: an orchestrator cannot act on "slow", and each hung probe held a connection, so probing during a blackhole exhausted the pool and made everything else 503 for the wrong reason. **(5) both are alarms that fail towards silence**: the dispatcher's catch-all answered a database error by issuing another statement on the session that error had poisoned, so up to fifty messages were charged an attempt per tick and eventually dead-lettered as poison they never were; and `refresh`'s promise held by the current contents of `_resolve` and `apply_platform_overrides` rather than by its own shape, with both of its alerts inside the `except` that did not cover the raising half. **Not fixed, and named**: `campaign_dispatch._dispatch_for_campaign` holds the identical vendor-call-inside-a-transaction shape as (2) and belongs to another agent's fence this pass — it is bounded to one dial at a time by the tick lease, and it is the next thing to restructure. |
| D-183 | **A promised alarm that cannot fire is worse than an absent one: the client spend cap announces itself, the poller's sweep survives one tenant, and the metric stream stops pretending to be telemetry** | Four things, one principle. (1) `billing/caps.py` gains `cap_fullness` + `announce_cap_headroom` (`CAP_WARN_AT = 0.80`, the same constant and the same argument as D-140's `PLATFORM_BRAKE_WARN_AT`), fired from the meter's `spend_state` upsert — which now RETURNs the new totals and the ceilings from the same `caps` CTE the flag was judged by, so "under before, over after" is decided from the writing statement's own return value with no second table. Codes `tenant_spend_cap_approaching` and `tenant_spend_capped`, most-severe-first with a break, ids and a percentage only. The other two writers of the flag (the client's own stop button, the ops recompute) deliberately stay silent: both are a human acting on purpose with a response body that already says `capped`. (2) `core/alerting.py`'s recorder section says at its head that every `record_*` is a log line with NO consumer and that **no alarm may depend on one** — alarms travel `alert()`'s email path (D-49) and announce at the producing write. (3) `reconcile_executions` gets per-execution `try/except` + an `unreached` counter on the job result and in a new `reconciliation_probe_incomplete` alert, and its cron gets `max_tries=WORKER_MAX_TRIES`. (4) `integrations.subscribed_endpoint_sql(alias)` becomes the ONE spelling of "which endpoints get this event", read by `enqueue_events` and by the poller's `crm_fanout_owed` probe. Plus: `outbox_messages.queue`'s inertness is now stated at the column declaration and on `OutboxMessageRow.queue` (D-162 unchanged, no router built), and `integrations/service.py` records what `webhook_deliveries` can and cannot answer — the OUT direction is complete, the IN direction holds only CLAIMED deliveries, so an intrusion investigation must be pointed at the alert stream instead. | OPERATIONS §4 listed "cap approaching (80%)/breached" as a live trigger and `caps.py` contained no `alert(` at all: a capped tenant's campaign stopped dialling, the console still read "running", and the only trace was a `record_compliance_block(rule="spend_cap")` log line that nothing reads. D-140 having built exactly this alarm for the platform's OWN AI spend is what makes it an omission rather than a decision, so it is fixed by following that implementation rather than inventing a second alarm shape. The same principle explains (2): a recorder that looks like telemetry and reaches no consumer is CLAUDE.md's "half-wired feature", and the honest fix while DEPLOYMENT §8 defers the metrics endpoint is to say so and to forbid alarms built on it, not to pretend. (3) is the guarantee of record failing towards silence — TRD §5 makes the poller, not webhook delivery, what makes call records trustworthy, and one tenant's probe error ended the sweep for every execution behind it, failed the job on its first attempt and alerted nothing; `report_stalled_pipeline` was fixed for this exact shape and its argument is quoted where the new `try` sits. (4) is unreachable today only because the CHECK constraint happens to allow exactly `DELIVERABLE_KINDS` — a third kind landing ahead of its worker would make every completed call for that tenant a permanent `unfinished_pipeline` re-driven hourly WITH A BILLED EXTRACTION, and two spellings of one rule is a defect while they still agree. **Not fixed, and named rather than left implied**: complaint-spike, engine-5xx-spike and cert-expiry are still on OPERATIONS §4's trigger list with no call site anywhere — the first two are rates over counters (they need the deferred metrics pipeline or a producer in files this pass did not own) and cert expiry is a systemd timer beside the backup units using `notify.sh`. §4's list still names triggers nothing implements. |
| D-184 | **Two mechanisms for one compliance rule is a defect even while both work — and the console had four of them** | Nine frontend findings, one principle each. (1) The unredacted transcript is a `useMutation`, not a `useQuery` with `staleTime: Infinity`: `GET /v1/calls/{id}/transcript/raw` writes an `audit_log` row in its read transaction, so a hide-then-show — or a return inside `gcTime` — used to re-expose the text with no request and NO SECOND ROW, and `reset()` on Hide now drops the personal data instead of parking it in the heap. (2) `ScrollRegion` (`components/ui.tsx`) replaces all eighteen bare `overflow-x-auto` wrappers, including the one site that had argued the case inline. (3) `SkipLink` + `MAIN_CONTENT_ID` in both shells, with `tabIndex={-1}` on `<main>`. (4) `lib/nav.currentNavItem` is the ONE 'you are here' rule both shells read for the header title AND the sidebar highlight. (5) The leads lens travels in a POST body — `POST /v1/leads/search` and `POST /v1/leads/export.csv` — and `lensQuery` now THROWS on a search term, so the URL cannot carry one. (6) `INLINE_EDIT` carries `touch:min-h-11`; four more `py-0.5` buttons widened. (7) `tests/responsive.test.ts` gains the keyboard-reachability and hairline-control rules. (8) Delivery-log rows key on identity plus occurrence. (9) `callback-check`, `campaign-check` and `campaign` gain the org slug, with `tests/queryKeys.test.ts` scanning every `useQuery` in `src/` for the next one. | **WHY IT IS ONE ROW.** Every finding is the same shape: a rule the repo had already decided, implemented a second time and worse. The raw transcript is the sharpest — `useRecordingLink` sits eleven lines below it on the same screen, correctly a mutation for exactly the same reason, so hard rule 5's audit row was lost to a mechanism the file itself had already replaced. `document.tsx` had the focusable scroll container with axe's `scrollable-region-focusable` cited in a comment, and seventeen tables did not. Both shells computed the title by longest prefix and the highlight by exact match, four lines apart, so no element in the document carried `aria-current` on ANY detail route. `POST /v1/dnc/check` and the messaging-consent lookup both take a phone number in a body and both say why in their own files; the leads box — 'Name or last digits', matched against `phone_e164` — was the busiest screen left out of it. **GUARDS THAT CAN ACTUALLY FAIL.** axe cannot fire `bypass` or `scrollable-region-focusable` under jsdom (no layout, detached container — `tests/a11y.ts` says so itself), so adding those rules would have been a green tick on a check that never runs. Each fix instead ships a test asserting the mechanism where it is observable: request COUNTS at the network seam for the audit row, markup for the skip link, the source tree for the scroll containers and the query keys. Every one was run red against the pre-fix tree before being kept. **NOT CLOSED, and named rather than implied.** `GET /v1/leads/facets` has no POST shape, so the facet rail is sent NO search term at all and its counts describe the wider set under an active search — the table states its own narrowing beside it, and over-counting offers a filter that returns rows where under-counting would hide leads. Closed by a `POST /v1/leads/facets` taking the same `LeadLensIn`, which is `apps/api`'s. And F-8's real fix is one field: `ingest/routes.py` maps both the `ingest:` and `meta:` keyspaces onto one `lead_source_id` and discards the provider on the way out, so the console is handed an identity it cannot disambiguate; the occurrence counter is unique by construction but is a client-side answer to a server-side omission. |
| D-185 | **An unverified address could carry a membership into somebody else's organisation, and a lifted DNC suppression left no record of the number** | Two seams from the 17 Aug audits (S-2, S-4), plus the four docs-truth findings that were ours (F-8, F-9, F-10, F-12). **S-2 is NOT closed by D-177**, which is the finding's own claim and it is wrong: the report located "`users.email` is an authorization input with no verification check" in `core/clerk_identity.py`, D-177 deleted that file, and the PROPERTY the vendor was assumed to supply was never replaced. `authn/invitations._find_or_create_user` wrote `email_verified_at = now()` on the premise that possession of the emailed token proves the mailbox — but `InvitationCreatedOut.token` hands the raw token to the INVITER, because the client realm has no invitation mailer. So: an owner of any tenant invites an address they do not control, reads the token out of their own 201, redeems it, and holds the only `users` row that address can ever have (`uq_users_email_lower`) with a password they chose. When the real organisation later invites the real person, redemption reused that row, correctly declined to overwrite the password, and attached the membership — up to and including `owner`. The client realm has no second factor (`MFA_REQUIRED_REALMS` is `{'admin'}`), so a password is the whole credential. **Fixed**: the address starts UNVERIFIED (an `email_verify` OTP round trip is what fills the column, and that path was already built and already mailed), and an account that already carries a credential may only take on a NEW membership once its address is proven. A first-time invitee is untouched; the refusal lands before the CAS so the token stays redeemable afterwards. `tests/authn_invitation_verification_test.py` drives all four steps. **S-4**: `DELETE /v1/dnc/{id}` hard-deleted, and the audit row correctly carried no number, so nothing anywhere recorded which number stopped being suppressed. `remove_entry`/`remove_global_entry` now return a `Removal` carrying `compliance.export.subject_ref` — the same one-way handle `deletion_requests.subject_ref` stores — and the routes put it in the audit summary. **A tombstoned ROW was rejected and the reasons are in `dnc.Removal`**: it keeps a number that no longer suppresses anybody (`ERASURE_LIMITATIONS` justifies retaining a DNC entry solely because removing it would make the person callable again, which is false of a released row — so it would be retained personal data with no purpose, DPDP §8(7)); `add_numbers`' dedupe would read the tombstone and report a re-add as `already_suppressed`, which is the one direction hard rule 5 cannot fail in; and it would require the `(tenant_id, phone_e164)` unique key that `check_compliance_invariants` treats as an invariant to become partial. **Docs**: SEC-COMP §4 stops claiming a live client-facing retention misstatement (the DPA delegates to `/legal/privacy` §9, which publishes the seed's numbers, which is what the sweep enforces — §4's own table is the only outlier, and the founder decision stays open); DEV-SETUP names test files that exist; FLOWS §6 states the recording copy's real 30s/120s ladder; and D-95's "`.env` drops to 6" is corrected to a count of `BOOTSTRAP_KEYS` rather than of lines. | **WHY THE SMALLER FIX.** The complete answer to S-2 is to stop returning the invitation token to its issuer and email it instead, which restores the premise and closes the squat as well as the hijack. That needs the invitation mail template and the owner-facing screen that displays the token — `apps/workers/` and `apps/web/`, both outside this change — so what ships here turns an ACCOUNT TAKEOVER into a DENIAL OF SERVICE and says so rather than claiming a closure. `tests/authn_invitation_verification_test.py::test_the_squat_itself_is_still_possible_and_that_is_recorded_here` pins the residual and fails when it goes, which is what closes this entry. **WHY NOT A SIGN-IN GATE.** Refusing a session to an unverified client subject is the more obvious control and is unreachable from here: the verify endpoint needs a session, so it would lock out the very people it is for, and the gate itself belongs in `core/auth.py`. The membership boundary is the one place the escalation actually crosses. **WHAT IS ASSUMED, NOT VERIFIED**: that no path other than invitation redemption writes `users` — proven by grep (one `INSERT INTO users` in `apps/`, `scripts/` and `alembic/` outside test fixtures) and by `POST /v1/auth/signup` requiring `current_identity`, not by an exhaustive audit of every future writer. **COST**: one person joining a second client business now has an extra step, and it is a screen that already exists. |
| D-188 | **The production image had an empty virtualenv, the `.env` the deploy demands was one `Settings()` could not load, and the nginx floor was two releases below what the config needs — a first-deploy readiness audit, executed rather than read** | Five findings from walking the whole first-deploy path with the commands actually run, and their fixes. **(1) `Dockerfile` called `uv sync` without `--all-packages` in both phases.** The workspace root is `package = false` with `dependencies = []`, so uv resolved the root's empty list, installed nothing and exited 0: the shipped image's `site-packages` held THREE entries and not one third-party package. No service could start, `alembic upgrade head` could not run, and all three `scripts.*` modules `vps-deploy.sh` invokes through `compose run` would have died on their first import. Measured both ways: 3 entries before, 127 after. `--group errors` is added in the same line, because DEPLOYMENT §8's per-host `uv sync --all-packages --group errors` is an instruction the containerized architecture cannot obey (§2 puts no Python on the host; the venv is an image layer owned by a non-root user), so `sentry-sdk` was unreachable and `SENTRY_DSN` could never be more than a setting. The uv image is also pinned by digest, closing §4d item 1. **(2) `Settings` refused the prescribed `.env`.** `model_config` is `env_file='.env', extra='forbid'`, and pydantic-settings applies `forbid` to keys read from the DOTENV FILE — so the three object-store credentials that DEPLOYMENT §6 requires, that `vps-deploy.sh::preflight` ABORTS without, and that `check_deploy_env` validates, made `Settings()` raise three `extra_forbidden` errors for every process whose cwd is the deploy root. `calevate_shared.config` gains `SDK_OWNED_ENV_KEYS` and a `DotEnvSettingsSource` subclass that drops exactly those keys; `check_deploy_env.OBJECT_STORE_CREDENTIALS` aliases the constant and `check_env_parity.SDK_ENV_KEYS` asserts against it. `tests/deploy_env_file_loadable_test.py` is the regression, run red first. **(3) nginx.** `infra/nginx/` uses the standalone `http2 on;` directive, which appeared in nginx **1.25.1**; §2 said `nginx >= 1.24` and §1 says Ubuntu 22.04 (1.18) — on 1.24 `nginx -t` answers `unknown directive "http2"` and NO server block loads. §2 now says 1.25.1 with the reason, and `preflight_plan` refuses below `NGINX_MIN_VERSION` before anything is built. **(4) §2's "Python is NOT needed on the host" is false** for `scripts/backup/app-python.sh` — `notify.sh` defaults its sink to `alert-to-app.sh`, which exits 78 without the host venv, so every backup alarm reaches journald and nobody else — and for `scripts/bootstrap_admin.py`, whose documented `uv run` form cannot execute on the VPS at all. Both corrected; the bootstrap gains its container invocation. **(5) `AUDIT_CHAIN_SECRET` is a bootstrap ordering**: the first admin's audit row cannot be signed without it, it is normally console-managed, and the console needs the admin — so the first run takes it from the environment. Documented at the command. Plus `docs/evidence/deploy-readiness.md` (the executed audit, VERIFIED/BROKEN/ABSENT per step) and `runbooks/first-deploy.md` (the operator procedure, every command in it run). | **WHY ONE ROW.** Every finding is the same defect and DEPLOYMENT §4d names it: nothing here had ever been run. Each artefact is individually careful — the preflight's refusals are excellent, the migration ordering argument is right, the nginx config is the modern spelling rather than the deprecated one — and each was wrong in the one way that only execution reveals. **The empty venv is the proof of the class**: a `uv sync` that installs nothing exits 0 and looks exactly like a cache hit, the repository's own README, CI workflow and DEPLOYMENT §3 all say `--all-packages`, and the single file that builds the production artefact did not. **WHAT WAS VERIFIED BY RUNNING IT, because that is the whole value of this row**: the builder stage builds and every COPY resolves; `alembic upgrade head` from an EMPTY database reaches a single head (`c7a1e93d40b8`, 62 tables, 48 FORCE-RLS'd) and `check_rls_coverage` passes against it; the api boots under `APP_ENV=prod` and `/healthz/live` 200 / `/healthz` 200 / `/healthz/ready` 503 is honest; `check_deploy_env` correctly refused a loopback DSN and went green when fixed; `vps-deploy.sh --dry-run` reaches its plan for api/voice-runtime/workers/nginx; `bootstrap_admin` runs end to end BOTH on the host and through the image, and with no mail provider it prints the setup link to stdout and says `email sent: NO` — so **email delivery does NOT block the first login**, which was the standing assumption and is wrong; and the rendered nginx set is `test is successful` on 1.27 and `[emerg]` on 1.24. **WHAT IS STILL UNVERIFIED AND IS SAID PLAINLY**: the runtime image stage never built here (`deb.debian.org` and ghcr's blob CDN are refused by this environment's egress policy), so `curl`, the non-root user and the compose healthchecks are unexercised; nothing has touched a VPS, a domain, Cloudflare or R2; `terraform validate` still has never run; and the backup drill OPERATIONS §8 ticks still has never run — `bash -n` passes on all twelve shell scripts and `systemd-analyze verify` finds no directive errors, which is syntax and not a restore. **THE VERDICT THIS ROW EXISTS TO RECORD: not deployable before these fixes; deployable to a first attended deploy after them, and the remaining blockers are external** — a provisioned VPS, DNS, a Cloudflare zone, an R2 bucket with a backup-scoped token, a non-Cloudflare offsite target, a verified Resend sender domain and a Sentry project. |
| D-189 | **An erased account went on collecting callers, a caller's opt-out was the client's to delete, and the owner of a closed account was told they were a stranger to it** | The five end-to-end journeys walked as a person rather than module by module, hunting the seams BETWEEN steps (`docs/evidence/deepdive-journeys.md`). Three defects fixed, each proven by execution before the fix. **(1) Hard rule 5, on the most ordinary path there is.** `compliance.service.add_to_dnc` wrote `ON CONFLICT DO NOTHING`, which is right for the row and wrong for `source`: somebody at the client pastes a number into the do-not-call page (`manual`, deletable, and it has to be — a mistyped digit must be fixable), that same person later rings in and says "stop calling me", and `record_call_optout` finds the row already there and changes nothing. The entry keeps `source = 'manual'`, `dnc.is_removable` returns True, the screen offers the delete button and `remove_entry` honours it — so the caller's opt-out is deleted by the account it was made against, with a `consent_ledger` row still saying they withdrew. That is "can never be removed by anyone" failing, and TCCCPR's ninety-day bar on re-soliciting an opted-out subscriber with it. The conflict now UPGRADES, monotonically: a client-deletable source is rewritten to an incoming non-deletable one and nothing else ever is, so a `manual` add can never weaken a `call_optout` row. `added_at` is untouched — the suppression has been continuous. `DNC_REMOVABLE_SOURCES` moves to `compliance/models.py` because `service.py` cannot import `dnc.py` (`dnc` → `ingest.service` → `compliance.service`); `dnc.REMOVABLE_SOURCES` re-exports it, so there is still one definition. **(2) The tenant erasure stopped what the account HELD and not what it could ACQUIRE.** `engine_agent_routes` is the one bridge from the voice platform's id space into ours, the vendor's agent is still configured and the DID still points at it — so the first inbound call after the certificate re-created a `calls` row, its transcript, its extraction, its lead and an archived engine payload, under a tenant whose certificate says it is erased and whose own people are locked out. Personal data with no purpose and no reader is DPDP §8(7) on its own; a certificate false within minutes of being signed is worse. The erasure now withdraws the routing in its own transaction, counts it on the certificate, and the certificate states in words that the agents remain configured at the voice platform and the numbers still route to them. `engine_agent_route_withdrawn` distinguishes that silence from a mis-provisioned agent, because they are different jobs for whoever is paged. **(3) `core/auth.py` told the owner of a closed account they were not a member of it** — false, on their own account, in a product whose offboarding flow hands them an export of the very data that screen shows. Zero rows is two facts; the closed one now answers `account_closed` with a next step, and a genuine stranger still gets the neutral refusal. Plus: FLOWS §5's "Recurrence is NOT built" (it is, and routed), and consent provenance whose summary, docstring and refusal said "draft only" while the statement accepts `scheduled`. | **WHY `delete_agent` WAS NOT CALLED FROM THE ERASURE.** It exists and is idempotent by contract, and it is a third-party round trip inside the one transaction that must not half-commit — a slow vendor would become a rolled-back erasure. The vendor-side deletion stays `unconfirmed_pending_vendor_api`, which is what the certificate has always claimed and all it can. **WHY CHURN WAS LEFT ALONE.** Churn also leaves the agent live and the number never released (FLOWS §9's "number released or ported" has no implementation anywhere), and the two states differ in what a wrong answer costs: an erasure carries a document asserting destruction, so continuing to collect makes it false; churn ends a commercial relationship, the data stays for its retention period by design, and withdrawing the routing there would leave a caller talking to an AI with no record kept — a worse position, and a founder's call rather than mine. **Externally blocked** on a telephony-provider account and a voice-platform account (OPERATIONS §2), which is what turns "release the number" into an API call an offboarding job can make. **CHECKED RATHER THAN ASSUMED**: the retention sweep and the outbox dispatcher enumerate tenants unfiltered on `active`, so the countdown FLOWS §9 promises survives the withdrawal; the drift sweeps do stop, which is right for agents being abandoned. **LEFT OPEN AND NAMED**: `publish_agent` asks nothing about the organisation's lifecycle, so an operator with a hand-typed uuid can put a churned or erased client's agent back on the phone. The honest fix moves `admin.service.assert_account_open` to a module both realms already depend on and migrates its two callers; it was not done here because reaching it from `agents/service.py` today risks an import cycle through `admin/`, and a second copy of the predicate is the drift this repo pays for. |
| D-190 | **The invitation token is emailed to the invitee and returned to nobody — D-185's residual, closed** | `POST /v1/invitations` enqueues `invite_password` through `authn.service.enqueue_invitation_email` in the request's OWN transaction (one fate for the row and its mail), and `InvitationCreatedOut.token` is replaced by `delivery`. The team screen shows "Invitation sent to p••••@…" instead of printing a link; `auth_email._body` now points at `/auth/accept-invitation` rather than the legacy `/invite` redirect, because this template is what MINTS links now and D-177's rule is that new links name the surviving page. Every test that used to read the token off the 201 reads it out of the queued email instead — the same path a real invitee's link travels, which makes those redemptions end-to-end checks of delivery rather than of a field the API echoed back. | **WHY.** D-185 found that an owner of any tenant could invite an address they do not control, read the token out of their own 201 and redeem it — taking the one global `users` row for that address. D-185 stopped the ESCALATION (the real person's redemption is refused rather than hijacked) and could not stop the SQUAT, because the squat lives for exactly as long as anyone but the invitee can see the token. It named the closure precisely: email the token instead of returning it. `InvitationCreatedOut.token`'s own docstring had said the same thing since it was written — *"when that job exists this field should stop being returned, because a token that is displayed is a token that can be pasted into the wrong window"* — and the job (`deliver_auth_email`, D-170) had existed for two slices with an `invite_password` kind already in its subject table and nothing sending it. **WHAT IT COSTS.** An invitation now depends on email delivery, which is an external blocker (OPERATIONS §2, Resend domain verification) — the same one the first administrator already depends on, so it widens no surface that was not already there, and until a transport exists the link is read out of the outbox exactly as the bootstrap link is read off the terminal. **THE TEST THAT REPLACED THE RESIDUAL IS WRITTEN FROM THE ATTACKER'S SIDE ON PURPOSE**: the shared `_invite` helper now reads the outbox, which is a database privilege no caller has, so a squat test built on it would prove nothing — it would grant itself the very access the fix removes. The closure test calls the route and inspects only the response, and additionally refuses any credential-shaped value under a different key, so a future field cannot smuggle the secret back. |
| D-191 | **The facet rail took the same lens as the table it describes, so the two stopped counting different populations** | `POST /v1/leads/facets` takes `LeadLensIn` — the same body the table and the export take, minus the page — and the GET now REFUSES `search` (422 `search_must_be_posted`) rather than ignoring it. `useLeadFacets` sends the term and keys on the body via `lensKey`. Registered in `tests/authz_audit_test.READS_SHAPED_AS_POSTS` beside `/v1/leads/search` with the same reason. | **WHY.** D-181 moved the Leads table's search into a body because the term is matched against `phone_e164` and a query string is written to nginx's access log (hard rule 6). It left the rail on the GET, so the only safe client dropped the term — and a rail counting the WIDER set is two numbers on one screen describing different populations: the table shows 1 hot lead matching "9876" and the rail says 12. That is the kind of thing a client reports as a bug in their data rather than in our UI, and it was recorded honestly as a residual rather than hidden, which is why it was still findable. **WHY REFUSING BEATS IGNORING** on the GET: ignoring a filter WIDENS the set, and this repo already made that asymmetry a rule for dropped column filters — a widened lead set is a mailed contact list. |
